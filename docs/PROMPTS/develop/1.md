You are the OneByTwo orchestrator agent. The agentic workspace under .github/ is configured and smoke-tested. Sprint-zero artefacts exist in docs/sprint-zero/. The design phase is complete and lives in docs/design/. We are now starting active development.

The goal of THIS session is narrow and specific: produce a single pull request titled `chore: bootstrap project skeleton` that brings up the Flutter app and Cloud Functions project to a "hello world" state, with all CI checks passing. This PR contains NO product features. Its sole purpose is to prove the pipeline works end-to-end with real code in it.

Re-read .github/copilot-instructions.md, .github/shared/invariants.md, .github/agents/orchestrator.md, .github/shared/handoffs.md, and docs/design/03-architecture/c4-container.md before starting. Honour the four invariants throughout.

────────────────────────────────────────
SCOPE — WHAT GOES IN THIS PR
────────────────────────────────────────

ONLY the following. Anything outside this list is a separate PR.

1. Flutter project initialisation
   - `pubspec.yaml` with the exact dependency set agreed in docs/design/07-technical/state-management.md and the design system. At minimum: flutter, flutter_test, flutter_lints (or very_good_analysis per the SRS choice), flutter_riverpod ^2, intl, firebase_core, firebase_auth, cloud_firestore, firebase_storage, firebase_messaging, firebase_crashlytics, firebase_analytics, firebase_app_check, firebase_remote_config. Pin major versions; do not float.
   - `analysis_options.yaml` with the linter rules.
   - `lib/main.dart` that boots a `ProviderScope`-wrapped MaterialApp showing a single placeholder screen with the text "OneByTwo". No business logic.
   - `lib/app/theme.dart` exposing the design tokens from docs/design/02-design-system/tokens.md as a `ThemeData` (light + dark). Use semantic names, not raw hex.
   - Feature-first folder structure SCAFFOLDED but empty per SRS §13.1 (lib/app, lib/core, lib/data, lib/features/{auth,friends,groups,expenses,settlements,profile,activity}, lib/l10n). Each empty folder gets a `.gitkeep` and a one-line README.md saying which feature owns it.
   - `test/widget_test.dart` with ONE test: app boots and shows the "OneByTwo" text. No mocking yet.
   - iOS and Android folders configured for the minimum SDK versions per SRS §3.3 (iOS 14, Android API 26). Set the bundle identifier / application id placeholder to `app.onebytwo`.

2. Cloud Functions project initialisation
   - `functions/package.json` with Node 20, TypeScript ^5, firebase-functions ^4, firebase-admin, eslint, jest, ts-jest. Pinned majors.
   - `functions/tsconfig.json` strict.
   - `functions/.eslintrc.cjs`.
   - `functions/src/index.ts` exporting one trivial healthcheck function: `healthcheck` HTTPS function returning `{ ok: true, region: 'asia-south1' }`. Region MUST be set to `asia-south1` via `functions.region('asia-south1').https.onRequest(...)` — this is the canonical example for every other function later.
   - `functions/src/__tests__/healthcheck.test.ts` with ONE jest test asserting the response shape.

3. Firebase project configuration files
   - `firebase.json` declaring emulators (auth 9099, firestore 8080, functions 5001, storage 9199, ui 4000) and pointing at the (single) production project. No second project.
   - `.firebaserc` with one project alias: `default` → the actual production project ID. (Use a placeholder `oneByTwo-prod` and add a comment that DevOps replaces this.)
   - `firestore.rules` with a default-deny baseline and one allow-rule for the user reading their own user document. Cite SRS §7.5 in a comment.
   - `firestore.indexes.json` empty array (no composite indexes needed yet).
   - `storage.rules` with default-deny baseline.

4. Emulator setup script
   - `scripts/dev/start-emulators.sh` POSIX-compatible, runs `firebase emulators:start --only auth,firestore,functions,storage` with seed data path. Make it executable.
   - `scripts/dev/seed-emulator.ts` with an empty seed function (placeholder for sprint 1).

5. CI must pass
   - The PR pipeline (`.github/workflows/pr.yml`) was already scaffolded in the bootstrap phase. Verify it runs cleanly against this skeleton:
       flutter pub get, dart format check, flutter analyze, flutter test, functions lint, functions test, emulator boot.
   - If anything in the workflow needs fixing for a real Flutter project (vs an empty repo), the devops agent fixes it AS PART OF THIS PR.

────────────────────────────────────────
WHAT DOES NOT GO IN THIS PR
────────────────────────────────────────

DO NOT add in this PR (each is a separate, later PR):
  - Phone auth flow, OTP screen, login state.
  - Any feature folder content beyond .gitkeep.
  - Firestore rules beyond the baseline above.
  - The simplified-debts function or any business logic.
  - Telemetry instrumentation (the package is added; events come later).
  - App Check enforcement (the package is added; configuration is later).
  - Production signing, store metadata, deployment.

If at any point an agent suggests adding any of the above to "save time later," refuse the suggestion and note it as a follow-up issue.

────────────────────────────────────────
EXECUTION PROTOCOL
────────────────────────────────────────

1. Orchestrator delegates to the architect agent first to confirm the dependency list and folder structure against the design docs. Architect produces the agreed list.

2. Orchestrator then sequences the work:
   - flutter-dev: scaffolds Flutter project per item 1 above.
   - functions-dev: scaffolds Cloud Functions per item 2 above.
   - architect: writes the baseline firebase config per item 3 above.
   - devops: writes the emulator script per item 4 above and verifies workflow per item 5 above.

3. As work progresses, ALL of the following must remain true:
   - Every commit message follows Conventional Commits.
   - Every file edit passes the lefthook pre-commit hooks.
   - Every file edit passes the agentic PreToolUse hooks (no platform-specific shares, no second Firebase project, no client-side simplifiedBalances writes — none of which should arise in this PR, but the hooks should still fire and pass).
   - The PR template is filled in completely with checkboxes for the four invariants.

4. qa agent reviews before merge: confirms the widget test runs, the jest test runs, and the emulators come up locally on the agent's environment.

5. Open the PR using `gh pr create` with the title `chore: bootstrap project skeleton`. The PR body cites this prompt and lists every file added.

────────────────────────────────────────
DEFINITION OF DONE FOR THIS PR
────────────────────────────────────────

  - PR is open with all CI checks green.
  - PR template is fully filled in.
  - The four invariant checkboxes in the PR template are ticked AND each one cites a one-line rationale.
  - At least one commit per agent role involved (so the audit trail shows who did what).
  - No application logic beyond what is listed in scope.
  - The PR body ends with: "Next PR: implement FR-AU-01 phone-entry screen, vertical slice."

────────────────────────────────────────
FAILURE PROTOCOL
────────────────────────────────────────

If CI fails:
  - Do NOT widen scope to fix it.
  - Identify the failing layer (lint, test, emulator, hook).
  - Fix only that layer.
  - If a hook fires unexpectedly (e.g., a false positive), STOP and report — do not disable the hook. Adjust the hook script in a clearly-marked separate commit, or escalate to me.

If the design docs say one thing and the SRS says another, the SRS wins. Note the discrepancy and propose an update to the design doc as a follow-up issue.

Begin by delegating to the architect for the dependency-and-structure check.