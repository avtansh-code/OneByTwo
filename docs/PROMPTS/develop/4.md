You are the OneByTwo orchestrator agent. The skeleton bootstrap PR has merged. CI is green. Local emulators are running. We now implement the first real feature.

The story is `docs/sprint-zero/first-story-FR-AU-01.md`. The design artefacts are `docs/design/04-wireframes/auth-flow.md`, `docs/design/05-mockups/02-phone-and-otp.html`, and `docs/design/06-screen-specs/<screen-id-for-phone-entry>.md`. Read them before delegating.

Honour the agentic workspace, the four invariants, and the test-first discipline from `.github/shared/test-strategy.md`.

────────────────────────────────────────
SCOPE — WHAT GOES IN THIS PR
────────────────────────────────────────

A vertical slice of FR-AU-01: the phone-number entry SCREEN only. No OTP yet. No Firebase Auth call yet. Just:

  - The screen widget (`lib/features/auth/presentation/phone_entry_screen.dart`).
  - A Riverpod controller/notifier that holds the phone-input state and exposes a validation result (`lib/features/auth/application/phone_entry_controller.dart`).
  - An InputFormatter that locks the +91 prefix (cannot be deleted, cannot be replaced) and restricts further input to 10 digits (`lib/features/auth/presentation/widgets/india_phone_input_formatter.dart`).
  - The validator that matches the SRS rule (FR-AU-02): exactly 10 digits after +91, valid Indian mobile starting digit (6/7/8/9 — confirm with PM if unclear).
  - The "Continue" button that is disabled until validation passes; on tap it currently does nothing (next PR wires OTP).
  - The telemetry event `signup_started` fires on button tap (Firebase Analytics package is already in pubspec from the skeleton PR; instantiate the analytics provider as part of this PR if not already done).
  - All states from the screen spec: default, validation error inline, dark mode, dynamic-type extra-large, TalkBack/VoiceOver labelled.

DO NOT include in this PR:
  - Firebase Phone Auth integration (next PR).
  - OTP screen (next PR).
  - Auto-read of OTP (Android SMS Retriever) (later PR).
  - Profile setup (FR-AU-06) (later PR).
  - Session persistence (FR-AU-07) (later PR).
  - Sign-out (FR-AU-08) (later PR).

────────────────────────────────────────
TEST-FIRST DISCIPLINE
────────────────────────────────────────

Tests are written BEFORE implementation. Specifically:

1. flutter-dev writes the widget test FIRST (`test/features/auth/phone_entry_screen_test.dart`):
   - Renders without crashing.
   - +91 prefix is visible and not editable (test attempts to delete and verifies it remains).
   - Continue button is disabled when input is empty.
   - Continue button is disabled when input is 9 digits.
   - Continue button enables when input is 10 valid digits.
   - Tapping Continue with invalid input shows the inline error message from the screen spec.
   - Telemetry `signup_started` fires on Continue tap with valid input (use a fake analytics).

2. flutter-dev writes the unit test for the formatter SECOND (`test/features/auth/india_phone_input_formatter_test.dart`):
   - Cannot delete past the +91 prefix.
   - Strips non-digit input.
   - Caps total length at 13 chars (+91 + 10 digits).
   - Empty input still shows +91.

3. flutter-dev writes the unit test for the validator THIRD (`test/features/auth/phone_validator_test.dart`):
   - Accepts a known-valid sample number (e.g., `+919876543210` — confirm with PM if 9 is the correct starting-digit set).
   - Rejects 9-digit input.
   - Rejects 11-digit input.
   - Rejects input starting with disallowed digit per the rule.
   - Rejects whitespace and special characters.

4. ONLY after the three test files exist and are red, flutter-dev writes the implementation code that makes them green.

5. The widget test must pass; coverage on `lib/features/auth/**` must hit the SRS §5.7 thresholds before the PR can be opened.

────────────────────────────────────────
EXECUTION PROTOCOL
────────────────────────────────────────

1. Orchestrator delegates to the PM agent to confirm the story is unambiguous and the negative-case ACs cover what we need to test.

2. Orchestrator delegates to the architect for the technical micro-design: confirm the file layout, the provider shape, the InputFormatter pattern, and where the analytics provider lives. Architect appends decisions to the story file as `## Architect Notes` (not a new file).

3. Orchestrator delegates to flutter-dev with explicit instruction: "Tests first. Do not write the screen widget until the three test files are red."

4. Once tests are red, flutter-dev writes the implementation in this order:
   a. The InputFormatter (smallest, no dependencies).
   b. The validator (smallest, no dependencies).
   c. The Riverpod controller.
   d. The screen widget.
   e. Wire the screen as the app's initial route, replacing the placeholder from the skeleton PR.

5. After tests are green, designer reviews against `docs/design/05-mockups/02-phone-and-otp.html` and `docs/design/06-screen-specs/<id>.md`. Any drift is fixed in this PR.

6. qa runs a manual pass on both an iOS simulator and an Android emulator, including the dynamic-type and dark-mode visual checks. Manual results recorded in the PR body.

7. PR opened by flutter-dev via `gh pr create`:
   Title: `feat(auth): phone-number entry screen with locked +91 prefix (FR-AU-01)`
   Body: links to the story, the screen spec, the wireframe, the mockup; lists every file added; ticks the four invariants; calls out the telemetry event added; explicitly states "OTP integration deferred to next PR".

────────────────────────────────────────
DEFINITION OF DONE
────────────────────────────────────────

  - All tests green in CI.
  - Coverage thresholds met for `lib/features/auth/**`.
  - PR template fully filled in, four invariants ticked with rationale.
  - Manual smoke test on iOS sim + Android emulator captured in PR body (screenshots permitted as text descriptions).
  - Telemetry event `signup_started` registered in `docs/design/07-technical/telemetry-plan.md` as "implemented" with PR link.
  - Next-PR pointer in PR body: "Next: FR-AU-03/04 OTP screen and Firebase Phone Auth integration."

────────────────────────────────────────
GUARDRAILS
────────────────────────────────────────

If at any point an agent proposes:
  - "Let's add the OTP screen too while we're here" → refuse, that's the next PR.
  - "Let's wire Firebase Phone Auth so it's not stubbed" → refuse, that's the next PR.
  - "Let's just use a String for the phone instead of paise-equivalent integer handling" → not applicable here (this is a phone number, not money), but if a similar invariant violation appears elsewhere, refuse and cite the relevant ADR.
  - "Let's skip the formatter and just validate at submit" → refuse, the screen spec mandates the formatter so the +91 prefix can never be deleted, not just rejected.

If the design docs are silent on a small implementation detail, flutter-dev decides and notes the choice in `## Implementation Notes` at the end of the story file. If the choice is non-trivial, escalate to the architect.

Begin by delegating to PM for story-readiness confirmation.