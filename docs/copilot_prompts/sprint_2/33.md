1. You are the OneByTwo orchestrator agent. PR #73 (the Bucket-B close-with-evidence bundle) has merged (squash commit `f9a4c54`, `Closes #21`). It was the first dedicated Bucket-B tracker-hygiene PR: it verified the Sprint-1 boundary-audit findings that Sprint 2 PRs already resolved (CV3 -> #36; R1-R3 -> #32; R5a -> #51; R7-R8 -> #48; PY3 Functions/emulator layer -> the `functions/test/integration/*` suite), posted per-issue evidence comments, **fully closed #21**, **re-scoped #20 and #23** (kept open), and reconciled the burndown + roadmap. Three review comments (R7/R8 avatars-path residual; the SC2/SC4 prompt `sic` footnote; the R5a tracker-ID clarification) were addressed in `4b46ddf` before merge. **With #73 merged, every P0 functional requirement EXCEPT the deferred Sprint-3 Groups epic (FR-GR-01..07) is shipped, every P1 is shipped, and #25 / #47 / #21 are closed.** The remaining Sprint-2 chore backlog (#15, #16, #17, #18, #19, #20, #23, #24, #28) is now being closed out via **three consolidated PRs** — see the new "Sprint 2 close-out — consolidated chore PRs (#74+)" section in `docs/sprint-zero/sprint-2-plan.md`. This is the **FIRST** of those three: the **`chore(auth)` client-housekeeping bundle**, which closes **#15 / #16 / #17 / #18** in one client-`lib/` branch (no internal conflict — all four touch the auth surface and its shared providers). The architect confirms the consolidation and the pick at kickoff.

2.

## 0. PR-number caveat (read first)

Do NOT assume a roadmap slot equals the next GitHub number. Issues and PRs share one sequential namespace; this work lands as the next available GitHub number (>= #74 now that the highest PR is #73 and the highest issue is #68). This bundle introduces NO new tracked issue — it acts ON existing audit-backlog issues **#15 / #16 / #17 / #18**, and it **fully closes all four** (each is a complete, mechanical/polish item with no deferred remainder), so the PR body carries a `Closes #15`, `Closes #16`, `Closes #17`, and `Closes #18` line. Reconcile the slot label at PR open.

## 0a. Planning-doc reconciliation (read second)

Per the rolling-roadmap convention, each PR reconciles the PREVIOUS PR's status. The planning docs currently list **#73 as "IN FLIGHT" / "Open (>= #73)"**. At THIS PR's open, reconcile **#73 -> Merged** (`f9a4c54`). #73 was **velocity-excluded** (documentation + issue-tracker hygiene, like #59 / #61), so the velocity Total **stays at 132 SP / 32 PRs** — do NOT add SP for #73. Then add the `chore(auth)` close-out row as Open. Apply in both:
- `docs/sprint-zero/sprint-2-plan.md` — PR Tracking (#73 Open -> Merged `f9a4c54`; add the #74 row) and Velocity (#73 Open -> Merged, Total unchanged at **132 / 32**; add the #74 row with its SP — this PR DOES carry SP, it is feature-adjacent client work, **5 SP** suggested).
- `docs/sprint-zero/next-three-prs.md` — header + roadmap-table row #73 (IN FLIGHT -> Merged); add the #74 row; flip the relevant close-out candidate to IN FLIGHT. Add a detailed `## PR #73 — Merged` section only if you judge it consistent with the #70/#71/#72 precedent (a short Merged entry mirroring those is fine).

## 1. Scope — what we build

**A single client-`lib/` housekeeping bundle closing the four auth-surface audit-backlog issues.** Read each issue and the audit source `docs/audits/sprint-1/01-documentation-drift.md` (findings M1, T3/T4/T5, M4, S1/S3/S4/S5). The four work-streams:

- **#15 (M1) — rename `authStateNotifierProvider` -> `authStateProvider`.** The provider is a `StreamProvider`, so the `Notifier` in the name is misleading. Definition at `lib/features/auth/application/auth_state_provider.dart:98`; **26 reference sites** across `lib/` + `test/` (verify with a fresh grep). Pure symbol rename; runtime behaviour unchanged. Update the `lib/features/auth/README.md` reference too.
- **#17 (M4) — relocate the shared app-scoped providers to `lib/core/providers/`** (the directory does NOT yet exist — create it). The three providers named by the issue: `firebaseFirestoreProvider` + `firebaseStorageProvider` (currently in `lib/features/auth/data/user_repository.dart:12,19`) and `phoneAuthRepositoryProvider` (`lib/features/auth/data/phone_auth_repository.dart:206`). Update every import/reference. **Architect's call at kickoff:** whether to ALSO relocate `currentUserIdProvider` (currently in `lib/features/friends/application/friends_list_provider.dart:15`) — it is app-scoped and a natural co-resident, but it is NOT named in #17's literal scope and it carries the **Riverpod scoped-`dependencies` gotcha** (a Provider that `ref.watch`-es a scoped provider must declare `dependencies: [thatProvider]`). RECOMMEND keeping #17 to the three named providers and tracking `currentUserIdProvider` rehoming as a separate follow-up to avoid scope creep, unless the architect rules otherwise.
- **#16 (T3/T4/T5) — secondary auth telemetry + `is_new_user` typing.** Emit the four secondary funnel events that are **already DECLARED** in `docs/design/07-technical/telemetry-plan.md` but not yet fired: `phone_entry_viewed` (`source`), `phone_validation_failed` (`reason`), `otp_send_requested`, `otp_verification_started` (`method`). Wire them at the correct moments in `phone_entry_controller.dart` / `otp_entry_controller.dart` per the plan's SCR-03 / SCR-04 rows. Decide `signup_otp_submitted` (keep vs rename — PM call). **`is_new_user` typing (T5) — judgement call, do NOT blindly "make it a bool".** `otp_entry_controller.dart:215` logs `'is_new_user': value.isNewUser ? 1 : 0`. Firebase Analytics event parameters are `String | num` only — a Dart `bool` is not a valid Analytics param value — so "int -> bool" is NOT a literal code change. Resolve at kickoff (PM + Flutter Dev): either keep the numeric encoding and correct the **telemetry-plan doc** to say `int (0/1)`, or emit `'true'`/`'false'` as a string — whichever the plan's `bool` column intends. Keep PII out (ADR-0013): `is_new_user` is non-identifying; the phone number / UID are NEVER parameters.
- **#18 (S1/S3/S4/S5) — auth screen-spec alignment.** S1: splash timeout `Duration(seconds: 3)` (`lib/features/auth/presentation/splash_screen.dart:19`) -> `1500 ms` per spec (or, if the PM ratifies 3 s as the intended behaviour, align the SPEC instead — but the audit lists the code as drifted, so the default is to fix the code). S3: phone-entry OTP-send error presentation (inline vs snackbar) — align to the screen spec. S4: live `XXXXX XXXXX` phone formatting on phone entry — the `IndiaPhoneInputFormatter` primitive already exists at `lib/core/widgets/india_phone_input_formatter.dart`; wire it (or explicitly defer with a recorded reason if the architect prefers). S5: OTP resend-exhausted copy (`lib/features/auth/presentation/otp_entry_screen.dart`) — align to the agreed wording. Cross-reference the mockups `docs/design/05-mockups/01-splash-and-onboarding.html` and `02-phone-and-otp.html`.

RECOMMENDED scope (ship this): all four issues in one branch, full close on each. Pair tests with each change (rename: existing tests follow the symbol; relocation: import-only churn; telemetry: assert the new event names + params; screen-spec: widget tests for splash duration, inline error, live formatting, resend copy). Keep the per-feature coverage gate green.

ESCALATION (architect / PM decide at kickoff): (a) `currentUserIdProvider` inclusion in #17 (RECOMMEND defer); (b) the `is_new_user` int-vs-bool resolution (RECOMMEND keep numeric + fix the doc, given the Analytics SDK constraint); (c) `signup_otp_submitted` keep-vs-rename; (d) splash S1 fix-code-vs-fix-spec (RECOMMEND fix code to 1500 ms); (e) whether S4 live formatting ships now or is explicitly deferred. **No new ADR** is expected — provider relocation is a documented architecture alignment, not a new decision. State that explicitly.

Story points: **5** (four issues, ~26-site rename + a new `lib/core/providers/` module + four telemetry events + four screen-spec edits, each with tests). NOT velocity-excluded (this is client feature-adjacent work, unlike the docs PR that follows).

## 2. Numbering / CI title-lint gotcha

The PR title lint (`.github/workflows/pr.yml`) enforces `^(feat|fix|chore|docs|test|refactor|ci|build)(\([a-z0-9_-]+\))?!?: .{1,72}$` — a single-token scope (no commas) and subject <= 72 chars. Suggested title (<= 72 chars): `chore(auth): provider housekeeping + secondary telemetry + spec polish`. Branch: `chore/auth-client-housekeeping` (repo convention: type-prefixed kebab; already created). This PR touches `lib/**/*.dart` + `docs/design/07-technical/telemetry-plan.md` — the lefthook **pre-commit** hooks run `dart format --set-exit-if-changed` and `flutter analyze` on staged files, which call bare `dart`/`flutter` and fail under fvm, so commit with **`fvm exec git commit`** (or put the fvm SDK bin on PATH). Run `dart format .` before committing (CI gates on it; analyze alone does not catch formatter drift). The **pre-push** `coverage-check` gate runs on touched feature folders (here `auth`) — keep coverage >= threshold; push with **`fvm exec git push`**.

## 3. First-PR-that framing (firsts)

- **First of the three consolidated Sprint-2 close-out PRs** (the others: a `docs` bundle for #19/#24/#28, and a `test` bundle for #20/#23).
- **First PR to establish `lib/core/providers/`** as the home for app-scoped providers (M4 architecture alignment).
- **First chore PR to bundle four audit-backlog issues** in a single branch (prior chores were single-issue or feature side-effects).

## 4. Invariants

Read `.github/shared/invariants.md`. No code path in this PR touches money, `simplifiedBalances`, the share sheet, or the Firebase project, so Invariants 1 / 2 / 3 / 4 are untouched by the diff. The one live concern is **telemetry PII** (ADR-0013 / the telemetry-plan rules): the four new events must carry only non-identifying params (`source`, `reason`, `method`) and `is_new_user` must stay non-identifying — the phone number and UID are NEVER event parameters. Keep the boundary-contract greps green.

## 5. Cross-cutting dependencies and risks

- **High-churn but mechanical.** The rename (#15) + relocation (#17) touch many import sites; the risk is a missed reference or a broken Riverpod scoped-`dependencies` list (the documented gotcha: a Provider that `ref.watch`-es a scoped provider must list it in `dependencies`, not the transitive root). Run `fvm flutter analyze lib test` and the full `fvm flutter test` after the moves.
- **Telemetry is declared, not new.** The four events already exist in the telemetry plan, so there is no schema churn beyond marking them implemented and resolving the `is_new_user` typing note. Do not invent new events.
- **Screen-spec polish must preserve behaviour.** Splash 1500 ms must keep the timeout/error transition intact; the inline OTP error must not regress the existing error path; live formatting must not break the validator or the `+91` 10-digit contract.
- **Coverage gate.** The `auth` feature folder is touched, so the pre-push scoped coverage gate runs — add tests alongside each change.

## 6. What this PR must NOT bundle

Carry forward the standing defer list, and specifically do NOT pull in the OTHER two close-out PRs' scope: the **`docs` bundle** (#19 coverage-tracking conventions + PR template, #24 CF PR checklist / Jest config docs, #28 Friends HTML mockup) and the **`test` bundle** (#20 SC1-SC4 coverage tests, #23 RT2 CI step-duration logging + INV2 share-sheet verification + the Flutter integration harness). Also out of scope: the deferred **#22** (dependency major upgrades — Sprint 4+), **#26** (release secrets + DPDP — Sprint 6), **#27** (float/double Invariant-1 hook — Sprint 4+); the Sprint 3 Groups epic (FR-GR-01..07); the `go_router` migration; FR-SR-01/02 Search (SCR-07); and any change to the rules, triggers, schema, or `firestore.indexes.json`. See `docs/sprint-zero/next-three-prs.md` for the full carry-forward set.

## 7. Read before starting

- `.github/copilot-instructions.md`, `.github/shared/invariants.md`, `.github/shared/coding-standards.md` (Conventional Commits; British English; no emojis), `.github/shared/test-strategy.md`.
- The four target issues: [#15](https://github.com/avtansh-code/OneByTwo/issues/15), [#16](https://github.com/avtansh-code/OneByTwo/issues/16), [#17](https://github.com/avtansh-code/OneByTwo/issues/17), [#18](https://github.com/avtansh-code/OneByTwo/issues/18), and the audit source `docs/audits/sprint-1/01-documentation-drift.md` (M1, T3/T4/T5, M4, S1/S3/S4/S5 definitions).
- The code: `lib/features/auth/application/auth_state_provider.dart`, `lib/features/auth/data/{user_repository,phone_auth_repository}.dart`, `lib/features/auth/application/{phone_entry_controller,otp_entry_controller}.dart`, `lib/features/auth/presentation/{splash_screen,phone_entry_screen,otp_entry_screen}.dart`, `lib/core/widgets/india_phone_input_formatter.dart`.
- `docs/design/07-technical/telemetry-plan.md` (SCR-03 / SCR-04 auth-funnel rows) and the mockups `docs/design/05-mockups/{01-splash-and-onboarding,02-phone-and-otp}.html`.
- The "Sprint 2 close-out — consolidated chore PRs (#74+)" section of `docs/sprint-zero/sprint-2-plan.md`.

## 8. Routing and sequencing (per `.github/shared/handoffs.md`)

This is a client-`lib/` chore bundle acting on existing audit-backlog issues; no new story is required. Sequence:

1. **PM** — ratify the screen-spec decisions (S1 fix-code-vs-spec; S3 inline-vs-snackbar; S5 resend copy), the `signup_otp_submitted` keep/rename, and the `is_new_user` int-vs-bool resolution; update `telemetry-plan.md` to mark the four secondary events implemented and to reflect the `is_new_user` typing decision. Hand the ratified decisions to the Architect.
2. **Architect** — ratify the `lib/core/providers/` relocation (and the `currentUserIdProvider` include-vs-defer call), confirm **no new ADR**, and confirm no rules / schema / index / trigger / Cloud Function change. **No Functions Dev is required** (no server-side code). Hand off to the Flutter Dev.
3. **Flutter Dev** — implement all four work-streams: the #15 rename, the #17 relocation, the #16 telemetry + typing, and the #18 screen-spec edits, each with tests. Run `dart format .`, `fvm flutter analyze lib test`, `fvm flutter test`.
4. **QA** — verify green analyze + full test suite, the four new event names/params asserted, zero PII in the new telemetry (boundary-contract grep), the scoped coverage gate met, and each issue's scope fully satisfied; then apply the SS0a #73 -> Merged reconciliation and open the PR with the four `Closes` lines.

## 9. Kickoff directive

Confirm the **`chore(auth)` client-housekeeping bundle** as the next-slot pick with the Architect (the first of the three consolidated Sprint-2 close-out PRs; closes #15 / #16 / #17 / #18 in one client-`lib/` branch), then delegate per section 8. Refuse any attempt to (a) bundle the other two close-out PRs' scope (#19/#24/#28 docs; #20/#23 tests) or the deferred #22/#26/#27; (b) start the Sprint 3 Groups epic, the `go_router` migration, or FR-SR-01/02 Search; (c) touch the rules, triggers, schema, or `firestore.indexes.json`; (d) emit any telemetry parameter that carries the phone number, a UID, or another identifier (ADR-0013); or (e) introduce a new ADR (this is documented architecture alignment, not a new decision). At PR open, reconcile `docs/sprint-zero/sprint-2-plan.md` (PR Tracking + Velocity: #73 -> **Merged** `f9a4c54`, Total unchanged at **132 / 32**, add the #74 row) and `docs/sprint-zero/next-three-prs.md` (#73 IN FLIGHT -> Merged; add #74; flip the close-out candidate to in-flight); open the PR with `Closes #15`, `Closes #16`, `Closes #17`, `Closes #18`.
