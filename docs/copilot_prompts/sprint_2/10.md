1. You are the OneByTwo orchestrator agent. PR #43 (FR-SE-05/06/07 Settle Up flow) has merged and is live in production at `asia-south1`. Both halves of the simplified-debts round-trip are now closed end-to-end from the user's point of view: the read-side (PR #42 Friend Detail screen) and the write-side (PR #43 settlement creation + recompute trigger snapshot stream re-render within NFR-PE-04's 2.5 s P95 budget). Velocity through end of PR #43: 34 SP across 10 PRs in Sprint 2 (sprint-2 plan + next-three-prs both reflect this). We now implement **PR #44: D5 deadline — Node 22 + firebase-functions 7.x** — the dedicated deployment-de-risking PR that closes issues [#39](https://github.com/avtansh-code/OneByTwo/issues/39) and [#40](https://github.com/avtansh-code/OneByTwo/issues/40) ahead of the **2026-10-31** Cloud Functions Node 20 decommission cutoff.

2. The numbering continues from PR #43. The post-PR #38 sequence so far is: #38 (PR), #39 (issue — Node 20 decommissioned 2026-10-31), #40 (issue — `firebase-functions` 6→7), #41 (PR — D5 deadline backlog cross-refs), #42 (PR — FR-FR-04), #43 (PR — FR-SE-05/06/07). PR #44 is therefore the next FEATURE/CHORE PR. The orchestrator should not assume PR #45 == GitHub issue 45 — issues and PRs share the same sequential namespace, so any new issues filed during PR #44 will consume intermediate numbers. Use the labels in `docs/sprint-zero/next-three-prs.md` to track the next-feature-PR vs next-issue mapping.

3. PR #44 is **infrastructure-only** — there is NO user-visible feature work. This is the first Sprint 2 PR that intentionally ships zero behavioural changes to the application surface. The deliverable is the upgrade of the Cloud Functions runtime + SDK matrix to (Node 22 LTS, `firebase-functions@7.x`) so that the next `firebase deploy --only functions` after 2026-10-31 succeeds. Bundling Node 22 + `firebase-functions@7.x` into a single PR keeps the rollback story atomic (a single revert restores the working state); doing them in separate PRs would double the breaking-change reconciliation surface (and the `firebase-functions@7.x` migration may itself depend on Node 22 features).

4. PR #44 has **no mandatory cross-PR bundle** — chore #25 closed in PR #38, the review §R3 + §R4 carry-forwards from PR #42 closed in PR #43, and the only deadline-bound work in the rolling plan is D5 itself which is PR #44's dedicated scope. PR #44 MUST NOT touch any Flutter source file (`lib/**`, `test/features/**`, `test/integration/**` except for confirming the integration test harness still passes against the new runtime). The diff surface is exclusively `functions/**`, `firebase.json`, the four `.github/workflows/*.yml` files that pin `actions/setup-node`, the burndown audit, and the rolling plan.

5. Read before starting:
   - `.github/copilot-instructions.md`
   - `.github/shared/invariants.md` — invariants 1 (paise integers; **no monetary surface changes in PR #44** — the upgrade must not regress how `recomputeSimplifiedBalances` returns paise to the client), 2 (`simplifiedBalances` client-read-only; the trigger remains the sole writer; the upgrade MUST NOT introduce a code path where the new `firebase-functions@7.x` callable signature allows a client to update the field — this is a defence-in-depth check), 3 (system share sheet only — N/A), 4 (single Firebase project — **load-bearing for this PR** because the upgrade is the first thing that touches the deploy path since PR #38; verify `.firebaserc` still contains exactly one project per the CI gate in `.github/workflows/pr.yml`).
   - `.github/shared/coding-standards.md` — TypeScript section. The Cloud Functions PR checklist (region pinning, error-code mapping, transaction usage, idempotency) applies to any code reconciled during the breaking-change sweep.
   - `.github/shared/decision-log.md` — ADR-0011 (Cloud Function module layout — `algorithm.ts` + `function.ts` + `index.ts`; the upgrade must preserve this layout), ADR-0003 (single Firebase project; emulator suite for non-production — the upgrade is verified against the emulator before merge).
   - `docs/patterns/feature-pr-conventions.md` — Cloud Functions PR Conventions section. The five-layer test pyramid (algorithm unit, algorithm property, boundary, rules, integration) must all stay green.
   - `docs/audits/sprint-1/07-bucket-b-burndown.md` rows D5a + D5b — the authoritative tracking entries for this PR's scope.
   - `docs/sprint-zero/next-three-prs.md` — PR #44 default plan section.
   - `https://firebase.google.com/docs/functions/upgrade` — canonical `firebase-functions` 6 → 7 migration notes. The orchestrator MUST consult this before kickoff (fetch via `web_fetch` if the tool is available).
   - `https://cloud.google.com/functions/docs/runtime-support` — canonical Node.js runtime calendar; verifies Node 22 is GA on Cloud Functions Gen 2 at filing time and Node 20 is decommissioned 2026-10-31.
   - `functions/package.json` — current pins: `firebase-functions ^6.1.2`, `firebase-admin ^13.0.2`, `engines.node = "20"`. The upgrade target is `firebase-functions@^7` (or the latest 7.x at PR-kickoff time) and `engines.node = "22"`.
   - `firebase.json` — `functions[].runtime = "nodejs20"` must become `"nodejs22"`. The `flutter` block and emulator port assignments are unrelated and MUST NOT change.
   - `.github/workflows/pr.yml` — five `actions/setup-node@v4` invocations with `node-version: '20'` at lines 116, 150, 301. All three become `'22'`.
   - `.github/workflows/release.yml` — two `actions/setup-node@v4` invocations with `node-version: '20'` at lines 39, 78. Both become `'22'`.
   - `functions/src/index.ts` + every file under `functions/src/**` that imports from `firebase-functions/v2/**` or `firebase-functions/logger` — reconcile any breaking-change drift. The five deployed functions are `healthcheck` (onRequest), `lookupUserByPhoneNumber` (onCall), `recomputeSimplifiedBalances` (onCall), `onExpenseWriteFriendship` (onDocumentWritten), `onSettlementWrite` (onDocumentWritten). All are v2 surfaces.
   - `functions/jest.config.js`, `functions/jest.rules.config.js`, `functions/jest.integration.config.js` — the test runner configs. `firebase-functions-test` may also need an upgrade.

6. Honour the four invariants. Invariants 1 + 2 are read-only for this PR (no behavioural changes to any monetary code path; no new writers of `simplifiedBalances`). Invariant 3 is N/A. Invariant 4 is **load-bearing** — the upgrade must verify the single-project deploy path remains intact (`.firebaserc` unchanged; `firebase.json` `singleProjectMode: true` preserved).

────────────────────────────────────────
PHASE 0 — DEPENDENCY-DAG CHECK
────────────────────────────────────────

0.1  Confirm PR #43 has merged to main and the post-merge state is clean:
     - `lib/features/settlements/` contains both the read-side (PR #42) and the write-side (PR #43) scaffolding; the boundary-contract grep tests pass.
     - `lib/features/friends/presentation/friend_detail_screen.dart` renders the `OBTSettleUpCard` between header and timeline in the `BalanceState.owes` branch.
     - `lib/features/friends/presentation/widgets/friend_detail_timeline.dart` renders the payer-aware `_SettlementRow` labels.
     - `flutter analyze --fatal-infos` + `flutter test` pass on a fresh checkout (794 pass / 24 skipped post-PR-#43).
     - `cd functions && npm run lint && npm run build && npm test` pass on Node 20 / `firebase-functions ^6.1.2` (the current pins — this is the pre-upgrade baseline).
     - `firebase emulators:exec` integration tests pass against the current runtime matrix.

0.2  Walk the dependency DAG for the upgrade:
     - **Node.js LTS calendar:** Node 22 is GA on Cloud Functions Gen 2 (verified against `https://cloud.google.com/functions/docs/runtime-support` at PR-kickoff time). Node 24 LTS is NOT yet GA on Cloud Functions at filing time, so the upgrade target is Node 22.
     - **`firebase-functions` 6.x → 7.x:** consult `https://firebase.google.com/docs/functions/upgrade` for the breaking-change catalogue. Likely impact zones:
         - `onCall` callable signature changes (affects `lookupUserByPhoneNumber`, `recomputeSimplifiedBalances`).
         - `onDocumentWritten` Firestore v2 event-shape changes (affects `onExpenseWriteFriendship`, `onSettlementWrite`).
         - `onRequest` HTTPS function changes (affects `healthcheck`).
         - `HttpsError` import path changes (we import from `firebase-functions/v2/https`).
         - `logger` API changes (we import from `firebase-functions/logger`).
     - **`firebase-functions-test` companion library:** typically pinned to a major version compatible with the parent `firebase-functions` major. If `firebase-functions@7.x` requires `firebase-functions-test@4.x` (or whatever the matching major is), upgrade together.
     - **`firebase-admin` 13.x:** unlikely to need a change for this PR; if the `firebase-functions@7.x` migration notes require it, bundle the upgrade. Otherwise leave at the current pin.
     - **Server-side trigger consumers (NONE in v1.0):** no client-side dependencies because Cloud Functions are infrastructure, not a user-facing surface. The client never imports from `functions/**`. The Flutter `cloud_functions` package (the client-side caller wrapper) is unrelated and MUST NOT be touched.
     - **CI workflow setup-node pins:** `.github/workflows/pr.yml` × 3 invocations and `.github/workflows/release.yml` × 2 invocations. All five must become `'22'` in lockstep with the runtime change. The pin `cache: 'npm'` and `cache-dependency-path: functions/package-lock.json` is unchanged.
     - **Release pipeline:** the deploy step in `.github/workflows/release.yml` performs `firebase deploy --only functions` after the upgrade; if the CLI accepts the new runtime AND all 5 functions migrate cleanly, the upgrade is verified.
     - **Out of scope for PR #44:**
         - Any Flutter source change.
         - Any new feature work — this is a pure infrastructure upgrade PR.
         - Migrating to Node 24 (not GA on Cloud Functions at filing time).
         - Upgrading `firebase-admin` beyond what the `firebase-functions@7.x` migration notes require.
         - Re-architecting the functions module layout (ADR-0011 stands).
         - Touching `firestore.rules`, `firestore.indexes.json`, `storage.rules` (deployed via the same release workflow but no rule changes needed for this PR).
         - The five remaining D-row bucket-B items (D1, D2, D4, D6, D7) — separate Sprint 4+ scope per the burndown.
         - Any change to `docs/copilot_prompts/**` or new ADRs unless the migration uncovers a non-trivial behavioural change requiring one.

0.3  Confirm DoR for the chore story:
     - There is NO existing story file for the D5 upgrade. PM creates one at `docs/sprint-zero/stories/CHORE-d5-runtime-upgrade.md` using the SRS §13.2 chore format (precedent: `docs/sprint-zero/stories/CHORE-post-sprint1-testing-fixes.md`). The chore story explicitly enumerates issues #39 + #40 it closes and the four-invariant applicability assessment (Inv-4 load-bearing).
     - Story points: **3 SP** is the working estimate (predictable shape; reconciliation surface is bounded by 5 functions × 2 SDK majors). If the architect discovers the `firebase-functions@7.x` migration is wider than expected (e.g. requires a major refactor of how the trigger context is consumed), escalate to 5 SP and split into PR #44a (Node 22 only) and PR #44b (`firebase-functions@7.x`). Default: single PR.

0.4  Cross-reference `docs/audits/sprint-1/07-bucket-b-burndown.md` rows D5a and D5b. PR #44 closes both rows by itself. No other Bucket-B items advance.

────────────────────────────────────────
PHASE 1 — STORY READINESS (PM)
────────────────────────────────────────

PM agent creates the chore story `docs/sprint-zero/stories/CHORE-d5-runtime-upgrade.md` using the SRS §13.2 format adapted for infrastructure work (the precedent is `CHORE-post-sprint1-testing-fixes.md`). Story points: **3** (or split per Phase 0.3).

Required Given/When/Then ACs (each MUST be testable):

  - **AC-1 — Node 22 runtime declared.** Given the `functions/package.json` and `firebase.json` files, when the upgrade is applied, then `functions/package.json` `engines.node` reads `"22"` AND `firebase.json` `functions[0].runtime` reads `"nodejs22"`. The two declarations are linked by Firebase tooling — both MUST agree.
  - **AC-2 — `firebase-functions` upgraded to 7.x.** Given the `functions/package.json` dependencies, when the upgrade is applied, then `firebase-functions` is pinned to a 7.x version (e.g. `^7.0.0` or the latest 7.x available at PR-kickoff time). `package-lock.json` is regenerated atomically with the version bump.
  - **AC-3 — CI runners upgraded to Node 22.** Given the five `actions/setup-node` invocations in `.github/workflows/pr.yml` (lines 116, 150, 301) and `.github/workflows/release.yml` (lines 39, 78), when the upgrade is applied, then every `node-version` value reads `'22'`. The `cache: 'npm'` and `cache-dependency-path: functions/package-lock.json` pins are unchanged.
  - **AC-4 — Lint + build + tests pass on the new matrix.** Given the Node 22 + `firebase-functions@7.x` runtime, when `cd functions && npm ci && npm run lint && npm run build && npm test` runs locally, then exit code 0 across every step. Coverage on `functions/src/simplified-debts/function.ts` stays at ≥ 90% (the PR #36 baseline).
  - **AC-5 — Rules tests pass.** Given the upgrade, when `cd functions && npm run test:rules` runs under `firebase emulators:exec`, then every Firestore Security Rules test (friendship + expense-friendship + settlements + simplifiedBalances negative) passes. The rules surface is unchanged so the test outcomes must be identical to the pre-upgrade baseline.
  - **AC-6 — Integration tests pass.** Given the upgrade, when `firebase emulators:exec --only auth,firestore,functions,storage "cd functions && npm run test:integration"` runs, then every integration test exits 0. The `onExpenseWriteFriendship` and `onSettlementWrite` triggers must register and fire under the new runtime AND the new SDK matrix.
  - **AC-7 — Healthcheck deploys and serves.** Given a successful local `cd functions && npm run build`, when the deploy step in `.github/workflows/release.yml` runs against the production project, then all 5 functions migrate cleanly with no invocation gap. Post-deploy, `GET /healthcheck` returns `200 OK { "ok": true, "region": "asia-south1" }`. (This AC is verified in QA's manual smoke matrix; CI does not execute the production deploy on the PR pipeline.)
  - **AC-8 — Reconciled breaking changes are documented.** Given the `firebase-functions` 6 → 7 migration notes, when the upgrade is applied, then the Architect Notes section of the chore story enumerates every breaking-change reconciliation made to `functions/src/**`. If zero changes were required (the 6 → 7 migration is a clean swap for our v2 surfaces), the Architect Notes explicitly state that — the audit story matters.
  - **AC-9 — Invariant 2 preserved.** Given the upgraded `firebase-functions@7.x` callable surface, when the rules-test from PR #37 (a client attempting `update simplifiedBalances`) runs, then the write is REJECTED with `permission-denied`. The upgrade MUST NOT introduce a code path where the new callable signature allows a client to update the field. This is enforced by re-running the existing rules tests; no new test needed.
  - **AC-10 — Invariant 4 preserved.** Given the upgrade, when `.firebaserc` and `firebase.json` are inspected, then `.firebaserc` contains exactly one project (the CI gate in `.github/workflows/pr.yml` enforces this on every PR; the upgrade MUST NOT regress it). `firebase.json` `singleProjectMode: true` is unchanged.
  - **AC-11 (Negative) — No Flutter source touched.** Given the PR #44 diff, when `git diff main --stat -- lib/ test/features/ test/integration/` runs, then the output is empty. This PR is server-side only.
  - **AC-12 (Negative) — No rules / indexes / storage changes.** Given the PR #44 diff, when `git diff main --stat -- firestore.rules firestore.indexes.json storage.rules` runs, then the output is empty.

────────────────────────────────────────
PHASE 2 — ARCHITECT HAND-OFF
────────────────────────────────────────

Architect agent appends `## Architect Notes` to the chore story ratifying:

2.1 **Runtime + SDK matrix.** Node 22 LTS + `firebase-functions@^7.0.0` (or the latest 7.x at PR-kickoff time). `firebase-admin` stays at the current `^13.0.2` unless the migration notes require a bump. `firebase-functions-test` upgrades in lockstep with `firebase-functions` (typically the matching major).

2.2 **Files to touch (exhaustive list — anything outside this set is scope creep):**

   - `functions/package.json` — `engines.node` + `firebase-functions` dependency + (if required) `firebase-functions-test` devDependency.
   - `functions/package-lock.json` — regenerated by `npm install`.
   - `firebase.json` — `functions[0].runtime`.
   - `.github/workflows/pr.yml` — three `node-version: '20'` → `'22'`.
   - `.github/workflows/release.yml` — two `node-version: '20'` → `'22'`.
   - `functions/src/**/*.ts` — IF AND ONLY IF the `firebase-functions@7.x` migration notes require a breaking-change reconciliation. The architect's default assumption is ZERO reconciliations (v2 surfaces are stable across the 6 → 7 boundary for the API patterns we use); if any are needed, list them explicitly here in §2.7.
   - `functions/jest.config.js`, `functions/jest.rules.config.js`, `functions/jest.integration.config.js` — IF the `firebase-functions-test` upgrade requires a test-config change. Same default-zero assumption.
   - `docs/audits/sprint-1/07-bucket-b-burndown.md` — mark D5a + D5b as closed with a ✅ + PR #44 cross-ref.
   - `docs/sprint-zero/sprint-2-plan.md` — PR #44 row in PR Tracking + Velocity tables.
   - `docs/sprint-zero/next-three-prs.md` — roll forward (PR #45 + PR #46 + PR #47 candidates).
   - `functions/README.md` (if it exists) or `docs/design/07-technical/cloud-functions-catalogue.md` — add a one-line note on the runtime + SDK pins.

2.3 **Node 22 LTS choice.** Node 22 is the current GA LTS on Cloud Functions Gen 2 at filing time. Node 24 is NOT yet GA on Cloud Functions, so it is rejected. The choice is conservative — Node 22's LTS window extends well beyond the next plausible Sprint 2 cycle, so this PR's upgrade is good through at least the end of the v1.0 release window.

2.4 **`firebase-functions@7.x` upgrade rationale.** The 6 → 7 major bump is required to clear the CLI deprecation warning on every deploy. The migration notes (per `https://firebase.google.com/docs/functions/upgrade`) MUST be consulted at PR-kickoff time; the architect summarises the breaking changes that affect our v2 surfaces here in §2.7. If the migration notes have moved on by then, the architect substitutes the canonical URL.

2.5 **Atomic upgrade rationale.** Node 22 + `firebase-functions@7.x` ship in the SAME PR for two reasons: (a) the rollback story is a single `git revert` rather than two; (b) the test surface only has to be exercised once on the new matrix. Splitting into two PRs would double the breaking-change reconciliation surface (and the `firebase-functions@7.x` migration may itself depend on Node 22 features for some of its internal modules).

2.6 **Test pyramid execution order.** The architect ratifies that every layer of the test pyramid is exercised on the new matrix BEFORE merge:

   - Layer 1 (algorithm unit, no Firebase): `cd functions && npm test` against `simplified-debts/algorithm.ts` etc.
   - Layer 2 (algorithm property): the `fast-check` property tests under `functions/test/simplified-debts/`.
   - Layer 3 (function boundary, mocked Firestore): the `function.ts` boundary tests for each of the 5 functions.
   - Layer 4 (Firestore Security Rules): `cd functions && npm run test:rules` under `firebase emulators:exec`.
   - Layer 5 (integration, full emulator suite): `firebase emulators:exec --only auth,firestore,functions,storage "cd functions && npm run test:integration"`.

2.7 **Anticipated breaking-change reconciliations.** The architect populates this section with the actual list discovered while reading the `firebase-functions` 6 → 7 migration notes at PR-kickoff time. As of filing this prompt, the default assumption is ZERO reconciliations (the v2 surfaces — `onCall`, `onDocumentWritten`, `onRequest`, `HttpsError`, `logger` — are stable across the 6 → 7 boundary for the API patterns we use). If the migration notes prove this wrong, the architect lists each reconciliation here with the file:line citation, the old API, the new API, and the rationale.

2.8 **No new ADR required.** ADR-0011 (CF module layout) and ADR-0003 (single Firebase project) both stand unchanged. The upgrade is a pure dependency bump; no architectural decisions are revisited.

2.9 **Forward-compatibility note.** Node 22's LTS window means this upgrade is good through at least the v1.0 release. The next foreseeable runtime forcing event is Node 22 deprecation (no announced date at filing time; typically ~3 years after GA, so ~2028+). The architect notes that the next D-row update will be a Node 22 → 24 (or whatever LTS is current) bump on a similar schedule.

────────────────────────────────────────
PHASE 3 — SCOPE GUARDRAILS
────────────────────────────────────────

WHAT GOES IN PR #44:

  Source / configuration:
  - `functions/package.json` — EDIT (engines.node + dependencies).
  - `functions/package-lock.json` — REGENERATE (npm install).
  - `firebase.json` — EDIT (runtime).
  - `.github/workflows/pr.yml` — EDIT (3× node-version).
  - `.github/workflows/release.yml` — EDIT (2× node-version).
  - `functions/src/**/*.ts` — EDIT IF AND ONLY IF the migration requires reconciliation (default: zero edits per §2.7).
  - `functions/jest.*.js` — EDIT IF AND ONLY IF the migration requires a test-config change (default: zero edits).

  Tests:
  - No new test files. The five-layer test pyramid is re-run against the new matrix; all existing tests must pass.
  - If the architect's §2.7 reconciliation list is non-empty, a regression test MUST be added for each reconciliation that changes observable behaviour. (Most v2 surface changes are signature-level only and need no new test — the existing tests cover the behaviour.)

  Documentation:
  - The chore story file from Phase 1 with Architect Notes.
  - `docs/audits/sprint-1/07-bucket-b-burndown.md` — D5a + D5b marked closed with the PR #44 cross-ref.
  - `docs/sprint-zero/sprint-2-plan.md` — PR #44 row added to PR Tracking + Velocity (3 SP, cumulative 37 SP / 11 PRs).
  - `docs/sprint-zero/next-three-prs.md` — roll PR #45 / PR #46 / PR #47 with PR #44's outcome.
  - `docs/design/07-technical/cloud-functions-catalogue.md` (if present) or `functions/README.md` — note the new runtime + SDK pins.

WHAT DOES NOT GO IN PR #44:

  - Any Flutter source change (`lib/**`, `test/features/**`, `test/integration/**` — except for confirming the existing integration test stubs pass against the new runtime).
  - Any new feature work — this is a pure infrastructure upgrade PR.
  - Migrating to Node 24 (not GA on Cloud Functions Gen 2 at filing time).
  - Upgrading `firebase-admin` beyond what the `firebase-functions@7.x` migration notes require.
  - Any change to `firestore.rules`, `firestore.indexes.json`, or `storage.rules`.
  - Any new ADR (ADR-0011 + ADR-0003 stand).
  - The five remaining D-row bucket-B items (D1, D2, D4, D6, D7) — Sprint 4+ scope.
  - The `lookup-user-by-phone-number` rate-limit bug fix (separate later PR; tracked elsewhere).
  - Post-PR #38 cleanup PR items (the three S4 items — separate later PR).
  - Any change to `pubspec.yaml`, `pubspec.lock`, `analysis_options.yaml`, the Android / iOS native shells, or any Flutter dependency.

If an agent suggests any of: "let's bundle the post-PR #38 cleanup since we're already touching CI", "let's add the FR-EX-05 receipt feature since we have a quiet PR slot", "let's bump `firebase-admin` to the latest while we're here", "let's go straight to Node 24", "let's split into two PRs for cleaner review", "let's add a new ADR codifying the upgrade cadence" — refuse. These are scope creep. The single exception: if the `firebase-functions@7.x` migration notes themselves require a `firebase-admin` bump (documented in §2.7), that bump is in scope.

────────────────────────────────────────
PHASE 4 — TEST-FIRST DISCIPLINE
────────────────────────────────────────

PR #44 is an infrastructure upgrade with no new behavioural surface, so test-first is interpreted as **regression-first**: confirm the existing test pyramid passes on the new matrix BEFORE merging. The execution order is:

1. **Snapshot the pre-upgrade baseline:**
   - Run `cd functions && npm ci && npm run lint && npm run build && npm test` on the current Node 20 / `firebase-functions ^6.1.2` matrix. Record the test count + coverage report as the baseline.
   - Run `cd functions && npm run test:rules` and `npm run test:integration` under `firebase emulators:exec`. Record the test count.

2. **Apply the Node 22 + `firebase-functions@7.x` upgrade:**
   - `cd functions && npm install --save firebase-functions@^7` (or the latest 7.x; the architect picks the exact version at kickoff).
   - `cd functions && npm install --save-dev firebase-functions-test@<matching-major>` if required.
   - Edit `functions/package.json` `engines.node` = `"22"`.
   - Edit `firebase.json` `functions[0].runtime` = `"nodejs22"`.
   - Edit the five `node-version: '20'` lines in `.github/workflows/*.yml` to `'22'`.

3. **Re-run every layer of the test pyramid on the new matrix:**
   - `cd functions && npm ci && npm run lint && npm run build && npm test` MUST exit 0.
   - `cd functions && npm run test:rules` under `firebase emulators:exec` MUST exit 0.
   - `firebase emulators:exec --only auth,firestore,functions,storage "cd functions && npm run test:integration"` MUST exit 0.
   - Coverage on `functions/src/simplified-debts/function.ts` MUST stay at ≥ 90% (the PR #36 baseline).

4. **Reconcile any breaking changes:**
   - For each test failure, identify the breaking change. Apply the minimum patch to `functions/src/**` that restores green. Document the reconciliation in the Architect Notes §2.7 with the file:line, old API, new API, and rationale.
   - If a reconciliation changes observable behaviour, add a regression test for that change.

5. **Cross-test the Flutter + functions integration:**
   - `flutter test test/integration/ --tags integration` under `firebase emulators:exec --only auth,firestore,functions,storage` — every Flutter integration test stub that depends on a Cloud Function (FR-FR-01 matching, FR-SE-03/04 trigger, FR-SE-05 settle-up trigger from PR #43) MUST pass on the new matrix.

6. **CI dry-run:**
   - Push the branch and open the PR. The CI pipeline must show ✅ on:
     * `PR Title Lint`
     * `Flutter Lint & Test`
     * `Cloud Functions Lint & Test`
     * `Integration Tests (Emulator Suite)`
     * `Coverage Gate (SRS 5.7)`
     * `Build Android (debug)`
     * `Build iOS (no signing)`

Coverage:
   - `functions/src/**`: maintain or improve the PR #36 baseline (≥ 90% branch on `simplified-debts/function.ts`).
   - `lib/**`: unchanged.

────────────────────────────────────────
PHASE 5 — EXECUTION PROTOCOL
────────────────────────────────────────

Mirror PR #43 patterns with the D5-specific commits.

 1. PM creates story `docs/sprint-zero/stories/CHORE-d5-runtime-upgrade.md`. Commits as `docs(stories): D5 runtime upgrade chore (closes #39 #40)`.
 2. Architect appends `## Architect Notes` (§2.1–§2.9). Commits as `docs(stories): D5 runtime upgrade architect notes`.
 3. DevOps applies the upgrade in **one atomic commit** because the runtime + SDK + CI pins must be consistent (a half-upgrade fails CI):
    - `chore(functions): upgrade to Node 22 + firebase-functions 7.x (closes #39 #40)`
    - Diff scope: `functions/package.json`, `functions/package-lock.json`, `firebase.json`, `.github/workflows/pr.yml`, `.github/workflows/release.yml`.
 4. Functions-dev applies any breaking-change reconciliations in a follow-up commit (only if §2.7 is non-empty):
    - `fix(functions): reconcile firebase-functions 7.x breaking changes`
    - Diff scope: `functions/src/**`. Include the cited migration notes in the commit body.
 5. QA runs the manual smoke matrix:
    - Start emulators (`scripts/dev/start-emulators.sh`).
    - Sign in as a user; create a friendship; add an expense; record a settlement.
    - Verify the `onExpenseWriteFriendship` trigger fires (`simplifiedBalances` updates within the 2.5 s P95 window).
    - Verify the `onSettlementWrite` trigger fires (header pill flips to "Settled up" after a full settlement; reduces after a partial settlement).
    - Tap into the friend; verify the FAB Add Expense flow + the OBTSettleUpCard flow both work end-to-end.
    - Verify `lookupUserByPhoneNumber` callable (the contact-picker friend-add flow) returns a match for a known phone number.
    - Verify the `recomputeSimplifiedBalances` callable (debug-only; the production caller is the trigger) returns successfully when invoked directly via the Functions emulator UI.
 6. Update plan and roll-forward:
    - `docs/sprint-zero/sprint-2-plan.md` (PR #44 status, velocity #11, cumulative 37 SP).
    - `docs/sprint-zero/next-three-prs.md` (roll PR #45 / PR #46 / PR #47 with PR #44's outcome).
    - `docs/audits/sprint-1/07-bucket-b-burndown.md` (D5a + D5b marked closed with PR #44 cross-ref).
    - `docs/design/07-technical/cloud-functions-catalogue.md` (if present) — update the runtime + SDK pins.
    - Commits as `docs(plan): D5 runtime upgrade shipped, prep PR #45`.
 7. PR opened by DevOps (or Functions-dev if §2.7 was non-empty).
    Title: `chore(functions): upgrade to Node 22 + firebase-functions 7.x`
    Body must:
      - Cite `docs/patterns/feature-pr-conventions.md` Cloud Functions section.
      - Confirm Invariant 4 (single Firebase project preserved; CI gate green).
      - Confirm Invariant 2 (rules-test for client write to `simplifiedBalances` still rejects).
      - Confirm the five-layer test pyramid passes on the new matrix (with the test counts cited from the pre / post-upgrade baseline).
      - Enumerate every breaking-change reconciliation made (or explicitly state "zero reconciliations required" if §2.7 was empty).
      - Note the production deploy verification plan (the release workflow runs `firebase deploy --only functions` on tag).
      - State explicitly the deferred items: PR #45 candidates (lookup-user-by-phone-number rate-limit fix, FR-EX-05 Receipt, FR-EX-06 Edit/Delete, FR-SE-09 Send Reminder, FR-SE-08 dedicated full-history screen, post-PR #38 cleanup), the five remaining D-row bucket-B items (D1, D2, D4, D6, D7 — Sprint 4+).
      - End with `Closes #39. Closes #40. Next PR: PR #45 — TBD per architect's call at kickoff.`

────────────────────────────────────────
PHASE 6 — DEFINITION OF DONE GATE
────────────────────────────────────────

Walk the DoD checklist. Particular emphasis:

  - DoD §2: every layer of the test pyramid green on the new matrix; coverage gate green (≥ 90% branch on `simplified-debts/function.ts`; existing thresholds maintained).
  - DoD §3: QA sign-off referencing the manual smoke matrix from Phase 5 step 5 with screenshots of (a) the friend-add flow succeeding, (b) the add-expense flow with the trigger firing, (c) the settle-up flow with the snapshot stream re-rendering, (d) the post-deploy healthcheck `200 OK`.
  - DoD §7: invariant compliance.
    * **Inv-1:** N/A (no monetary surface changes).
    * **Inv-2:** rules-test for client write to `simplifiedBalances` still rejects with `permission-denied` (re-run as part of `npm run test:rules`).
    * **Inv-3:** N/A.
    * **Inv-4:** `.firebaserc` contains exactly one project (CI gate enforces); `firebase.json` `singleProjectMode: true` preserved.
  - DoD §8: documentation updated. Story file with Architect Notes. Sprint plan + next-three-prs updated. Burndown D5a + D5b marked closed. Cloud Functions catalogue (if present) updated.
  - DoD §9: deploy verification. The release pipeline executes `firebase deploy --only functions` on tag; QA verifies the five functions migrate cleanly with no invocation gap. Use the post-deploy `GET /healthcheck` as the canary signal.

QA posts the explicit DoD §3 sign-off. Flutter app should be re-tested locally (`flutter test` + `flutter analyze --fatal-infos`) before merge to confirm no client-side regressions — the upgrade should be Flutter-invisible.

**Pre-push CI hygiene:** run `cd functions && npm run lint && npm run build && npm test` AFTER `dart format .` AND BEFORE `git push`. The Cloud Functions lint job is the first CI gate after PR Title Lint; failing locally here saves a CI roundtrip.

────────────────────────────────────────
PHASE 7 — UPDATE THE ROLLING PLAN AND BURNDOWN
────────────────────────────────────────

PM agent updates:
  - `docs/sprint-zero/sprint-2-plan.md` — PR #44 status (merged), velocity data point #11 (3 SP, total now 37 SP across 11 PRs).
  - `docs/sprint-zero/next-three-prs.md`:
      * PR #45: **TBD** per architect's call at kickoff. Top candidates (in rough priority order):
          - `lookup-user-by-phone-number` rate-limit doc-path bug fix (5 skipped integration tests blocked on this).
          - FR-EX-05 Receipt attachment (pairs with Storage rules R7-R8 from Bucket-B).
          - FR-EX-06 Edit / delete expense (natural follow-up to PR #38; closes a usability gap PR #42 surfaced).
          - FR-SE-09 Send Reminder (closes the OBTSettleUpCard receiving-direction omission from PR #43 §2.5).
          - FR-SE-08 dedicated `/settlements/history` full-screen (P0; in-timeline rows from PR #42 satisfy v1.0).
          - Post-PR #38 cleanup PR (3 S4 items — pure docs / test-description fixes).
      * PR #46: TBD per PR #45 outcome.
      * PR #47: TBD per PR #46 outcome.
  - `docs/audits/sprint-1/07-bucket-b-burndown.md` — D5a + D5b marked closed with the PR #44 cross-ref. Remaining D-row items (D1, D2, D4, D6, D7) stay open under umbrella #22 for Sprint 4+.

Commits as `docs(plan): D5 runtime upgrade shipped, prep PR #45`.

────────────────────────────────────────
GUARDRAILS
────────────────────────────────────────

If during this PR an agent proposes:
  - "Let's bundle the post-PR #38 cleanup since we're already touching CI" → REFUSE. The cleanup is 3 S4 items (stale event names in 3 design docs; splitter test cap labels; missing `// TODO(SCR-08)` comment); none of them are deadline-bound. Keep PR #44 atomic.
  - "Let's bump `firebase-admin` to the latest while we're here" → REFUSE unless the `firebase-functions@7.x` migration notes require it (in which case bundle and document in §2.7). Otherwise it doubles the breaking-change surface for no deadline reason.
  - "Let's go straight to Node 24 (it's surely GA by now)" → CHALLENGE. Verify against `https://cloud.google.com/functions/docs/runtime-support` at PR-kickoff time. If Node 24 is GA on Cloud Functions Gen 2 AND has at least 18 months of LTS runway, ratifying the choice in §2.3 is acceptable. If unsure, stay on Node 22 (the more conservative choice).
  - "Let's split into PR #44a (Node 22) + PR #44b (firebase-functions 7.x) for cleaner review" → REFUSE per §2.5. The atomic-rollback property is more valuable than the cleaner-diff property; bundle.
  - "Let's add an ADR codifying the runtime-upgrade cadence" → DEFER. Not blocking. Open an issue for the ADR if the architect wants to enshrine the cadence; this PR ships the upgrade.
  - "Let's add FR-EX-05 / FR-EX-06 / FR-SE-09 / FR-SE-08 since we have a quiet PR slot" → REFUSE. Each is a P0/P1 feature with its own story, ACs, and test surface. They belong in PR #45+.
  - "Let's defer this PR until after the next feature PR — the deadline is months away" → REFUSE. The deadline is 2026-10-31; deferring past mid-September 2026 risks burning the buffer. The rolling plan slots D5 as the default PR #44 for this reason. Sprint 2 cannot ship a feature PR that touches `functions/**` (e.g. FR-SE-09 Send Reminder requires FCM + a new Cloud Function — see `docs/design/07-technical/cloud-functions-catalogue.md`) without first clearing the deprecation backlog.
  - "Let's touch `pubspec.yaml` to upgrade `cloud_functions` while we're at it" → REFUSE. `cloud_functions` is the Flutter client wrapper; it is unrelated to the Cloud Functions runtime + SDK. Track separately if a deprecation warning surfaces; do not bundle.
  - "Let's add a new test that exercises the Node 22 + firebase-functions 7.x matrix specifically" → CHALLENGE. The existing test pyramid IS the matrix exercise — every test runs on the new matrix in CI. A test that "exercises the new matrix" with no behavioural assertion is theatre. If the migration notes surface a behavioural change, add a regression test for THAT change instead.
  - "Let's silently downgrade if anything breaks" → REFUSE. The deadline is hard; downgrading just defers the problem by one PR cycle. Reconcile the breaking change and document it in §2.7.

If something genuinely surprises (e.g., the `firebase-functions@7.x` migration notes require a `firebase-admin` bump that itself has incompatible changes, or Node 22 is not yet GA on Cloud Functions Gen 2 at PR-kickoff time, or the rules-test for Invariant 2 starts failing after the upgrade indicating the new SDK opens a write path), STOP and surface the friction. PR #44 closes a deployment deadline; get it right rather than fast.

Begin with Phase 0 — verify the dependency DAG and post-merge state of PR #43, then consult `https://firebase.google.com/docs/functions/upgrade` and `https://cloud.google.com/functions/docs/runtime-support` for the canonical migration + LTS facts before kickoff.
