1. You are the OneByTwo orchestrator agent. PR #46 (FR-EX-06 — edit / delete an existing expense, friendship context) has merged (squash commit `4b24652`) and the expense lifecycle (create / edit / soft-delete) now closes end-to-end on the friendship axis: PR #38 added the create-bottom-sheet, PR #46 added the bottom-sheet edit-mode + the dedicated Expense Detail screen + the reusable `OBTConfirmationDialog` widget, and the `onExpenseWriteFriendship` trigger from PR #36 recomputes `simplifiedBalances` on all three branches. Sprint 2 velocity through end of PR #46: **45 SP across 13 PRs**. The CI integration-tests step now runs `npm run test:rules` in a dedicated `firebase emulators:exec --only firestore,storage` session (PR #46 ratified architect chore-D5 §2.7 — separates the rules-test layer from the trigger-loaded emulator session so the 409 transaction-lock-timeout flake is gone). We now implement **PR #48: FR-EX-05 — Receipt attachment** in the friendship context. This is the natural P1 follow-on: the Expense Detail screen built in PR #46 is the canonical viewing surface for receipts; the bottom-sheet edit-mode generalises to a Step-3 attachment slot; and Storage rules R7-R8 (long-open Bucket-B items) finally land. FR-EX-05 is **P1** in the SRS (§4.5 line 210); FR-EX-07 (activity feed, P0) and FR-SE-09 (send reminder, P1) sit one slot behind and remain PR #49+ candidates.

2. The numbering continues from PR #46 — and **PR #47 is taken by an ISSUE**, not a PR. PR #46 review filed `https://github.com/avtansh-code/OneByTwo/issues/47` ("Firestore rules: tighten friendship-expense update/delete to creator-only") — a 2 SP defence-in-depth chore deferred to the Sprint 3 hardening sweep. So the post-PR-#46 sequence so far is: #46 (PR — feat, merged `4b24652`), #47 (issue — OPEN, rules-hardening). PR #48 is the next FEATURE PR. The orchestrator should not assume PR #49 == GitHub issue 49 — issues and PRs share the same sequential namespace, so any new issues filed during PR #48 will consume intermediate numbers. **Pre-merge CI title-lint gotcha (verified PR #45, re-verified PR #46):** the workflow regex `[a-z0-9_-]+` rejects comma-separated scopes — use a single-token scope (e.g. `feat(expenses)`), not `feat(expenses,storage)`.

3. PR #48 is the **first PR that uploads anything to Firebase Storage from the expense feature**. The seams are already in place:
   - `ExpenseDoc` already declares `receiptUrl: string | null` on the create-map (`lib/features/expenses/domain/expense_doc.dart:119` writes `'receiptUrl': null`). PR #48 populates it when the user attaches a receipt.
   - `firestore.rules` already allows `(!('receiptUrl' in data) || data.receiptUrl == null || data.receiptUrl is string)` at line 205 — no rules change required on the Firestore side.
   - The schema doc at `docs/design/07-technical/firestore-schema.md` lines 298–313 spells out the canonical Storage layout: path `receipts/{contextType}/{contextId}/{expenseId}`, max 10 MB, JPEG/PNG only, readable + writable by members of the parent context.
   - `pubspec.yaml` already pins `firebase_storage: ^13.3.0` (line: "Receipt and avatar uploads") and `image_picker: ^1.1.2` — no new dependencies.
   - `lib/features/auth/data/image_picker_service.dart` already abstracts `ImagePicker` with a `pickFromGallery` + `pickFromCamera` API used by the avatar flow. PR #48 reuses (and at architect's call extracts to `lib/core/services/`).
   - The Expense Detail screen at `lib/features/expenses/presentation/expense_detail_screen.dart` (PR #46) is the read-side viewing surface — receipt thumbnail goes there when `receiptUrl != null`.
   - The bottom sheet at `lib/features/expenses/presentation/add_expense_bottom_sheet.dart` currently runs 2 steps; the header label is `'Add Expense (N/2)'` / `'Edit Expense (N/2)'`. PR #48 adds the third step per SCR-21 and the label becomes `'Add Expense (N/3)'` / `'Edit Expense (N/3)'`.
   - **No server-side work is required for the upload itself.** The orphan-cleanup Cloud Function (SRS schema doc line 312 — "Files under `receipts/` that are not referenced by any expense document's `receiptUrl` field shall be deleted after 90 days") is FUTURE work and out of scope; architect's call at §2.x whether to file a tracking issue.

4. PR #48 has **no mandatory cross-PR bundle**. PR #48 must NOT bundle FR-EX-07 (activity feed — separate P0 story; the trigger already emits activity entries; PR #48 trusts it), FR-SE-09 (send reminder — separate P1 story; FCM dependency), FR-EX-02 (group-context expenses — Sprint 3 groups epic; FR-EX-05's Storage rules are written defensively to cover group context too, but the UI ships friendship-only), the deferred FR-EX-06 concurrent-edit detection (separate operational-hardening PR), issue #47 rules-hardening (separate small chore PR), the rate-limit transaction race refactor (PR #45 §2.2 deferred), or any D-row Bucket-B item (D1, D2, D4, D6, D7 stay on issue #22 for Sprint 4+). PR #46 closed cleanly and merged — there are no review-carry-forward items from PR #46 to action in PR #48.

5. Read before starting:
   - `.github/copilot-instructions.md`
   - `.github/shared/invariants.md` — invariant 1 (paise integers — N/A for the upload path; the receipt URL is a string, no monetary surface), invariant 2 (`simplifiedBalances` client-read-only — PR #48 must NOT write to that field; receipt-attached / receipt-removed are pure metadata mutations and the existing trigger STILL re-runs `recomputeSimplifiedBalances` on every expense write because the trigger fires on any `update`), invariant 3 (system share sheet — N/A; no sharing surface), invariant 4 (single Firebase project — defence-in-depth re-check; the new Storage rules MUST target the same single project).
   - `.github/shared/coding-standards.md` — Dart, Riverpod, widget-test, and Conventional Commits rules. The Cloud Functions section is **N/A for PR #48** (no `functions/src/**` changes; the orphan-cleanup function is filed as a follow-up issue, not implemented).
   - `.github/shared/decision-log.md` — ADR-0002 (paise integers; N/A for receipt URL), ADR-0006 (Riverpod state management), ADR-0007 (feature-first folder layout), ADR-0013 (PII / telemetry hashing — every `expense_id` parameter in PR #48 telemetry MUST be SHA-256 truncated via `hashId()` from `lib/core/telemetry/event_id_hash.dart`; the receipt URL itself is NOT PII per se but the storage path embeds `expenseId` and SHOULD NOT be logged in raw form).
   - `docs/patterns/feature-pr-conventions.md` — Flutter Testing Layers, Boundary-Contract Discipline, Telemetry-Event Discipline. The `formatInrFromPaise` grep contract continues to apply (PR #48 must not introduce any `.toDouble()` / `/100` / `double` declarations even on the receipt-upload code path).
   - `docs/OneByTwo_Requirements_Spec.md` — section 4.5 line 210 (FR-EX-05 = P1: "Users shall be able to attach a receipt image to an expense (camera or gallery), stored in Firebase Storage."), section 4.8 (Firebase Storage usage and lifecycle), section 5.6 (Add Expense flow — Step 3 was previously labelled "(deferred)" in the FR-EX-01 architect notes and is now reactivated), section 5.7 (NFR-PE-04 P95 budget — uploads count toward the budget; the sheet must still resolve to Success within 2.5s on the 95th percentile; for receipts > a few hundred KB this is a stretch and the architect's call at §2.x is whether to compress more aggressively), section 5.10 (telemetry funnel — adds the Step 3 events listed in SCR-21 §Telemetry), section 7.3 (Invariants 1 + 2 — Invariant 1 is N/A for the receipt path because the receiptUrl is a string; Invariant 2 negative-guard load-bearing as always), section 7.5 (Storage rules surface — PR #48 writes `storage.rules` for the first time since avatars; the friendship-context rules ship now, the group-context rules ship in Sprint 3 — architect's call at §2.x whether to write both predicate-blocks now to avoid a Sprint 3 follow-up).
   - `docs/design/06-screen-specs/19-22-expenses.md` — **SCR-21 Add Expense — Receipt and Confirm** (lines 293–395), the authoritative spec. Cover the full SCR-21 surface: receipt picker section (Take Photo + From Gallery buttons), thumbnail preview, replace / remove affordances, summary card, 6 telemetry events (lines 357–362), 6 states (lines 333–342). The wireframe-vs-spec discrepancy on "single scrollable step vs separate steps" (line 392) — the SCR resolves to a single Step 3 with the receipt picker on top and the summary card below; PR #48 ships this resolution.
   - `docs/design/06-screen-specs/19-22-expenses.md` — SCR-22 §Components Used "All SCR-21 components" line 429 — confirms the edit flow MUST surface the existing receipt thumbnail and allow replace / remove. PR #46 deferred this because SCR-21 was unbuilt; PR #48 closes the loop. The "Changed-field indicator" semantics from SCR-22 §449 also apply to a changed `receiptUrl`.
   - `docs/design/04-wireframes/expense-flow.md` — wireframe baseline; the receipt step is wireframed as a dedicated screen but the SCR collapses it with the summary into one Step 3.
   - `docs/design/02-design-system/components.md` — items used: image picker area (custom — not a design-system component; inline render is acceptable for the first iteration), receipt thumbnail (custom; inline), summary card (custom; inline). **No new design-system extractions are mandated by SCR-21.** Architect's call at §2.x whether to (a) extract an `OBTReceiptThumbnail` widget on first use, or (b) inline and extract later when the Expense Detail screen renders the same thumbnail (mirroring `OBTConfirmationDialog`'s PR #46 extract-on-first-use precedent). Recommend **(b) inline first** — the Expense Detail and Step 3 use sites have different sizing and tap behaviours; extracting prematurely couples them.
   - `docs/design/07-technical/firestore-schema.md` lines 298–313 — Storage layout for receipts. The path `receipts/{contextType}/{contextId}/{expenseId}` is the architectural contract; the Storage rules at §2.x MUST enforce it. The orphan-cleanup lifecycle (line 312) is documented but NOT implemented in PR #48.
   - `storage.rules` — currently has the `avatars/{userId}` rule (the only non-default rule). PR #48 adds the friendship-receipts rule per SRS schema:
     ```
     match /receipts/friendships/{friendshipId}/{expenseId} {
       allow read, write: if request.auth != null
         && request.auth.uid in
            firestore.get(/databases/(default)/documents/friendships/$(friendshipId)).data.memberIds
         && request.resource.size < 10 * 1024 * 1024
         && request.resource.contentType.matches('image/(jpeg|png)');
     }
     ```
     **Architect's call at §2.x** whether the group-context rule (`receipts/groups/{groupId}/{expenseId}`) ships in the same PR (recommended: YES — same predicate shape; same review surface; closes R7/R8 in one shot; the UI surface stays friendship-only but the rules surface is defensive). The `firestore.get()` predicate is the canonical pattern from the existing Firestore `friendships` rules.
   - `firestore.rules` — unchanged. The existing `(!('receiptUrl' in data) || data.receiptUrl == null || data.receiptUrl is string)` predicate at line 205 covers the receipt URL string.
   - `docs/design/07-technical/cloud-functions-catalogue.md` — section on `onExpenseWriteFriendship`. The trigger re-fires on every expense `update`, so a receipt attach / remove re-runs `recomputeSimplifiedBalances` even though the balances themselves don't change. This is harmless but produces unnecessary log noise; architect's call at §2.x whether to file a follow-up "trigger should skip recompute when the update touches only `receiptUrl` + `updatedAt`" optimisation issue. Recommend YES — file but defer.
   - `docs/design/07-technical/telemetry-plan.md` — confirm the canonical event names match SCR-21 (`expense_step3_opened`, `expense_receipt_attached`, `expense_receipt_removed`, `expense_save_succeeded` already exists and gains the `has_receipt: true` parameter value, `expense_save_failed` already exists and gains the `error_code` parameter, `expense_step3_abandoned`). **The `expense_save_succeeded` payload schema must extend to carry `receipt_size_bytes` (only when `has_receipt: true`) per SCR-21 line 360** — architect's call at §2.x whether to add a new constant `paramReceiptSizeBytes` to the constants module or to widen the existing `paramAmountRange` analogue.
   - `docs/design/03-architecture/extension-points-register.md` — ARCH-EXT-02 (currency INR — unchanged), ARCH-EXT-07 (`source: 'manual'` — unchanged), ARCH-EXT-03 (`recurringRule` — stays omitted). No new extension points are introduced by PR #48.
   - `lib/features/expenses/` — **PR #38's + PR #46's full feature folder is the architectural blueprint** PR #48 extends in-place:
     * `lib/features/expenses/presentation/add_expense_bottom_sheet.dart` — the bottom-sheet host. PR #48 adds the Step 3 hand-off when the user taps "Next" on Step 2; the header label flips from `'(N/2)'` to `'(N/3)'`; the `SuccessAction.editSaved` and `SuccessAction.createSaved` snackbar messages stay unchanged (the receipt is part of the same atomic save).
     * `lib/features/expenses/presentation/steps/step_2_split_and_payer.dart` — "Save" CTA becomes "Next" when the architect-elected flow is 3 steps; the actual save fires from Step 3.
     * **NEW** `lib/features/expenses/presentation/steps/step_3_receipt_and_confirm.dart` — the SCR-21 surface. Receipt picker on top, summary card on the bottom, "Save Expense" / "Save Changes" CTA at the bottom. State machine extension: the controller adds `Uploading` to the sealed state hierarchy (architect's call at §2.x whether to extend `AddExpenseState` with a new `Uploading` variant or fold uploading into the existing `Saving` state with a sub-discriminator — recommend **extend with `Uploading`** because the upload precedes the save and the UI surface differs).
     * `lib/features/expenses/application/add_expense_controller.dart` — add `setReceipt(XFile?)`, `removeReceipt()`, `pickReceiptFromCamera()`, `pickReceiptFromGallery()`, `_uploadReceipt(XFile)`. The `save()` method becomes a two-phase chain: (1) if `draft.receiptFile != null && (isCreateMode || receiptChanged)`, upload to `receipts/friendships/{fid}/{eid}` and obtain the download URL; (2) then write the Firestore document (create or update) with the URL populated.
     * `lib/features/expenses/domain/expense_draft.dart` — add `final XFile? receiptFile;` and `final String? existingReceiptUrl;` (for the edit flow's pre-fill). The `toCreateMap()` and `toUpdateMap()` on `ExpenseDoc` already account for `receiptUrl` (the create-map writes `null`; the update-map adds the key on the `_changedFields.contains('receiptUrl')` branch — verify the existing implementation).
     * `lib/features/expenses/domain/expense_doc.dart` — extend `fieldReceiptUrl = 'receiptUrl';` constant; extend `toUpdateMap` to write the new URL when in `_changedFields`; extend `fromMap` to parse the URL (read-side).
     * `lib/features/expenses/data/expense_repository.dart` — N/A (the upload is a Storage operation, not a Firestore operation; the repository's `updateExpense` / `createExpense` already accept arbitrary maps). The new `ReceiptStorageService` lives in `lib/features/expenses/data/receipt_storage_service.dart`.
     * **NEW** `lib/features/expenses/data/receipt_storage_service.dart` — thin abstraction over `FirebaseStorage`: `Future<String> uploadFriendshipReceipt({required String friendshipId, required String expenseId, required XFile file})` returns the download URL; `Future<void> deleteFriendshipReceipt({required String friendshipId, required String expenseId})` removes the file (called when the user removes a receipt during edit). Mirrors the `ExpenseStore` abstract / `FirestoreExpenseStore` concrete pattern.
     * `lib/features/expenses/application/expense_telemetry.dart` — add 4 new event constants (`step3Opened`, `receiptAttached`, `receiptRemoved`, `step3Abandoned`) + extend `saveSucceeded` payload to include `has_receipt: true|false` and `receipt_size_bytes` (when receipt attached). The `expense_save_succeeded` + `expense_save_failed` constants themselves stay unchanged.
     * `lib/features/expenses/presentation/expense_detail_screen.dart` — extend the body to render a receipt thumbnail when `doc.receiptUrl != null`. Tap on thumbnail opens a fullscreen viewer (architect's call at §2.x for viewer complexity — recommend a simple `Dialog` with `InteractiveViewer + Image.network` for v1.0; no zoom-gesture polish).
     * `lib/features/expenses/application/expense_detail_provider.dart` — N/A (the existing fetch returns the doc with `receiptUrl` already parsed via `ExpenseDoc.fromMap`).
   - `lib/features/auth/data/image_picker_service.dart` — `ImagePickerService` abstraction. **Architect's call at §2.x** whether to (a) extract to `lib/core/services/image_picker_service.dart` so both auth + expenses depend on the core copy, or (b) leave the auth one in place and have expenses import from `lib/features/auth/data/`. Recommend **(a) extract to `lib/core/services/`** — the auth feature owning the picker for the receipt flow is the wrong dependency direction. Update the auth feature to import from core.
   - `lib/core/widgets/dialogs/obt_confirmation_dialog.dart` — PR #46 extract. Reused (if needed) for the "Remove receipt?" confirmation flow. Architect's call at §2.x whether removing a receipt during edit requires confirmation or is one-tap.
   - `functions/test/storage-rules/avatars.test.ts` — existing pattern for storage-rules tests. PR #48 adds `functions/test/storage-rules/receipts.test.ts` mirroring the structure: minimum 8 tests covering authenticated member upload (succeeds), non-member upload (fails), unauthenticated upload (fails), oversized upload (fails — 10 MB+ rejects), wrong MIME type (fails — text/plain rejects), authenticated member read (succeeds), non-member read (fails), unauthenticated read (fails). The architect's call at §2.x is whether to also include group-context tests (recommended: YES if the group rule ships in PR #48 per §2.x).
   - `firestore.rules` — unchanged. **Negative scope guard** — PR #48 must NOT touch this file.
   - `docs/audits/sprint-1/07-bucket-b-burndown.md` lines 136 + 187 + 232 — **R7-R8 Storage rules test gaps** are explicitly listed as Sprint 2 chore. PR #48 closes BOTH R7 and R8 (architect's call at §2.x whether to also close any Bucket-B receipt-adjacent items; recommend a clean two-item close for R7-R8 only).
   - `scripts/dev/start-emulators.sh` — the canonical wrapper. The new storage-rules tests need the Storage emulator on 9199; the existing local-dev setup already starts it.
   - `.github/workflows/pr.yml` — `Run Firestore Security Rules tests (no functions loaded)` step (PR #46 split). The new `receipts.test.ts` runs in the same dedicated `--only firestore,storage` session as the existing `avatars.test.ts` — no workflow change needed.
   - **CI hygiene** — `dart format .` MUST be run before push because CI runs Flutter 3.44.1 while local toolchains may be older. Run `dart format .` as the final pre-push gate after `flutter analyze --fatal-infos`.

6. Honour the four invariants. Invariant 1 (paise integers) is **N/A on the receipt path itself** — the upload pipeline carries an `XFile` and emits a `String` URL; no monetary surface; the `formatInrFromPaise` boundary-contract grep continues to walk `lib/features/expenses/**` and any new files PR #48 introduces (`receipt_storage_service.dart`, `step_3_receipt_and_confirm.dart`) are auto-covered. Invariant 2 (`simplifiedBalances` server-only) — load-bearing negative guard as always; the upload + the Firestore `update` of `receiptUrl` must never touch `simplifiedBalances`; the existing trigger handles the (no-op) recompute. Invariant 3 N/A. Invariant 4: no second Firebase project — the receipt upload targets `gs://onebytwo-avtanshgupta.appspot.com` (the single production bucket); local-dev tests target the emulator on 127.0.0.1:9199 (single emulated bucket).

────────────────────────────────────────
PHASE 0 — DEPENDENCY-DAG CHECK
────────────────────────────────────────

0.1  Confirm PR #46 has merged to main and the post-merge state is clean:
     - `git log --oneline -1` on `main` shows `4b24652 feat(expenses): FR-EX-06 edit and delete expense (friendship) (#46)`.
     - `lib/features/expenses/presentation/expense_detail_screen.dart` exists and renders the AppBar Edit + Delete actions; the file body has no `receiptUrl` rendering yet (PR #48 adds it).
     - `lib/core/widgets/dialogs/obt_confirmation_dialog.dart` exists (PR #46 extract; reused by PR #48 if the architect elects).
     - `lib/features/expenses/domain/expense_doc.dart` declares the `fieldReceiptUrl` candidate constant? — if not, add it.
     - `flutter analyze --fatal-infos` + `flutter test` exit 0 (883 pass / 27 skipped post-PR-#46).
     - `cd functions && npm run lint && npm run build && npm test` exit 0 (9 suites / 100 tests pass).
     - `cd functions && npm run test:rules` exit 0 (7 suites / 153 tests pass).
     - `dart format --set-exit-if-changed .` exits 0.
     - `.firebaserc` contains exactly one project (Invariant 4 CI gate green).
     - Issue #47 (rules-hardening for non-creator update/delete) is OPEN and unchanged — PR #48 does NOT close it.

0.2  Walk the dependency DAG for FR-EX-05 (friendship context only — group context's UI surface belongs to the Sprint 3 groups epic; the group-context Storage rule MAY ship now per architect §2.x but the UI does not):
     - **Storage side (consumer of PR #48's writes):** `gs://onebytwo-avtanshgupta.appspot.com` is the production bucket. Path: `receipts/friendships/{friendshipId}/{expenseId}`. PR #48 is the first writer of this path tree from a client. The `firestore.get()` predicate in `storage.rules` reads the parent `friendships/{friendshipId}` doc — this cross-collection rule read is a non-trivial first for the Storage rules; verify the predicate works against the emulator.
     - **Firestore side:** unchanged. `firestore.rules` already accepts `receiptUrl: string | null`. The existing `onExpenseWriteFriendship` trigger re-fires on the `update` that populates `receiptUrl`; the trigger does (no-op) recompute work but no balance change. The `lastActivityAt` monotonicity guard is unchanged.
     - **Client read side:** `expenseDetailProvider` already returns the parsed `ExpenseDoc` (with `receiptUrl` now populated post-PR-#48). The Expense Detail screen renders the thumbnail when present.
     - **Image-picker side:** `lib/features/auth/data/image_picker_service.dart` exists. Architect's call at §2.x — extract to `lib/core/services/` (recommended) or import in-place.
     - **Telemetry side:** 4 new event constants + extended `saveSucceeded` payload. PII per ADR-0013 — every `expense_id` parameter MUST be SHA-256 truncated via `hashId()`.
     - **Boundary contracts:** the `formatInrFromPaise` grep at `test/features/expenses/expense_creation_boundary_contract_test.dart` walks `lib/features/expenses/**` — auto-covers new files.

0.3  Confirm DoR for the feature story:
     - **No story file exists yet** for FR-EX-05 — `docs/sprint-zero/stories/FR-EX-05-receipt-attachment.md` must be created by the PM in Phase 1.
     - Story points: **5 SP** (justification: new Step 3 widget + receipt picker + storage service + storage-rules + storage-rules tests + Expense Detail thumbnail viewer + 4 new telemetry events + edit-flow integration). Escalate to 8 SP only if the architect calls for an aggressive image-compression pipeline (out of scope — `image_picker`'s `maxWidth/maxHeight` is the v1.0 strategy).
     - The story must explicitly enumerate: (a) the GitHub issue this PR closes — **none** (FR-EX-05 is tracked as a P1 SRS row; no separate GitHub issue exists), (b) the four-invariant applicability assessment (Inv-1 N/A on the receipt path; Inv-2 negative-guard load-bearing; Inv-4 defence-in-depth single-bucket), (c) the cross-cutting telemetry contract (4 new + 1 extended events, all PII-hashed where they carry IDs).

0.4  Cross-reference the burndown:
     - PR #48 closes **R7 + R8** (Storage rules test gaps for file size + content type). The Bucket-B remaining count drops from 30/37 to 28/37.
     - PR #48 also makes partial progress on **PY3** (Expand integration tests for Sprint 2 flows) — add a skipped integration-test stub for the upload-then-view round-trip. Partial credit only; PY3 stays open pending the Flutter emulator harness.
     - The orchestrator notes that the deferred orphan-cleanup Cloud Function (SRS schema doc line 312) is FUTURE work and should be filed as a new issue (architect §2.x). The deferred trigger-skip optimisation (skip recompute when only `receiptUrl` + `updatedAt` change) is similarly filed.

────────────────────────────────────────
PHASE 1 — STORY READINESS (PM)
────────────────────────────────────────

PM agent creates the feature story `docs/sprint-zero/stories/FR-EX-05-receipt-attachment.md` using the SRS §13.2 feature format. Story points: **5** (per §0.3).

Required Given/When/Then ACs (each MUST be testable):

  **Receipt attach flow (positive ACs)**

  - **AC-1 — Step 3 opens after Step 2.** Given the user has completed Step 1 + Step 2 of the add-expense bottom sheet, when they tap "Next" on Step 2, then Step 3 opens with the title `'Add Expense (3/3)'` (or `'Edit Expense (3/3)'` in edit mode) and renders the receipt picker section + the summary card. Telemetry: `expense_step3_opened` fires with `has_receipt_from_edit: false` (create mode) or `true|false` based on edit-mode presence.
  - **AC-2 — Camera and gallery pickers.** Given Step 3 is open with no receipt attached, when the user taps "Take Photo", then `ImagePickerService.pickFromCamera` is invoked; when the user taps "From Gallery", then `ImagePickerService.pickFromGallery` is invoked. On success, the picked `XFile` is stored in the draft and a thumbnail renders inline. Telemetry: `expense_receipt_attached` fires with `source: 'camera'|'gallery'` and `file_size_bytes`.
  - **AC-3 — Replace + remove affordances.** Given a receipt is attached, when the user taps "Replace", then the picker reopens; when the user taps "Remove", then the receipt is cleared from the draft and the empty-state picker UI returns. Telemetry: `expense_receipt_removed` fires.
  - **AC-4 — File-size validation client-side.** Given the user picks an image > 10 MB, when the file is selected, then the receipt is REJECTED with an `OBTSnackbar` type `error`: "Image is too large. Please choose a photo under 10 MB." No telemetry attach event fires; no upload starts.
  - **AC-5 — MIME-type validation client-side.** Given the user picks a non-JPEG/PNG file, when the file is selected, then the receipt is REJECTED with an `OBTSnackbar` type `error`: "This file format is not supported. Please use a JPEG or PNG image." No telemetry attach event fires; no upload starts. (HEIC files captured via camera are converted to JPEG by `image_picker` automatically — this is verified, not blocked.)
  - **AC-6 — Save with receipt.** Given a receipt is attached and the user taps "Save Expense", then (a) the state transitions to `Uploading`; (b) the file is uploaded to `gs://onebytwo-avtanshgupta.appspot.com/receipts/friendships/{fid}/{eid}`; (c) the download URL is obtained; (d) the state transitions to `Saving`; (e) the Firestore expense doc is written with `receiptUrl: <download-url>`; (f) on success, `expense_save_succeeded` fires with `has_receipt: true` and `receipt_size_bytes: <bytes>`; (g) the sheet dismisses and the snackbar reads "Expense added." (create) or "Changes saved." (edit).
  - **AC-7 — Save without receipt.** Given no receipt is attached and the user taps "Save Expense", then the create / edit flow proceeds as today; `receiptUrl: null` (create) or unchanged (edit). Telemetry `expense_save_succeeded` fires with `has_receipt: false` and no `receipt_size_bytes` key.

  **Read flow (positive ACs)**

  - **AC-8 — Expense Detail thumbnail.** Given an expense with `receiptUrl != null` is viewed on the Expense Detail screen, when the screen renders, then the receipt thumbnail (constrained to 240x320 dp per SCR-21) is visible below the summary card. Tapping the thumbnail opens a fullscreen viewer (architect-chosen complexity per §2.x).
  - **AC-9 — Expense Detail no-receipt.** Given an expense with `receiptUrl == null` is viewed, when the screen renders, then NO thumbnail and NO placeholder appears (the receipt section is omitted entirely; receipts are optional).

  **Edit flow ACs (integration with PR #46 edit-mode)**

  - **AC-10 — Pre-fill existing receipt.** Given an expense with `receiptUrl != null` is opened in the edit-mode bottom sheet, when Step 3 renders, then the existing thumbnail is displayed in the receipt section (NOT re-uploaded; the existing URL is preserved unless the user replaces it).
  - **AC-11 — Replace receipt in edit flow.** Given the user replaces the receipt in edit mode and saves, then the new file is uploaded to the same canonical path (Firebase Storage overwrites the existing object); the Firestore `update` map includes `receiptUrl` (the new download URL); `changedFields` includes `'receiptUrl'`; the changed-field indicator from PR #46 marks the receipt row.
  - **AC-12 — Remove receipt in edit flow.** Given the user removes the receipt in edit mode and saves, then the Firestore `update` map sets `receiptUrl: null`; `changedFields` includes `'receiptUrl'`; the Storage object at `receipts/friendships/{fid}/{eid}` is DELETED (architect's call at §2.x — recommend delete immediately on save to avoid stale storage; the orphan-cleanup Cloud Function will reap any race-condition stragglers when it ships).

  **Cross-cutting and negative ACs**

  - **AC-13 — Storage rules: authenticated member upload succeeds.** Given an authenticated friendship member, when they upload a JPEG ≤ 10 MB to `receipts/friendships/{fid}/{their-friendship's-expense-id}`, then the upload succeeds. Rules test at `functions/test/storage-rules/receipts.test.ts`.
  - **AC-14 — Storage rules: non-member upload rejected.** Same as AC-13 but the caller is NOT a member of the friendship → `permission-denied`.
  - **AC-15 — Storage rules: unauthenticated upload rejected.** Anonymous client → `permission-denied`.
  - **AC-16 — Storage rules: oversized upload rejected.** Authenticated member, file size = 11 MB (> 10 MB cap) → rejected.
  - **AC-17 — Storage rules: wrong MIME rejected.** Authenticated member, content-type `text/plain` → rejected.
  - **AC-18 — Storage rules: member read succeeds; non-member read rejected.** Two read tests symmetric to AC-13 / AC-14.
  - **AC-19 — Telemetry PII guard.** Given any of the 4 new telemetry events fires with an `expense_id`-derived parameter, then the parameter value is the SHA-256-truncated hash via `hashId()` (NOT the raw expense ID, NOT the raw storage path). The per-feature PII-leak test pattern from PR #38 / PR #46 confirms.
  - **AC-20 — Invariant 2 negative guard.** Given the PR diff, when scanned, then ZERO client-side writes to `simplifiedBalances` exist in any new or modified file. The existing PR #36 rules test continues to enforce.
  - **AC-21 — Invariant 1 boundary contract.** Given the PR diff, when scanned for `double` arithmetic on any monetary value, then ZERO `double` operations exist on the receipt path. The `formatInrFromPaise` boundary-contract grep auto-covers all new files under `lib/features/expenses/**`.
  - **AC-22 — Integration test stub.** Given the PR diff, when `test/integration/expenses/` is inspected, then a new (skipped) integration-test file exists documenting the canonical upload-then-view round-trip (open sheet → attach receipt → save → trigger fires → Expense Detail re-renders → thumbnail visible). Partial PY3 credit.
  - **AC-23 — `OBTConfirmationDialog` not required for remove-receipt.** Given the user taps "Remove" on an attached receipt, when the action fires, then the receipt is cleared immediately with NO confirmation dialog (architect-elected per §2.x — the remove operation is reversible until save, no user data loss; a confirmation would add friction without protection).

────────────────────────────────────────
PHASE 2 — ARCHITECT HAND-OFF
────────────────────────────────────────

Architect agent appends `## Architect Notes` to the feature story ratifying:

2.1  **Storage path schema.** Adopt the SRS canonical path `receipts/friendships/{friendshipId}/{expenseId}` (single object per expense; overwrite-on-replace; delete-on-remove). The group-context rule (`receipts/groups/{groupId}/{expenseId}`) ships in the SAME PR per §2.x — same predicate shape, same review surface, closes R7-R8 in one shot. The UI surface stays friendship-only per Phase 3 guardrails.

2.2  **Upload-then-save chain.** RATIFY the two-phase `save()` chain in the controller: (1) `Uploading` state → upload file → obtain URL; (2) `Saving` state → Firestore write with URL populated. Add `Uploading` as a new sealed-state variant in `AddExpenseState` (does NOT replace `Saving`; both are distinct UI states per SCR-21 §State 3 + State 4). On upload failure → `AddExpenseError` with `errorType: ExpenseCreateErrorType.unknown` (or a new `receiptUploadFailed` variant — recommend reuse existing `unknown` to keep the enum narrow; surface the user-facing message via `state.message`).

2.3  **`ReceiptStorageService` API surface.** New file `lib/features/expenses/data/receipt_storage_service.dart`:
     - Abstract `ReceiptStorageService` with `Future<String> uploadFriendshipReceipt({required String friendshipId, required String expenseId, required XFile file})` and `Future<void> deleteFriendshipReceipt({required String friendshipId, required String expenseId})`.
     - Concrete `FirebaseReceiptStorageService` backed by `FirebaseStorage.instance.ref(...)`. Map `FirebaseException` to a typed `ReceiptUploadError` (`permissionDenied`, `oversize`, `unsupportedType`, `network`, `unknown`). The size + MIME-type validation is ALSO performed client-side before the upload starts (so the rules-layer rejections are belt-and-braces).
     - Riverpod provider `receiptStorageServiceProvider`.

2.4  **`ImagePickerService` extraction.** RATIFY **Option (a)**: extract from `lib/features/auth/data/image_picker_service.dart` to `lib/core/services/image_picker_service.dart`. Update both `lib/features/auth/data/user_repository.dart` (avatar flow) and the new `lib/features/expenses/application/add_expense_controller.dart` to import from core. Tests for both features inject the same `FakeImagePickerService`.

2.5  **Image compression strategy.** RATIFY: rely on `image_picker`'s `maxWidth: 1920, maxHeight: 1920` (lossy-resize) as the v1.0 strategy. Any post-PR optimisation (WebP conversion, server-side OCR) is FUTURE work (file as issue).

2.6  **Telemetry constants.** Add to `lib/features/expenses/application/expense_telemetry.dart`:
     - `step3Opened = 'expense_step3_opened'`
     - `receiptAttached = 'expense_receipt_attached'`
     - `receiptRemoved = 'expense_receipt_removed'`
     - `step3Abandoned = 'expense_step3_abandoned'`
     - `paramSource = 'source'` (`'camera'|'gallery'`)
     - `paramFileSizeBytes = 'file_size_bytes'` (int)
     - `paramHasReceiptFromEdit = 'has_receipt_from_edit'` (bool)
     - `paramReceiptSizeBytes = 'receipt_size_bytes'` (int; only present on `saveSucceeded` when `has_receipt: true`)
     - The existing `paramHasReceipt = 'has_receipt'` already exists from PR #38 — extend its semantic from "always false in FR-EX-01" to "true when receipt attached".

2.7  **Files to touch (exhaustive — anything outside this set is scope creep):**
     - `storage.rules` — add friendship-receipts predicate AND group-receipts predicate.
     - `lib/core/services/image_picker_service.dart` — NEW file (extracted from auth).
     - `lib/features/auth/data/image_picker_service.dart` — DELETE (or re-export from core for backward compat — architect's call; recommend delete + update auth imports).
     - `lib/features/auth/data/user_repository.dart` — update import path.
     - `lib/features/expenses/data/receipt_storage_service.dart` — NEW.
     - `lib/features/expenses/domain/expense_doc.dart` — add `fieldReceiptUrl` constant + extend `toUpdateMap` + extend `fromMap`.
     - `lib/features/expenses/domain/expense_draft.dart` — add `receiptFile: XFile?` + `existingReceiptUrl: String?`.
     - `lib/features/expenses/domain/add_expense_state.dart` — add `Uploading` sealed variant.
     - `lib/features/expenses/application/expense_telemetry.dart` — 4 new event constants + 4 new param-key constants.
     - `lib/features/expenses/application/add_expense_controller.dart` — add receipt setters + upload chain + 4 new emit helpers.
     - `lib/features/expenses/presentation/add_expense_bottom_sheet.dart` — flip header label to `(N/3)` + wire Step 3.
     - `lib/features/expenses/presentation/steps/step_2_split_and_payer.dart` — CTA flips from `'Save'` / `'Save Changes'` to `'Next'` (Step 3 owns the final save CTA).
     - `lib/features/expenses/presentation/steps/step_3_receipt_and_confirm.dart` — NEW file (SCR-21 surface).
     - `lib/features/expenses/presentation/expense_detail_screen.dart` — render thumbnail when `receiptUrl != null`.
     - `lib/features/expenses/presentation/widgets/receipt_fullscreen_viewer.dart` — NEW (simple `Dialog` + `InteractiveViewer` + `Image.network`).
     - `firestore.rules` — N/A.
     - `firestore.indexes.json` — N/A.
     - `functions/**` — N/A (orphan cleanup is FUTURE).
     - `docs/sprint-zero/stories/FR-EX-05-receipt-attachment.md` — NEW story (Phase 1).
     - `docs/sprint-zero/sprint-2-plan.md` — add PR #48 row (5 SP; cumulative 50 SP / 14 PRs).
     - `docs/sprint-zero/next-three-prs.md` — roll PR #48 to merged; PR #49 / #50 / #51 candidates.
     - `docs/audits/sprint-1/07-bucket-b-burndown.md` — close R7 + R8; remaining drops to 28/37.
     - `test/features/expenses/expense_creation_boundary_contract_test.dart` — extend (auto-covers new files via glob walk).
     - `test/features/expenses/add_expense_controller_test.dart` — add receipt-attach / receipt-remove / upload-success / upload-failure tests.
     - `test/features/expenses/expense_telemetry_pii_leak_test.dart` — extend with 4 new events.
     - `test/features/expenses/step_3_receipt_and_confirm_widget_test.dart` — NEW.
     - `test/features/expenses/expense_detail_screen_widget_test.dart` — extend with thumbnail render test.
     - `test/core/services/image_picker_service_test.dart` — NEW (smoke test for the extracted service).
     - `functions/test/storage-rules/receipts.test.ts` — NEW (8+ tests).
     - `test/integration/expenses/receipt_upload_flow_test.dart` — NEW (skipped stub).

2.8  **Files explicitly NOT to touch (negative scope guardrails):**
     - `firestore.rules` (no changes — `receiptUrl: string | null` already accepted).
     - `firestore.indexes.json` (no new queries).
     - Any `functions/src/**` file (the orphan-cleanup function is FUTURE work; file as issue).
     - `functions/package.json` / `package-lock.json` / `firebase.json` / any `.github/workflows/*.yml`.
     - `lib/features/settlements/**` (unrelated).
     - `lib/features/friends/**` (unrelated).
     - The notification-type schema discriminator in `firestore-schema.md:202` / `cloud-functions-catalogue.md` / `notifications.md` (AC-X4 carry-forward).
     - `pubspec.yaml` / `pubspec.lock` / `analysis_options.yaml` / Android / iOS native shells.

2.9  **Anticipated reconciliations.** Document EXACTLY:
     1. **`firestore.get()` inside Storage rules** — the friendship-membership check requires a cross-service rule read. Verify against the emulator (rules tests in §2.7); this is a non-trivial first for this codebase.
     2. **Storage emulator local-dev gap** — `scripts/dev/start-emulators.sh` already starts the Storage emulator; confirm the new tests pass against it.
     3. **`image_picker` HEIC → JPEG auto-conversion** — confirm the picker's `XFile.path` / `XFile.mimeType` on a HEIC source returns the JPEG-converted file (iOS-only concern; Android does not have HEIC).
     4. **Trigger no-op recompute on receipt-only updates** — file as a FUTURE optimisation issue. NOT fixed in PR #48.
     5. **Orphan cleanup Cloud Function** — file as a FUTURE issue per SRS schema doc line 312. NOT implemented in PR #48.
     6. **PR #46 changed-field indicator integration** — the receipt row in Step 3 must also surface the `secondary` left-border + `, changed.` semantic label when `changedFields.contains('receiptUrl')` per SCR-22 §449. Reuse `ChangedFieldIndicator` from PR #46.

────────────────────────────────────────
PHASE 3 — SCOPE GUARDRAILS
────────────────────────────────────────

WHAT GOES IN PR #48:

  - All files listed in §2.7.
  - Storage rules friendship-receipts predicate + group-receipts predicate (defensive; UI ships friendship-only).
  - Storage rules tests covering both predicates.
  - Step 3 widget, receipt picker, upload service, thumbnail viewer.
  - Edit-flow integration: pre-fill, replace, remove.
  - 4 new telemetry events + extended `saveSucceeded` payload.
  - The feature story + Architect Notes.
  - Plan + burndown roll-ups (Phase 7).

WHAT DOES NOT GO IN PR #48:

  - **Group-context UI** (the bottom sheet still launches from friendship surfaces only; the group-context rule is defensive only).
  - **FR-EX-07 activity feed** — separate P0 story; PR #49+.
  - **FR-SE-09 send reminder** — separate P1; PR #49+.
  - **Orphan-cleanup Cloud Function** — FUTURE issue; out of v1.0 unless required for compliance.
  - **Trigger no-op-recompute optimisation** — FUTURE issue.
  - **Concurrent-edit detection for FR-EX-06** — still deferred per PR #46 §2.4.
  - **Issue #47 rules-hardening** — separate small chore PR.
  - **Rate-limit transaction race refactor** — PR #45 §2.2 deferred.
  - **Hard delete of expenses or receipts independently of the soft-delete flow** — out of SRS scope.
  - **OCR / AI suggestions on receipts** — SRS §12.3 explicit out-of-scope.
  - **Multi-receipt expenses** — out of scope (the schema is 1 receipt per expense).
  - **WebP / HEIC client conversion** — `image_picker`'s built-in compression is the v1.0 strategy.

If an agent suggests any of: "while we're touching the bottom sheet, let's also implement FR-EX-07", "let's also extract a generic OBTImagePicker design-system component now", "let's stand up the orphan cleanup function while we're here", "let's add OCR to the receipt", "let's allow multiple receipts" — REFUSE. These are scope creep.

────────────────────────────────────────
PHASE 4 — TEST-FIRST DISCIPLINE
────────────────────────────────────────

PR #48 follows the standard test-first cadence:

1. Snapshot the pre-fix baseline: 883/27 Flutter; 100/100 functions; 153/153 rules.
2. Write the failing storage-rules tests first: `functions/test/storage-rules/receipts.test.ts` — 8+ tests covering AC-13 through AC-18 plus group-context symmetric tests. They will FAIL until `storage.rules` adds the predicates.
3. Write the failing controller tests next: `setReceipt`, `removeReceipt`, `pickReceiptFromCamera`, `pickReceiptFromGallery`, the `Uploading → Saving → Success` chain, the upload-failure path.
4. Write the failing widget tests: Step 3 receipt picker, thumbnail render, replace / remove affordances, file-size + MIME-type rejection UI, Expense Detail thumbnail.
5. Implement the source per §2.7. Run test suites incrementally; each test flips from failing to passing as the corresponding code lands.
6. Re-run the boundary-contract grep — must stay green for all new files under `lib/features/expenses/**`.
7. Re-run `dart format --set-exit-if-changed .` — 0 changes.
8. Re-run `flutter analyze --fatal-infos` — "No issues found".
9. Re-run `flutter test` — expected M+883 pass / N+27 skipped (M = controller + widget + telemetry tests; N = integration stub).
10. Re-run `cd functions && npm run lint && npm run build && npm test` — still 9 suites / 100 tests pass (no `functions/src/**` changes).
11. Re-run `cd functions && npm run test:rules` — 8 suites / **161+ tests pass** (153 baseline + 8+ new for receipts; verify against the dedicated `--only firestore,storage` session).

**Combined CI dry-run:** all jobs must be green.

────────────────────────────────────────
PHASE 5 — EXECUTION PROTOCOL
────────────────────────────────────────

Mirror the PR #46 commit cadence:

 1. PM creates story `docs/sprint-zero/stories/FR-EX-05-receipt-attachment.md`. Commits as `docs(stories): FR-EX-05 receipt attachment story`.
 2. Architect appends `## Architect Notes` (§2.1–§2.9). Commits as `docs(stories): FR-EX-05 architect notes`.
 3. QA writes the 8+ new storage-rules tests (failing first). Commits as `test(rules): FR-EX-05 storage-rules tests for friendship-receipts (and groups)`.
 4. Flutter-dev / Devops adds the storage-rules predicates. Commits as `feat(storage-rules): friendship + group receipts predicates`.
 5. Flutter-dev extracts `ImagePickerService` to core + updates auth. Commits as `refactor(core): extract ImagePickerService to lib/core/services`.
 6. Flutter-dev adds `ReceiptStorageService` + typed errors + tests. Commits as `feat(expenses): receipt storage service`.
 7. Flutter-dev extends `AddExpenseController` with receipt setters + upload chain + telemetry. Commits as `feat(expenses): receipt-attach controller path + 4 new telemetry events`.
 8. Flutter-dev adds `Step3ReceiptAndConfirm` widget + thumbnail viewer + integrates into the bottom sheet + flips header to `(N/3)`. Commits as `feat(expenses): step 3 receipt + confirm UI`.
 9. Flutter-dev extends Expense Detail screen with thumbnail. Commits as `feat(expenses): expense detail receipt thumbnail`.
10. Flutter-dev adds skipped integration-test stub. Commits as `test(integration): FR-EX-05 receipt upload round-trip stub (skipped)`.
11. QA runs the manual smoke matrix (emulators + simulator):
    - Start emulators; sign in; create an expense with a JPEG attached; verify thumbnail in Expense Detail.
    - Edit the same expense; replace the receipt; verify the new thumbnail.
    - Edit again; remove the receipt; verify the Expense Detail no longer shows a thumbnail.
    - Try to upload a 11 MB file; verify the client rejects with the size snackbar.
    - Try to upload a `.txt` file; verify the MIME snackbar.
    - Sign in as the friend (other member); verify they can READ the thumbnail.
    - Sign in as an outsider; verify they CANNOT read (rules enforcement).
12. Docs hand updates plan + roll-forward:
    - `docs/sprint-zero/sprint-2-plan.md` (PR #48 row; 5 SP; cumulative 50 SP / 14 PRs).
    - `docs/sprint-zero/next-three-prs.md` (PR #49 / #50 / #51 candidates — top is FR-EX-07 activity feed).
    - `docs/audits/sprint-1/07-bucket-b-burndown.md` (close R7 + R8; remaining 28/37).
    - Commits as `docs(plan): PR #48 shipped, prep PR #49`.
13. PR opened. Title: `feat(expenses): FR-EX-05 receipt attachment (friendship)`. Body must:
    - Cite SCR-21 + SCR-22 §Components Used "All SCR-21 components" integration.
    - Confirm Invariants 1 / 2 / 4 (Inv-3 N/A).
    - Confirm AC-X4 (notification-type schema discriminator UNCHANGED).
    - State explicitly the deferred items: orphan-cleanup function, trigger no-op-recompute optimisation, group-context UI, OCR / AI.
    - State explicitly that R7 + R8 from Bucket-B are CLOSED.
    - End with `Next PR: PR #49 — TBD per architect's call at kickoff (top candidate: FR-EX-07 activity feed).`

────────────────────────────────────────
PHASE 6 — DEFINITION OF DONE GATE
────────────────────────────────────────

Walk the DoD checklist. Particular emphasis:

  - DoD §2: every layer green (Flutter / Functions / 8-suite Rules / format / analyze).
  - DoD §3: QA sign-off with evidence for cross-user real-time receipt read (member can read; non-member cannot).
  - DoD §7: invariant compliance.
    * Inv-1: N/A on the receipt path; boundary-contract grep auto-covers new files.
    * Inv-2: ZERO client writes to `simplifiedBalances`; rules test enforces.
    * Inv-3: N/A.
    * Inv-4: `.firebaserc` unchanged; single bucket targeted in both production and emulator.
  - DoD §8: documentation updated (story + architect notes + sprint-2-plan + next-three-prs + burndown + 2 future-work issues filed: orphan cleanup + trigger no-op optimisation).
  - DoD §9: deploy verification. The new Storage rules deploy via `firebase deploy --only storage` on tag. QA verifies the upload + read round-trip in production post-deploy on the canary device.

QA posts the explicit DoD §3 sign-off, including the negative-scope greps from AC-X4, AC-20, AC-21.

**Pre-push CI hygiene:** run `dart format .` AND all test layers BEFORE `git push`.

────────────────────────────────────────
PHASE 7 — UPDATE THE ROLLING PLAN AND BURNDOWN
────────────────────────────────────────

PM agent updates:
  - `docs/sprint-zero/sprint-2-plan.md` — PR #48 status (merged), velocity #14 (5 SP, total now 50 SP across 14 PRs).
  - `docs/sprint-zero/next-three-prs.md`:
      * PR #48 row marked merged; numbering note updated.
      * PR #49: **TBD** per architect's call at kickoff. Top candidates (in rough priority order):
          - **FR-EX-07 Activity feed** (P0 follow-on; the trigger already emits activity entries; remaining is read-side screen + composite-index design). Now the highest unplaced P0.
          - **FR-SE-09 Send Reminder** (P1; FCM dependency + 24-hour rate-limit subcollection).
          - **FR-SE-08 dedicated `/settlements/history`** screen (P0; PR #42's in-timeline rows satisfy v1.0 but a dedicated screen remains).
          - **Issue #47 — Firestore rules-hardening for non-creator update/delete** (chore; 2 SP; defence-in-depth; small standalone PR).
          - **Concurrent-edit detection for FR-EX-06** (chore; operational hardening).
          - **Orphan-cleanup Cloud Function for receipts** (chore; filed during PR #48; FUTURE).
          - **Trigger no-op-recompute optimisation** (chore; filed during PR #48; FUTURE).
          - **Rate-limit transaction race refactor** (PR #45 §2.2 deferred).
      * PR #50: TBD per PR #49 outcome.
      * PR #51: TBD per PR #50 outcome.
  - `docs/audits/sprint-1/07-bucket-b-burndown.md`:
      * Header timestamp: PR #46 → PR #48.
      * Append a PR #48 section: closes **R7 + R8** (Storage rules tests for file size + content type). Remaining drops to **28 / 37**.

Commits as `docs(plan): PR #48 shipped, prep PR #49`.

────────────────────────────────────────
GUARDRAILS
────────────────────────────────────────

If during this PR an agent proposes:
  - "While we're in the bottom sheet, let's also implement FR-EX-07 activity feed" → REFUSE. Separate P0 story; the trigger already emits the activity entries; PR #48 trusts the future read-side implementation.
  - "Let's stand up the orphan-cleanup Cloud Function while we're here" → REFUSE. FUTURE work; file as a new issue at Phase 5 step 12.
  - "Let's add the trigger no-op-recompute optimisation so receipt-only updates don't recompute balances" → REFUSE. FUTURE optimisation; file as a new issue.
  - "Let's add OCR / category prediction on the receipt" → REFUSE. SRS §12.3 explicit out-of-scope.
  - "Let's allow multiple receipts per expense" → REFUSE. The schema is 1 receipt per expense; multi-receipt is out of v1.0.
  - "Let's bump `firebase_storage` or `image_picker` while we're touching them" → REFUSE. Dependency bumps stay on issue #22 for Sprint 4+.
  - "Let's extract an OBTReceiptThumbnail / OBTImagePicker design-system component now" → DEFER per architect §2.x — inline first, extract on the SECOND use site (PR #49+).
  - "Let's close issue #47 (rules-hardening) at the same time" → REFUSE. Separate small chore PR.
  - "Let's wire the group-context UI" → REFUSE. Friendship-only for v1.0 PR #48; group context ships with the Sprint 3 groups epic.

If something genuinely surprises (e.g. the `firestore.get()` cross-collection predicate in Storage rules doesn't work against the emulator, or the `image_picker` HEIC-on-iOS path returns a non-JPEG file, or the Storage emulator's `request.resource.size` predicate doesn't match the actual upload size), STOP and surface the friction. PR #48 is a P1 feature with real-world image data — get the rules + the upload contract right before shipping.

Begin with Phase 0 — verify the dependency DAG (PR #46 merge state at `4b24652`; baseline test counts; emulators reachable for the new storage-rules test) and re-read the three reference docs (SCR-21 in `docs/design/06-screen-specs/19-22-expenses.md` lines 293–395, the `firestore-schema.md` Storage layout at lines 298–313, and the existing `storage.rules` + `functions/test/storage-rules/avatars.test.ts` for the precedent pattern) before kickoff.
