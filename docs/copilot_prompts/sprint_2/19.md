1. You are the OneByTwo orchestrator agent. PR #55 (FR-PR-03 + FR-AC-04 — Notification Preferences UI + production `cloud_functions` adapter wiring) has merged (squash commit `ac5b05a`) and the **`notificationPrefs` round-trip** is now live end-to-end in production: the `lib/features/profile/presentation/notification_preferences_screen.dart` SCR-27 ships under `/profile/notifications`, the `NotificationPreferencesController` drives per-toggle 500 ms debounced auto-save (optimistic-with-revert) through the new `UserRepository.updateNotificationPrefs(uid, prefs)` dot-path partial-map writer, the new `lib/core/connectivity/connectivity_provider.dart` `IsOnline` seam (backed by `connectivity_plus: ^6.1.5` — pinned to 6.x because 7.x uses `NWPath.isUltraConstrained` which is iOS 17+ only) drives the AC-10 single-fire-per-session offline snackbar, the screen-level auth guard short-circuits to "Please sign in again." when the auth state is not `AuthenticatedWithProfile`, the OS-permission banner ships WITHOUT the "Open Settings" CTA per architect §2.4 fallback (`firebase_messaging: ^16.2.0` does not expose `openAppNotificationSettings()` on either platform), the long-deferred `reminderRepositoryProvider` + `matchingRepositoryProvider` production overrides are wired in `lib/main.dart` via `FirebaseFunctions.instanceFor(region: 'asia-south1')` with emulator binding under the existing `_useEmulator` guard, and 3 new `users-update.test.ts` rules tests validate the partial-map `notificationPrefs.<key>` update path (AC-17 allow / AC-18 non-bool reject / AC-19 attempted-removal reject). Sprint 2 velocity through end of PR #55: **84 SP across 19 PRs**. We now implement **PR #56: `OBTBottomNav` shell — UX foundation chore**. This is the natural follow-on per `docs/sprint-zero/next-three-prs.md` line 122-127: PR #55 shipped the LAST feature that owned its own AppBar navigation entry (the Profile row's Notification Preferences sub-route), so every primary surface needed by the Sprint 3 groups epic — Home / Friends / Groups / Activity / Profile — now has a real screen waiting to be wired into the canonical tab cluster. The current `lib/features/auth/presentation/home_placeholder_screen.dart:18-47` renders a placeholder Scaffold with two temporary `IconButton` AppBar actions (`Icons.notifications_none` → `ActivityFeedScreen` push at lines 21-33, `Icons.person_outline` → `ProfileScreen` push at lines 34-46) — these are the temporary `// PR #52 §2.1 deferral` plumbing that PR #56 replaces. The new work is the **`OBTBottomNav` widget (per `docs/design/02-design-system/components.md §2`) + `AuthenticatedShell` (the stateful host that swaps tabs via `IndexedStack` and renders `OBTBottomNav` at the bottom) + 2 new placeholder screens (`HomeDashboardPlaceholder` for tab 0 — FR-HD-01..04 dashboard is a separate v1.0 story; `GroupsListPlaceholder` for tab 2 — the Groups epic is Sprint 3) + the `lib/main.dart:133` wire change that swaps `HomePlaceholderScreen` for `AuthenticatedShell`**. Story points: **3** (1 `OBTBottomNav` widget + golden test + accessibility test; 1 `AuthenticatedShell` with IndexedStack tab preservation + selection-state controller + telemetry + 2 placeholder screens; 1 main.dart wiring change + removal of the temporary `HomePlaceholderScreen` AppBar action plumbing + widget tests for shell-level navigation + tab persistence + telemetry). Escalate to 5 only if the architect bundles **either** (a) the FAB for FR-HD-04 ("A persistent floating action button shall allow adding a new expense from any primary tab" — P0) wired to a context-picker bottom sheet that lets the user choose Friend or Group as the AddExpense target (Group path stubbed with "Coming soon" snackbar pending Sprint 3), OR (b) the `go_router` migration that the design spec at `docs/design/01-information-architecture/navigation-flow.md §4.4` ratifies as the long-term shell mechanism (NOT recommended for this PR — `go_router` is a load-bearing migration that touches every existing `Navigator.push(MaterialPageRoute)` call site and warrants its own dedicated PR in Sprint 3). RECOMMENDED: ship the IndexedStack-based shell + defer FAB to a focused FR-HD-04 PR + defer go_router migration to Sprint 3.

2. The numbering continues from PR #55 — **PR #56 is the next CHORE/UX-FOUNDATION PR.** The post-PR-#55 sequence so far: #55 (PR — feat, merged `ac5b05a`), no new issues filed during PR #55 review (the reviewer's six recommendations were addressed in-PR via `3d4c6af` + `8a2814a` + `627440b` + `be17306` before merge). Issues #47 (rules-hardening — Sprint 3 candidate), #49 (orphan-cleanup — FUTURE), #50 (trigger no-op optimisation — **EXPLICITLY CONSTRAINED** by FR-EX-07 AC-2: cannot close because optimisation would break activity emission on receipt-only updates) remain OPEN and are NOT touched by PR #56. The orchestrator should not assume PR #57 == GitHub issue 57 — issues and PRs share the same sequential namespace, so any new issues filed during PR #56 will consume intermediate numbers. **Pre-merge CI title-lint gotcha (verified PR #45 through PR #55):** the workflow regex `[a-z0-9_-]+` rejects comma-separated scopes — use a single-token scope, and the SUBJECT (everything after `<scope>: `) must be ≤ 72 characters total. Since this PR primarily adds a new shell widget under `lib/features/shell/` (NEW feature folder; mirror of `lib/features/profile/`) plus a new design-system primitive at `lib/core/widgets/obt_bottom_nav.dart` (mirror of the existing `OBTAppBar` pattern), recommend the scope `shell` (the load-bearing surface). Suggested title (54 chars subject): `feat(shell): OBTBottomNav + authenticated tab shell`. Alternative if the architect prefers framing it as a chore (this is a structural UX foundation PR, not a SRS-functional-requirement closure): `chore(shell): OBTBottomNav + authenticated tab shell` (54 chars). RECOMMENDED `feat` because the shell satisfies the SCR-27 / SCR-26 / SCR-09 / SCR-13 / SCR-25 navigation contract enumerated in `docs/design/01-information-architecture/navigation-flow.md §1` table line "Bottom nav tab" rows.

3. PR #56 is the **first PR that introduces a `lib/features/shell/` feature folder** (the host for `AuthenticatedShell`) and the **first PR that adds a stateful authenticated-area wrapper between the auth-state branch in `main.dart` and the per-feature screens**. The seams are already in place:
   - **Client side — existing screens (each becomes a tab content surface):**
     - Tab 0 (Home): currently `lib/features/auth/presentation/home_placeholder_screen.dart` — the temporary placeholder with the AppBar Activity/Profile icons. PR #56 EXTRACTS the Scaffold body content (Icon + "Home" text + "The real dashboard is coming soon." copy at lines 49-71) into a NEW `lib/features/shell/presentation/home_dashboard_placeholder.dart` that omits the AppBar actions (the tab cluster supersedes them). The original `HomePlaceholderScreen` becomes DEAD CODE; architect's call at §2.x on whether to delete it in this PR (RECOMMENDED YES — it's a single-use placeholder and the rename is cleaner than leaving a dead file).
     - Tab 1 (Friends): `lib/features/friends/presentation/friends_list_screen.dart` — UNCHANGED. Mounted as-is in the shell's IndexedStack at index 1.
     - Tab 2 (Groups): **NO SCREEN EXISTS** — the `lib/features/groups/` feature folder is empty (Sprint 3 epic). PR #56 ships a NEW `lib/features/shell/presentation/groups_list_placeholder.dart` mirroring the Home placeholder pattern (Icon + "Groups" headline + "Coming in Sprint 3." copy). Architect's call at §2.x on the placeholder location — placing it under `lib/features/shell/presentation/` (mirror of HomeDashboardPlaceholder) keeps every PR #56 placeholder colocated; placing it under `lib/features/groups/presentation/` (matching the natural feature folder) creates the seed that the Groups epic will extend. RECOMMENDED `lib/features/shell/presentation/` — it ships AND is deleted together when the Groups epic lands, keeping `lib/features/groups/` clean for the future implementation.
     - Tab 3 (Activity): `lib/features/activity/presentation/activity_feed_screen.dart` — UNCHANGED. Mounted as-is at index 3.
     - Tab 4 (Profile): `lib/features/profile/presentation/profile_screen.dart` — UNCHANGED. Mounted as-is at index 4. The SCR-26 → SCR-27 push (Notification Preferences row) and the SCR-26 → SCR-29 push (Edit Profile row) continue to work because they use `Navigator.of(context).push(MaterialPageRoute(...))` which stacks on top of the shell route (the bottom nav stays hidden during the push, matching the `EditProfileScreen` precedent — the architect noted this in PR #55 §2.x).
   - **Client side — NEW `lib/features/shell/` feature folder:**
     - **NEW** `lib/features/shell/presentation/authenticated_shell.dart`: `ConsumerStatefulWidget` with an `int _currentIndex = 0` private field. Body is an `IndexedStack` over the 5 tab content widgets (so tab state is preserved across tab switches — per the `OBTBottomNav` design spec's "Pressed: Ink ripple; 200 ms transition to active state" semantics; the IndexedStack guarantees the underlying `ListView` scroll positions / form state on each tab survive a switch). `bottomNavigationBar: OBTBottomNav(currentIndex: _currentIndex, onTabSelected: _onTabSelected)`. `_onTabSelected(int i)` calls `setState(() => _currentIndex = i)` and fires `bottom_nav_tab_selected` telemetry with `tab_index: i` and `tab_label: <Home|Friends|Groups|Activity|Profile>`. The shell has NO AppBar (each tab content widget owns its own AppBar per the existing precedents — Profile owns `OBTAppBar`; Friends owns its own; Activity owns its own). **Architect's call at §2.x** on whether to wrap the shell in `WillPopScope` / `PopScope` to handle Android back-button on non-zero tabs by snapping back to tab 0 (RECOMMENDED YES — matches Material 3 guidance; tap "Home" tab is the canonical "I'm done with this subtree" gesture; without this, the Android back button on tab 4 closes the app which is jarring).
     - **NEW** `lib/features/shell/application/shell_navigation_controller.dart` (RECOMMENDED if scope budget allows; OPTIONAL — the simple `setState` approach above is fine for v1.0): Riverpod 2.x `Notifier<int>` exposing the current tab index. Necessary IF the design needs cross-feature programmatic tab switches (e.g. FCM deep-link payload landing on Activity tab from a cold start). Architect's call at §2.x — RECOMMENDED defer to a FOLLOW-UP PR alongside the FR-AC-05 deep-link expansion; the `setState`-only shell is fine for PR #56 because the only entry point is the user tapping a tab. If the architect ratifies the deferral, the in-shell `_currentIndex` field is sufficient and the controller doesn't ship.
     - **NEW** `lib/features/shell/presentation/home_dashboard_placeholder.dart`: stateless widget extracted from `home_placeholder_screen.dart` body (lines 49-71). The Scaffold has its own `AppBar(title: Text('Home'))` (NO actions — the tab cluster owns navigation). Body is the existing Icon + headline + sub-copy.
     - **NEW** `lib/features/shell/presentation/groups_list_placeholder.dart`: stateless widget. Scaffold with `AppBar(title: Text('Groups'))`. Body is an Icon + "Groups" headline + "Coming in Sprint 3 — group expense splitting and shared balances." copy. NO functional widgets — Groups feature is fully deferred.
     - **NEW** `lib/features/shell/application/shell_telemetry.dart`: event-name + parameter-key constants for `bottom_nav_tab_selected` (mirror of `lib/features/reminders/application/reminder_telemetry.dart` pattern). The event is PRE-DECLARED in `docs/design/07-technical/telemetry-plan.md` (verify exact line number at kickoff — search for `bottom_nav_tab_selected` or `nav_tab_changed` or similar; if absent, the architect appends to the telemetry plan in §2.x).
   - **Client side — NEW `lib/core/widgets/obt_bottom_nav.dart` design-system primitive:**
     - `OBTBottomNav extends StatelessWidget` with two required params per `components.md §2`: `final int currentIndex` (0..4) + `final ValueChanged<int> onTabSelected`. Implementation is a `BottomNavigationBar` with `type: BottomNavigationBarType.fixed` (the design spec says "five-tab bottom navigation bar" — fixed not shifting) + 5 `BottomNavigationBarItem` entries per the spec table: (0) `Icons.home_outlined` / `Icons.home` + "Home"; (1) `Icons.people_outline` / `Icons.people` + "Friends"; (2) `Icons.groups_outlined` / `Icons.groups` + "Groups"; (3) `Icons.notifications_outlined` / `Icons.notifications` + "Activity"; (4) `Icons.person_outline` / `Icons.person` + "Profile". `selectedItemColor: Theme.of(context).colorScheme.primary`, `unselectedItemColor: Theme.of(context).colorScheme.onSurfaceVariant`. Accessibility per spec: each `BottomNavigationBarItem` has `label` as the semantic label; active tab announces the `"Selected"` suffix via Material's default behaviour (verify with the accessibility test); minimum tap target 48×48 dp is guaranteed by Material's `BottomNavigationBar` default sizing. **Architect's call at §2.x** on whether to extract the 5 tab definitions into a const tuple list (RECOMMENDED YES — single source of truth for icons + labels; the shell's telemetry call also reads from this list).
   - **Client side — `lib/main.dart` wiring change:**
     - Line 133: `AuthenticatedWithProfile() => const HomePlaceholderScreen()` → `AuthenticatedWithProfile() => const AuthenticatedShell()`. Drops the `home_placeholder_screen.dart` import; adds the `authenticated_shell.dart` import.
   - **Client side — `lib/features/auth/presentation/home_placeholder_screen.dart` cleanup:**
     - DELETE the file (no longer referenced anywhere after the main.dart swap). Architect's call at §2.x on delete-vs-keep — RECOMMENDED DELETE because the file's sole purpose was the placeholder content + the temporary AppBar action plumbing; both are superseded by the shell. If the architect ratifies the deletion, also remove the corresponding test file at `test/features/auth/home_placeholder_screen_test.dart` (verify it exists at kickoff; if it does, the deletion must include it; if the test asserts the temporary "Activity" / "Profile" AppBar action paths, those assertions are obsoleted and the test goes with the screen).
   - **No new server-side surface.** Zero changes to `functions/src/**`. The Cloud Functions emulator wiring in `lib/main.dart` lines 57-61 is UNCHANGED.
   - **Telemetry contract:** **client** event `bottom_nav_tab_selected` (parameters `tab_index: int` ∈ {0,1,2,3,4} and `tab_label: String` ∈ {"home","friends","groups","activity","profile"}; fires on every tap, NOT on programmatic-switch — mirror of `friend_row_tapped` per PR #35). PII guard per ADR-0013: the tab index/label parameters are SAFE non-identifying tokens — no UID-derived parameters; no hashing needed.

4. PR #56 has **two mandatory cross-PR bundles**: the **`HomePlaceholderScreen` deletion** (chore; ~0.2 SP) AND the **golden-test infrastructure adoption for `OBTBottomNav`** (chore; ~0.5 SP — IF the architect ratifies a golden test for the new design-system primitive; OPTIONAL per the existing component-test precedents). Without the first, the codebase carries a dead file for the rest of v1.0; without the second (if ratified), regressions to the bottom-nav visual layer won't be caught. PR #56 must NOT bundle: the FR-HD-01..04 home dashboard implementation (separate P0 story; depends on a `home_balance_summary` aggregator + the donut/bar chart widget; ~5-8 SP), the FR-HD-04 FAB wiring (P0 — but the FAB needs a context picker that depends on the Groups epic being live for the Group path; ship the FAB in a focused FR-HD-04 PR after the Groups epic OR ship it as part of THIS PR's scope escalation if the architect ratifies — RECOMMENDED defer), the `go_router` migration (Sprint 3), the Groups feature implementation (Sprint 3 epic), the FR-PR-02 phone-number-change flow (separate P1 story), the FR-PR-05 Contact Support `mailto:` flow (separate P0 story), the FR-AU-09 account-deletion flow (separate P1 story), the `shared_preferences` adoption (still deferred per PR #54 §2.6 + PR #55 §2.4), the `OBTRupeeText` primitive (still deferred per PR #52 §2.6 — no second use site precedent), the `cloud-functions-catalogue.md §7` docs roll-up (separate cosmetic chore PR), the activity-writer rename cleanup (still deferred per PR #52 §2.3), the FR-SE-09 message-compose dialog follow-up (separate UX PR per FR-SE-09 §2.5), Issue #47 rules-hardening (separate chore), Issue #49 orphan-cleanup (FUTURE), Issue #50 trigger no-op optimisation (EXPLICITLY CONSTRAINED), FR-SE-08 dedicated settlement-history screen (separate P0 story — natural pair with this shell PR but adds ~3-5 SP that pushes scope over the 3 SP envelope), the `app_settings` / `permission_handler` Open Settings CTA chore (separate ~1-2 SP follow-up per PR #55 deviation 1), the FR-AC-05 cold-start deep-link expansion to land on a specific tab (separate enhancement — requires the `shellNavigationControllerProvider` recommended for follow-up).

5. Read before starting:
   - `.github/copilot-instructions.md`
   - `.github/shared/invariants.md` — invariant 1 (paise integers — N/A on this PR; no monetary values flow through the shell), invariant 2 (`simplifiedBalances` server-only — N/A; the shell does not access `simplifiedBalances`), invariant 3 (system share sheet — N/A), invariant 4 (single Firebase project — N/A; no new Firebase SDK usage).
   - `.github/shared/coding-standards.md` — Dart, Riverpod, widget-test, Conventional Commits rules. The PR is 100% client-side (no Functions changes); the Dart half is load-bearing.
   - **Memory: PR# references in code/test files are forbidden.** Repository memory: "Do not include PR numbers (e.g. 'PR #36', 'PR #37') in comments or descriptions inside code or test files. PR references belong in docs and commit messages only." Use design-spec references (`components.md §2`, `navigation-flow.md §4.4`) in code comments instead.
   - **Memory: dart format.** Repository memory: "CI runs Flutter 3.44.1 and gates on `dart format --set-exit-if-changed .`. Always run `dart format .` locally before pushing — `flutter analyze` alone does not catch formatter drift."
   - **Memory: commit message scope.** Repository memory: "PR title lint enforces conventional commit scope `[a-z0-9_-]+` — a single token only." AND subject must be ≤ 72 chars. Recommend `feat(shell): OBTBottomNav + authenticated tab shell` (54 chars).
   - **Memory: telemetry PII hashing.** Repository memory: "Use hashFriendshipId() in lib/core/telemetry/event_id_hash.dart for any telemetry parameter that would otherwise carry a deterministic friendshipId or other UID-composite identifier." The new `bottom_nav_tab_selected` event carries NO UID-derived parameters — no hashing required, but DEFENCE-IN-DEPTH PII-leak test SHOULD assert that the event emitted by the shell does not include any `uid` / `userId` / `friendship_id` / `friendship_id_hash` parameter.
   - `.github/shared/decision-log.md` — ADR-0006 (Riverpod state management — `StateNotifier`/`Notifier` for any shell-level navigation controller IF the architect ratifies one; OTHERWISE the in-shell `setState` is sufficient), ADR-0007 (feature-first folder layout — NEW `lib/features/shell/` folder is the host), ADR-0013 (PII hashing — see memory above).
   - `docs/patterns/feature-pr-conventions.md` — Feature-First Folder Layout (`lib/features/shell/{application,presentation}` — the shell + placeholders go under `presentation/`; the optional telemetry constants under `application/`), Telemetry-Event Discipline (`bottom_nav_tab_selected` follows the verb-past pattern correctly).
   - `docs/OneByTwo_Requirements_Spec.md` — section 4.8 (FR-HD-01..04 home dashboard; FR-HD-04 P0 specifically mandates the FAB which is the in-scope-escalation candidate per §1 above), section 6 (navigation contracts), section 13.1 (Flutter folder layout).
   - `docs/design/01-information-architecture/navigation-flow.md` — full file (the route table at lines 50-78 enumerates the 5 tab destinations; §4.4 ratifies GoRouter `ShellRoute` as the long-term mechanism but explicitly allows the IndexedStack interim per the architect's choice).
   - `docs/design/02-design-system/components.md` — section **§2 OBTBottomNav** in FULL (the visual + accessibility + behavioural spec; the architect transcribes the §Tabs table + §States + §Accessibility into the new widget's doc comment).
   - `docs/design/06-screen-specs/06-08-home-and-search.md` — SCR-06 Home Dashboard (line range to verify at kickoff) — the PR ships the **placeholder** for this screen; the real implementation is deferred to a focused FR-HD-01..04 PR.
   - `docs/design/06-screen-specs/13-18-groups.md` — SCR-13 Groups List (line range to verify) — the PR ships the **placeholder** for this screen; the real Groups feature is the Sprint 3 epic.
   - `docs/design/07-technical/telemetry-plan.md` — search for `bottom_nav_tab_selected` or `nav_tab_changed`; if pre-declared, the AC test names MUST match exactly; if absent, the architect appends a new row (PM PR-bundle question — RECOMMENDED append in this PR's §2.x because the new event ships in this PR).
   - `docs/sprint-zero/stories/FR-AC-01-activity-feed-read-side.md` §2.1 Bottom-nav shell deferral (lines ~395-420) — the explicit PR #52 deferral that PR #56 closes. The story file ratifies "bottom-nav shell is a separate UX PR that lands when Friends + Groups + Activity + Profile tabs are all needed simultaneously (likely Sprint 3 alongside the groups epic)" — PR #56 brings that PR forward by one sprint because Profile (PR #55) was the last missing tab content.
   - `lib/features/auth/presentation/home_placeholder_screen.dart` — the file being replaced. Read in full to understand the temporary AppBar action plumbing being removed.
   - `lib/main.dart` lines 127-138 — the `AuthenticatedWithProfile()` branch where the wire change lands.
   - `lib/features/friends/presentation/friends_list_screen.dart` — verify it owns its own AppBar (it does — confirmed in the seam audit); the shell does NOT wrap each tab in an outer AppBar.
   - `lib/features/activity/presentation/activity_feed_screen.dart` — same verification.
   - `lib/features/profile/presentation/profile_screen.dart` — same verification (uses `OBTAppBar`).
   - `lib/core/widgets/` — existing design-system primitives folder where `obt_bottom_nav.dart` ships alongside `OBTAppBar` and the existing `OBT*` widgets (verify the exact folder name + path conventions at kickoff).
   - `lib/features/auth/application/auth_state_provider.dart` — `AuthenticatedWithProfile` state shape; the shell does not read auth state directly (the auth state machine in `main.dart` ensures the shell is only mounted when authenticated).
   - **NEW** `docs/sprint-zero/stories/CHORE-pr56-obt-bottom-nav-shell.md` — the story to create in Phase 1. Mirror of `docs/sprint-zero/stories/CHORE-pr45-lookup-rate-limit-and-pr38-cleanup.md` (chore-story format) OR `docs/sprint-zero/stories/FR-PR-03-FR-AC-04-notification-preferences.md` (feature-story format) — RECOMMENDED feature format because the shell satisfies the SCR navigation contract; even though no SRS-functional-requirement is closed, the design-system spec at `components.md §2` is the closure target.
   - **NEW** `lib/core/widgets/obt_bottom_nav.dart` — the design-system primitive.
   - **NEW** `lib/features/shell/presentation/authenticated_shell.dart` — the shell host.
   - **NEW** `lib/features/shell/presentation/home_dashboard_placeholder.dart` — tab 0 placeholder.
   - **NEW** `lib/features/shell/presentation/groups_list_placeholder.dart` — tab 2 placeholder.
   - **NEW** `lib/features/shell/application/shell_telemetry.dart` — telemetry constants.
   - `lib/main.dart` — EXTEND with the `AuthenticatedShell` import + the line-133 wire change. Verify no other reference to `HomePlaceholderScreen` remains after the swap.
   - `lib/features/auth/presentation/home_placeholder_screen.dart` — DELETE per architect §2.x ratification (RECOMMENDED).
   - **NEW** `test/features/shell/authenticated_shell_test.dart` — comprehensive widget tests (tab 0 renders Home placeholder on mount; tap tab 1 renders Friends; tab state preserved across switches via IndexedStack; tap tab 3 renders Activity; tap tab 4 renders Profile; tab 2 renders Groups placeholder; bottom_nav_tab_selected telemetry fires per tap with correct parameters; Android back-button on non-zero tab returns to tab 0 per architect §2.x).
   - **NEW** `test/core/widgets/obt_bottom_nav_test.dart` — design-system widget tests (renders 5 tabs with correct labels + icons; `currentIndex: 0` shows Home filled icon and rest outlined; tapping a non-active tab fires `onTabSelected(int)` with the correct index; tapping the active tab is a no-op OR fires the callback with the same index per architect §2.x; minimum tap target ≥ 48×48 dp per accessibility spec; accessibility semantics labels match `components.md §2`).
   - **NEW** `test/features/shell/shell_telemetry_test.dart` — telemetry constants exist + parameter contract (mirror of `notification_preferences_telemetry_test.dart` if one exists; if not, the asserts go in the shell widget test instead).
   - **NEW** `test/features/shell/shell_boundary_contract_test.dart` — Inv-1 + Inv-2 + PII-leak guards over the new `lib/features/shell/**` + `lib/core/widgets/obt_bottom_nav.dart` files (mirror of `test/features/profile/notification_preferences_boundary_contract_test.dart` template; assert zero `.toDouble()` / `/100` / `parseFloat` / `simplifiedBalances` / `'userId'` / `'uid'` literals in the new files).
   - `test/features/auth/home_placeholder_screen_test.dart` — DELETE if it exists (per the `HomePlaceholderScreen` deletion). Verify at kickoff.
   - `firestore.rules` — UNCHANGED.
   - `firestore.indexes.json` — UNCHANGED.
   - `storage.rules` — UNCHANGED.
   - `functions/package.json` — UNCHANGED.
   - `functions/src/**` — UNCHANGED (zero new server-side code).
   - `pubspec.yaml` — UNCHANGED (no new dependencies; the shell uses only `flutter/material.dart` + `flutter_riverpod`).
   - `pubspec.lock` — UNCHANGED.
   - `ios/Podfile.lock` — UNCHANGED.
   - `docs/sprint-zero/sprint-2-plan.md` — add PR #56 row (3 SP; cumulative 87 SP / 20 PRs).
   - `docs/sprint-zero/next-three-prs.md` — roll PR #56 to merged; PR #57 / #58 / #59 candidates (top remaining: FR-HD-04 FAB + Add Expense context picker; FR-SE-08 dedicated settlement-history screen; FR-PR-05 Contact Support; Issue #47 rules-hardening; `app_settings` Open Settings CTA chore).
   - `docs/audits/sprint-1/07-bucket-b-burndown.md` — PR #56 entry; closes the long-standing **PR #52 §2.1 OBTBottomNav deferral** (this is the named follow-up item that has been carried forward through PR #53, PR #54, PR #55).
   - **CI hygiene** — `dart format .` MUST be run before push. `flutter analyze --fatal-infos` MUST exit 0. `flutter pub get` should be a no-op (no pubspec changes). **Functions hygiene** — `npm run lint && npm run build && npm test` MUST be green (UNCHANGED — 319 / 22 suites; zero server-side code changes); `npm run test:rules` MUST be green (191 / 9 suites — unchanged).

6. Honour the four invariants. Invariant 1 (paise integers) is **N/A** on this PR — no monetary values flow through the shell, bottom-nav widget, or placeholder screens. The boundary-contract grep at `test/features/shell/shell_boundary_contract_test.dart` is a defence-in-depth assertion that no `.toDouble()`, `/100`, `double` declarations exist in the new files. Invariant 2 (`simplifiedBalances` server-only) is **N/A** — no `simplifiedBalances` access on any new code path. The same boundary-contract grep asserts zero references to `simplifiedBalances` in the new files. Invariant 3 is N/A — no share-sheet code paths. Invariant 4 is N/A — no new Firebase SDK usage; the shell mounts existing Riverpod-backed widgets that consume the existing Firebase initialisation from `lib/main.dart`.

────────────────────────────────────────
PHASE 0 — DEPENDENCY-DAG CHECK
────────────────────────────────────────

0.1  Confirm PR #55 has merged to main and the post-merge state is clean:
     - `git log --oneline -1` on `main` shows `ac5b05a feat(profile): FR-PR-03+FR-AC-04 notification prefs UI (#55)`.
     - `lib/features/profile/presentation/notification_preferences_screen.dart` exists and is wired from `profile_screen.dart`.
     - `lib/features/profile/application/notification_preferences_controller.dart` exists with the per-toggle 500ms debounce + offline latch + telemetry.
     - `lib/core/connectivity/connectivity_provider.dart` exists with the `IsOnline` typedef + `connectivityCheckProvider`.
     - `pubspec.yaml` has `connectivity_plus: ^6.1.5` and `cloud_functions: ^6.3.2`.
     - `lib/main.dart` has the `ProviderScope` overrides for `reminderRepositoryProvider` + `matchingRepositoryProvider` (lines 81-93) and the Functions emulator wiring (lines 57-61).
     - `lib/features/auth/presentation/home_placeholder_screen.dart` exists with the temporary AppBar actions (lines 21-46) — the file PR #56 deletes.
     - `lib/main.dart:133` renders `HomePlaceholderScreen()` under `AuthenticatedWithProfile()` — the wire change target.
     - `lib/features/friends/presentation/friends_list_screen.dart`, `lib/features/activity/presentation/activity_feed_screen.dart`, `lib/features/profile/presentation/profile_screen.dart` all exist and own their own AppBars.
     - `lib/features/groups/` is empty / does not exist — the Groups epic placeholder is greenfield.
     - `flutter analyze --fatal-infos` + `flutter test` exit 0 (1170 pass / 30 skipped post-PR-#55).
     - `cd functions && npm run lint && npm run build && npm test` exit 0 (22 suites / 319 tests pass).
     - `cd functions && npm run test:rules` exits 0 against the emulator (9 suites / 191 tests pass — pre-existing 6 storage `receipts.test.ts` failures reproduce on `main` per PR #54 reviewer note; UNRELATED to this PR).
     - `dart format --set-exit-if-changed .` exits 0.
     - `.firebaserc` contains exactly one project (Invariant 4 CI gate green).
     - Issues #47 (rules-hardening), #49 (orphan-cleanup), #50 (trigger no-op optimisation) are OPEN and unchanged — PR #56 does NOT close any of them.

0.2  Walk the dependency DAG for the shell:
     - **`HomePlaceholderScreen` (PR #10 auth landing):** the temporary placeholder being replaced; the AppBar actions plumbing is removed; the body content is extracted to `HomeDashboardPlaceholder`.
     - **`ProfileScreen` (PR #29 SCR-26):** stable; rendered as tab 4 content. The SCR-26 → SCR-27 push (Notification Preferences) + SCR-26 → SCR-29 push (Edit Profile) continue to work because the existing `Navigator.of(context).push(MaterialPageRoute(...))` calls stack on top of the shell, hiding the bottom nav per the EditProfileScreen precedent.
     - **`FriendsListScreen` (PR #35 SCR-09):** stable; rendered as tab 1 content. The SCR-09 → SCR-12 push (FriendDetailScreen via FR-FR-04 PR #42) continues to work.
     - **`ActivityFeedScreen` (PR #52 SCR-25):** stable; rendered as tab 3 content. The SCR-25 row-tap → friend/group detail push (via `notification_deep_links.dart`) continues to work.
     - **`OBTAppBar` precedent (PR #29 design-system):** the per-tab AppBars own their own titles; the shell does NOT inject an outer AppBar.
     - **`AddExpenseBottomSheet` (PR #38 SCR-21):** stable; reached today only via the per-friend AddExpense FAB on FriendDetailScreen. The shell-level FR-HD-04 FAB is OUT OF SCOPE for this PR per §4 above.
     - **`go_router` / `ShellRoute`:** NOT in pubspec; the design spec at `navigation-flow.md §4.4` ratifies them as the long-term mechanism but the IndexedStack interim is permitted per the architect's choice. PR #56 ships IndexedStack; `go_router` migration is a separate Sprint 3 story.
     - **Boundary contracts:** the existing Flutter-side grep template at `test/features/profile/notification_preferences_boundary_contract_test.dart` does NOT cover the new `lib/features/shell/**` files. PR #56 ships a parallel grep — but only over the NEW files in `lib/features/shell/` + the new `lib/core/widgets/obt_bottom_nav.dart` (the existing `profile_screen.dart` + other tab content files are out of scope for this PR's grep).

0.3  Confirm DoR for the shell story:
     - **No story file exists yet** — `docs/sprint-zero/stories/CHORE-pr56-obt-bottom-nav-shell.md` must be created by the PM in Phase 1.
     - Story points: **3 SP** (per §1).
     - The story must explicitly enumerate: (a) the GitHub issue this PR closes — **none** (the OBTBottomNav shell is a UX-foundation chore; the deferral was tracked in `next-three-prs.md` and in PR #52's architect §2.1, not as a separate GitHub issue), (b) the four-invariant applicability assessment (1: N/A, 2: N/A, 3: N/A, 4: N/A), (c) the cross-cutting telemetry contract (1 new client event — `bottom_nav_tab_selected`; verify pre-declaration in `telemetry-plan.md` at kickoff), (d) the explicit decision on IndexedStack vs `go_router` (architect §2.x ratifies IndexedStack per recommendation), (e) the explicit decision on the shell navigation controller (architect §2.x ratifies in-shell `setState` for PR #56; controller adoption deferred to follow-up FR-AC-05 expansion), (f) the explicit decision on the FAB inclusion (architect §2.x ratifies OUT OF SCOPE per recommendation; FR-HD-04 FAB ships in a focused follow-up), (g) the explicit decision on `HomePlaceholderScreen` deletion (architect §2.x ratifies DELETE per recommendation), (h) the explicit decision on the `groups_list_placeholder.dart` location (architect §2.x ratifies `lib/features/shell/presentation/` per recommendation), (i) the explicit decision on `PopScope` Android back-button handling (architect §2.x ratifies snap-back-to-tab-0 per recommendation).

0.4  Cross-reference the burndown:
     - PR #56 closes **the PR #52 §2.1 bottom-nav-shell deferral** (the named follow-up item tracked through PR #53, PR #54, PR #55).
     - PR #56 closes the long-standing temporary `HomePlaceholderScreen` AppBar action plumbing (lines 21-46 of that file).
     - PR #56 makes partial progress on **UX foundation hardening** — the canonical entry point for the 5-tab cluster lands; Sprint 3 features (Groups epic + FR-HD-01..04 dashboard) inherit a stable shell.
     - Issues #47, #49, #50 remain OPEN and are NOT touched by PR #56.
     - PR #56 may file follow-up issues for: (1) FR-HD-04 FAB wiring (P0 — natural pair with the Add Expense context picker), (2) FR-HD-01..04 home dashboard implementation (P0 — separate ~5-8 SP story), (3) `go_router` migration (Sprint 3 chore), (4) shell-navigation-controller adoption for FR-AC-05 deep-link tab-switching expansion.

────────────────────────────────────────
PHASE 1 — STORY READINESS (PM)
────────────────────────────────────────

PM agent creates the feature/chore story `docs/sprint-zero/stories/CHORE-pr56-obt-bottom-nav-shell.md` using the SRS §13.2 feature format (even though no SRS-functional-requirement closes; the design-system spec `components.md §2` is the closure target). Story points: **3** (per §0.3).

Required Given/When/Then ACs (each MUST be testable):

  **Shell render contract — positive ACs**

  - **AC-1 — Shell renders Home tab on initial mount.** Given an `AuthenticatedWithProfile` user lands on the shell, when the shell mounts, then `currentIndex` is 0, the Home tab is visually selected (filled icon + primary colour label), the body renders `HomeDashboardPlaceholder` (Icon + "Home" + "The real dashboard is coming soon." copy), AND the `OBTBottomNav` is visible at the bottom of the Scaffold.
  - **AC-2 — Tap Friends tab switches to FriendsListScreen.** Given the shell is rendered with `currentIndex: 0`, when the user taps the Friends tab (index 1), then `currentIndex` becomes 1, the Friends tab is visually selected, the body renders `FriendsListScreen`, AND a `bottom_nav_tab_selected` telemetry event fires with `{tab_index: 1, tab_label: "friends"}`.
  - **AC-3 — Tap Groups tab renders the Groups placeholder.** Given the shell is on any tab, when the user taps Groups (index 2), then the body renders `GroupsListPlaceholder` (Icon + "Groups" + "Coming in Sprint 3" copy), AND telemetry fires with `{tab_index: 2, tab_label: "groups"}`.
  - **AC-4 — Tap Activity tab renders ActivityFeedScreen.** Given the shell is on any tab, when the user taps Activity (index 3), then the body renders `ActivityFeedScreen`, AND telemetry fires with `{tab_index: 3, tab_label: "activity"}`.
  - **AC-5 — Tap Profile tab renders ProfileScreen.** Given the shell is on any tab, when the user taps Profile (index 4), then the body renders `ProfileScreen`, AND telemetry fires with `{tab_index: 4, tab_label: "profile"}`.
  - **AC-6 — Tab state preservation via IndexedStack.** Given the user scrolls the FriendsListScreen halfway down, then switches to the Profile tab, then switches back to Friends, when the Friends tab re-renders, then the scroll position is PRESERVED (not reset to top). The IndexedStack guarantee is the canonical Material 3 tab-shell behaviour.

  **OBTBottomNav design-system contract**

  - **AC-7 — `OBTBottomNav` renders 5 tabs with the spec-ratified labels and icons.** Given the widget is mounted with `currentIndex: 0`, when scanned, then 5 `BottomNavigationBarItem` entries are present with labels (Home, Friends, Groups, Activity, Profile), the outlined icons are used on inactive tabs, the filled icons are used on the active tab, AND the active tab uses `Theme.of(context).colorScheme.primary`.
  - **AC-8 — Tapping a tab invokes `onTabSelected` with the correct index.** Given the widget is mounted with `currentIndex: 0` and an `onTabSelected` callback that records its arguments, when the user taps the Activity tab, then the callback fires exactly once with `3`.
  - **AC-9 — Minimum tap target is 48×48 dp per accessibility spec.** Given the widget is mounted, when each tab's `BottomNavigationBarItem` is measured, then each tap-target's rendered size is ≥ 48 dp in both axes (Material's `BottomNavigationBar` default).
  - **AC-10 — Active tab announces "Selected" suffix via Semantics.** Given the widget is mounted with `currentIndex: 2`, when scanned via `tester.getSemantics(find.text('Groups'))`, then the semantics node carries the `isSelected` flag (Material's default; the `"Selected"` suffix announcement is the accessibility framework's responsibility).

  **Shell navigation + auth + back-button contract**

  - **AC-11 — Android back-button on non-zero tab snaps to tab 0.** Given the shell is on tab 3 (Activity), when the user invokes the Android back-button, then `currentIndex` becomes 0 (Home tab is rendered) AND the app is NOT closed. **Architect's call at §2.x** on whether to ALSO fire `bottom_nav_tab_selected` telemetry for the back-driven switch — RECOMMENDED NO (telemetry distinguishes user-initiated tab taps; back-button is a different intent).
  - **AC-12 — Android back-button on tab 0 closes the app (default behaviour).** Given the shell is on tab 0 (Home), when the user invokes the Android back-button, then the default `PopScope` behaviour fires (the framework closes the app since there are no routes to pop).
  - **AC-13 — Shell-internal `Navigator.push` stacks ON TOP of the bottom nav.** Given the user is on tab 4 (Profile) and taps the "Notification Preferences" row (SCR-26 → SCR-27 navigation), when the SCR-27 screen pushes via `MaterialPageRoute`, then the `OBTBottomNav` is HIDDEN (the SCR-27 Scaffold paints over the entire screen) — the existing `EditProfileScreen` precedent. When the user pops SCR-27 via the back button or AppBar back arrow, then the shell re-renders with `currentIndex: 4` and the bottom nav is visible again.

  **`HomePlaceholderScreen` deletion**

  - **AC-14 — `HomePlaceholderScreen` and its test are deleted from the codebase.** Given the PR diff, when scanned, then `lib/features/auth/presentation/home_placeholder_screen.dart` no longer exists, NO file imports it, AND `test/features/auth/home_placeholder_screen_test.dart` is deleted IF it existed before this PR.

  **Cross-cutting and negative guards**

  - **AC-15 — Telemetry PII guard.** Given the `bottom_nav_tab_selected` event fires, when scanned, then the payload does NOT include a `userId` / `uid` / `friendship_id` / `friendship_id_hash` parameter. The PII-leak grep test asserts this.
  - **AC-16 — Invariant 1 boundary contract.** Given the PR diff, when scanned for `.toDouble()`, `parseFloat`, `/ 100`, `.toFixed`, or `double ` declarations on the new files, then ZERO violations exist anywhere in `lib/features/shell/**` or `lib/core/widgets/obt_bottom_nav.dart`. The new client-side grep at `test/features/shell/shell_boundary_contract_test.dart` is the affirmative gate.
  - **AC-17 — Invariant 2 negative guard.** Given the PR diff, when scanned, then ZERO references to `simplifiedBalances` exist anywhere in the new files.
  - **AC-18 — Shell does NOT inject an outer AppBar.** Given any tab is rendered, when the widget tree is inspected, then the shell's Scaffold has `appBar: null` — each tab content widget owns its own AppBar. The negative widget test asserts this is true.

────────────────────────────────────────
PHASE 2 — ARCHITECT HAND-OFF
────────────────────────────────────────

Architect agent appends `## Architect Notes` to the chore story ratifying:

2.1  **Shell mechanism: IndexedStack vs go_router ShellRoute.** RATIFY: `IndexedStack` for PR #56. The `go_router ShellRoute` ratified by `navigation-flow.md §4.4` is the long-term mechanism but requires migrating every existing `Navigator.push(MaterialPageRoute(...))` call site (currently 20+ sites across friends / profile / activity / expenses). That migration is a Sprint 3 standalone chore PR (~3-5 SP). For PR #56 the IndexedStack interim is correct: tab state preservation works out of the box, no per-tab `Navigator` keys needed (each tab uses the root `Navigator` so pushes stack ON TOP of the shell — the canonical "EditProfileScreen pushes over the profile tab and hides the bottom nav" precedent).

2.2  **Shell navigation controller adoption.** RATIFY: in-shell `setState` for PR #56 (no Riverpod `Notifier<int>`). The only entry point is user tap; programmatic switches are deferred to the FR-AC-05 cold-start deep-link expansion (separate follow-up PR — that PR introduces the `shellNavigationControllerProvider` because the FCM handler must call `controller.switchTo(tabIndex)` from outside the widget tree). Keeping PR #56 simple avoids inventing a controller that has only one caller.

2.3  **FAB inclusion (FR-HD-04).** RATIFY: OUT OF SCOPE for PR #56. The FR-HD-04 P0 FAB requires a context picker (Friend or Group), and the Group path depends on the Groups epic. Ship the FAB in a focused follow-up that lands after the Groups epic (Sprint 3) OR as a near-term ~1-2 SP follow-up that stubs the Group path with "Coming soon" snackbar. Adding the FAB in this PR pushes scope from 3 to 5 SP without buying meaningful coverage.

2.4  **`HomePlaceholderScreen` deletion.** RATIFY: DELETE in this PR. The file's sole purpose was the placeholder + temporary AppBar plumbing; both are superseded. The `home_placeholder_screen_test.dart` (verify existence at kickoff) is deleted IF it asserts the temporary AppBar action paths. If it asserts only the body copy (which the new `HomeDashboardPlaceholder` preserves), then the test is RENAMED + REPOINTED to the new file. RECOMMENDED at kickoff: read the test once and decide.

2.5  **Placeholder file locations.** RATIFY:
     - `lib/features/shell/presentation/home_dashboard_placeholder.dart` — colocated with the shell because the placeholder is a SHELL artefact (the real `HomeDashboardScreen` lands in the FR-HD-01..04 PR under `lib/features/home/presentation/`).
     - `lib/features/shell/presentation/groups_list_placeholder.dart` — colocated with the shell because the Groups feature folder does not exist yet. The placeholder is DELETED when the Sprint 3 Groups epic ships the real `GroupsListScreen` under `lib/features/groups/presentation/`.

2.6  **`PopScope` Android back-button handling.** RATIFY: snap to tab 0 on back-button-from-non-zero-tab. The implementation is a `PopScope(canPop: _currentIndex == 0, onPopInvoked: (didPop) { if (!didPop) setState(() => _currentIndex = 0); })`. Without this, the Android back button on tab 3 closes the app (default Material behaviour) which is jarring. The back-driven switch does NOT fire telemetry (different intent than user-initiated tap).

2.7  **`OBTBottomNav` location.** RATIFY: `lib/core/widgets/obt_bottom_nav.dart` (mirror of the existing design-system primitives folder). Verify the exact folder name at kickoff — if the existing `OBT*` widgets live under a different path (e.g. `lib/core/design_system/` or `lib/app/widgets/`), use that.

2.8  **Files to touch (exhaustive — anything outside this set is scope creep):** [list per §5 above]

2.9  **Files explicitly NOT to touch (negative scope guardrails):**
     - `firestore.rules`, `firestore.indexes.json`, `storage.rules` — UNCHANGED.
     - `functions/package.json`, all of `functions/src/**`, all of `functions/test/**` — UNCHANGED.
     - `lib/features/expenses/**`, `lib/features/settlements/**`, `lib/features/activity/**`, `lib/features/notifications/**`, `lib/features/reminders/**`, `lib/features/friends/**`, `lib/features/profile/**` (the tab content surfaces stay stable; the shell merely mounts them) — UNCHANGED.
     - `lib/features/auth/**` except for `home_placeholder_screen.dart` (DELETED per §2.4) — UNCHANGED.
     - `lib/core/connectivity/**`, `lib/core/balances/**`, `lib/core/formatters/**`, `lib/core/routing/**`, `lib/core/services/**`, `lib/core/telemetry/**` — UNCHANGED.
     - `pubspec.yaml`, `pubspec.lock`, `ios/Podfile.lock` — UNCHANGED.
     - `.github/workflows/*.yml` — UNCHANGED.
     - `docs/design/**` — read-only references; no spec updates in this PR.

2.10 **Anticipated reconciliations:**
     1. `lib/features/profile/presentation/profile_placeholder_screen.dart` exists alongside the real `profile_screen.dart` (noted in PR #55 §2.10 reconciliation 1) — still dead code, still NOT touched by this PR (separate cleanup chore).
     2. The 5 tab content widgets each own their own AppBar; the shell does NOT inject an outer AppBar. If a future PR adds a global app-level AppBar (e.g. for a notification badge or search), it ships above the shell or under the shell's Scaffold — that's a Sprint 3 design call.
     3. The `bottom_nav_tab_selected` telemetry event MAY not be pre-declared in `docs/design/07-technical/telemetry-plan.md`. If absent, the architect appends a new row in this PR's diff under "Navigation events". Verify at kickoff.
     4. The `OBTBottomNav` design spec ratifies a "filled icon + primary colour tint + indicator pill behind icon" for the active tab. Flutter's `BottomNavigationBar` does NOT render an indicator pill by default. RATIFY: ship without the pill for v1.0 (Material 3 `NavigationBar` has the pill but is a different widget with different defaults — a `NavigationBar` migration is its own design call). The icon-fill-on-select + colour-tint-on-select are correct out of the box.
     5. The `go_router` migration is a long-deferred Sprint 3 story. If it lands in Sprint 3 ahead of this PR's tab-state-preservation guarantee, the shell is refactored to a `StatefulShellRoute.indexedStack` (the canonical go_router replacement for `IndexedStack`) — the migration preserves the per-tab scroll positions because the canonical pattern uses `IndexedStack` under the hood.

────────────────────────────────────────
PHASE 3 — SCOPE GUARDRAILS
────────────────────────────────────────

WHAT GOES IN PR #56:

  - All files listed in §2.8.
  - The `OBTBottomNav` design-system primitive + accessibility + golden test (golden test OPTIONAL per §4).
  - The `AuthenticatedShell` host with IndexedStack tab preservation + `PopScope` back-button handling + telemetry.
  - The 2 placeholder screens (Home dashboard + Groups list).
  - The `lib/main.dart:133` wire change swapping `HomePlaceholderScreen` for `AuthenticatedShell`.
  - The `HomePlaceholderScreen` deletion (+ its test file deletion if applicable).
  - The 1 new client telemetry event (`bottom_nav_tab_selected`) + the telemetry-plan.md append IF the event isn't pre-declared.
  - The new boundary-contract grep + PII-leak grep over `lib/features/shell/**` + `lib/core/widgets/obt_bottom_nav.dart`.
  - The story + Architect Notes.
  - Plan + burndown roll-ups (Phase 7).

WHAT DOES NOT GO IN PR #56:

  - **FR-HD-01..04 home dashboard implementation** — separate P0 story; ~5-8 SP.
  - **FR-HD-04 FAB wiring** — defer to focused follow-up; depends on Add Expense context picker which itself depends on the Groups epic for the Group path.
  - **`go_router` migration** — Sprint 3 standalone chore.
  - **Groups feature implementation** — Sprint 3 epic.
  - **FR-PR-02 phone-number-change flow** — separate P1 story.
  - **FR-PR-05 Contact Support `mailto:` flow** — separate P0 story.
  - **FR-AU-09 account-deletion flow** — separate P1 story.
  - **`shared_preferences` adoption** — defer until third consumer needs it.
  - **`OBTRupeeText` primitive** — still deferred per PR #52 §2.6.
  - **`cloud-functions-catalogue.md §7` docs roll-up** — separate cosmetic chore PR.
  - **Activity-writer rename cleanup** — still deferred per PR #52 §2.3.
  - **FR-SE-09 message-compose dialog follow-up** — separate UX PR.
  - **FR-SE-08 dedicated settlement-history screen** — separate P0 story.
  - **`app_settings` / `permission_handler` Open Settings CTA chore** — separate ~1-2 SP follow-up per PR #55 deviation 1.
  - **Shell navigation controller for programmatic tab-switching** — defer to FR-AC-05 cold-start deep-link expansion.
  - **FR-AC-05 deep-link expansion to land on a specific tab** — separate enhancement.
  - **`profile_placeholder_screen.dart` cleanup** — defer per architect §2.10 reconciliation 1.
  - **Issue #47 rules-hardening, Issue #49 orphan-cleanup, Issue #50 trigger no-op optimisation** — separate chores; #50 EXPLICITLY CONSTRAINED.
  - **OAuth / SSO** — not in v1.0.
  - **Material 3 `NavigationBar` migration (replacing `BottomNavigationBar`)** — separate design call per architect §2.10 reconciliation 4.

If an agent suggests any of: "while we're shipping the shell, let's also wire FR-HD-04 FAB", "let's also migrate to go_router", "let's also ship the Groups epic", "let's also ship the FR-HD-01..04 dashboard", "let's also adopt Material 3 NavigationBar", "let's also add a notification badge to the Activity tab", "let's also add a global app-level AppBar above the shell", "let's also wire the shell to the FCM deep-link tab-switching", "let's also ship the `shellNavigationControllerProvider` for testability", "let's also delete `profile_placeholder_screen.dart` since we're already deleting `home_placeholder_screen.dart`" — REFUSE. These are scope creep.

────────────────────────────────────────
PHASE 4 — TEST-FIRST DISCIPLINE
────────────────────────────────────────

PR #56 follows the standard test-first cadence:

1. Snapshot the pre-fix baseline: 1170 Flutter / 30 skipped; 319 functions / 22 suites; 191 rules / 9 suites.
2. Write the failing Flutter tests FIRST: `obt_bottom_nav_test.dart` (5 tabs render; tap fires callback with correct index; outlined/filled icons swap on selection; minimum tap target ≥ 48 dp; accessibility semantics); `authenticated_shell_test.dart` (each tab renders the correct content widget; tab state preserved via IndexedStack; PopScope back-button snaps to tab 0; telemetry per tap with correct parameters; shell Scaffold has appBar: null); `shell_boundary_contract_test.dart` (Inv-1 + Inv-2 + PII-leak greps over the new files).
3. Implement Flutter source: `shell_telemetry.dart` → `obt_bottom_nav.dart` → `home_dashboard_placeholder.dart` → `groups_list_placeholder.dart` → `authenticated_shell.dart` → `main.dart` wire change → `home_placeholder_screen.dart` deletion.
4. Re-run `dart format --set-exit-if-changed .` — 0 changes.
5. Re-run `flutter analyze --fatal-infos` — "No issues found".
6. Re-run `flutter test` — expected 1170 + ~15 new = ~1185 tests pass.
7. Re-run `cd functions && npm run lint && npm run build && npm test` — 319 (UNCHANGED — no server-side code changed).
8. Re-run `cd functions && npm run test:rules` — 191 (UNCHANGED).
9. Re-run `cd functions && npm run test:integration` — UNCHANGED.
10. Manual smoke (Phase 5 §10) on a real device with the real app.

**Combined CI dry-run:** all jobs must be green.

────────────────────────────────────────
PHASE 5 — EXECUTION PROTOCOL
────────────────────────────────────────

Mirror the PR #55 commit cadence:

 1. PM creates story `docs/sprint-zero/stories/CHORE-pr56-obt-bottom-nav-shell.md`. Commits as `docs(stories): PR #56 OBTBottomNav shell story`.
 2. Architect appends `## Architect Notes` (§2.1–§2.10). Commits as `docs(stories): PR #56 architect notes`.
 3. Flutter-dev writes failing tests for the OBTBottomNav primitive + the AuthenticatedShell + boundary contract. Commits as `test(shell): OBTBottomNav and authenticated shell tests`.
 4. Flutter-dev ships the OBTBottomNav design-system primitive + the telemetry constants. Commits as `feat(shell): OBTBottomNav design-system primitive`.
 5. Flutter-dev ships the AuthenticatedShell host + the 2 placeholder screens. Commits as `feat(shell): authenticated tab shell with IndexedStack`.
 6. Flutter-dev wires `main.dart` and deletes `HomePlaceholderScreen`. Commits as `chore(shell): swap HomePlaceholderScreen for AuthenticatedShell`.
 7. QA runs the manual smoke matrix:
    - Sign in as a fresh user; verify the AuthenticatedShell mounts with the Home tab active.
    - Tap Friends; verify the FriendsListScreen renders and the bottom nav stays visible.
    - Scroll halfway down; switch to Profile; switch back to Friends; verify the scroll position is preserved.
    - Tap Profile; tap "Notification Preferences"; verify SCR-27 pushes OVER the shell (bottom nav hidden); tap back; verify the shell re-renders with Profile tab still active.
    - Tap Activity; verify the activity feed renders.
    - Tap Groups; verify the Groups placeholder renders with the "Coming in Sprint 3" copy.
    - On Android, press the system back button from each tab; verify the back button on non-zero tabs snaps to Home (tab 0) and the back button on Home closes the app.
    - Tap each tab and verify the `bottom_nav_tab_selected` event fires in the analytics log with the correct `tab_index` + `tab_label` parameters.
 8. Docs hand updates plan + roll-forward:
    - `docs/sprint-zero/sprint-2-plan.md` (PR #56 row; 3 SP; cumulative 87 SP / 20 PRs).
    - `docs/sprint-zero/next-three-prs.md` (PR #57 / #58 / #59 candidates — top is FR-HD-04 FAB + Add Expense context picker OR FR-SE-08 settlement-history screen).
    - `docs/audits/sprint-1/07-bucket-b-burndown.md` (PR #56 entry; closes PR #52 §2.1 OBTBottomNav deferral).
    - Commits as `docs(plan): PR #56 shipped, prep PR #57`.
 9. PR opened. Title (≤72 chars subject): `feat(shell): OBTBottomNav + authenticated tab shell`. Body must:
    - Cite `components.md §2`, `navigation-flow.md §4.4`, the relevant SRS rows (4.8 FR-HD-01..04 deferral + 6 navigation contracts).
    - Confirm Invariants 1 / 2 / 3 / 4 (all N/A on this PR).
    - State explicitly the deferred items: FR-HD-04 FAB, FR-HD-01..04 dashboard, go_router migration, Groups epic, `shellNavigationControllerProvider`, FCM tab-switching, profile_placeholder cleanup.
    - End with `Next PR: PR #57 — TBD per architect's call at kickoff (top candidate: FR-HD-04 FAB + Add Expense context picker).`

────────────────────────────────────────
PHASE 6 — DEFINITION OF DONE GATE
────────────────────────────────────────

Walk the DoD checklist. Particular emphasis:

  - DoD §2: every layer green (Flutter + ~15 new tests / Functions unchanged / Rules unchanged / format / analyze).
  - DoD §3: QA sign-off with evidence for the 5-tab cluster end-to-end (tab tap → correct content → telemetry), tab state preservation, SCR-27 push-over-shell pattern, Android back-button snap-to-zero, Groups placeholder renders.
  - DoD §7: invariant compliance.
    * Inv-1: N/A; defence-in-depth grep returns 0 violations in the new files.
    * Inv-2: N/A; defence-in-depth grep returns 0 references to `simplifiedBalances` in the new files.
    * Inv-3: N/A.
    * Inv-4: N/A; no new Firebase SDK usage.
  - DoD §8: documentation updated (story + architect notes + sprint-2-plan + next-three-prs + burndown).
  - DoD §9: deploy verification. The Flutter client ships in the next mobile build with the new shell; the Functions side is UNCHANGED so no Functions deploy is required.

QA posts the explicit DoD §3 sign-off, including the negative-scope greps from AC-16 (no monetary doubles in `lib/features/shell/**` + `lib/core/widgets/obt_bottom_nav.dart`) and AC-17 (no `simplifiedBalances` references) and AC-15 (PII-leak grep clean for the new telemetry event).

**Pre-push CI hygiene:** run `dart format .` AND all test layers BEFORE `git push`.

────────────────────────────────────────
PHASE 7 — UPDATE THE ROLLING PLAN AND BURNDOWN
────────────────────────────────────────

PM agent updates:
  - `docs/sprint-zero/sprint-2-plan.md` — PR #56 status (merged), velocity #20 (3 SP, total now 87 SP across 20 PRs).
  - `docs/sprint-zero/next-three-prs.md`:
      * PR #56 row marked merged; numbering note updated.
      * PR #57: **TBD** per architect's call at kickoff. Top candidates (in rough priority order):
          - **FR-HD-04 FAB + Add Expense context picker** (P0 — natural pair with the shell that just shipped; ~2-3 SP; Group path stubbed with "Coming soon" pending Sprint 3).
          - **FR-SE-08 dedicated full-history screen** at `/settlements/history` (P0; in-timeline rows satisfy v1.0 but the dedicated screen is still backlog; ~3-5 SP).
          - **FR-PR-05 Contact Support `mailto:` flow** (P0; depends on Remote Config wiring; small standalone PR ~2 SP).
          - **FR-PR-02 phone-number-change flow** (P1; ~5 SP).
          - **FR-AU-09 account-deletion flow** (P1; depends on new Cloud Function; ~5-8 SP).
          - **`app_settings` Open Settings CTA chore** (~1-2 SP per PR #55 deviation 1).
          - **Bucket-B chore close-out** (~3 SP).
          - **Issue #47 rules-hardening** (chore; 2 SP).
          - **`shared_preferences` adoption** (~2-3 SP).
          - **FR-SE-09 message-compose dialog follow-up** (~1-2 SP).
          - **`cloud-functions-catalogue.md §7` docs roll-up** (cosmetic chore; ~1 SP).
          - **Activity-writer rename cleanup** (cosmetic chore; ~1-2 SP).
          - **`OBTRupeeText` primitive** (~1 SP — wait for second use site).
          - **`go_router` migration** (Sprint 3 — ~3-5 SP).
          - **`shellNavigationControllerProvider` + FR-AC-05 deep-link tab-switching** (follow-up to PR #56; ~2 SP).
      * PR #58 / #59: TBD per PR #57 outcome.
  - `docs/audits/sprint-1/07-bucket-b-burndown.md`:
      * Header timestamp: PR #55 → PR #56.
      * Append a PR #56 section: CLOSES the long-standing PR #52 §2.1 OBTBottomNav shell deferral. Partial UX-foundation progress: shell + 5 tabs + bottom nav widget + tab state preservation + back-button handling + telemetry + 2 placeholder screens.

Commits as `docs(plan): PR #56 shipped, prep PR #57`.

────────────────────────────────────────
GUARDRAILS
────────────────────────────────────────

If during this PR an agent proposes:
  - "While we're shipping the shell, let's also wire FR-HD-04 FAB" → REFUSE. Separate focused PR; depends on Add Expense context picker which depends on the Groups epic for the Group path. Add 1 SP if the architect ratifies stubbing the Group path with "Coming soon" — but RECOMMENDED defer.
  - "Let's also migrate to go_router since we're touching navigation" → REFUSE. Sprint 3 standalone ~3-5 SP chore. The IndexedStack interim is architect-ratified per §2.1.
  - "Let's also ship the Groups epic since we're adding a Groups tab" → REFUSE. Sprint 3 epic.
  - "Let's also ship the FR-HD-01..04 dashboard since we're adding a Home tab" → REFUSE. Separate P0 ~5-8 SP story.
  - "Let's migrate to Material 3 NavigationBar instead of BottomNavigationBar" → REFUSE per §2.10 reconciliation 4. Separate design call.
  - "Let's also add a notification badge to the Activity tab" → REFUSE. Out of scope.
  - "Let's also wire a global app-level AppBar above the shell" → REFUSE. Each tab owns its own AppBar; a global AppBar is a Sprint 3 design call.
  - "Let's also wire the shell to the FCM deep-link tab-switching" → REFUSE. Separate FR-AC-05 enhancement; requires `shellNavigationControllerProvider`.
  - "Let's also ship the `shellNavigationControllerProvider` for testability" → REFUSE per §2.2. The `setState` approach is testable via widget tests; controller adoption needs a concrete second consumer.
  - "Let's also delete `profile_placeholder_screen.dart` since we're already deleting `home_placeholder_screen.dart`" → REFUSE per §2.10 reconciliation 1. Separate cleanup chore.
  - "Let's NOT delete `home_placeholder_screen.dart` and just leave it as dead code" → REFUSE per §2.4. The deletion is mandatory; dead code carries forward through every future PR.
  - "Let's use the Riverpod `Notifier<int>` from the start instead of `setState`" → REFUSE per §2.2. Defer until a concrete second consumer.
  - "Let's skip the PopScope back-button handling — Android default is fine" → REFUSE per §2.6. The default closes the app from tab 3 which is jarring; the snap-to-zero pattern is canonical Material 3.
  - "Let's add a 6th tab for Reminders" → REFUSE. The `components.md §2` spec ratifies exactly 5 tabs.
  - "Let's swap the tab order to put Profile first" → REFUSE. The order is design-spec ratified; matches the navigation-flow route table.
  - "Let's close issue #47 (rules-hardening) at the same time" → REFUSE. Separate small chore PR.
  - "Let's close issue #49 (orphan-cleanup) at the same time" → REFUSE. FUTURE.
  - "Let's close issue #50 (trigger no-op-recompute optimisation)" → **EXPLICITLY REFUSE** — closing it would BREAK FR-EX-07 AC-2.

If something genuinely surprises (e.g. the `BottomNavigationBar` Flutter widget API has changed in 3.44.x and the spec's icon-fill-on-select pattern requires custom code; OR the `IndexedStack` does NOT preserve scroll state for one of the existing tab content widgets due to a `PageStorageBucket` interaction; OR the `PopScope` API has been deprecated in favour of `Navigator.popUntil` predicates; OR the existing `lib/core/widgets/` folder does not exist and the design-system primitives live elsewhere; OR `home_placeholder_screen_test.dart` asserts something subtle about the temporary AppBar action plumbing that needs preserving in a new shell-level test; OR the existing `friends_list_screen.dart` or `activity_feed_screen.dart` has a hardcoded assumption that it's the root route — e.g. a `WillPopScope` that intercepts the back button), STOP and surface the friction. PR #56 is the first PR that (a) introduces a `lib/features/shell/` feature folder, (b) introduces a stateful authenticated-area wrapper, and (c) adds a design-system primitive that ships independently from a feature — get the patterns right before shipping.

Begin with Phase 0 — verify the dependency DAG (PR #55 merge state at `ac5b05a`; baseline test counts; `HomePlaceholderScreen` and its temporary AppBar plumbing intact at lines 21-46; `FriendsListScreen` / `ActivityFeedScreen` / `ProfileScreen` all own their own AppBars; `lib/main.dart:133` renders `HomePlaceholderScreen()`; `lib/features/groups/` is empty; `lib/core/widgets/` folder exists or its analogue is identified) and re-read the four reference docs (`docs/design/02-design-system/components.md §2 OBTBottomNav` in full, `docs/design/01-information-architecture/navigation-flow.md §1 + §4.4` in full, the existing `lib/features/auth/presentation/home_placeholder_screen.dart` as the file being replaced, and the existing `lib/features/profile/presentation/profile_screen.dart` as the precedent for tab content widgets owning their own AppBar) before kickoff.
