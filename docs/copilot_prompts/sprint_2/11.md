1. You are the OneByTwo orchestrator agent. PR #44 (D5 runtime upgrade — Node 22 + `firebase-functions@7.x`) has merged (squash commit `b0fce99`) and the production canary deploy against `onebytwo-avtanshgupta` confirmed all 5 functions are running on `nodejs22` in `asia-south1` with no invocation gap (post-deploy `GET /healthcheck` → `200 OK { "ok": true, "region": "asia-south1" }` in 254 ms). Issues [#39](https://github.com/avtansh-code/OneByTwo/issues/39) and [#40](https://github.com/avtansh-code/OneByTwo/issues/40) auto-closed. Sprint 2 velocity through end of PR #44: 37 SP across 11 PRs. The deploy path is now de-risked through Node 22 deprecation (2027-04-30). We now implement **PR #45: bundled hygiene PR** — a small atomic chore PR that (a) fixes the `lookup-user-by-phone-number` rate-limit doc-path bug surfaced by PR #36's CI workflow change and (b) closes the three S4 items from the PR #38 QA sign-off "Post-Merge Cleanup Backlog".

2. The numbering continues from PR #44. The post-PR #38 sequence so far is: #38 (PR), #39 (issue — closed), #40 (issue — closed), #41 (PR — D5 backlog cross-refs), #42 (PR — FR-FR-04), #43 (PR — FR-SE-05/06/07), #44 (PR — D5 runtime upgrade). PR #45 is therefore the next CHORE PR. The orchestrator should not assume PR #46 == GitHub issue 46 — issues and PRs share the same sequential namespace, so any new issues filed during PR #45 will consume intermediate numbers. Use the labels in `docs/sprint-zero/next-three-prs.md` to track the next-feature-PR vs next-issue mapping.

3. PR #45 is a **bundled small-hygiene PR** — NOT a feature PR. Two independent streams of work ship together because each is too small to warrant its own PR and both are non-deadline-bound deferred items:
   - **Stream A (functions bug fix, ~2 SP):** the `lookup-user-by-phone-number` rate-limit doc-path bug at `functions/src/lookup-user-by-phone-number/function.ts:108` — `db.doc(`_rateLimits/${callerUid}/lookups`)` is a 3-segment (odd-component) string which Firestore rejects as a document reference (doc paths require even segments; collection paths require odd). Every invocation throws at the rate-limit pre-check step, which means the rate-limit gate has NEVER enforced in production since FR-FR-01 shipped (PR #32 / #34). It was masked because (i) until PR #36 the PR pipeline never ran `npm run test:integration`, and (ii) the live phone-lookup CTAs hit the function via the contact-picker happy path, not in volumes that would have surfaced a 100/hour throttle. The five `describe.skip`'d tests in `functions/test/integration/lookup-user-by-phone-number.integration.test.ts` document the canonical seed path `_rateLimits/${userId}/lookups/counter` (4-segment subcollection doc) — the test author had the architect-intended layout in mind; production code just never aligned.
   - **Stream B (docs + test description hygiene, ~1 SP):** the three S4 items from the PR #38 QA sign-off "Post-Merge Cleanup Backlog" (`docs/sprint-zero/sprint-2-plan.md` lines 285–355). All three are non-functional — runtime behaviour and live analytics dashboards are already correct. Items: (i) six stale `expense_added` / `expense_add_failed` **telemetry** references in three design docs that the architect's Camp B propagation scope in PR #38 missed; (ii) eleven splitter-test description references to `99999999` paise as "the maximum permitted total" left over from the 8-nines → 9-nines amount-cap typo fix (commit `684cd09`); (iii) a missing `// TODO(SCR-08)` comment at the top of `friends_list_screen.dart` prescribed by FR-EX-01 Architect Notes §2.10.

4. PR #45 has **no mandatory cross-PR bundle** beyond the two streams above. The two streams ship together for atomic-rollback reasons (a single `git revert` restores both) and because each alone is too small to warrant a dedicated PR. They are independent in scope: Stream A touches only `functions/**` + maybe rules tests; Stream B touches only docs + tests + one Flutter file (single-line comment). PR #45 MUST NOT bundle any other deferred work — no FR-EX-05 / FR-EX-06 / FR-SE-09 / FR-SE-08 (those are P0/P1 features with their own stories and ACs; they belong in PR #46+). No Riverpod 3.x / share_plus / build_runner / Jest 30 dependency bumps (those are non-urgent D-row items under umbrella #22; no deadline pressure post-D5).

5. Read before starting:
   - `.github/copilot-instructions.md`
   - `.github/shared/invariants.md` — invariants 1 (paise integers; **N/A this PR**), 2 (`simplifiedBalances` client-read-only; **N/A this PR** — the rate-limit collection is `_rateLimits/**`, an internal-only namespace), 3 (system share sheet only — N/A), 4 (single Firebase project — **defence-in-depth re-check** because the rate-limit fix touches a Cloud Function and any deploy goes to the single production project).
   - `.github/shared/coding-standards.md` — TypeScript section. The Cloud Functions PR checklist (region pinning, error-code mapping, transaction usage, idempotency) applies to Stream A.
   - `.github/shared/decision-log.md` ADR-0011 (Cloud Function module layout — `algorithm.ts` + `function.ts` + `index.ts`; the bug fix preserves the layout), and the lookup-user-by-phone-number entry around lines 695–765 which canonicalises `_rateLimits/{userId}/lookups` as a logical path — the fix lands the architect-intended subcollection layout.
   - `docs/patterns/feature-pr-conventions.md` — Cloud Functions PR Conventions section. The five-layer test pyramid (algorithm unit, algorithm property, boundary, rules, integration) must all stay green; Stream A flips 5 previously-skipped integration tests from `describe.skip` to `describe`.
   - `docs/audits/sprint-1/07-bucket-b-burndown.md` — Stream A is tracked as a "NEW finding (not Bucket B)" surfaced by PR #36; Stream B has no Bucket-B linkage (it's PR #38 follow-up only).
   - `docs/sprint-zero/sprint-2-plan.md` Post-Merge Cleanup Backlog section (lines 278–355) — the authoritative spec for Stream B's three items, including the exact file:line citations and the "why deferred" rationale for each.
   - `docs/sprint-zero/next-three-prs.md` — PR #45 default-plan section. Updated post-PR-#44 to list the candidate set in priority order; this PR consumes the top two candidates as one bundle.
   - `docs/sprint-zero/stories/CHORE-d5-runtime-upgrade.md` — precedent for the chore-story format (Architect Notes §2.1–§2.9 ratification). PR #45's chore story follows the same structure.
   - `docs/sprint-zero/stories/FR-EX-01-expense-creation.md` Architect Notes §2.0 (Camp B ratification) and §2.10 (multi-context FAB chooser deferral) — Stream B items (i) and (iii) both reference these decisions explicitly.
   - `functions/src/lookup-user-by-phone-number/function.ts` (existing implementation), `functions/src/lookup-user-by-phone-number/index.ts`, `functions/src/lookup-user-by-phone-number/algorithm.ts`, `functions/test/lookup-user-by-phone-number/function.test.ts`, `functions/test/integration/lookup-user-by-phone-number.integration.test.ts` (the SKIPPED suite — its `seedDoc('_rateLimits/${userId}/lookups/counter', ...)` already encodes the canonical architect-intended path).
   - `firestore.rules` `match /_rateLimits/{document=**}` rule (around line 355) — uses recursive wildcard so it already denies client access at any depth; no rules change required for the fix.
   - `docs/design/07-technical/cloud-functions-catalogue.md` lines 725–745 — the rate-limit counter documentation; needs updating to reflect the canonical 4-segment doc path.

6. Honour the four invariants. Invariants 1, 3 are N/A. Invariant 2 is N/A (no `simplifiedBalances` write surface touched). Invariant 4 is the only load-bearing one — preserved through the existing `.firebaserc` single-project pin (CI gate in `.github/workflows/pr.yml` flutter-checks job).

────────────────────────────────────────
PHASE 0 — DEPENDENCY-DAG CHECK
────────────────────────────────────────

0.1  Confirm PR #44 has merged to main and the post-merge state is clean:
     - `functions/package.json` `engines.node = "22"` and `firebase-functions ^7.0.0`.
     - `firebase.json` `functions[0].runtime = "nodejs22"`.
     - All five `actions/setup-node@v4` invocations in `.github/workflows/*.yml` pinned to `'22'`.
     - Local fresh checkout: `cd functions && npm ci && npm run lint && npm run build && npm test` exits 0 (9 suites, 100 tests pass).
     - Production verification: `GET https://healthcheck-7bmmo7hdba-el.a.run.app` returns `200 OK { "ok": true, "region": "asia-south1" }`.
     - Flutter baseline: `flutter analyze --fatal-infos` + `flutter test` exit 0 (794 pass / 24 skipped).

0.2  Walk the dependency DAG for both streams:

     **Stream A (rate-limit doc-path fix):**
     - Bug location: `functions/src/lookup-user-by-phone-number/function.ts:108` — `db.doc(`_rateLimits/${callerUid}/lookups`)`.
     - Reproduction: Firestore SDK throws `Error: Document path must point to a document, but '_rateLimits/{uid}/lookups' was passed in. Specify a document path with an even number of components.`
     - Fix target: `db.doc(`_rateLimits/${callerUid}/lookups/counter`)` — 4-segment subcollection doc reference. Matches the architect-canonical path declared in `.github/shared/decision-log.md` (the surrounding prose refers to "`_rateLimits/{userId}/lookups`" as the LOGICAL container; the actual write target is the `counter` doc inside that subcollection).
     - Rules surface: `firestore.rules:355` `match /_rateLimits/{document=**}` uses recursive-wildcard match so all depths are denied — no rules change needed.
     - Test surface:
         * `functions/test/lookup-user-by-phone-number/function.test.ts` — boundary tests for the handler. The mock-firestore tests will need to be re-pointed at the new path. If the existing mock uses an in-memory key, just update the key.
         * `functions/test/integration/lookup-user-by-phone-number.integration.test.ts` — the SKIPPED suite. Once Stream A lands, the suite header comment (lines 112–131) is removed, `describe.skip` becomes `describe`, and the five tests run end-to-end against the emulator. The `seedDoc('_rateLimits/${userId}/lookups/counter', ...)` calls inside the suite are unchanged (they already use the canonical path).
     - Side effect: the deploy of the fix to production rewrites the rate-limit policy enforcement from "silently broken since PR #32" to "actually counts to 100 lookups/hour". This is the intended behaviour per FR-FR-01 AC; no user-facing surface changes.

     **Stream B (post-PR #38 cleanup, three items):**

     - **Item B-1 — Stale `expense_added` / `expense_add_failed` telemetry references in 3 design docs:**
       * `docs/design/03-architecture/non-functional-design.md:399` — one occurrence (telemetry callout listing canonical funnel events).
       * `docs/design/06-screen-specs/06-08-home-and-search.md:152, 517, 519` — three occurrences (telemetry tables for the home dashboard).
       * `docs/design/06-screen-specs/19-22-expenses.md:360, 386` — two occurrences (telemetry rows for the expense flow).
       * Find-and-replace: `expense_added` → `expense_save_succeeded` AND `expense_add_failed` → `expense_save_failed`, **ONLY in these three files**.
       * **CRITICAL GUARDRAIL — DO NOT TOUCH:** the unrelated string `type: 'expense_added'` which appears as a Firestore document-field VALUE in `docs/design/01-information-architecture/navigation-flow.md:322`, `docs/OneByTwo_Requirements_Spec.md:352, 464`, `docs/design/07-technical/cloud-functions-catalogue.md:223`, `docs/design/07-technical/firestore-schema.md:202`, `docs/design/07-technical/notifications.md:114, 131, 207`, `docs/design/04-wireframes/notifications-and-deeplinks.md:627, 640`, and `docs/sprint-zero/stories/FR-FR-01-matching-and-friendship.md`. These are the **notification document type discriminator** (a Firestore SCHEMA field per SRS §7.2 — see `firestore-schema.md:202` for the canonical enum: `'expense_added' | 'expense_edited' | 'expense_deleted' | 'settlement' | 'group_change'`). The chore-#25 Camp B ratification was **explicitly scoped to telemetry only** (FR-EX-01 Architect Notes §2.0); the notification type discriminator is NOT a telemetry event name and MUST NOT be renamed. Conflating these would break the notification-handler routing matrix in `notifications.md` and `notifications-and-deeplinks.md`.
       * Verification post-fix: `grep -rn 'expense_added\|expense_add_failed' docs/design/03-architecture/ docs/design/06-screen-specs/` returns zero matches; `grep -rn 'expense_added\|expense_add_failed' docs/` still returns the legitimate notification-type references (they MUST NOT be touched).

     - **Item B-2 — Splitter test descriptions still label `99999999` as "the maximum permitted total":**
       * `test/features/expenses/split_calculator_test.dart` — lines 87, 91, 192, 216 (four occurrences of `99999999` as both a numeric literal and a description string).
       * `test/features/expenses/split_calculator_property_test.dart` — lines 10, 37, 40, 63, 65, 88, 109, 134, 159 (nine occurrences as numeric literals + comments + `sample(1, 99999999)` bounds).
       * Find-and-replace: `99999999` → `999999999` in both files. Update the description string at `split_calculator_test.dart:87` from "(99999999 paise, odd)" to "(999999999 paise, odd)".
       * **GUARDRAIL:** the splitter is provably correct by construction (integer `~/` and `%`, no overflow at any positive `int`). The `OBTAmountInput` widget tests at `test/core/widgets/inputs/obt_amount_input_test.dart` correctly assert behaviour at the REAL cap (`_kMaxPaise = 999999999`, defined at `lib/core/widgets/inputs/obt_amount_input.dart:60`), so the live boundary is covered. The splitter property tests under-sample the upper 90% of the legal cap range but do not miss any defect — they just leave the upper range less exercised. Once the cap labels match, the test descriptions are accurate.
       * Verification post-fix: `grep -n '99999999' test/features/expenses/split_calculator_test.dart test/features/expenses/split_calculator_property_test.dart` returns only matches that include `999999999` (the new 9-nines value); zero standalone 8-nines `99999999` remain.

     - **Item B-3 — Missing `// TODO(SCR-08)` comment in `friends_list_screen.dart`:**
       * Architect Notes §2.10 of `docs/sprint-zero/stories/FR-EX-01-expense-creation.md` prescribed a no-op FAB on `friends_list_screen.dart` with a top-of-file TODO comment marking the deferred multi-context FAB chooser. The implementation chose to add no FAB at all (functionally equivalent to no-op), so the TODO is absent. The deferred chooser remains discoverable via Architect Notes §2.10, the SCR-08 screen spec, and `lib/features/expenses/README.md`'s Hand-off Seams section, but in-source discoverability is reduced.
       * Fix: insert at the top of `lib/features/friends/presentation/friends_list_screen.dart` (immediately after the `dart:async` import block, before the `import 'package:flutter/material.dart';` line — or wherever the file's existing convention places top-of-file comments; pick the location that minimises diff and matches local style):
         ```dart
         // TODO(SCR-08): wire the multi-context FAB chooser when the SCR-08
         // home dashboard ships. Today the only Add Expense entry point is the
         // FAB on FriendDetailScreen (FR-EX-01).
         ```
         Note: the original Architect Notes §2.10 wording referenced `FriendDetailPlaceholderScreen` but PR #42 (FR-FR-04) replaced that with the real `FriendDetailScreen`. Update the comment to cite the current screen name.

     - **Out of scope for PR #45:**
         * Any feature work (FR-EX-05 / FR-EX-06 / FR-SE-08 / FR-SE-09 / etc.).
         * Any rules change (`firestore.rules` and `storage.rules` are unchanged — Stream A's fix lands inside the existing `_rateLimits/{document=**}` deny rule; no new rule needed).
         * Any other Bucket-B item (D1 Riverpod 3.x, D2 share_plus, D4 build_runner, D6 npm audit, D7 Jest 30 — all stay on issue #22 for Sprint 4+).
         * Any new ADR (the architect-canonical path was decided in Sprint 1 per `.github/shared/decision-log.md`; PR #45 implements the existing decision, not a new one).
         * Refactoring the rate-limit logic itself (transaction semantics, window-reset behaviour, the 100/hour limit). The bug is the path string; everything else is correct.
         * Touching the `type: 'expense_added'` notification-type schema field anywhere.
         * Bumping `firebase-admin`, `firebase-functions`, or any other deps already pinned by PR #44.

0.3  Confirm DoR for the chore story:
     - Create `docs/sprint-zero/stories/CHORE-pr45-lookup-rate-limit-and-pr38-cleanup.md` using the SRS §13.2 chore format (precedent: `CHORE-d5-runtime-upgrade.md` from PR #44, which itself was modelled on `CHORE-post-sprint1-testing-fixes.md`).
     - Story points: **3 SP** (Stream A ≈ 2 SP, Stream B ≈ 1 SP). If the architect discovers the rate-limit fix requires a wider refactor (e.g. moving to a `runTransaction` to avoid the read-write race the current implementation has, which would be a behavioural change), escalate to 5 SP — but the default is "fix the path, change nothing else". The race-to-transaction refactor is a separate concern and explicitly out of scope.
     - The story must explicitly enumerate: (a) the GitHub issue this PR closes (there is NO open issue for either stream — both are tracked in markdown only); (b) the four-invariant applicability assessment (Inv-4 defence-in-depth only); (c) the two streams as distinct AC clusters so QA can sign each off independently.

0.4  Cross-reference the burndown:
     - Neither stream is a formal Bucket-B item.
     - The rate-limit fix is logged as a "NEW finding (not Bucket B)" in `docs/audits/sprint-1/07-bucket-b-burndown.md` (PR #36 section, lines 208–214). PR #45's burndown update converts that note from "open — candidate for PR #44 (alternate) or PR #45" to "closed by PR #45".
     - The three S4 items are NOT in the burndown — they are in `docs/sprint-zero/sprint-2-plan.md` "Post-Merge Cleanup Backlog" section. PR #45's plan-update removes them from the "open" list and cites the PR #45 closure.
     - PR #45's net contribution to Bucket-B totals: **zero** (the rate-limit bug was not a Bucket-B item; the three S4 items were not Bucket-B items). The remaining count stays at 30 / 37.

────────────────────────────────────────
PHASE 1 — STORY READINESS (PM)
────────────────────────────────────────

PM agent creates the chore story `docs/sprint-zero/stories/CHORE-pr45-lookup-rate-limit-and-pr38-cleanup.md` using the SRS §13.2 chore format. Story points: **3** (2 for Stream A + 1 for Stream B). Two distinct AC clusters so QA can sign each off independently.

Required Given/When/Then ACs (each MUST be testable):

  **Stream A — lookup-user rate-limit doc-path fix**

  - **AC-A1 — Production code uses an even-component doc path.** Given `functions/src/lookup-user-by-phone-number/function.ts`, when the fix is applied, then the rate-limit doc reference reads `db.doc(`_rateLimits/${callerUid}/lookups/counter`)` (4 segments) AND no other call site in `functions/src/**` creates a malformed `_rateLimits/**` reference.
  - **AC-A2 — Boundary tests pass with the new path.** Given `functions/test/lookup-user-by-phone-number/function.test.ts`, when `cd functions && npm test` runs, then every boundary test passes AND any test that mocks the rate-limit document uses the same `_rateLimits/{uid}/lookups/counter` key as production.
  - **AC-A3 — Integration suite unskipped and green.** Given `functions/test/integration/lookup-user-by-phone-number.integration.test.ts`, when the fix lands, then (a) the suite header comment block citing the bug (lines 112–131 in the current file) is REMOVED, (b) `describe.skip("lookupUserByPhoneNumber — integration", ...)` becomes `describe("lookupUserByPhoneNumber — integration", ...)`, AND (c) all 5 integration tests pass under `firebase emulators:exec --only auth,firestore,functions,storage`.
  - **AC-A4 — Rate-limit behaviour observable end-to-end.** Given a seeded counter at `_rateLimits/${uid}/lookups/counter` with `count: 100, windowStart: Date.now() - 1000`, when the function is invoked, then it throws `HttpsError` with `code: 'resource-exhausted'` and `details.errorCode: 'RATE_LIMITED'`. (This is one of the 5 unskipped integration tests; this AC just calls it out explicitly because it is the load-bearing user-facing behaviour the fix restores.)
  - **AC-A5 — Rules surface unchanged but verified.** Given `firestore.rules`, when the rules tests run (`cd functions && npm run test:rules`), then the `match /_rateLimits/{document=**}` deny rule continues to reject ALL client reads/writes at every depth (existing test in `functions/test/firestore-rules/` covers this — no new test required). The path-depth fix in production code does NOT open a client write path.
  - **AC-A6 — Catalogue updated.** Given `docs/design/07-technical/cloud-functions-catalogue.md` lines 725–745, when the fix lands, then the rate-limit counter row in the "Firestore paths written" table reads `_rateLimits/{userId}/lookups/counter` (the actual doc path) AND the prose paragraph below explicitly notes the subcollection layout (`counter` doc inside the `lookups` subcollection).

  **Stream B — Post-PR #38 cleanup (three S4 items)**

  - **AC-B1 — Three design docs use Camp B telemetry names.** Given the three documents from PR #38 cleanup item (1), when the find-and-replace is applied, then `grep -rn 'expense_added\|expense_add_failed' docs/design/03-architecture/ docs/design/06-screen-specs/` returns ZERO matches AND the same scope still references the new names (`expense_save_succeeded` / `expense_save_failed`) in the same positions. **CRITICAL:** the notification-type schema discriminator `type: 'expense_added'` in `docs/design/07-technical/firestore-schema.md`, `notifications.md`, `notifications-and-deeplinks.md`, `navigation-flow.md`, `cloud-functions-catalogue.md`, `OneByTwo_Requirements_Spec.md`, and `FR-FR-01-matching-and-friendship.md` MUST remain unchanged — `grep -n "type.*['\"]expense_added['\"]" docs/` returns the same set of matches before and after the fix.
  - **AC-B2 — Splitter test descriptions match the real cap.** Given the two test files from PR #38 cleanup item (2), when the find-and-replace is applied, then every standalone `99999999` becomes `999999999` AND the test-description string at `split_calculator_test.dart:87` reads "the maximum permitted total (999999999 paise, odd)". The Flutter test suite (`flutter test`) continues to pass with identical pass/skip counts (794 pass / 24 skipped).
  - **AC-B3 — `friends_list_screen.dart` carries the SCR-08 TODO.** Given `lib/features/friends/presentation/friends_list_screen.dart`, when the comment is added, then the top of the file (above the first non-trivial declaration) contains the `// TODO(SCR-08)` comment block citing the deferred multi-context FAB chooser and naming `FriendDetailScreen` (not the pre-PR-#42 placeholder name) AS the only current Add Expense entry point. `flutter analyze --fatal-infos` passes (comments are inert; this is a no-op for the static analyser but the file MUST stay clean).

  **Cross-cutting (both streams)**

  - **AC-X1 — Five-layer test pyramid green.** Given the PR diff, when CI runs, then every layer passes on the merged matrix (Node 22 / `firebase-functions@7.x`): unit 9 suites / 100 tests + 5 newly-unskipped integration tests; rules 7 suites / 149 tests; integration suites pass (4 of 4 now — the previously-skipped lookup-user suite is now active). Coverage on `functions/src/lookup-user-by-phone-number/function.ts` improves modestly because the rate-limit branch is now exercised end-to-end; coverage on `functions/src/simplified-debts/function.ts` stays at ≥ 89% branch (PR #36 baseline). Coverage on `lib/features/friends/**` and `lib/features/expenses/**` unchanged (Stream B touches only test descriptions and a comment).
  - **AC-X2 — Invariant 4 preserved.** Given the PR diff, when `.firebaserc` is inspected, then it contains exactly one project (CI gate in `pr.yml` flutter-checks job still enforces). `firebase.json` `singleProjectMode: true` unchanged.
  - **AC-X3 (Negative) — No feature work.** Given the PR #45 diff, when scanned for new feature flags, new screens, new routes, new collections, or new public APIs, then the count is ZERO. This is a hygiene PR.
  - **AC-X4 (Negative) — No notification-type schema regression.** Given the PR #45 diff, when `grep -rn "type.*['\"]expense_added['\"]" docs/` is run before and after, then the matched line count and content are IDENTICAL.

────────────────────────────────────────
PHASE 2 — ARCHITECT HAND-OFF
────────────────────────────────────────

Architect agent appends `## Architect Notes` to the chore story ratifying:

2.1 **Stream A — canonical rate-limit doc-path choice.** The architect-canonical path is `_rateLimits/{userId}/lookups/{counterDocId}` (4-segment subcollection doc reference) where `counterDocId` is the literal string `counter`. Rationale:
    - Matches the existing integration test seed path (`functions/test/integration/lookup-user-by-phone-number.integration.test.ts:243` already encodes this). The test author had the architect-intended layout in mind; only the production code drifted.
    - Matches the architect-canonical "logical container" wording in `.github/shared/decision-log.md` (where `_rateLimits/{userId}/lookups` is described as the LOCATION; the actual write target inside that location is `counter`).
    - Naturally extends to additional rate-limit categories (e.g. a future `_rateLimits/{userId}/sends/counter` for FR-SE-09 reminder send rate-limit, or `_rateLimits/{userId}/uploads/counter` for FR-EX-05 receipt upload rate-limit) by adding new subcollection siblings — no schema migration required.
    - The recursive-wildcard rule `match /_rateLimits/{document=**}` covers all depths so no rules change is needed.
    - Alternative considered: `db.doc(`_rateLimits/${callerUid}_lookups`)` (single doc with composite ID). Rejected because (a) composite IDs are harder to enumerate, (b) the test author's intent was clearly the subcollection model, and (c) future rate-limit categories would need their own composite-ID rules per category.

2.2 **Stream A — no transaction refactor.** The current implementation uses a non-transactional read-then-increment pattern which has a known time-of-check-to-time-of-use race (two concurrent invocations could both read `count: 99` and both increment to `100`, briefly allowing 101 lookups in the window). The architect explicitly **defers** this refactor to a separate PR — it is a behavioural concern (correctness under concurrent load), not a path-string bug, and conflating it with PR #45 would expand the test surface and the risk profile. Track as a follow-up under issue #22 or a new "operational hardening" chore PR; PR #45 only fixes the path-string bug.

2.3 **Stream A — files to touch (exhaustive — anything outside this set is scope creep):**
    - `functions/src/lookup-user-by-phone-number/function.ts` — line 108 (the one bug-fix line).
    - `functions/test/lookup-user-by-phone-number/function.test.ts` — update any boundary-test mock keys that reference `_rateLimits/{uid}/lookups` to `_rateLimits/{uid}/lookups/counter`.
    - `functions/test/integration/lookup-user-by-phone-number.integration.test.ts` — remove the SKIPPED header comment block (lines 112–131); change `describe.skip` to `describe`. No body changes (the seed path is already canonical).
    - `docs/design/07-technical/cloud-functions-catalogue.md` — lines 725–745: update the path string in the "Firestore paths written" table row and the surrounding prose to reflect the 4-segment doc path.

2.4 **Stream B — files to touch (exhaustive):**
    - `docs/design/03-architecture/non-functional-design.md` — line 399 only (one occurrence).
    - `docs/design/06-screen-specs/06-08-home-and-search.md` — lines 152, 517, 519 (three occurrences).
    - `docs/design/06-screen-specs/19-22-expenses.md` — lines 360, 386 (two occurrences).
    - `test/features/expenses/split_calculator_test.dart` — lines 87, 91, 192, 216 (four `99999999` occurrences; one description string).
    - `test/features/expenses/split_calculator_property_test.dart` — lines 10, 37, 40, 63, 65, 88, 109, 134, 159 (nine `99999999` occurrences).
    - `lib/features/friends/presentation/friends_list_screen.dart` — one top-of-file comment block addition.

2.5 **Files explicitly NOT to touch (negative scope guardrails):**
    - `firestore.rules` (the recursive-wildcard deny rule already covers all `_rateLimits/**` depths).
    - `firestore.indexes.json` (no new queries).
    - `storage.rules` (unrelated).
    - Any file containing `type: 'expense_added'` as a notification-type schema discriminator value — see the full list in §6 of the PR-#45 prompt and AC-B1.
    - `lib/features/expenses/application/expense_telemetry.dart` (already uses the Camp B names since PR #38).
    - `lib/features/expenses/**` runtime code (the telemetry constants are already correct).
    - `docs/design/07-technical/telemetry-plan.md` (already uses the Camp B names since PR #38).
    - The rate-limit logic itself (transaction semantics, window-reset behaviour, the 100/hour limit constant).

2.6 **Test pyramid execution order.** Same as PR #44 (codified in `CHORE-d5-runtime-upgrade.md` Architect Notes §2.6). The architect ratifies that every layer is exercised:
    - Layer 1 (algorithm unit): `cd functions && npm test` includes the existing `lookup-user-by-phone-number/algorithm.test.ts` (unchanged by this PR — the algorithm is unrelated to the path-string bug).
    - Layer 2 (algorithm property): N/A for this PR.
    - Layer 3 (function boundary): `function.test.ts` updated to reflect the new path key in any mock-firestore lookup.
    - Layer 4 (rules): existing rules tests cover the `_rateLimits/**` deny — no new test required (AC-A5).
    - Layer 5 (integration): the 5 previously-skipped tests in `lookup-user-by-phone-number.integration.test.ts` now run and pass.

2.7 **Anticipated reconciliations.** Beyond the path-string fix in production code and the corresponding mock-key update in boundary tests, the architect anticipates ZERO additional source-code changes. The fix is intentionally surgical. If the functions-dev discovers the boundary tests use a different mock-Firestore key scheme that needs broader updating, document the reconciliation in this section with the file:line citation.

2.8 **No new ADR required.** ADR-0011 (CF module layout) and the existing `.github/shared/decision-log.md` rate-limit entry both stand unchanged. PR #45 implements the existing architect-canonical decision; no architectural decisions are revisited.

2.9 **Forward-compatibility note.** The 4-segment subcollection pattern (`_rateLimits/{userId}/{category}/counter`) extends naturally to future rate-limit categories (FR-SE-09 reminder send rate-limit, FR-EX-05 receipt upload rate-limit, etc.) without schema migration. The next functions-dev to add a rate-limit category copies this pattern, picks a new `category` subcollection name (e.g. `sends`, `uploads`), and writes to `_rateLimits/${uid}/${category}/counter`. The recursive-wildcard rule covers all categories.

────────────────────────────────────────
PHASE 3 — SCOPE GUARDRAILS
────────────────────────────────────────

WHAT GOES IN PR #45:

  Source / configuration:
  - `functions/src/lookup-user-by-phone-number/function.ts` — EDIT (one-line path-string fix).
  - `functions/test/lookup-user-by-phone-number/function.test.ts` — EDIT (mock-key update if needed).
  - `functions/test/integration/lookup-user-by-phone-number.integration.test.ts` — EDIT (remove SKIPPED comment block, flip `describe.skip` → `describe`).
  - `lib/features/friends/presentation/friends_list_screen.dart` — EDIT (top-of-file TODO comment, Item B-3 only).
  - `test/features/expenses/split_calculator_test.dart` — EDIT (cap label propagation, Item B-2).
  - `test/features/expenses/split_calculator_property_test.dart` — EDIT (cap label propagation, Item B-2).

  Tests:
  - No new test files. The 5 unskipped integration tests are the "regression-first" evidence that Stream A landed cleanly.

  Documentation:
  - The chore story file from Phase 1 with Architect Notes.
  - `docs/design/07-technical/cloud-functions-catalogue.md` — rate-limit path documentation updated (Stream A AC-A6).
  - `docs/design/03-architecture/non-functional-design.md`, `docs/design/06-screen-specs/06-08-home-and-search.md`, `docs/design/06-screen-specs/19-22-expenses.md` — Camp B telemetry name propagation (Stream B Item B-1).
  - `docs/audits/sprint-1/07-bucket-b-burndown.md` — mark the "NEW finding (not Bucket B)" rate-limit note as closed with PR #45 cross-ref. Append a PR #45 section noting zero Bucket-B item movement.
  - `docs/sprint-zero/sprint-2-plan.md` — add PR #45 row to PR Tracking + Velocity (3 SP, cumulative 40 SP / 12 PRs). Mark the three S4 items in "Post-Merge Cleanup Backlog" as closed with PR #45 cross-ref.
  - `docs/sprint-zero/next-three-prs.md` — roll PR #45 to merged, roll PR #46 / PR #47 / PR #48 candidates.

WHAT DOES NOT GO IN PR #45:

  - Any feature work (FR-EX-05, FR-EX-06, FR-SE-08, FR-SE-09, etc.).
  - Any new ADR (ADR-0011 + existing rate-limit decision both stand).
  - The rate-limit transaction race refactor (§2.2 — separate concern, separate PR).
  - Any other Bucket-B item (D1, D2, D4, D6, D7 stay on issue #22).
  - Any rule / index / storage-rule change.
  - Any change to the notification-type schema discriminator (`type: 'expense_added'` etc. — see §2.5 + AC-B1 + AC-X4).
  - Any change to `pubspec.yaml`, `pubspec.lock`, `analysis_options.yaml`, Android / iOS native shells, or any Flutter dependency.
  - Any change to `functions/package.json`, `functions/package-lock.json`, `firebase.json`, or any GitHub Actions workflow file (the runtime + SDK matrix is fixed by PR #44 and is not revisited).
  - The five remaining D-row bucket-B items (Sprint 4+ scope).

If an agent suggests any of: "while we're in `lookup-user-by-phone-number/function.ts`, let's add the transaction guard for the rate-limit race", "while we're updating telemetry references, let's also rename the `type: 'expense_added'` notification discriminator for consistency", "let's bundle FR-EX-06 since we have a quiet PR slot", "let's also fix the `Unused variable: otherUserId` warning surfaced by PR #44's deploy" — REFUSE. These are scope creep. The PR #45 promise is "two small hygiene streams, atomic"; widening the scope changes the risk profile.

The single exception: if the boundary-test mock-Firestore key update reveals a test infrastructure issue that blocks AC-A2 (e.g. the mock Firestore doesn't accept the new path because of an unrelated bug in the mock library), surface it as friction and ask before fixing — the architect may decide to defer the affected test to a follow-up.

────────────────────────────────────────
PHASE 4 — TEST-FIRST DISCIPLINE
────────────────────────────────────────

PR #45 is a hygiene PR; test-first is interpreted as "regression-first plus the unskip flip".

**Stream A:**

1. Snapshot the pre-fix baseline: 9 suites / 100 tests pass on the merged Node 22 + ff7 matrix; `lookup-user-by-phone-number.integration.test.ts` is SKIPPED (5 tests).
2. Apply the path-string fix to `function.ts:108`.
3. Re-run `cd functions && npm test` — confirm still 9 suites / 100 tests pass (the boundary tests may need a mock-key update; do that in the same commit).
4. Remove the SKIPPED header comment block and flip `describe.skip` → `describe` in the integration test.
5. Run `firebase emulators:exec --only auth,firestore,functions,storage --project demo-onebytwo "cd functions && npm run test:integration"` — confirm all 4 suites / 33 tests pass (the lookup-user suite is now active, contributing the 5 previously-skipped tests).
6. Confirm rules tests still pass (`cd functions && npm run test:rules`) — the `_rateLimits/{document=**}` deny rule still rejects client access.

**Stream B:**

1. Snapshot the pre-fix baseline: `flutter analyze --fatal-infos` returns "No issues found"; `flutter test` returns 794 pass / 24 skipped.
2. Apply the three find-and-replace operations and the one comment insertion.
3. Re-run `dart format --set-exit-if-changed .` — must return 0 changes. (`flutter analyze --fatal-infos` only catches code issues, not docs; the formatter check is the standard pre-push hygiene.)
4. Re-run `flutter analyze --fatal-infos` — must return "No issues found".
5. Re-run `flutter test` — must return identical 794 pass / 24 skipped.
6. Run the verification greps from AC-B1, AC-B2, AC-X4 to confirm the find-and-replace scope was correctly bounded.

**Combined CI dry-run:**

Push the branch and open the PR. The CI pipeline must show ✅ on:
  * `PR Title Lint`
  * `Flutter Lint & Test`
  * `Cloud Functions Lint & Test`
  * `Integration Tests (Emulator Suite)` (now includes the 5 unskipped lookup-user tests)
  * `Coverage Gate (SRS 5.7)`
  * `Build Android (debug)`
  * `Build iOS (no signing)`

Coverage:
  - `functions/src/lookup-user-by-phone-number/function.ts`: improves modestly because the rate-limit branch is now exercised end-to-end in CI.
  - `functions/src/simplified-debts/function.ts`: unchanged at ≥ 89% branch (PR #36 baseline).
  - `lib/**`: unchanged (Stream B touches only test descriptions and a comment).

────────────────────────────────────────
PHASE 5 — EXECUTION PROTOCOL
────────────────────────────────────────

Mirror the PR #44 commit cadence (PM story → architect notes → functional change → docs roll-up):

 1. PM creates story `docs/sprint-zero/stories/CHORE-pr45-lookup-rate-limit-and-pr38-cleanup.md`. Commits as `docs(stories): PR #45 lookup rate-limit fix and PR #38 cleanup chore`.
 2. Architect appends `## Architect Notes` (§2.1–§2.9). Commits as `docs(stories): PR #45 architect notes`.
 3. Functions-dev applies Stream A in **one atomic commit** because the production code, boundary test, integration test, and catalogue documentation must all align on the new path string:
    - `fix(functions): correct rate-limit doc path in lookup-user-by-phone-number`
    - Diff scope: `functions/src/lookup-user-by-phone-number/function.ts`, `functions/test/lookup-user-by-phone-number/function.test.ts`, `functions/test/integration/lookup-user-by-phone-number.integration.test.ts`, `docs/design/07-technical/cloud-functions-catalogue.md`.
 4. Flutter-dev / docs hand applies Stream B in **one atomic commit** because all three S4 items are independent of Stream A and trivially small:
    - `chore(docs,tests): post-PR-#38 cleanup (3 S4 items)`
    - Diff scope: `docs/design/03-architecture/non-functional-design.md`, `docs/design/06-screen-specs/06-08-home-and-search.md`, `docs/design/06-screen-specs/19-22-expenses.md`, `test/features/expenses/split_calculator_test.dart`, `test/features/expenses/split_calculator_property_test.dart`, `lib/features/friends/presentation/friends_list_screen.dart`.
 5. QA runs the manual smoke matrix:
    - Start emulators (`scripts/dev/start-emulators.sh`).
    - From a Flutter integration test or the Functions emulator UI, invoke `lookupUserByPhoneNumber({phoneNumber: '+91…'})` 100 times in a tight loop and confirm the 101st call returns `RATE_LIMITED`. (This is the user-facing observable behaviour the fix restores.)
    - Confirm the Friends list still loads with the new TODO comment present at the top of `friends_list_screen.dart`.
    - Run `flutter test test/features/expenses/` — confirm splitter tests pass with the updated cap labels.
 6. Docs hand updates plan + roll-forward:
    - `docs/sprint-zero/sprint-2-plan.md` (PR #45 status, velocity #12, cumulative 40 SP; mark all three S4 items in "Post-Merge Cleanup Backlog" as closed with PR #45 cross-ref).
    - `docs/sprint-zero/next-three-prs.md` (roll PR #46 / PR #47 / PR #48 with PR #45's outcome).
    - `docs/audits/sprint-1/07-bucket-b-burndown.md` (append a PR #45 section noting zero Bucket-B item movement; mark the "NEW finding" rate-limit note as closed with PR #45 cross-ref).
    - Commits as `docs(plan): PR #45 shipped, prep PR #46`.
 7. PR opened by functions-dev (or the docs hand if Stream B was the final commit).
    Title: `chore(functions,docs): lookup rate-limit doc-path fix + post-PR-#38 cleanup`
    Body must:
      - Cite `docs/patterns/feature-pr-conventions.md` Cloud Functions section for Stream A.
      - Cite `docs/sprint-zero/sprint-2-plan.md` Post-Merge Cleanup Backlog section for Stream B.
      - Confirm Invariant 4 preserved (.firebaserc single-project; CI gate green).
      - Confirm AC-X4 (notification-type schema discriminator UNCHANGED — show the before/after grep counts).
      - Confirm the five-layer test pyramid passes (with the test counts: 9 suites / 100 unit + 5 newly-active integration; 7 rules suites / 149 tests).
      - State explicitly the deferred items: the rate-limit transaction race refactor (§2.2; separate operational hardening PR); the five remaining D-row bucket-B items (D1, D2, D4, D6, D7); FR-EX-05 / FR-EX-06 / FR-SE-09 / FR-SE-08 (PR #46+ candidates).
      - State explicitly: "No `type: 'expense_added'` notification-type schema field was touched in this PR" (AC-B1 + AC-X4 sign-off).
      - End with `Next PR: PR #46 — TBD per architect's call at kickoff.`

────────────────────────────────────────
PHASE 6 — DEFINITION OF DONE GATE
────────────────────────────────────────

Walk the DoD checklist. Particular emphasis:

  - DoD §2: every layer of the test pyramid green on the merged matrix; coverage on `functions/src/lookup-user-by-phone-number/function.ts` measurably improves (the rate-limit branch is no longer dead code in CI).
  - DoD §3: QA sign-off referencing the manual smoke matrix from Phase 5 step 5 with evidence for (a) the 101st-lookup `RATE_LIMITED` response, (b) the SCR-08 TODO comment visible at the top of `friends_list_screen.dart`, (c) the splitter test descriptions reading "999999999" not "99999999".
  - DoD §7: invariant compliance.
    * **Inv-1:** N/A (no monetary surface).
    * **Inv-2:** N/A (no `simplifiedBalances` write surface). Defensively note: the `_rateLimits/**` collection is internal-only and unrelated to the `simplifiedBalances` invariant.
    * **Inv-3:** N/A.
    * **Inv-4:** `.firebaserc` contains exactly one project (CI gate enforces); `firebase.json` `singleProjectMode: true` preserved.
  - DoD §8: documentation updated. Story file with Architect Notes. Sprint plan + next-three-prs updated. Burndown PR #45 section appended. Cloud Functions catalogue rate-limit path updated. Three S4 design docs use Camp B telemetry names.
  - DoD §9: deploy verification. PR #45 deploys via the release pipeline on tag (same path as PR #44). QA verifies `lookupUserByPhoneNumber` callable still returns matches for known phone numbers in production post-deploy — and additionally that the 101st invocation in a one-hour window returns `RATE_LIMITED` (this is the first production observation of the rate-limit gate enforcing).

QA posts the explicit DoD §3 sign-off, including the negative-scope greps from AC-X4 (notification-type schema discriminator unchanged) and AC-B1 (no stray `expense_added` / `expense_add_failed` telemetry references remain in the targeted docs).

**Pre-push CI hygiene:** run `dart format .` AND `cd functions && npm run lint && npm run build && npm test` BEFORE `git push`. The Cloud Functions lint job and the Flutter formatter check are the most common failure modes for a missed local step.

────────────────────────────────────────
PHASE 7 — UPDATE THE ROLLING PLAN AND BURNDOWN
────────────────────────────────────────

PM agent updates:
  - `docs/sprint-zero/sprint-2-plan.md` — PR #45 status (merged), velocity data point #12 (3 SP, total now 40 SP across 12 PRs). Mark all three S4 items in "Post-Merge Cleanup Backlog" as **CLOSED BY PR #45** with the PR #45 cross-ref and the squash-commit SHA. The PR #38 cleanup section can be condensed to a one-line resolution note.
  - `docs/sprint-zero/next-three-prs.md`:
      * PR #45 row marked merged (move from "Next up" to the merged-PRs table); update the post-PR-#44 numbering table to show #45 closed.
      * PR #46: **TBD** per architect's call at kickoff. Top candidates (in rough priority order, post-PR-#45):
          - FR-EX-06 Edit / delete expense (natural follow-on to PR #38; PR #42 made the read-only rows visible; bottom-sheet pattern established).
          - FR-EX-05 Receipt attachment (pairs with Storage rules R7-R8 from Bucket-B).
          - FR-SE-09 Send Reminder (closes OBTSettleUpCard receiving-direction branch; requires FCM + 24-hour rate-limit rules — note this PR will exercise the new `_rateLimits/{uid}/sends/counter` subcollection pattern PR #45 established).
          - FR-SE-08 dedicated `/settlements/history` full-screen.
          - The deferred rate-limit transaction race refactor (operational hardening; small standalone PR).
      * PR #47: TBD per PR #46 outcome.
      * PR #48: TBD per PR #47 outcome.
  - `docs/audits/sprint-1/07-bucket-b-burndown.md`:
      * Header timestamp: PR #44 → PR #45.
      * Append a PR #45 section noting zero Bucket-B item movement (the rate-limit fix was a "NEW finding" not a Bucket-B item; the three S4 items were post-PR-#38 cleanup, not Bucket-B). Cross-link the PR #36 burndown note that flagged the rate-limit bug.
      * Update the burndown chart text row count if the chart format requires it (likely add a `PR #45 (chore): 30 remaining` line).

Commits as `docs(plan): PR #45 shipped, prep PR #46`.

────────────────────────────────────────
GUARDRAILS
────────────────────────────────────────

If during this PR an agent proposes:
  - "While we're in `lookup-user-by-phone-number/function.ts`, let's add the transaction guard for the rate-limit race" → REFUSE per §2.2. Track as a separate operational hardening chore PR; the race is a behavioural concern, not a path-string bug.
  - "Let's rename the `type: 'expense_added'` notification discriminator for consistency with Camp B" → REFUSE per §2.5 + AC-B1 + AC-X4. The notification type is a Firestore SCHEMA field (per SRS §7.2 and `firestore-schema.md:202`); the Camp B ratification was scoped to telemetry only.
  - "Let's bundle FR-EX-05 / FR-EX-06 / FR-SE-09 since we have a quiet PR slot" → REFUSE. Each is a P0/P1 feature with its own story, ACs, and test surface. They belong in PR #46+.
  - "Let's also fix the `Unused variable: otherUserId` warning that surfaced during PR #44's deploy" → REFUSE for PR #45 unless the architect explicitly approves a tiny scope expansion. The warning is a `firestore.rules` lint issue (line 25:30) — pre-existing, non-functional, non-deadline. Track as a separate test-hygiene chore for PR #46+ (it pairs naturally with the rules-test-emulator-flake observation flagged in PR #44 §2.7).
  - "Let's separate Stream A and Stream B into two PRs for cleaner review" → REFUSE. The two streams are intentionally bundled — each alone is too small for a dedicated PR cycle; bundling keeps Sprint 2 velocity moving without proliferating chore PRs.
  - "Let's introduce a new ADR codifying the `_rateLimits/{uid}/{category}/counter` subcollection convention so future rate-limit categories know where to write" → DEFER. The convention is already documented in this PR's Architect Notes §2.9; an ADR can be filed later if the pattern proliferates. Not blocking.
  - "Let's relocate the `_rateLimits` collection to `_internal/rateLimits/{uid}/{category}/counter` while we're touching it" → REFUSE. The `_rateLimits` collection has been live in production since PR #32; relocating it is a migration concern (orphaned docs, rules updates, multiple PRs of coordination). Out of scope.
  - "Let's add a Firestore TTL policy on the rate-limit counter docs so they auto-expire after the hour window" → REFUSE for PR #45. Operational nicety; not deadline-bound; track as a follow-up.

If something genuinely surprises (e.g., the boundary-test mock-Firestore key scheme can't represent the new 4-segment path, or the integration test reveals a deeper bug in the rate-limit logic that the SKIPPED suite was masking, or the find-and-replace for Stream B Item B-1 accidentally matches a `type: 'expense_added'` schema reference because of a missed quoting nuance), STOP and surface the friction. PR #45 is small but its negative AC (AC-X4) is a load-bearing safety net — get it right rather than fast.

Begin with Phase 0 — verify the dependency DAG (PR #44 merge state + production canary verified) and re-read the three reference docs (chore-story precedent `CHORE-d5-runtime-upgrade.md`, the rate-limit catalogue prose at `cloud-functions-catalogue.md` lines 725–745, and the Post-Merge Cleanup Backlog section in `sprint-2-plan.md`) before kickoff.
