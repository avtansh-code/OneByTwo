1. 1. You are the OneByTwo orchestrator agent. PR #38 (FR-EX-01 expense creation UI + chore #25) has merged and is live in production at `asia-south1`. The simplified-debts round-trip is now end-to-end from a user's point of view: a friendship member taps the FAB on `FriendDetailPlaceholderScreen`, fills in the two-step `AddExpenseBottomSheet`, the controller writes to `friendships/{fid}/expenses/{eid}` via `ExpenseRepository`, the `onExpenseWriteFriendship` trigger (PR #36) fires `recomputeAndWrite`, `simplifiedBalances` + `lastActivityAt` update atomically, and the friends list row from PR #35 reorders and re-renders the new net balance. PR #41 (a docs-only chore PR for the D5 deadline backlog cross-refs) merged immediately after PR #38. We now implement **PR #42: FR-FR-04 — Per-friend transaction history (the real Friend Detail screen)**. This is the **read-side closure to the simplified-debts round-trip beyond the friends-list chip** — PR #35 surfaced the net balance per friend; PR #42 surfaces the underlying expense ledger that produces that net balance. The placeholder screen from PR #35 (`FriendDetailPlaceholderScreen`) gets replaced; the FAB call site from PR #38 is preserved.

2. 2. The numbering jumped because the post-PR #38 sequence consumed three intermediate GitHub numbers: **#39** (issue — Cloud Functions Node 20 decommissioned 2026-10-31), **#40** (issue — `firebase-functions` 6.x → 7.x), **#41** (docs PR — D5 deadline backlog cross-refs). PR #42 is therefore the next FEATURE PR. The orchestrator should not assume PR #43 == GitHub issue 43 — issues and PRs share the same sequential namespace, so any new issues filed during PR #42 will consume intermediate numbers. Use the labels in `docs/sprint-zero/next-three-prs.md` to track the next-feature-PR vs next-issue mapping.

3. 3. PR #42 is the **first PR that reads** the `friendships/{fid}/expenses/{eid}` subcollection PR #38 wrote to, and the **first PR that reads** the top-level `settlements/{settlementId}` collection PR #37 shipped rules + a trigger for. Until PR #42 ships, the only consumer of either collection in production is `recomputeAndWrite` inside `onExpenseWriteFriendship` and `onSettlementWrite` — and the only writes ever produced for `settlements` have been by integration tests and ops scripts because **FR-SE-08 (the settle-up UI that will be the first client producer of settlement writes) is PR #43's default plan** per `docs/sprint-zero/next-three-prs.md`. PR #42's settlement read path is therefore **defensively wired** — it will typically render an empty settlements section until FR-SE-08 ships, but the wiring is in place so PR #43 needs no read-side change.

4. 4. PR #42 has **no mandatory cross-PR bundle** (unlike PR #38's chore #25). Critical Constraint C-1 is RESOLVED. The only deadline-bound work in the rolling plan is **D5 (issues #39 + #40)** with a 2026-10-31 cutoff (~5 months runway), but that does not block PR #42 — it is its own dedicated PR (PR #44 default plan per `next-three-prs.md`). PR #42 must not absorb the D5 upgrade.

5. 5. Read before starting:
   - `.github/copilot-instructions.md`
   - `.github/shared/invariants.md` — invariants 1 (paise integers; PR #42 is read-side, formats paise → INR via `formatInrFromPaise()` only — no rupee math), 2 (`simplifiedBalances` client-read-only — PR #42 reads `simplifiedBalances` for the net-balance display via `netBalancePaise()`; it must NOT write to that field), 4 (single Firebase project).
   - `.github/shared/coding-standards.md` — Dart, Riverpod, widget-test, and Conventional Commits rules.
   - `.github/shared/decision-log.md` — ADR-0001 (simplified debts as canonical view), ADR-0002 (paise integers), ADR-0006 (Riverpod state management), ADR-0007 (feature-first folder layout), ADR-0013 (PII / telemetry hashing).
   - `docs/patterns/feature-pr-conventions.md` — Flutter Testing Layers, Boundary-Contract Discipline, Telemetry-Event Discipline.
   - `docs/OneByTwo_Requirements_Spec.md` — sections 4.3 (FR-FR-04 — per-friend transaction history, reverse chronological), 4.6 (FR-SE-01 — simplified debts as the canonical view; FR-SE-07 — Settle Up CTA on every screen with non-zero balance — NOTE the CTA is deferred to FR-SE-08 / PR #43, but the screen-spec position is reserved), 7.3 (Invariants 1 and 2), 7.5 (security rules — already shipped for both expenses and settlements).
   - `docs/design/06-screen-specs/09-12-friends.md` — **SCR-11 Friend Detail** (the authoritative spec for PR #42), SCR-12 Delete Friend Dialog (deferred — FR-FR-05 P1 is a separate PR).
   - `docs/design/02-design-system/components.md` — `OBTAppBar`, `OBTUserAvatar` (11), `OBTBalancePill` (4), `OBTSettleUpCard` (13 — render position reserved but the CTA is deferred to FR-SE-08), `OBTExpenseListTile` (15), `OBTEmptyState` (18), `OBTErrorState` (19), `OBTSkeletonLoader` (20). **None of these exist as reusable widgets in the codebase yet** (PR #38 extracted only `OBTAmountInput`). The architect's call at 2.6 below: inline most, or extract one or two if a clear second use-site exists.
   - `docs/design/07-technical/firestore-schema.md` — `friendships/{fid}/expenses/{eid}` and `settlements/{settlementId}` schemas. The read-side queries use the schema-natural ordering.
   - `docs/design/07-technical/telemetry-plan.md` — the events SCR-11 introduces (`friend_detail_viewed`, `settle_up_tapped` — deferred since CTA is deferred, `friend_history_tapped` — deferred since full-history screen is out of scope, `friend_delete_menu_tapped` — deferred). PR #42 emits only `friend_detail_viewed` (the others gate on out-of-scope features). PII hashing per ADR-0013 applies to `friendship_id` → `friendship_id_hash` via `hashFriendshipId()`.
   - `docs/design/03-architecture/extension-points-register.md` — ARCH-EXT-02 (currency INR), ARCH-EXT-06 (verificationStatus settlement field — read-only, defensive guard if PR #42 surfaces it in a tooltip).
   - `lib/features/friends/` — the architectural reference for everything in PR #42. `lib/features/friends/data/friendship_repository.dart` (the `FriendshipStore` abstract + `FirestoreFriendshipStore` + `FakeFriendshipStore` pattern PR #42 mirrors for the expense + settlement read paths), `lib/features/friends/application/friends_list_provider.dart` (the snapshot-stream provider pattern), `lib/features/friends/domain/friendship_doc.dart` (strict `fromFirestore` parsing), `lib/features/friends/presentation/friends_list_screen.dart` (the navigation + state pattern).
   - `lib/features/expenses/` — PR #38's full feature folder. PR #42 EXTENDS `lib/features/expenses/data/expense_repository.dart` with the read path (`watchExpenses({friendshipId})`); the existing `ExpenseStore` abstract gets a new method. The `ExpenseDoc` model from PR #38 is reused (no schema change).
   - `lib/features/friends/presentation/friend_detail_placeholder_screen.dart` — the placeholder that PR #42 REPLACES. Currently a `ConsumerWidget` with three required params (`friendshipId`, `currentUserUid`, `otherUserUid`) and a FAB that opens `AddExpenseBottomSheet`. The new `FriendDetailScreen` keeps the same constructor signature so `FriendsListScreen._onRowTapped` needs no change.
   - `lib/core/formatters/inr_formatter.dart` — `formatInrFromPaise(int)`. Boundary-contract grep enforces no inline `/100` arithmetic in `lib/features/friends/**` (the existing test in `test/features/friends/friends_list_boundary_contract_test.dart`); PR #42 must extend the grep to cover the new `friend_detail_screen.dart` file or add a sibling grep file.
   - `lib/core/balances/net_balance.dart` — `netBalancePaise()`. The Friend Detail screen reads `simplifiedBalances` and projects via this helper, same as the friends list. **PR #42 must NOT re-implement the projection** — call the existing helper.
   - `lib/core/telemetry/event_id_hash.dart` — `hashFriendshipId()` (existing, since PR #35) and `hashId()` (added in PR #38). The `friend_detail_viewed` event carries `friendship_id_hash`; if the architect decides to emit expense-row-tap events later (FR-EX-06's responsibility), they use `expense_id_hash` via `hashId()`.
   - PR #36's `functions/test/firestore-rules/expenses-friendship.test.ts` and PR #37's `functions/test/firestore-rules/settlements.test.ts` — the rules tests that constrain what the read-side queries are allowed to do. PR #42 must NOT regress these tests.
   - `scripts/dev/start-emulators.sh` — the canonical wrapper. Widget tests do NOT need the emulator but the new integration test does.
   - `.github/workflows/pr.yml` — `Flutter Lint & Test`, `Integration Tests (Emulator Suite)`, `Coverage Gate (SRS 5.7)`. No workflow change needed.

6. 6. Honour the four invariants. Invariant 1 is **read-side** in PR #42 (all paise → INR conversion through `formatInrFromPaise()`; no rupee math anywhere). Invariant 2 is the **load-bearing read** for the net-balance display (the controller calls `netBalancePaise(simplifiedBalances, currentUid)`; the controller does NOT write `simplifiedBalances`). Invariant 3 is N/A. Invariant 4: no second Firebase project.

────────────────────────────────────────
PHASE 0 — DEPENDENCY-DAG CHECK
────────────────────────────────────────

0.1  Confirm PR #38 + PR #41 have merged to main and the post-merge state is clean:
     - `lib/features/expenses/` is populated (PR #38 — controller, repository, domain, presentation).
     - `lib/features/friends/presentation/friend_detail_placeholder_screen.dart` exists with the FAB wired to `AddExpenseBottomSheet`.
     - `lib/core/widgets/inputs/obt_amount_input.dart` exists (PR #38's `OBTAmountInput` extract).
     - `lib/core/telemetry/event_id_hash.dart` exports BOTH `hashFriendshipId()` and `hashId()` (PR #38 generalised).
     - `lib/core/balances/net_balance.dart` exports `netBalancePaise()` (PR #35).
     - `lib/core/formatters/inr_formatter.dart` exports `formatInrFromPaise()` (PR #35).
     - `functions/src/triggers/on-expense-write/` and `on-settlement-write/` are live in production at `asia-south1` (5 functions deployed per `firebase functions:list --project onebytwo-avtanshgupta` post-PR-#38 deploy).
     - `firestore.rules` contains the expense subcollection (PR #36 lines 153–302), the settlements collection (PR #37), and the friendships collection (pre-PR-#38 baseline).
     - `firestore.indexes.json` contains BOTH the `friendships(memberIds + lastActivityAt)` index (PR #35) AND the `settlements(contextType + contextId)` composite (PR #37; deployed in the post-PR-#38 deploy sweep).
     - `flutter analyze --fatal-infos` and `flutter test` pass on a fresh checkout of `main` post-PR-#41.
     - `cd functions && npm run lint && npm run build && npm test` pass.

0.2  Walk the dependency DAG for FR-FR-04 (friendship context only):
     - **Server side (consumers of PR #42's reads):** none. PR #42 is a read-side PR; the server doesn't react to reads. The existing triggers run on writes only.
     - **Client write side (producers of the documents PR #42 reads):** `AddExpenseBottomSheet` from PR #38 is the only client producer of `friendships/{fid}/expenses/{eid}` documents. No client producer of `settlements/{settlementId}` yet (FR-SE-08 / PR #43 is the default plan); PR #42 renders an empty settlements section until FR-SE-08 ships.
     - **Design-system components:** the catalogue lists 8 widgets used by SCR-11 (see paragraph 5). None exist in the codebase. Per the PR #38 precedent, the default is to inline most and extract only what has a clear second use-site. Architect's call at 2.6 below.
     - **Telemetry plumbing:** the existing `lib/core/telemetry/` helpers (Firebase Analytics wrapper + `hashFriendshipId()` / `hashId()`) are reused. No new telemetry infrastructure. The four SCR-11 telemetry rows are PARTIALLY in scope: only `friend_detail_viewed` ships in PR #42; the other three gate on out-of-scope features and are deferred.
     - **Snapshot-stream reads:** the friends-list pattern from PR #35 (StreamProvider + `FriendshipStore.watchByMember(uid)`) generalises directly. PR #42 adds two new snapshot streams: expense subcollection per `friendshipId`, and settlements top-level filtered by `contextType == 'friendship' && contextId == friendshipId`. The settlements query needs the composite index that was deployed in the post-PR-#38 sweep.
     - **Placeholder replacement:** `FriendDetailPlaceholderScreen` → `FriendDetailScreen`. Same constructor signature; same FAB wiring (the FAB call site stays put; PR #42's screen just hosts a richer body). Existing widget tests for the placeholder become widget tests for the new screen.
     - **Out of scope for PR #42:** Settle-Up CTA (FR-SE-07 / FR-SE-08 — separate PR; render position is reserved per SCR-11 but the button is omitted or rendered as a deferred-affordance per architect's call), Delete Friend overflow menu (FR-FR-05 P1 — separate PR), full-history screen at `/friends/:friendshipId/history` (only "top 5 recent expenses" per the SCR-11 spec), edit / delete expense (FR-EX-06 — separate PR; expense rows are read-only in PR #42), receipt thumbnails in expense rows (FR-EX-05 hasn't shipped — every expense doc has `receiptUrl == null`), Send Reminder (FR-SE-09 — separate PR).

0.3  Confirm DoR for the new story:
     - There is NO existing story file for FR-FR-04. PM creates one at `docs/sprint-zero/stories/FR-FR-04-friend-detail.md` using the SRS §13.2 format ratified by FR-FR-03 / FR-EX-01 / FR-SE-05-06.
     - SRS sections 4.3 (FR-FR-04, FR-FR-03 net balance, FR-FR-05 deferred), 4.6 (FR-SE-01 canonical view, FR-SE-07 settle-up CTA deferred), 7.3 (Invariants 1 and 2 — read-side this time), 7.5 (security rules — already shipped for both expenses and settlements) are the authoritative requirements.
     - Screen spec `docs/design/06-screen-specs/09-12-friends.md` SCR-11 is the authoritative UI design. SCR-12 (Delete Friend Dialog) is referenced but explicitly DEFERRED.
     - Story points: **5 SP** is the working estimate. Scope is: scaffold the Friend Detail screen + header + balance pill + expense list + states (loading / populated / empty / error); extend `ExpenseRepository` + `ExpenseStore` with `watchExpenses({friendshipId})`; add a new `SettlementRepository` + `SettlementStore` with `watchSettlements({contextType, contextId})`; add a combined timeline provider; widget tests + provider tests + boundary-contract grep + PII-leak test + an integration test that closes the round-trip from inside this new screen (write an expense via the FAB → assert it appears in the list). If the architect concludes the scope is larger (e.g., the timeline projection turns intermixed and folding both event types is non-trivial, or `SettlementStore` needs more shape than `ExpenseStore`), consider splitting into PR #42a (screen + expenses) and PR #42b (settlements + integration). Default: single PR.

0.4  Cross-reference `docs/audits/sprint-1/07-bucket-b-burndown.md` and `docs/sprint-zero/next-three-prs.md`. PR #42 closes **no Bucket-B items by itself** but may make partial progress on **PY3** (integration tests for Sprint 2 flows) by adding a new integration test that exercises the friends-list → friend-detail → add-expense → real-time-update round-trip.

────────────────────────────────────────
PHASE 1 — STORY READINESS (PM)
────────────────────────────────────────

PM agent creates the story `docs/sprint-zero/stories/FR-FR-04-friend-detail.md` using the SRS §13.2 format. Story points: **5** (or split per Phase 0.3).

Required Given/When/Then ACs (each MUST be testable):

  - **AC-1 — Open from Friends List.** Given the user is viewing the friends list (FR-FR-03) and the friendship A↔B is rendered, when they tap the row, then the Friend Detail screen opens at `/friends/{friendshipId}` with the friend's identity pre-bound. Telemetry: `friend_detail_viewed { friendship_id_hash, balance_state: 'owed' | 'owes' | 'settled' }`.
  - **AC-2 — Header rendering.** Given the screen opens, when the friendship document is loaded, the header renders the friend's avatar (large variant, 80 dp), display name, and balance pill colour-coded per the balance state. Names + avatars are read via the existing `userProfileProvider.family(uid)` from PR #35 (no re-fetch logic).
  - **AC-3 — Balance pill semantics.** When `simplifiedBalances[currentUid][friendUid] > 0`, the pill reads "You are owed ₹X.XX" in `success` colour. When `simplifiedBalances[friendUid][currentUid] > 0`, the pill reads "You owe ₹X.XX" in `danger` colour. When both are zero, the pill reads "Settled up" in `onSurface` muted colour. Paise → INR via `formatInrFromPaise()` exclusively.
  - **AC-4 — Expense list (top 5).** Given the friendship has at least one non-deleted expense in `friendships/{fid}/expenses/`, when the expense snapshot stream emits, the screen renders up to 5 most recent rows ordered by `date` descending. Each row shows description, category icon, payer ("You" / friend's first name), date (locale-formatted), and the current user's share (`"you lent ₹X.XX"` if the user is the payer, `"you borrowed ₹X.XX"` otherwise — read from the `splits[]` entry for the current user's UID).
  - **AC-5 — Settlement read path (defensive empty).** Given the friendship currently has zero settlements in the top-level `settlements/` collection (production state pre-FR-SE-08), when the settlement snapshot stream emits, the screen does not render a settlement section header or any settlement rows. When a settlement IS present (seeded by the integration test or by a future FR-SE-08 client write), the row renders inline in the timeline OR in a separate section per the architect's choice at 2.3.
  - **AC-6 — Empty state (no expenses).** Given the friendship has zero non-deleted expenses AND zero settlements, when both snapshot streams emit, the body shows an `OBTEmptyState` with title "No expenses yet" and subtitle "Add an expense with [Friend name] to start tracking." The FAB remains visible and opens `AddExpenseBottomSheet` (the PR #38 call site).
  - **AC-7 — Loading state.** Given the screen opens, while the friendship document and the two snapshot streams are loading, the header renders an `OBTSkeletonLoader` (profile-header variant) and the body renders three `OBTSkeletonLoader` rows (list-tile variant). The app-bar title shows the friend's display name if it was passed as a navigation argument; otherwise it renders empty.
  - **AC-8 — Error state.** Given the friendship document load throws OR either snapshot stream errors, the body shows an `OBTErrorState` with title "Something went wrong", subtitle "We couldn't load this friend's details. Please try again.", a Retry button, and a "Contact Support" link. Tapping Retry re-initialises both streams.
  - **AC-9 — Real-time update via the round-trip.** Given user A opens the Friend Detail screen for friend B with `simplifiedBalances == {}` and an empty expense list, when user A taps the FAB and creates a ₹100 equal-split expense, then within the integration-test polling window (≤ 2.5 s per NFR-PE-04) the screen re-renders: (a) the balance pill shows "You are owed ₹50.00" in `success` colour, (b) the new expense row appears at the top of the expense list, (c) the empty state is replaced with the populated state. This closes the round-trip from a different angle than PR #38's integration test — PR #38 asserted via the friends list; PR #42 asserts inside the per-friend ledger.
  - **AC-10 — Invariant 1 (paise, read-side) at the boundary.** Boundary-contract grep test: `grep -rEn '\.toDouble\(\)|/100\b|/100\.0\b|double ' lib/features/friends/presentation/friend_detail_screen.dart lib/features/friends/application/friend_detail_provider.dart` returns 0 matches. The amount path from `ExpenseDoc.splits[].sharePaise` → row text is all integer paise; rupee conversion is `formatInrFromPaise()` ONLY.
  - **AC-11 — Invariant 2 (`simplifiedBalances` server-only).** Boundary-contract grep test: `grep -rEn 'simplifiedBalances\s*=' lib/features/friends/presentation/friend_detail_screen.dart lib/features/friends/application/friend_detail_provider.dart` returns 0 matches (i.e. no assignment to that field). The screen READS `simplifiedBalances` via `netBalancePaise()`; it never writes.
  - **AC-12 — Telemetry PII guard.** The single emitted event `friend_detail_viewed` carries `friendship_id_hash` (SHA-256-truncated via `hashFriendshipId()`), not the raw composite UID. A new `friend_detail_pii_leak_test.dart` asserts via parameter inspection that no raw `friendships/A_B` literal ever appears in the event payload. Mirrors the PII-leak test patterns from PR #35 and PR #38.

DoR §9 invariant applicability:
  - **Invariant 1 (paise integers, conversion at UI):** APPLIES on the read side. The screen formats `simplifiedBalances` and `ExpenseDoc.splits[].sharePaise` via `formatInrFromPaise()` exclusively. No `double`, no `/100`. Boundary-contract test enforces.
  - **Invariant 2 (`simplifiedBalances` server-only):** APPLIES. The screen reads `simplifiedBalances` for the balance pill display; it never writes. The trigger from PR #36 and PR #37 remains the sole writer.
  - **Invariant 3 (system share sheet):** N/A for PR #42.
  - **Invariant 4 (single Firebase project):** APPLIES. No new project; the providers use the existing `FirebaseFirestore.instance` (or the injected fakes in tests).
  - **ADR-0013 (PII hashing):** APPLIES to the `friend_detail_viewed` event. AC-12 test enforces.

Telemetry events PR #42 introduces:
  - `friend_detail_viewed { friendship_id_hash, balance_state }` — fired once per screen-open.

Telemetry events DEFERRED (gate on out-of-scope features):
  - `settle_up_tapped` — deferred to FR-SE-08 / PR #43 (Settle Up CTA is out of scope).
  - `friend_history_tapped` — deferred (full-history screen `/friends/:friendshipId/history` is out of scope; the SCR-11 "View full history" link is omitted in PR #42).
  - `friend_delete_menu_tapped` — deferred to FR-FR-05 (Delete Friend P1, separate PR; overflow menu is omitted in PR #42).

Out of scope for PR #42 (document explicitly in the story):
  - Settle-Up CTA (FR-SE-07 + FR-SE-08) — the screen-spec position is RESERVED but the CTA itself is omitted; FR-SE-08 / PR #43 (default plan) will populate the position. Architect's call at 2.4: render a disabled "Settle up — coming soon" affordance OR omit entirely. Default recommendation: **omit entirely** to avoid promising what hasn't shipped.
  - Delete Friend overflow menu (FR-FR-05) — P1, separate later PR. The `OBTAppBar` trailing action position is RESERVED in the layout but the icon is omitted.
  - Full-history screen at `/friends/:friendshipId/history` — separate later PR. The "View full history" text-link above the expense list is omitted; PR #42 ships only the top-5 recent expenses.
  - Edit / delete expense (FR-EX-06) — separate PR. Expense rows are READ-ONLY in PR #42; tapping a row is a no-op (or shows a minimal read-only detail sheet — architect's call at 2.5).
  - Receipt thumbnail in expense rows (FR-EX-05 not yet shipped) — every `ExpenseDoc.receiptUrl` is `null` in production.
  - Send Reminder (FR-SE-09 P1) — separate later PR.
  - Activity-feed item writes (FR-EX-07 / FR-AC-01) — separate later sprint.
  - Groups context (FR-GR-04 group detail screen) — Sprint 3 groups epic.

────────────────────────────────────────
PHASE 2 — ARCHITECT NOTES
────────────────────────────────────────

Owner: architect. Append `## Architect Notes` to the new story.

2.1  **Source layout for the new screen.**
     - Folder additions (extending `lib/features/friends/` and `lib/features/expenses/`, plus a new `lib/features/settlements/` minimal feature folder):
       ```
       lib/features/friends/
         presentation/
           friend_detail_screen.dart            # replaces friend_detail_placeholder_screen.dart
           widgets/
             friend_detail_header.dart          # avatar + name + balance pill
             friend_detail_timeline.dart        # expense + settlement rows
             friend_detail_states.dart          # loading / empty / error
         application/
           friend_detail_provider.dart          # combined timeline + balance projection
       lib/features/expenses/
         data/
           expense_repository.dart              # EXTEND with watchExpenses({friendshipId})
       lib/features/settlements/
         data/
           settlement_repository.dart           # NEW — SettlementStore + FirestoreSettlementStore + SettlementRepository
         domain/
           settlement_doc.dart                  # NEW — strict fromFirestore parsing (mirror friendship_doc.dart pattern)
       ```
     - Tests at `test/features/friends/`, `test/features/expenses/`, `test/features/settlements/` mirroring the source layout, plus `test/integration/friends/friend_detail_flow_test.dart`.
     - The `lib/features/settlements/` folder is intentionally minimal in PR #42 (read path only); FR-SE-08 / PR #43 will populate `application/` (settle-up controller) and `presentation/` (settle-up bottom sheet).

2.2  **`FriendDetailScreen` widget structure.**
     - `ConsumerWidget` (Riverpod 2.x) mirroring `friends_list_screen.dart`.
     - Reads three providers: the friendship doc (existing `friendsListProvider` projection or a new `friendshipDocProvider.family(friendshipId)` if the projection doesn't expose the doc), the expense stream (`expensesProvider.family(friendshipId)`), the settlement stream (`settlementsProvider.family(friendshipId)`).
     - A new `friendDetailProvider.family(friendshipId)` combines all three into a single `AsyncValue<FriendDetailState>` so the widget can `state.when(...)`.
     - The screen accepts the SAME constructor params as the placeholder (`friendshipId`, `currentUserUid`, `otherUserUid`) so `FriendsListScreen._onRowTapped` needs no change.

2.3  **Combined timeline projection.**
     - Architect's choice: tabbed (Expenses tab + Settlements tab) OR intermixed timeline (one chronological list of expense + settlement rows).
     - Recommendation: **intermixed** for v1.0 — matches the SCR-11 spec ("Recent Expenses" section which implicitly includes settlements when surfaced) and is the simpler implementation. The combined projection sorts by `date` (expenses) or `createdAt` (settlements; settlements have no user-picked `date` field) descending, capped at 5 rows total.
     - Each row is rendered via a polymorphic `TimelineRow` widget: `_ExpenseRow` for `ExpenseDoc` items, `_SettlementRow` for `SettlementDoc` items.

2.4  **Settle-Up CTA position — architect's call.**
     - The SCR-11 spec reserves a position for `OBTSettleUpCard` below the balance pill (state #2 / state #6).
     - PR #42 default: **omit the card entirely** (do not promise what hasn't shipped); FR-SE-08 / PR #43 inserts it at the same position.
     - Alternate: render a disabled "Settle up — coming soon" affordance. Picks up the user's intent but risks the "marked-as-coming-soon affordance accumulating dust" anti-pattern called out in `feature-pr-conventions.md`.

2.5  **Per-row tap behaviour — architect's call.**
     - Default: tap on an expense row is a **no-op** in PR #42 (FR-EX-06 / PR #45-ish will define the edit/delete sheet).
     - Alternate: tap surfaces a read-only detail bottom sheet showing the expense's description, category, date, payer, all splits, and (when receipt ships) the receipt thumbnail. This adds scope for a `ReadOnlyExpenseDetailSheet` widget; not recommended for PR #42.

2.6  **Design-system widget extractions — same Option E vs Option I framework as PR #38 §2.8.**
     - SCR-11 references 8 catalogue widgets; **none exist in the codebase** (PR #38 extracted only `OBTAmountInput`).
     - Default: **inline everything new in `lib/features/friends/presentation/widgets/`** with TODO comments for the future extraction (mirror PR #38's pattern for `OBTBottomSheet`, `OBTPrimaryButton`, `OBTSnackbar`, `OBTCategoryChip` which were all inlined despite being catalogue items). Extract only if a clear second use-site exists in this PR. The `OBTBalancePill` is a strong candidate for extraction because the friends-list already uses an equivalent pill (`friends_list_screen.dart` rendering); the architect should look at whether the friends-list pill can be retroactively folded into a shared widget in this PR or whether it stays inline for now.
     - Whatever the architect picks, record the choice and the per-widget rationale.

2.7  **`SettlementDoc` strict parsing.**
     - Mirror the `FriendshipDoc.fromFirestore` pattern from PR #35: null / missing → return null and report via an `onParseFailure` sink (so production observability sees malformed settlement docs without the UI silently coercing bad data to a zero-balance "settled up" state).
     - Required fields: `contextType`, `contextId`, `fromUserId`, `toUserId`, `amountPaise`, `createdAt`, `deleted`. Optional / extension-locked: `verificationStatus` (ARCH-EXT-06 — read-only in v1.0; ignore but don't crash).
     - Soft-delete filter: drop entries with `deleted == true`.

2.8  **Repository / Store abstraction for settlements.**
     - New `SettlementStore` interface (parallels `FriendshipStore` at `lib/features/friends/data/friendship_repository.dart` top of file) with one method initially: `Stream<List<SettlementDoc>> watchByContext({String contextType, String contextId})`.
     - Implementations: `FirestoreSettlementStore` (production, queries `settlements` collection with `where('contextType', '==', contextType).where('contextId', '==', contextId).orderBy('createdAt', 'desc')`) and `FakeSettlementStore` (test-side, returns a controllable in-memory list).
     - `SettlementRepository` is a thin wrapper that exposes a typed `Stream<List<SettlementDoc>>` to the application layer.
     - Does NOT add a `fake_cloud_firestore` dependency. Follows the PR #38 pattern.

2.9  **`ExpenseRepository.watchExpenses` extension.**
     - Add to the existing `ExpenseStore` interface in `lib/features/expenses/data/expense_repository.dart`:
       ```dart
       Stream<List<ExpenseDoc>> watchExpensesByFriendship(String friendshipId);
       ```
     - `FirestoreExpenseStore` queries `friendships/{friendshipId}/expenses/` with `where('deleted', '==', false).orderBy('date', 'desc').limit(5)` (the 5-row cap is the SCR-11 spec).
     - `FakeExpenseStore` returns a controllable in-memory list ordered by `date desc`.
     - The existing `createExpense` write path from PR #38 is unchanged.

2.10 **Coverage gate posture.**
     - `lib/features/friends/presentation/friend_detail_screen.dart` + the new widgets and provider: per-module ≥70% gate applies. Aim for ≥80% on the provider; ≥70% on the widget.
     - `lib/features/settlements/` is a NEW feature folder. Same ≥70% per-module gate.
     - `lib/features/expenses/data/expense_repository.dart` gains a new method; the existing repository tests extend with `watchExpensesByFriendship` cases.
     - `lib/core/**`: unchanged (no edits).
     - Integration test runs against the emulator and exercises the round-trip from inside the new screen.

2.11 **No new ADR required.** The provider + repository + read-side projection patterns all reuse ADR-0006 + ADR-0007 precedent. The settlements repository pattern reuses the `FriendshipStore` / `ExpenseStore` precedent.

────────────────────────────────────────
PHASE 3 — SCOPE
────────────────────────────────────────

WHAT GOES IN PR #42:

  Flutter source:
  - `lib/features/friends/presentation/friend_detail_screen.dart` — REPLACES `friend_detail_placeholder_screen.dart`.
  - `lib/features/friends/presentation/widgets/friend_detail_header.dart`
  - `lib/features/friends/presentation/widgets/friend_detail_timeline.dart`
  - `lib/features/friends/presentation/widgets/friend_detail_states.dart`
  - `lib/features/friends/application/friend_detail_provider.dart` — combined provider.
  - `lib/features/expenses/data/expense_repository.dart` — EXTEND with `watchExpensesByFriendship`.
  - `lib/features/settlements/data/settlement_repository.dart` — NEW.
  - `lib/features/settlements/domain/settlement_doc.dart` — NEW.
  - `lib/features/friends/presentation/friends_list_screen.dart` — UPDATE the `_onRowTapped` import to point at the new `FriendDetailScreen` (the constructor signature stays the same so no other change is needed).
  - `lib/features/friends/presentation/friend_detail_placeholder_screen.dart` — DELETE (replaced).

  Tests:
  - `test/features/friends/friend_detail_screen_widget_test.dart` — widget tests covering all 6 states from SCR-11 (loading, populated non-zero balance, populated settled, no expenses, error, real-time update on a new expense doc).
  - `test/features/friends/friend_detail_provider_test.dart` — provider tests with fake stores.
  - `test/features/friends/friend_detail_boundary_contract_test.dart` — grep contract for the new files.
  - `test/features/friends/friend_detail_pii_leak_test.dart` — `friend_detail_viewed` event payload check.
  - `test/features/expenses/expense_repository_test.dart` — EXTEND with `watchExpensesByFriendship` tests (order desc, limit 5, soft-delete filter).
  - `test/features/settlements/settlement_repository_test.dart` — NEW; full coverage of the read path + soft-delete filter + parse-failure handling.
  - `test/features/settlements/settlement_doc_test.dart` — strict-parsing tests (mirror `friendship_doc_test.dart`).
  - `test/integration/friends/friend_detail_flow_test.dart` — end-to-end against the emulator: seed friendship A↔B; mount FriendDetailScreen; drive the FAB to create a ₹100 equal-split expense; poll until the screen's balance pill reads "₹50" and the new row appears.

  Test deletions / migrations:
  - The existing `test/features/friends/friend_detail_placeholder_screen_test.dart` (or equivalent — verify whether one exists) is REPLACED by `friend_detail_screen_widget_test.dart`. Migrate any reusable test fixtures.

  Documentation:
  - The new story file from Phase 1 with Architect Notes.
  - `docs/sprint-zero/sprint-2-plan.md` — PR #42 status (merged), velocity entry (5 SP, cumulative 29 SP / 9 PRs).
  - `docs/sprint-zero/next-three-prs.md` — roll PR #43 (FR-SE-08 default), PR #44 (D5 deadline default), PR #45 (lookup bug or post-PR-#38 cleanup).
  - `lib/features/friends/README.md` — append a note on the new screen and the `friend_detail_provider`.
  - `lib/features/settlements/README.md` — NEW; describe the read-path-only scope of PR #42 and the hand-off to FR-SE-08 for the write path.

WHAT DOES NOT GO IN PR #42:

  - Settle-Up flow (FR-SE-08) — separate PR (PR #43 default plan).
  - Edit / delete expense (FR-EX-06) — separate later PR.
  - Receipt attachment (FR-EX-05) — separate later PR.
  - Activity feed (FR-EX-07 / FR-AC-01) — separate later sprint.
  - FCM push notifications (FR-AC-03) — separate later sprint.
  - Delete Friend (FR-FR-05 P1) — separate later PR; overflow menu position is reserved in the layout but the icon is omitted.
  - Full-history screen at `/friends/:friendshipId/history` — separate later PR; "View full history" link omitted in PR #42.
  - Send Reminder (FR-SE-09 P1) — separate later PR.
  - Groups context (FR-GR-04) — Sprint 3 groups epic.
  - Any change to `firestore.rules`. The expense subcollection rules (PR #36) and the settlements rules (PR #37) are sufficient.
  - Any change to `functions/src/**`. The triggers are already live; this PR only reads.
  - Any change to `firestore.indexes.json`. The two needed indexes (`friendships memberIds + lastActivityAt` and `settlements contextType + contextId`) are already deployed.
  - The D5 deadline upgrade (issues #39 + #40) — its own dedicated PR (PR #44 default plan). PR #42 must NOT absorb it.
  - Extraction of any design-system widget beyond what the architect explicitly authorises at §2.6. Default: inline everything.

If an agent suggests any of: "let's ship the Settle Up CTA since the position is right there", "let's bundle the D5 deadline upgrade since we have to touch functions anyway" (PR #42 does NOT touch functions), "let's add the full-history screen since we already have the data path", "let's add the Delete Friend menu since the icon space is reserved" — refuse. These are scope creep.

────────────────────────────────────────
PHASE 4 — TEST-FIRST DISCIPLINE
────────────────────────────────────────

Tests written before implementation, in this order:

1. **`SettlementDoc` strict-parsing tests** (`test/features/settlements/settlement_doc_test.dart`):
   - Valid doc → fully populated `SettlementDoc`.
   - Missing required field → returns null AND fires `onParseFailure`.
   - Bad type (e.g., `amountPaise` as String) → same.
   - `deleted == true` → still parses but the repository filters it out at the next layer.

2. **`SettlementRepository` tests** (`test/features/settlements/settlement_repository_test.dart`):
   - `watchByContext({contextType: 'friendship', contextId: 'A_B'})` emits the seeded list ordered by `createdAt desc`.
   - Soft-deleted entries excluded.
   - Parse failures dropped + reported.
   - Stream completes / errors propagate.

3. **`ExpenseRepository.watchExpensesByFriendship` tests** (extend `test/features/expenses/expense_repository_test.dart`):
   - Returns up to 5 most recent non-deleted expenses ordered by `date desc`.
   - Soft-deleted entries excluded.
   - Empty friendship → empty list.

4. **`friend_detail_provider` tests** (`test/features/friends/friend_detail_provider_test.dart`):
   - With fake stores: provider emits `FriendDetailState.loading` initially, then `FriendDetailState.populated(...)` when streams emit, with the combined timeline in `date desc` order.
   - With one stream erroring: provider emits `FriendDetailState.error`.
   - With both streams empty: provider emits `FriendDetailState.empty`.
   - The balance pill projection via `netBalancePaise()` is the SAME helper as the friends list.

5. **`FriendDetailScreen` widget tests** (`test/features/friends/friend_detail_screen_widget_test.dart`):
   - Renders the loading state with skeleton placeholders.
   - Renders the populated state with header + up-to-5 expense rows + balance pill in the correct colour for each balance-state branch.
   - Renders the empty state with the "No expenses yet" message and a working FAB.
   - Renders the error state with Retry button; tapping Retry re-initialises both streams.
   - The FAB opens `AddExpenseBottomSheet` (the PR #38 sheet).
   - Accessibility: every interactive widget has a semantics label per SCR-11.

6. **Boundary-contract grep tests** (`test/features/friends/friend_detail_boundary_contract_test.dart`):
   - `grep -rEn '\.toDouble\(\)|/100\b|/100\.0\b|double ' lib/features/friends/presentation/friend_detail_screen.dart lib/features/friends/application/friend_detail_provider.dart` → 0 hits.
   - `grep -rEn 'simplifiedBalances\s*=' lib/features/friends/presentation/friend_detail_screen.dart lib/features/friends/application/friend_detail_provider.dart` → 0 hits.

7. **PII-leak test** (`test/features/friends/friend_detail_pii_leak_test.dart`):
   - Drive the screen open with a known `friendshipId == 'uidA_uidB'`.
   - Capture the emitted `friend_detail_viewed` payload via a fake telemetry sink.
   - Assert: no raw `'uidA_uidB'` substring; the hashed value (length-16 hex from `hashFriendshipId()`) IS present in the `friendship_id_hash` parameter.

8. **Integration test** (`test/integration/friends/friend_detail_flow_test.dart`):
   - `@Tags(['integration'])`, `skip: 'Requires emulator suite'` locally.
   - Wires up the Firebase Emulator (Auth + Firestore + Functions).
   - Seeds two users (A + B) and a friendship doc.
   - Mounts `FriendDetailScreen(friendshipId, currentUserUid: A, otherUserUid: B)` with the production providers + emulator-backed `FirebaseFirestore.instance`.
   - Drives the FAB → enters ₹100 → equal split → Save.
   - Polls for ≤ 10 s until the screen's balance pill text reads "₹50" AND the new expense row appears at the top.
   - Tear-down: deletes the docs + users.

9. ONLY after all test files are red (or explicitly skipped if the harness isn't available), flutter-dev implements:
   - Step A: `SettlementDoc` + `SettlementStore` + `SettlementRepository` + `FakeSettlementStore`. Verify settlements tests green.
   - Step B: extend `ExpenseRepository.watchExpensesByFriendship`. Verify extended expense-repository tests green.
   - Step C: `friend_detail_provider`. Verify provider tests green.
   - Step D: `FriendDetailScreen` + sub-widgets + state branches. Verify widget tests + boundary-contract + PII-leak tests green.
   - Step E: replace `FriendDetailPlaceholderScreen` import in `friends_list_screen.dart` and delete the placeholder file. Verify the existing friends-list widget tests still pass.
   - Step F: run the integration test against the emulator (or confirm it stays skipped locally).

Coverage:
   - `lib/features/friends/presentation/friend_detail_screen.dart` + the new widgets: ≥70% per-module (target 80% on the provider).
   - `lib/features/settlements/**`: ≥70% per-module.
   - `lib/features/expenses/data/expense_repository.dart`: unchanged or improved.
   - `lib/core/**`: unchanged.

────────────────────────────────────────
PHASE 5 — EXECUTION PROTOCOL
────────────────────────────────────────

Mirror PR #38 patterns with the FR-FR-04-specific commits.

 1. PM creates story `docs/sprint-zero/stories/FR-FR-04-friend-detail.md`. Commits as `docs(stories): FR-FR-04 friend detail full screen story`.
 2. Architect appends `## Architect Notes` (§2.1–§2.11). Commits as `docs(stories): FR-FR-04 architect notes`.
 3. Flutter-dev writes the test files BEFORE any implementation. Commit splits per the test-first precedent:
    - `test(settlements): settlement doc strict-parsing tests (FR-FR-04)`
    - `test(settlements): settlement repository read path tests (FR-FR-04)`
    - `test(expenses): expense repository watchExpensesByFriendship tests (FR-FR-04)`
    - `test(friends): friend detail provider tests (FR-FR-04)`
    - `test(friends): friend detail screen widget tests (FR-FR-04)`
    - `test(friends): friend detail boundary contract + PII leak tests (FR-FR-04)`
    - `test(friends): friend detail integration flow against emulator (FR-FR-04)`
 4. Flutter-dev implements in the Phase 4 step order:
    - `feat(settlements): settlement doc + repository read path (FR-FR-04)`
    - `feat(expenses): expense repository watchExpensesByFriendship (FR-FR-04)`
    - `feat(friends): friend detail provider (FR-FR-04)`
    - `feat(friends): friend detail screen + header + timeline + states (FR-FR-04)`
    - `feat(friends): swap FriendDetailPlaceholderScreen for FriendDetailScreen (FR-FR-04)`
 5. Update plan and roll-forward:
    - `docs/sprint-zero/sprint-2-plan.md` (PR #42 status, velocity #9, cumulative 29 SP).
    - `docs/sprint-zero/next-three-prs.md` (roll PR #43 / PR #44 / PR #45 with PR #42's outcome).
    - `lib/features/friends/README.md` + `lib/features/settlements/README.md` (NEW).
    - Commits as `docs(plan): FR-FR-04 friend detail shipped, prep PR #43`.
 6. QA runs the manual smoke matrix:
    - Start emulators (`scripts/dev/start-emulators.sh`).
    - Sign in as user A; seed user B and a friendship A↔B.
    - Open the app → Friends tab → tap into friend B → the new Friend Detail screen opens.
    - Verify the loading skeleton, then the populated state with header + balance pill ("Settled up" since no expenses) + empty state in the body.
    - Tap the FAB → add an expense (₹100 equal split).
    - Verify the screen re-renders: balance pill reads "You are owed ₹50.00" in green, the new expense row appears at the top.
    - Switch the device to airplane mode → verify the screen continues rendering from the cache; restore connectivity.
    - Open the screen with an inaccessible `friendshipId` (e.g., a UID-pair the current user isn't a member of) → verify the error state.
 7. PR opened by flutter-dev.
    Title: `feat(friends): FR-FR-04 per-friend transaction history`
    Body must:
      - Cite `docs/patterns/feature-pr-conventions.md`.
      - Confirm Invariant 1 (read-side; boundary contract + property-test on net-balance projection).
      - Confirm Invariant 2 (no writes to `simplifiedBalances` from `lib/features/friends/presentation/friend_detail_screen.dart`).
      - Confirm the round-trip via the integration test: FAB → write → trigger → snapshot stream → screen re-renders.
      - State explicitly the deferred items: Settle Up CTA (FR-SE-08), Edit/Delete (FR-EX-06), Receipt (FR-EX-05), Delete Friend (FR-FR-05), full-history screen, Send Reminder (FR-SE-09), groups context (Sprint 3).
      - End with `Next PR: PR #43 — FR-SE-08 Settle-Up UI (default plan per next-three-prs.md).`

────────────────────────────────────────
PHASE 6 — DEFINITION OF DONE GATE
────────────────────────────────────────

Walk the DoD checklist. Particular emphasis:

  - DoD §2: widget tests green; integration test green against the emulator; coverage gate green; per-module ≥70% on `lib/features/friends/presentation/friend_detail_screen.dart` + provider + new widgets, and on `lib/features/settlements/**`.
  - DoD §3: QA sign-off referencing the manual smoke matrix from Phase 5 step 6 with screenshots of (a) the populated state with a non-zero balance, (b) the empty state, (c) the real-time re-render after the FAB write, (d) the error state.
  - DoD §7: invariant compliance.
    * **Inv-1:** grep across `lib/features/friends/presentation/friend_detail_screen.dart` + `lib/features/friends/application/friend_detail_provider.dart` returns 0 violations. The boundary-contract test asserts the same.
    * **Inv-2:** grep across the same files for `simplifiedBalances\s*=` returns 0 (no assignment). The read via `netBalancePaise()` is the only contact.
    * **Inv-4:** no second Firebase project.
  - DoD §8: documentation updated. Story file with Architect Notes. Sprint plan + next-three-prs updated. `lib/features/friends/README.md` extended. `lib/features/settlements/README.md` created.

QA posts the explicit DoD §3 sign-off. Flutter app should be built locally (`flutter build apk --debug` and `flutter build ios --no-codesign`) before merge to confirm no platform-specific regressions. This PR is Flutter-only and does not deploy server changes.

────────────────────────────────────────
PHASE 7 — UPDATE THE ROLLING PLAN AND BURNDOWN
────────────────────────────────────────

PM agent updates:
  - `docs/sprint-zero/sprint-2-plan.md` — PR #42 status (merged), velocity data point #9 (5 SP, total now 29 SP across 9 PRs).
  - `docs/sprint-zero/next-three-prs.md`:
      * PR #43: **FR-SE-08 Settle-Up UI** (default plan — closes the settlement round-trip, symmetric to PR #38 for settlements).
      * PR #44: **D5 deadline PR** (Node 22 + firebase-functions 7.x — closes #39 + #40) — slot before mid-September 2026.
      * PR #45: TBD per Sprint 2 velocity — `lookup-user-by-phone-number` bug fix OR the post-PR-#38 cleanup OR a Bucket-B chore.

Commits as `docs(plan): FR-FR-04 friend detail shipped, prep PR #43`.

────────────────────────────────────────
GUARDRAILS
────────────────────────────────────────

If during this PR an agent proposes:
  - "Let's ship the Settle Up CTA since the position is already reserved" → REFUSE. The CTA depends on FR-SE-08 (PR #43); without a write path, the button would be a dead-end. The screen-spec position is RESERVED but the affordance is omitted.
  - "Let's add the full-history screen since the data path is already there" → REFUSE. Out of scope; separate PR. PR #42 ships only top-5 recent expenses per SCR-11.
  - "Let's add the Delete Friend overflow menu — the icon space is reserved" → REFUSE. FR-FR-05 is P1, separate PR with its own confirmation-dialog flow (SCR-12).
  - "Let's bundle the D5 deadline upgrade since we're already touching the functions test surface" → REFUSE. PR #42 does NOT touch `functions/src/**`. D5 is PR #44's dedicated scope.
  - "Let's write `simplifiedBalances` from the controller because optimistic updates feel snappier" → REFUSE. Invariant 2; the trigger is the sole writer; the friends-list snapshot listener typically emits sub-second after the trigger fires (NFR-PE-04: ≤ 2.5 s P95).
  - "Let's coerce paise to `double` for the balance-pill animation" → REFUSE. Invariant 1; the animation is a UI affordance — animate the rendered text via `AnimatedSwitcher` or similar, NOT by converting the underlying paise value to a double.
  - "Let's emit the `friend_detail_viewed` event with the raw `friendshipId` for easier debugging" → REFUSE. ADR-0013; hash via `hashFriendshipId()` at the emit boundary. PII-leak test enforces.
  - "Let's add a recurring-expense toggle / a non-INR currency picker / any new design-system widget extraction" → REFUSE. All out of scope or premature.

If something genuinely surprises (e.g., the SCR-11 spec disagrees with what's discoverable in the codebase, the settlements composite index hasn't actually deployed yet, the snapshot-stream provider pattern needs a tweak for the per-friendship query, the integration test surfaces a latency above NFR-PE-04's P95 budget), STOP and surface the friction. PR #42 closes the read-side round-trip; get it right rather than fast.

Begin with Phase 0 — verify the dependency DAG and post-merge state of PR #38 + PR #41.
