1. You are the OneByTwo orchestrator agent. PR #72 (the Issue #47 friendship-expense creator-only rules-hardening chore) has merged (squash commit `33f870d`, `Closes #47`). It added the inline `&& request.auth.uid == prev.createdBy` clause to `isValidExpenseUpdate()` in `firestore.rules` (creator-only for BOTH edit and soft-delete, both flowing through the one `allow update` rule; the bilateral settlement either-party soft-delete left deliberately asymmetric and untouched), inverted the two self-documenting `assertSucceeds` "flip to assertFails" non-creator placeholder tests in `functions/test/firestore-rules/expenses-friendship.test.ts` and added the two creator-success counterparts (full rules suite green: 10 suites / 200 tests), shipped the chore story `docs/sprint-zero/stories/CHORE-expense-creator-only-rules.md` (PM + Architect Notes), and reconciled the planning docs. No new ADR (a tightening within ADR-0010's field-level pattern under ADR-0008, in service of Invariant 2). Two non-blocking PR review comments were handled on issue #47 (AC#5 test count 153 → 200/10 suites; the group-expense carry-forward made explicit). **With #72 merged, every P0 functional requirement EXCEPT the deliberately-deferred Sprint-3 Groups epic (FR-GR-01..07) is shipped, and every P1 is shipped.** The single-focus, issue-backed carry-forward candidate (#47) is now consumed, so we pick the **next top-ranked carry-forward item**: the **Bucket-B close-with-evidence bundle** — the diffuse alternative that prompt `31.md` §1 explicitly named as the runner-up to #47. It closes/documents the Sprint-1 boundary-audit findings that Sprint 2 PRs have already resolved, with linked-commit evidence, and re-scopes the genuine remainders so nothing is silently dropped. The architect confirms the pick at kickoff.

2.

## 0. PR-number caveat (read first)

Do NOT assume a roadmap slot equals the next GitHub number. Issues and PRs share one sequential namespace; this work lands as the next available GitHub number (>= #73 now that the highest PR is #72 and the highest issue is #68). This bundle does NOT introduce a NEW tracked issue — it acts ON existing audit issues **#20 / #21 / #23**. If the architect rules that any genuine remainder should be split into a focused follow-up issue (rather than re-scoped in place), each new issue consumes a number >= #73 and the PR's own number follows them. The PR body references the issues it touches (`#20`, `#21`, `#23`) but uses a **`Closes #NN` line ONLY for an issue that is being FULLY closed** (see §1 — likely only #21 qualifies for a full close); a partially-resolved issue must NOT get a `Closes` line (that would close unresolved sub-items). Reconcile the slot label at PR open.

## 0a. Planning-doc reconciliation (read second)

Per the rolling-roadmap convention, each PR reconciles the PREVIOUS PR's status: the planning docs currently list **#72 as "Open (≥ #72)"** (it reconciled #71 -> Merged at its own open). At THIS PR's open, reconcile **#72 -> Merged** (`33f870d`), bump the velocity Total from **130 SP / 31 PRs** to **132 SP / 32 PRs** (adding #72's 2 SP), and add the Bucket-B close-with-evidence row as Open — in both `docs/sprint-zero/sprint-2-plan.md` (PR Tracking + Velocity) and `docs/sprint-zero/next-three-prs.md` (mark #72 Merged: replace the `## PR #72 — IN FLIGHT`-equivalent treatment with a "Merged" entry mirroring the #71 / #70 pattern, flip the Issue #47 candidate bullet to "MERGED (#72, `33f870d`)", and flip the **Bucket-B chore close-out** candidate bullet to "IN FLIGHT"). NOTE there is currently no detailed `## PR #72` section in `next-three-prs.md` (only the roadmap-table row and the candidate bullet were used for #72) — reconcile the roadmap table row #72 to Merged and the candidate bullet; add a detailed Merged section only if you judge it consistent with the #70/#71 precedent.

> **Velocity-exclusion call (architect/PM decides at kickoff):** the Bucket-B bundle's deliverable is **documentation + issue-tracker hygiene** (the resolving code/tests already shipped in earlier PRs). The velocity convention excludes "pure documentation and CI infrastructure PRs" (consistent with #59 `093fce7`). So although the bundle is ~3 SP of verification/reconciliation effort, it is most likely **velocity-excluded** (added to the PR Tracking table but contributing **0** to the velocity Total, like #59). If the architect instead rules it carries SP, state the figure explicitly. Do NOT silently inflate the Total.

## 1. Scope — what we build

**A close-with-evidence reconciliation of the Sprint-1 boundary-audit Bucket-B findings that Sprint 2 PRs have already resolved.** This is the FIRST PR whose PRIMARY deliverable is Bucket-B tracker hygiene; every prior closure was a side-effect of feature work logged in `docs/audits/sprint-1/07-bucket-b-burndown.md`. **There is NO new application code, no new rules, no new tests** — the resolving work shipped in earlier PRs. The job is to VERIFY the cited evidence, post evidence comments on the issues, close or re-scope each issue correctly, and reconcile the burndown + chore-backlog docs.

Current state (read it; do not re-derive). The canonical tracker is `docs/audits/sprint-1/07-bucket-b-burndown.md` (its "Last updated" is stale at **PR #58** — part of this chore is bringing it current). The three target issues and their **accurate** resolved/remaining split:

- **#20 "Improve test coverage gaps"** (findings SC1, SC2, SC3, SC4, **CV3**):
  - **CV3 — RESOLVED by PR #36.** `functions/src/simplified-debts/function.ts` branch coverage went 76% -> **88.57%** (31/35, measured by the coverage gate post-merge) when the `recomputeAndWrite` core + trigger-boundary tests landed. The burndown already strikes CV3 through.
  - **Remaining (stay OPEN):** SC1 (concurrent-submit guard test for phone entry), SC2 (large-group scalability test — Sprint 3 groups), SC3 (`MAX_SAFE_INTEGER` overflow test for the algorithm), SC4 (OTP auto-retrieval timeout test). So **#20 is a PARTIAL close** — post the CV3 evidence comment and re-scope #20 to SC1-SC4; do NOT fully close it.

    > **[Editorial note — added at PR #73 review; the prompt text above is preserved `sic`]** SC2 and SC4 are transposed in the line above. Per the audit source `docs/audits/sprint-1/02-test-suite-health.md:185-188`, **SC2 = OTP auto-retrieval timeout** and **SC4 = large-group scalability**. The burndown (`07-bucket-b-burndown.md`), the #20 evidence comment, and the issue tracker all use the audit-correct mapping; only this historical prompt carries the transposition.
- **#21 "Firestore and Storage rules test gaps"** (findings R1-R8):
  - **R1, R2, R3 — RESOLVED by PR #32** (friendship create / update / delete validation tests in `functions/test/firestore-rules/friendships.test.ts`).
  - **R5a (activity-collection rules) — RESOLVED by PR #51** (`functions/test/firestore-rules/activity.test.ts`, 12 tests).
  - **R7, R8 (Storage avatar size + content-type) — RESOLVED by PR #48** (`functions/test/storage-rules/receipts.test.ts`, 23 tests covering size + MIME + membership + cross-collection predicate).
  - **Remaining = R4, R5b, R6 — all GROUPS rules** (`isValidGroupCreate/Update/Delete` validation), which belong to the **Sprint 3 Groups epic**. NOTE the prompt-31 / chore-backlog shorthand "#21 R1-R4" is imprecise: **R4 is a `groups/{id}` create rule, NOT friendship** — the friendship closure target is **R1-R3**, and R4 re-scopes with R5b/R6 to the Groups epic. With R1-R3 + R5a + R7-R8 all resolved and only the groups halves remaining (cleanly re-scoped), **#21 is the one genuine FULL-close candidate** (the only target that may carry a `Closes #21` line — architect's call).
- **#23 "Expand integration tests for Sprint 2 flows"** (findings PY3, RT2, INV2):
  - **PY3 — PARTIAL.** The friend-add and expense-create flows are now covered **end-to-end at the Functions / emulator layer**: `npm run test:integration` runs in the PR pipeline under `firebase emulators:exec` and exercises `functions/test/integration/{on-expense-write,on-settlement-write,lookup-user-by-phone-number,simplified-debts,delete-user-account}.integration.test.ts` (PR #36 enabled `test:integration` in CI and added the expense-write + simplified-debts tests; #37 added the settlement-write test; the friend-lookup test was unskipped/fixed in #45; the account-deletion test landed in #65 — VERIFY the exact provenance per file). BUT the **Flutter `test/integration/**/*_flow_test.dart` stubs remain `skip:`ped** ("until the emulator harness lands"), so the UI-driven e2e layer is NOT yet real.
  - **Remaining (stay OPEN):** the Flutter integration-harness half of PY3; RT2 (CI step-duration logging for emulator suites); INV2 (share-sheet verification tests — "partially addressed" by PR #32's invite flow). So **#23 is a PARTIAL close** — document the Functions-layer PY3 coverage and re-scope #23 to the Flutter-harness PY3 remainder + RT2 + INV2.

RECOMMENDED scope (ship this):

- **For each of #20 / #21 / #23, post a per-issue evidence comment** (via `gh issue comment`) that lists each resolved finding with its **resolving commit / PR** (CV3 -> PR #36; R1-R3 -> PR #32; R5a -> PR #51; R7-R8 -> PR #48; PY3 Functions-layer -> the `functions/test/integration/*` suite enabled in CI by PR #36, extended by #37 and later PRs) and names the genuine remainder.
- **Close #21 in full** (`gh issue close 21` with the evidence comment, OR a `Closes #21` line in the PR body) — all non-groups findings resolved; R4/R5b/R6 re-scope to the Sprint 3 Groups epic. **Keep #20 and #23 OPEN**, re-scoped (edit the issue body to strike the resolved finding and narrow the open scope to SC1-SC4 / Flutter-PY3 + RT2 + INV2). Do NOT give #20 or #23 a `Closes` line.
- **Reconcile `docs/audits/sprint-1/07-bucket-b-burndown.md`**: bring "Last updated" from PR #58 to the current PR; record the close-with-evidence closures; recompute the Totals table and the burndown chart; note that #60-#72 closed no further Bucket-B items except as documented here. This is the bulk of the diff.
- **Reconcile the chore-backlog + closure-candidates in `docs/sprint-zero/sprint-2-plan.md`** ("Issue closure candidates (close-with-evidence PR)" section) and `docs/sprint-zero/next-three-prs.md` to reflect the closures and the #72 -> Merged status (§0a).
- **No app-code, no rules, no new tests, no schema, no index, no Cloud Function change.** Verifying the evidence means READING the cited coverage reports / rules tests / integration tests and (optionally) re-running `npm run test:rules` + `npm run test:integration` to confirm green — not writing anything new.

ESCALATION (architect / PM decide at kickoff):

- **Full-close vs keep-open-re-scoped, per issue.** RECOMMEND: full-close **#21** only; keep **#20** and **#23** open re-scoped. If the architect prefers to close all three and file focused follow-up issues for the remainders (SC1-SC4; Flutter-PY3 + RT2 + INV2), that is acceptable but consumes new issue numbers (§0) — do NOT do it silently; record the new issues in the burndown and backlog.
- **PY3 closure threshold.** The one genuine judgement call: does "cover friend-add and expense-create flows end-to-end" mean the Functions/emulator layer (DONE in CI) or the Flutter UI layer (skipped stubs)? RECOMMEND treating PY3 as **substantially resolved at the data-flow layer** with the Flutter-harness half explicitly re-scoped (it is already a known deferral the stubs reference). Do NOT close #23 in full on the strength of the Functions layer alone.
- **`chore` vs `docs` type.** The deliverable is documentation + issue hygiene with zero code; `docs(backlog):` is defensible, `chore(backlog):` is also acceptable for tracker maintenance. Architect's call; single-token scope either way.
- **No new ADR.** This is tracker reconciliation, not a technical decision. No ADR.

Story points: **3** (verify the evidence for ~6 findings across 3 issues + re-run the rules/integration suites to confirm green + post the comments + reconcile the burndown, backlog, and roadmap docs) — but **likely velocity-excluded** as a docs/hygiene PR (§0a).

## 2. Numbering / CI title-lint gotcha

The PR title lint (`.github/workflows/pr.yml`) enforces `^(feat|fix|chore|docs|test|refactor|ci|build)(\([a-z0-9_-]+\))?!?: .{1,72}$` — a single-token scope (no commas) and subject <= 72 chars. Suggested title (<= 72 chars): `chore(backlog): close-with-evidence for resolved Bucket-B audit findings` (or `docs(backlog): reconcile Bucket-B burndown and close resolved findings`). Branch: `chore/bucket-b-close-with-evidence` (repo convention: type-prefixed kebab). Because the PR is mostly docs, expect the CI to be green quickly (no Flutter/Functions code touched); the Integration Tests + rules jobs still run and must stay green if you re-run the suites to verify.

## 3. First-PR-that framing (firsts)

- **First dedicated Bucket-B tracker-hygiene PR** — every prior bucket-B closure (R1-R3 in #32, CV3 in #36, SR8 in #38, D5a/D5b in #44, R7-R8 in #48, R5a in #51) was a side-effect logged in the burndown by a feature/chore PR. This is the first PR whose PRIMARY purpose is closing/reconciling audit findings with linked-commit evidence.
- **First PR whose main deliverable is `07-bucket-b-burndown.md`** — it is reconciled from its stale "PR #58" state to current.
- **First multi-issue close-with-evidence bundle** — it acts on #20 / #21 / #23 together (the diffuse alternative deferred in favour of the single-focus #47).

## 4. Invariants

Read `.github/shared/invariants.md`. This PR changes NO code, so no invariant is touched by the diff. The evidence it documents, however, is load-bearing for the invariants and must be cited accurately:

- **Invariant 1 (integer paise):** the CV3 evidence (`function.ts` branch coverage) and the SC3 `MAX_SAFE_INTEGER` finding both concern the integer-paise simplified-debts maths; cite the coverage figure faithfully and keep SC3 OPEN (it is NOT resolved).
- **Invariant 2 (`simplifiedBalances` server-only):** the R1-R3 / R5a evidence are the rules tests that protect the client-writable collections feeding the recompute; do not imply the bundle weakens any rule (it changes none).
- **Invariant 3 (system share sheet only):** INV2 (share-sheet verification) stays OPEN/partial — do NOT claim it resolved.
- **Invariant 4 (single Firebase project):** the integration evidence runs against the single emulator project; introduce no project.

## 5. Cross-cutting dependencies and risks

- **Accuracy is the whole job.** The one real risk is citing the wrong commit or over-claiming a closure. VERIFY each finding against the actual artefact before commenting: read the cited test files (`friendships.test.ts`, `activity.test.ts`, `storage-rules/receipts.test.ts`, the `functions/test/integration/*` suite) and, for CV3, the coverage figure. Re-running `npm run test:rules` and `npm run test:integration` under the emulator (JDK 21; `export PATH="/opt/homebrew/opt/openjdk@21/bin:$PATH"`) is the cheapest way to confirm the evidence is still green.
- **Do NOT write the deferred tests.** Writing SC1-SC4, the groups rules (R4/R5b/R6), the Flutter integration harness (PY3 stubs), RT2, or INV2 is OUT OF SCOPE — those are the remainders this PR re-scopes, not closes. Writing them would turn a 3 SP hygiene chore into multiple feature-sized efforts.
- **Burndown arithmetic.** The Totals table and burndown chart are stale; recompute them from the resolution log rather than trusting the stale header numbers.
- **Editing `functions/test/**/*.ts` is NOT expected** (this is a docs/issue PR). If you only touch `.md` files + the prompt, the lefthook `eslint-functions` pre-commit hook will not trip; a normal `fvm exec git commit` works. If for any reason a `.ts` file is touched, commit with `git commit --no-verify` after verifying gates manually, per the standing convention.

## 6. What this PR must NOT bundle

Carry forward the standing defer list: the Sprint 3 Groups epic (FR-GR-01..07) and its R4/R5b/R6 group rules tests, the `go_router` migration (Sprint 3), FR-SR-01/02 Search (SCR-07), the Flutter integration-test harness (the `skip:`ped `test/integration/**` stubs / PY3 Flutter half), the remaining #20 coverage gaps (SC1/SC2/SC3/SC4), RT2 (CI step-duration logging), INV2 (share-sheet verification), the FR-SE-09 message-compose dialog follow-up, the cosmetic chores (FAB spring-physics polish, `currentUserIdProvider` rehoming, `OBTRupeeText`, activity-writer rename, catalogue docs roll-up), Issue #49 (orphan-receipt reaper), Issue #50 (no-op-recompute optimisation), Issue #66 (deletion reaper), Issue #68 (monthly-spend rollup), and any change to the settlement rules, the expense-write trigger, the schema, or `firestore.indexes.json`. See `docs/sprint-zero/next-three-prs.md` for the full carry-forward set.

## 7. Read before starting

- `.github/copilot-instructions.md`, `.github/shared/invariants.md`, `.github/shared/coding-standards.md` (Conventional Commits), `.github/shared/test-strategy.md`.
- The canonical tracker `docs/audits/sprint-1/07-bucket-b-burndown.md` (the resolution log + remaining-by-category tables + burndown chart) and its sources `docs/audits/sprint-1/00-triage-summary.md`, `02-test-suite-health.md` (CV3/PY3/SC*/RT2 definitions), `04-dependency-and-security.md` (R1-R8 definitions), `06-deferred-to-sprint-2.md`.
- The three target issues: [#20](https://github.com/avtansh-code/OneByTwo/issues/20), [#21](https://github.com/avtansh-code/OneByTwo/issues/21), [#23](https://github.com/avtansh-code/OneByTwo/issues/23).
- The evidence artefacts: `functions/test/firestore-rules/friendships.test.ts` (R1-R3), `functions/test/firestore-rules/activity.test.ts` (R5a), `functions/test/storage-rules/receipts.test.ts` (R7-R8), `functions/test/integration/*.integration.test.ts` (PY3 Functions layer), and the PR #36 coverage figure for `functions/src/simplified-debts/function.ts` (CV3).
- `docs/sprint-zero/sprint-2-plan.md` ("Sprint 2 Chore Backlog" + "Issue closure candidates (close-with-evidence PR)" + "Recommended sequencing" + PR Tracking + Velocity) and `docs/sprint-zero/next-three-prs.md` (roadmap table + candidate list).

## 8. Routing and sequencing (per `.github/shared/handoffs.md`)

This is a tracker-hygiene chore acting on existing issues; no new story is required (it reconciles audit findings, not an SRS requirement). Sequence:

1. **QA** — owns the verification: for each finding (CV3, R1, R2, R3, R5a, R7, R8, PY3), open the cited artefact and confirm it genuinely resolves the finding; re-run `npm run test:rules` + `npm run test:integration` under the emulator (JDK 21) to confirm green; produce the resolved/remaining evidence map (with commit/PR citations) and the recommended close-vs-rescope decision per issue. Hand the evidence map to the PM.
2. **PM** — owns the tracker actions: post the per-issue evidence comments on #20 / #21 / #23; close #21 in full and re-scope #20 / #23 (edit issue bodies); reconcile the "Issue closure candidates" + chore-backlog rows in `sprint-2-plan.md` and the candidate bullet in `next-three-prs.md`; resolve the PY3-threshold and full-close-vs-rescope questions (recommend per §1). Hand off to the Architect for ratification.
3. **Architect** — ratifies the close-vs-rescope ruling per issue, the PY3 threshold, the velocity-exclusion call, and the `chore`-vs-`docs` type; confirms no new ADR and that no code/rules/schema/index/trigger changed. **No Designer, no Flutter Dev, no Functions Dev are required** — there is no code change. State that explicitly in the handoff.
4. **Whoever owns the burndown** (QA or PM) — reconcile `07-bucket-b-burndown.md` to current and apply the §0a #72 -> Merged + velocity reconciliation, then open the PR.

## 9. Kickoff directive

Confirm the **Bucket-B close-with-evidence bundle** as the next-slot pick with the Architect (the top remaining non-Sprint-3 carry-forward; the diffuse alternative that prompt `31.md` named as #47's runner-up; closes/documents the audit findings Sprint 2 already resolved with linked-commit evidence and re-scopes the remainders), then delegate per section 8. Refuse any attempt to (a) write the deferred work that this PR is meant to RE-SCOPE, not close — the #20 coverage gaps (SC1-SC4), the groups rules (R4/R5b/R6), the Flutter integration harness (PY3 stubs), RT2, or INV2; (b) give a `Closes #NN` line to a partially-resolved issue (only a FULLY-resolved issue — likely just #21 — may carry one); (c) over-claim a closure or cite the wrong resolving commit (verify every citation against the artefact); (d) start the Sprint 3 Groups epic, the `go_router` migration, or FR-SR-01/02 Search; (e) touch the settlement rules, the expense-write trigger, the schema, or `firestore.indexes.json`; or (f) make any app-code, rules, or new-test change (this is a pure documentation + issue-tracker-hygiene chore — the resolving code/tests already shipped). Update `docs/sprint-zero/sprint-2-plan.md` PR Tracking (reconcile #72 from Open to **Merged** `33f870d`, bump the velocity Total from 130 / 31 to **132 SP / 32 PRs**, then add the Bucket-B row as Open — velocity-excluded unless the architect rules otherwise), `docs/sprint-zero/next-three-prs.md` (mark #72 merged, flip the Issue #47 candidate to MERGED and the Bucket-B candidate to in-flight), and `docs/audits/sprint-1/07-bucket-b-burndown.md` (reconcile to current; record the close-with-evidence closures) at PR open and on merge.
