You are the OneByTwo orchestrator agent. PR #4 (`feat(auth): phone-number entry screen with locked +91 prefix (FR-AU-01)`) has merged. We now run a retrospective and produce PR #5 to land its findings.

For clarity on prior work:
  - PR #1: agentic configuration bootstrap (out of retro scope — it is the constitution).
  - PR #2: sprint-zero + design phase artefacts (out of retro scope — docs only).
  - PR #3: project skeleton — IN scope for a brief sanity check, NOT a full retro.
  - PR #4: phone-number entry screen — the primary subject of this retrospective.

This session produces NO product feature code. PR #5 will contain:
  - A short sanity-check note on PR #3 under `docs/retros/`.
  - A full retrospective document on PR #4 under `docs/retros/`.
  - Updates to files under `.github/agents/`, `.github/skills/`, `.github/hooks/`, `.github/shared/` if findings require fixes.
  - A new `docs/patterns/feature-pr-conventions.md` that PR #6 onwards will reference.
  - An updated `docs/sprint-zero/sprint-1-plan.md` and a new `docs/sprint-zero/next-three-prs.md`.

Re-read .github/copilot-instructions.md, .github/shared/invariants.md, and .github/shared/handoffs.md before starting.

────────────────────────────────────────
PR-#5 GROUND RULES
────────────────────────────────────────

  - Work on a feature branch. Suggested name: `chore/pr-004-retrospective`.
  - Every commit follows Conventional Commits.
  - Every file edit passes the lefthook pre-commit hooks and the agentic PreToolUse hooks.
  - PR #5 is a `chore:` PR, not a `feat:` PR — it does not implement an FR-XX-NN.
  - PR #5 is exempt from the "Follows docs/patterns/feature-pr-conventions.md" citation in its description, because it is the PR that introduces that document. Note this exemption explicitly in the PR body.
  - PR #5 does follow the existing PR template (the four-invariant checklist still applies, even for a chore PR — every box gets ticked with rationale, even if the rationale is "N/A: no application code in this PR").

────────────────────────────────────────
PHASE A — DATA GATHERING
────────────────────────────────────────

Owner: orchestrator (collects), then qa (analyses).

A1. Read the merged PR #4 from start to finish:
    - PR title, description, all comments.
    - Every commit message and the diff for each commit.
    - The CI runs: which jobs ran, how long each took, any retries, any flakes.
    - The hook events that fired during the work (PreToolUse blocks, PostToolUse formatters, lefthook outputs).

A2. Read every file PR #4 produced, grouped by which agent's work it represents:
    - flutter-dev: lib/features/auth/**, test/features/auth/**.
    - architect: any architect notes appended to the story file.
    - qa: any test plan or manual smoke notes.
    - devops: any workflow or hook adjustments.

A3. Compare PR #4's actual artefacts against the design phase outputs:
    - docs/design/04-wireframes/auth-flow.md (phone-entry step) — does the implemented screen match?
    - docs/design/05-mockups/02-phone-and-otp.html (phone-entry half) — does the implementation match the mockup at iPhone-12 viewport size? Note any drift.
    - docs/design/06-screen-specs/<phone-entry-screen>.md — does the implementation cover every state, every validation rule, every accessibility requirement listed in the spec?

A4. Compare PR #4's actual artefacts against the SRS:
    - FR-AU-01 acceptance criteria — every one met?
    - FR-AU-02 validation rule (10-digit, +91 lock) — implemented at the formatter level, not just validator level?
    - SRS §5.6 accessibility — semantic labels, focus order, dark mode, dynamic-type tested?
    - SRS §5.7 maintainability — coverage thresholds hit, lint clean, public APIs documented?
    - SRS §5.10 observability — `signup_started` telemetry event fires correctly?

A5. PR #3 sanity check — examine the skeleton choices NOW that they have been load-tested by PR #4:
    - The `lib/` folder scaffolding from PR #3 — did PR #4 fight against it or fit into it?
    - The theme setup in `lib/app/theme.dart` — did PR #4 need to extend or work around it?
    - The `pubspec.yaml` dependency choices — did PR #4 add new deps, indicating a gap in the skeleton's pinning?
    - The `widget_test.dart` from PR #3 — does it still pass? Was it updated, replaced, or left orphaned?
    - The `analysis_options.yaml` — did PR #4 hit any lint rules that turned out to be too strict or too lenient?
    - The Firebase initialisation in `main.dart` (if it was wired in PR #3) — did PR #4 need to touch it? If yes, that's a smell that some setup logic should move into a dedicated bootstrap module.

This phase produces no commits — it produces structured findings in chat that subsequent phases turn into committed files.

────────────────────────────────────────
PHASE B — PR #3 SANITY-CHECK NOTE (commit 1)
────────────────────────────────────────

Owner: architect.

Produce `docs/retros/2025-XX-XX-pr-003-sanity.md` (use today's date). Keep it short — half a page is the target. Sections:

  ## Skeleton choices that held up
  Anything from PR #3 that PR #4 confirmed was the right call.

  ## Skeleton choices that need adjustment
  Anything that wants fixing before PR #6 starts. Each item: what to change, where, who owns it.

  ## Items deferred
  Things that are visibly suboptimal but small enough to live with for now. Each item: a short note on the cost of fixing later vs. now.

If the second section is empty, that is a great outcome — write "No skeleton-level findings; PR #3 choices held up." and move on.

Commit: `docs(retros): PR-#3 skeleton sanity-check note`

────────────────────────────────────────
PHASE C — PR #4 STRUCTURED RETRO (commit 2)
────────────────────────────────────────

Owner: orchestrator delegates each section to the most relevant agent.

Produce `docs/retros/2025-XX-XX-pr-004-retro.md`. Use this exact structure — do not deviate, even if a section ends up empty:

  ## What worked well
  Concrete observations only. No platitudes. For each, say WHY it worked so we can preserve it.

  ## What was friction
  Things that took longer than they should have, agent confusion, redundant work, places where the orchestrator had to re-route.

  ## Drift from the design
  Every place the implementation deviated from the wireframe, mockup, or screen spec. For each: was the deviation an improvement, an oversight, or a defensible trade-off? Who decided?

  ## Drift from the SRS
  Same as above but for SRS requirements. Should be empty or near-empty; anything here is a serious finding.

  ## Hooks and invariants
  Did the PreToolUse hooks fire? On what? Were the firings accurate (true positives) or noisy (false positives)? Did anything slip past the hooks that should have been caught?

  ## Test discipline
  Was the test-first rule honoured? If not, where did it slip and why? What is the actual coverage on lib/features/auth/** versus the threshold?

  ## Patterns to lock in
  The half-dozen patterns from PR #4 that are good enough to become "the way we do it" for the next forty PRs. Be specific: file path conventions, Riverpod provider naming, test structure, PR description shape, commit message style, hook outputs format.

  ## Patterns to fix before PR #6
  The patterns from PR #4 that should NOT be replicated. For each, the specific change needed in `.github/agents/`, `.github/skills/`, `.github/hooks/`, or `.github/shared/`.

  ## Action items
  A numbered list of concrete changes to make in this PR. Each item has an owner agent and a "done when" assertion.

Be honest. Agentic teams have a tendency to produce "everything is fine" retros because the agents don't have egos to bruise — and consequently they don't have the instinct to surface awkward things unless explicitly asked. Surface them.

Commit: `docs(retros): PR-#4 retrospective findings`

────────────────────────────────────────
PHASE D — APPLY THE FIXES (commits 3..N)
────────────────────────────────────────

Owner: each action item's named owner.

For each action item from Phase B (skeleton sanity check) and Phase C (PR #4 retro):
  - If it requires changing an agent description, skill procedure, hook script, shared invariant, or PR template, make the change in this PR.
  - If it requires changing the SRS or a design document, STOP and escalate to me — those are not changed casually.
  - If it requires running a script or tool that is not committable, document the command and ask me to run it.

Each fix is its own commit, with a Conventional Commits message scoped to what changed:
  - Agent description fixes: `chore(agents): tighten architect description for schema-only edits`
  - Skill procedure fixes: `chore(skills): require negative case in scaffold-flutter-feature`
  - Hook script fixes: `chore(hooks): block test files from being deleted in feature PRs`
  - Shared invariant fixes: `chore(shared): clarify simplifiedBalances invariant wording`

After all in-scope fixes are committed, run a quick verification (no commit needed):
  - The three PreToolUse refusal scripts still pass on a known-bad diff.
  - The lefthook hooks still fire on a sample commit.
  - The PR template renders correctly (mentally paste the markdown into a sample issue body and verify).

If verification fails, fix and commit again before proceeding.

────────────────────────────────────────
PHASE E — RATIFY THE PATTERNS (commit N+1)
────────────────────────────────────────

Owner: architect.

Produce `docs/patterns/feature-pr-conventions.md`. This file is the canonical reference for how every feature PR from PR #6 onwards is structured. It is short and prescriptive — bullet lists, no prose essays.

Sections (mandatory):

  1. File layout for a new feature
     The exact folder structure under `lib/features/<feature>/`, the test mirror under `test/features/<feature>/`, and the rule for where shared widgets live.

  2. State management
     Where providers go, how they are named, when to use `Provider` vs `StateNotifierProvider` vs `AsyncNotifierProvider` (Riverpod 2.x), how scoping works.

  3. Test discipline
     Test-first ordering, the three test layers (unit, widget, integration), what each is responsible for, the minimum coverage shape for a feature.

  4. Telemetry
     Where event names live, the analytics provider's location, the standard for parameter naming, who reviews new events. Reference the telemetry plan from the design phase.

  5. Accessibility
     The minimum bar for every screen — semantic labels, focus order, dark mode, dynamic type. The accessibility checks that run before a PR can be opened.

  6. PR shape
     Title format, description sections, the four-invariant checklist, the "next PR" pointer at the end.

  7. Commit rhythm
     Conventional Commit scopes that map to feature folders, how to split commits for auditability (one commit per agent role where reasonable).

This file gets cited at the top of every feature PR description from PR #6 onwards: "Follows `docs/patterns/feature-pr-conventions.md` as ratified after PR #4."

Commit: `docs(patterns): ratify feature-PR conventions after PR-#4`

────────────────────────────────────────
PHASE F — UPDATE THE WORKING ROADMAP (commit N+2)
────────────────────────────────────────

Owner: pm.

Update `docs/sprint-zero/sprint-1-plan.md` to reflect:
  - PR #4 status: merged (link).
  - PR #5: in flight (this PR).
  - Velocity observed (rough estimate based on PR #4 effort vs original estimate).
  - Any sequencing changes implied by the retro (e.g., if a pattern fix took longer than expected, push later stories by one slot).

Then produce `docs/sprint-zero/next-three-prs.md`:
  - PR #6: OTP screen UI only (FR-AU-03 partial — UI side).
  - PR #7: Firebase Phone Auth wiring (FR-AU-03 / FR-AU-04 / FR-AU-05 fully).
  - PR #8: Profile setup screen and user-doc creation (FR-AU-06).

For each, the user story file path, the design artefacts referenced, the agents involved, and a one-line scope statement. Keep this rolling — replace it after PR #8 with the next three.

Commit: `docs(plan): update roadmap and rolling next-three-prs`

────────────────────────────────────────
PHASE G — OPEN PR #5
────────────────────────────────────────

Owner: orchestrator (delegates to whichever agent has been doing the bulk of the commits — likely architect).

Push the branch and open the PR via `gh pr create`:

  Title: `chore: PR-#4 retrospective findings and ratified patterns`

  Body must include:
    - One-paragraph summary explaining this is a retrospective PR, not a feature PR.
    - Explicit note that PR #5 is exempt from the "Follows feature-pr-conventions.md" citation because it introduces that document.
    - Link to the two retro documents (PR-#3 sanity, PR-#4 retro).
    - Link to the new feature-pr-conventions.md.
    - Link to the updated next-three-prs.md.
    - The four-invariant checklist with rationale (likely all "N/A: no application code in this PR" — but each box still ticked with rationale, never silently skipped).
    - A list of every file added or modified, grouped by purpose (retros, agentic config fixes, patterns, roadmap).
    - "Next PR: PR #6 — OTP screen UI (FR-AU-03/04 partial)."

────────────────────────────────────────
DEFINITION OF DONE FOR PR #5
────────────────────────────────────────

  - All commits follow Conventional Commits.
  - lefthook hooks pass on every commit.
  - CI passes (lint, format, analyse, tests — even though no application code changed, the tests still must run and pass to confirm we haven't accidentally broken anything via a config or hook change).
  - PR description filled in completely; the four-invariant checkboxes are ticked with explicit rationale (no silent skips).
  - The two retro documents exist and are non-empty.
  - `docs/patterns/feature-pr-conventions.md` exists and covers all seven mandatory sections.
  - `docs/sprint-zero/next-three-prs.md` lists PRs #6, #7, #8 with story paths and scope.
  - QA agent has reviewed and posted a sign-off comment on the PR before merge.

────────────────────────────────────────
EXECUTION PROTOCOL
────────────────────────────────────────

Phase by phase, stop after each, wait for "proceed". Phase A is data gathering and produces no commits. Phases B–F each produce one or more commits. Phase G opens the PR.

Begin Phase A.