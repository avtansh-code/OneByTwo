You are the OneByTwo orchestrator agent. Sprint 3 — **Design Conversion (the "Direction A — Haldi" visual system, `design_handoff_one_by_two/`)** — is the active sprint. **Twelve** PRs have merged on `main`: DC-01 (design-token + type foundation) as PR #126 (`d9205d8`), DC-02 (the six `OBT*` shared-widget reskins) as PR #127 (`aca8582`), DC-03 (the eleven new Haldi shared components) as PR #129 (`b0ce9a6`), the DC-03/06 scope doc as PR #130 (`bd27c38`), DC-04 (Auth & onboarding) as PR #131 (`0391bf2`), DC-05 (Home dashboard hero) as PR #132 (`1c3e194`), DC-06 (Friends flow + Friend-history) as PR #133 (`006a0fb`), DC-07 (Expenses flow) as PR #134 (`262107d`), DC-08 (Settlements flow) as PR #135 (`b55e9fb`), DC-09 (Activity & Notifications surfaces) as PR #136 (`1ce84cc`), DC-10 (Profile & Settings cluster; closed Epic C) as PR #137 (`d58581b`), and **DC-11 (Dark-mode parity across the six hero screens; first PR of Epic D) as PR #138** (squash `c6bb785`). Every per-flow screen conversion is complete (Epic C closed) and every hero now ships **light and dark** with the warm-fill-takes-ink contrast gate and outline-not-shadow separation locked (Epic D opened by DC-11). The Haldi `ColorScheme` (light + dark), the `OBTColors` `ThemeExtension`, the `AppTheme` radius / soft-warm-shadow / motion tokens, the Bricolage Grotesque / Hanken Grotesk `TextTheme`, the reskinned `OBT*` widgets, the eleven DC-03 components, and the converted Auth / Home / Friends / Expenses / Settlements / Activity / Notifications / Profile flows are all on `main`.

The goal of THIS session is to deliver **Sprint 3 PR #13 — DC-12: Accessibility re-verification against the new palette** (issue **#124**). This is the only PR of **Epic E (Accessibility Re-Verification), 3 SP, P0 — Must have (a release gate)**, per ADR-0024 and `docs/sprint-zero/sprint-3-plan.md`. DC-12 converts **no** screen and adds **no** new behaviour. It **re-verifies** that the shipped Haldi palette + type + motion meet **WCAG 2.1 AA** across the conversion, **closes any accessibility gap** it finds, and — the load-bearing Definition-of-Done clause — **wires the contrast and dynamic-type accessibility gates into CI as a named, blocking gate** so the conversion can never silently regress on a future PR. DC-13 (#125, the golden / visual-regression harness) is a **separate, later** PR and owns the golden baselines + the ubuntu `golden-a11y-checks` job; **DC-12 does not author or commit golden PNGs.**

The three Acceptance Criteria (from `sprint-3-plan.md` story DC-12):

- **AC-1 — the verified contrast pairings hold.** Against the **resolved** Haldi tokens: ink on marigold (`onPrimary`/`primary`) **>= 4.5 (AA)**, ink on background (`onSurface`/scaffold) **AAA**, positive `#0F7D6B` on white **>= 4.5 (AA, body)**, negative `#BC4030` on white **>= 4.5 (AA, body)**, and dark text `#F3EBDD` on `#1A1510` **AAA** — every pairing meeting WCAG 2.1 AA (>= 4.5:1 body, >= 3:1 large/UI/non-text).
- **AC-2 — dynamic type to 2.0x.** Every converted **hero** (and the dense amount/pill/chip rows) renders at **2.0x** text scale at the **390** reference width **and** the **320** narrow stress width without text clipping or truncation; layouts reflow; a truncated rupee figure is a defect.
- **AC-3 — colour-blind-safe balance signals + reduced motion.** Every balance signal stays distinguishable in **greyscale / colour-blind** simulation because it carries **icon + label**, not colour alone (the **negative case**: a colour-only balance signal **fails** the check — the assertion must prove direction **without reading colour**); and **reduced motion** is honoured (transitions collapse to instant cross-fades; skeleton shimmers freeze).

────────────────────────────────────
LOAD-BEARING ITEMS (read first)
────────────────────────────────────

1. **DC-12 is "wire the gate", not "build the gate from scratch".** Most of the accessibility infrastructure already exists and runs today inside `flutter test --coverage`:
   - `test/app/theme_contrast_test.dart` (`@Tags(['a11y-contrast'])`) — the authoritative WCAG pairing gate. It already reads the **resolved** `AppTheme.light`/`.dark` `ColorScheme` + `OBTColors` (never hard-coded hex), computes the WCAG ratio via `Color.computeLuminance()`, asserts the five handoff-verified pairings + the foundation design-intent pairings + the balance trio on surface + meta-text AA, and (from DC-11) the **dark warm-fill-takes-ink negative cases** (white on dark marigold/salmon/terracotta FAILS < 3.0; ink passes).
   - `test/app/theme_dynamic_type_test.dart` (`@Tags(['a11y-dynamic-type'])`) — the 2.0x overflow/clip + reduced-motion gate for representative dense rows.
   - Per-hero 2.0x overflow tests now exist in `test/features/{home,friends,expenses,settlements,activity}/*_haldi_reskin_test.dart` (and auth), **light + dark** after DC-11.
   - The balance signal carries colour + icon + label via `OBTBalancePill` / the balance trio, asserted in the per-hero reskin gates.
   The DoD's "the contrast and dynamic-type gates are wired into CI" means: make the `a11y-contrast` + `a11y-dynamic-type` tagged families a **named, blocking CI gate** (today `pr.yml` runs `flutter test --coverage` at lines 148-149 with no tag selection, so they run but are not a *visible* gate that a reviewer sees pass/fail by name). DevOps adds the named step/job; do **not** fold it into DC-13's golden `golden-a11y-checks` ubuntu job (that is goldens, authored on Linux later). These accessibility tests **run for real** on every host.

2. **The published-figure vs measured-ratio reconciliation (a genuine re-verification task).** The story's AC-1 quotes the Haldi handoff figures — ink on marigold **5.6:1**, ink on background **13.9:1**, positive **4.6:1**, negative **5.1:1**, dark text **14.2:1**. The shipped gate `theme_contrast_test.dart` asserts `closeTo` the values Flutter's `Color.computeLuminance()` actually produces — **6.25 / 14.66 / 5.04 / 5.36 / 15.31** — which all clear the AA/AAA thresholds but differ from the published figures (different luminance rounding/method). DC-12 must **reconcile this explicitly**: confirm the measured ratios clear their WCAG role thresholds (they do), and in `04-qa-test-strategy.md` §B.1 record both the handoff figure **and** the Flutter-measured figure with a one-line note on the method difference, so the "within +/-0.2 of the figure" tripwire is unambiguous and a reviewer is never surprised. Do **not** change the tokens to chase a figure — the tokens are correct; this is a documentation/measurement reconciliation.

3. **Colour-blind-safe is proved WITHOUT reading colour (AC-3 negative case).** Per `04-qa-test-strategy.md` §B.3, the balance-signal test must assert the **direction** survives greyscale/colour-blindness by checking the **icon (`arrow_upward`/`arrow_downward`/`check`) and the text label**, never by reading the colour value. The negative case to lock: a balance rendered with **colour alone** (no icon, no label) **fails** the check. Every balance surface — Home net header + top-balance pills, Friends rows + Friend-detail header, the shared `OBTBalancePill`, Settle-up header, Expense-detail per-person split — carries the triple. Confirm each; this is invariant-grade (SRS section 5.6 — no colour-only meaning).

────────────────────────────────────
INVARIANTS (all hold — DC-12 is verification + test/CI only)
────────────────────────────────────

DC-12 touches accessibility tests, the QA strategy doc, CI wiring, and at most a small a11y gap-fix in a hero file (e.g. a missing semantic label or a sub-threshold tint). **Invariant 1** (money in paise): no on-screen `paise / 100` or `double`; every amount still routes through `formatInrFromPaise()`. **Invariant 2** (`simplifiedBalances` server-only): no projection read/write change. **Invariant 3** (system share sheet only): untouched. **Invariant 4** (single Firebase project): untouched. No India-only constraint (`+91`) is touched.

────────────────────────────────────
READ BEFORE STARTING
────────────────────────────────────

- `.github/copilot-instructions.md`, `.github/shared/invariants.md`, `.github/shared/srs-pointer.md` (SRS = `docs/OneByTwo_Requirements_Spec.md` v1.1), `.github/shared/coding-standards.md`, `.github/shared/handoffs.md`, `.github/shared/milestone-tracking.md`, `.github/shared/test-strategy.md`, `.github/shared/decision-log.md` -> ADR-0024 (Haldi; visual layer only).
- **The authoritative spec for this PR:**
  - `docs/sprint-zero/sprint-3-plan.md` — story **DC-12** (the User Story, the three Acceptance Criteria above, the Definition of Done — "the contrast and dynamic-type gates are wired into CI against the verified pairings" — and the Invariant-Compliance clause), and story **DC-13** (so the DC-12/DC-13 boundary is exact: DC-13 owns goldens + the ubuntu golden job; DC-12 owns the real-running contrast/dynamic-type/balance/reduced-motion gates).
  - `docs/audits/design-conversion/04-qa-test-strategy.md` — **§B.1/§B.2** (the authoritative measured-contrast table + the contrast gate, incl. the white-on-marigold and DC-11 white-on-warm-fill negative cases), **§B.3** (balance signal = colour + icon + label, the greyscale/colour-blind survival proof that does not read colour), **§B.4** (every control labelled), **§C.1–§C.3** (the 2.0x overflow/clip gate, the dense rows that must survive 2.0x, reduced motion), the **§E** CI job intent, and **Appendix 2** (the test-tag registry: `golden`, `a11y-contrast`, `a11y-dynamic-type`).
  - `docs/audits/design-conversion/03-foundation-plan.md` — **§1.2** (ink-not-white), **§1.5** (the balance trio = colour + icon + label, never colour alone), **§2.3** (motion + the reduced-motion accessibility requirement), and the light/dark token table (§1).
  - `design_handoff_one_by_two/README.md` — the **Accessibility** section (the handoff-verified WCAG pairings and the colour-blind-safe palette intent).
- **The gate scaffolding now on `main` (extend / wire — do not rebuild):** `test/app/theme_contrast_test.dart`, `test/app/theme_dynamic_type_test.dart`, the per-hero `test/features/**/*_haldi_reskin_test.dart` 2.0x + balance-trio + contrast assertions, the harnesses `test/support/widget_test_harness.dart` (`pumpThemed(..., brightness:, textScale:, disableAnimations:)`, `expectAllInteractiveNodesLabelled`, `expectAllTapTargetsMeetMinSize`) and `test/golden/golden_harness.dart`.
- **The CI wiring to extend (consume — read first):** `.github/workflows/pr.yml` (the "Flutter Lint & Test" job runs `flutter test --coverage` at ~lines 148-149 with no tag selection; PR Title Lint, the path-filter `Detect changed paths`, Build Android, Build iOS, Integration Tests, Coverage Gate). Add the named accessibility gate here (DevOps), selecting the `a11y-contrast` + `a11y-dynamic-type` tagged families, blocking on failure.
- Do **not** read or re-plan the backend/data docs; no Cloud Function, rule, trigger, schema, callable, or telemetry change.

────────────────────────────────────
STANDING CAVEATS (read first)
────────────────────────────────────

- **This is verification + CI-wiring + targeted gap-fix, NOT a re-theme or a re-build.** The palette/type/motion are already shipped and correct; DC-12 proves they meet AA, fills any accessibility gap (a missing semantic label, a sub-threshold meta-text tint, a colour-only balance surface), and makes the gate a named CI check. Change ONLY what an accessibility gap requires. Do **not** edit `lib/app/theme.dart` / `lib/core/theme/*` / the DC-02/03 components to chase a number — any token/component gap routes to the Architect / issue **#128** (which already carries the DC-11-surfaced `outlineVariant`/`onBackground` foundation gap and the `OBTSegmentedSplitControl` dark gap).
- **Do not author or commit golden PNGs — DC-13 owns that.** DC-12's load-bearing proof is the **real-running** contrast / dynamic-type / balance-signal / reduced-motion tests + the **named CI gate**, not images. If a hero golden scaffold is missing a state, note it for DC-13; do not author baselines on macOS (non-deterministic bytes).
- **Reconcile, don't restate, the contrast figures.** Item 2 above — record the Flutter-measured ratio alongside the handoff figure in §B.1; do not silently leave the AC's published numbers diverging from the gate's `closeTo` values.
- **No new behaviour, no telemetry change.** An accessibility fix must not drop/add an event, change a controller/callable, or alter the simplified-debts read. If a planned event for an interaction would be affected, confirm it still fires (the DC-06 lesson).
- **Tooling + lessons carried from DC-01..DC-11:**
  - fvm-pinned (Flutter 3.44.3 / Dart 3.12.2). The `fvm` wrapper may be broken ("Can't load Kernel binary ... expected 127, found 130") — bypass it: `export PATH="/Users/avtanshgupta/fvm/versions/stable/bin:$PATH"` then bare `flutter analyze lib test`, `flutter test`, `dart format .`, and commit/push/merge with bare `git`/`gh` (the pinned SDK on PATH satisfies the lefthook `dart`/`flutter` pre-commit + the pre-push `coverage-check`/`unit-tests` hooks).
  - CI gates on `dart format --set-exit-if-changed .` — run `dart format .` before pushing. `very_good_analysis` is strict (`directives_ordering`, `prefer_int_literals`, `avoid_redundant_argument_values`, `lines_longer_than_80_chars` incl. `test`/`testWidgets` descriptions **and comments**, `prefer_const_constructors`, `unnecessary_import`, `comment_references`, `discarded_futures`).
  - **Never build `AppTheme.light`/`.dark` (or call GoogleFonts) at test COLLECTION time** — only inside `test`/`testWidgets` bodies (resolve `AppTheme.x.colorScheme` inside the test body, not in a `for`-loop scope). Put any pure-`test()` contrast group **last** so earlier `testWidgets` populate the GoogleFonts cache. (This bit DC-11 — a loop-scope `AppTheme.x.colorScheme` failed every test with "did not complete".)
  - **Animated surfaces never settle** (skeleton shimmers, OTP cursor, slides) — pump with `disableAnimations: true`, or `settle: false` / a single frame.
  - `colorScheme.outlineVariant` is **unset** in both Haldi `ColorScheme`s and falls back to `onSurface` (near-white in dark / near-black in light) — never use it for a border; use `colorScheme.outline`. (Foundation hardening to define it is routed to #128.)
  - **`Closes #124` MUST be plain text, NOT backticked,** in the PR body, or the Sprint 3 milestone will not advance.
  - **Merge needs an OWNER approval.** `main`'s ruleset requires an approving review from a write-access reviewer; the session authenticates as the author `avtanshgupta` (a collaborator, cannot self-approve), so the PR reads `mergeStateStatus=BLOCKED`/`REVIEW_REQUIRED` until the **`avtansh-code` OWNER** approves; then squash-merge (`--delete-branch`). **iOS (CocoaPods) is the slowest check (~11-20 min)** — wait it out; cross-check `gh run view <id> --json jobs` if `gh pr checks` shows a stale "pending".
  - **Handle review comments.** After CI is green, check the PR for inline review comments, address them with focused commits, reply on each thread, and resolve the threads before merge.

────────────────────────────────────
THE TASK — DC-12 (3 SP)
────────────────────────────────────

1. **Issue + milestone + roadmap (PM).** DC-12 is issue **#124**, already under the `Sprint 3` milestone — confirm it; this PR carries `Closes #124` (plain). Per `.github/shared/milestone-tracking.md`, reconcile the Sprint 3 milestone, and on this PR flip the **DC-11** line in `docs/sprint-zero/next-three-prs.md` from "IN FLIGHT — Sprint 3 (Design Conversion) PR #12: DC-11 (#123)" to **"MERGED (#138, `c6bb785`)"** and set DC-12 in flight. In `02-conversion-checklist.md`, add a DC-12 accessibility-sign-off status note (mirror the DC-11 `> **Status — ...**` blockquote convention). No SRS gap (accessibility is ADR-0024, already specified).
2. **Architect + QA — the accessibility-gate ruling (review, read-only).** Confirm: the verified contrast pairings against the resolved tokens (AC-1) and the measured-vs-published reconciliation (load-bearing item 2); the per-hero 2.0x coverage gaps, if any (AC-2 — is every converted hero covered at 390 AND 320, light + dark?); the balance-signal triple and the colour-blind negative case (AC-3); reduced-motion honoured at the page-transition + skeleton level; and the Invariant contract (verification only). Return the gap list (likely small) for the Flutter Dev and the gate/CI spec for QA + DevOps.
3. **Flutter Dev — close any accessibility gap (hero files only):** if the audit surfaces a real AA gap (a missing semantic label, a sub-threshold meta/caption tint at a call site, a colour-only balance surface, a 2.0x clip), fix it at the call site. Change nothing the gate does not require; route any token/component-level gap to #128.
4. **QA (owns the gate) + Designer:** make the accessibility gate **complete and authoritative**:
   - **Contrast (AC-1):** confirm the verified-pairing gate covers the full Haldi palette (every `on*` on its fill, both themes; the balance trio + meta text on every surface they appear on) and the negative cases (white-on-marigold, white-on-warm-fill dark); reconcile the published vs Flutter-measured figures in `04-qa-test-strategy.md` §B.1.
   - **Dynamic type (AC-2):** confirm **every** converted hero (and the dense amount/pill/chip rows) renders at 2.0x at 390 AND 320 without clip/truncation, light + dark; fill any missing hero (mirror the per-hero reskin-gate pattern); keep the known frozen-component 2.0x overflows (the `OBTSettleUpSheet` `amountHero` / `OBTActivityRow` at 320) as the documented #128 exceptions (amount stays whole).
   - **Colour-blind balance + reduced motion (AC-3):** assert the balance direction survives greyscale/colour-blindness via icon + label (NOT colour), with the colour-only negative case failing; assert reduced motion collapses the page transition and freezes skeletons.
   - **Designer (read-only):** review the palette/type/balance signals against the Haldi README Accessibility section and return any finding (as DC-11's were actioned).
5. **DevOps — wire the named CI gate.** Add an explicit, blocking accessibility gate to `.github/workflows/pr.yml` that runs the `a11y-contrast` + `a11y-dynamic-type` tagged families (e.g. `flutter test --tags a11y-contrast --tags a11y-dynamic-type`) as a named step/job a reviewer sees pass/fail — separate from DC-13's golden job. Keep `flutter test --exclude-tags golden` fast for developers (Appendix 2 intent). Document the gate in `04-qa-test-strategy.md` §E.
6. **Verify:** `flutter analyze lib test` (no issues), `dart format .` (clean), `flutter test` (all pass; the new named gate green; per-feature coverage >= 70% and overall >= 50%). No behaviour / controller / callable / telemetry change.

────────────────────────────────────
BRANCH / PR
────────────────────────────────────

- Branch: `feat/haldi-accessibility-reverification` (type-prefixed kebab, repo convention).
- Conventional Commits, single-token scope, ASCII subject <= 72 chars — e.g. `feat(a11y): re-verify WCAG AA and wire the accessibility CI gate`.
- This is a **code + CI** PR — the full PR pipeline must be green (Flutter Lint & Test, the new accessibility gate, Build Android, Build iOS, Integration Tests, Coverage Gate) before merge.
- PR body: map each AC to its evidence (the contrast pairings + the published-vs-measured reconciliation; the per-hero 2.0x coverage; the colour-blind balance negative case + reduced motion; the named CI gate); tick the invariants checklist (all verification-only — 1 no money path change, 2 no projection write, 3 share-sheet untouched, 4 single project); note "contrast + dynamic-type gates wired into CI as a named blocking gate; goldens remain DC-13; no behaviour change"; **`Closes #124` (plain, NOT backticked)**.
- Include the `Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>` trailer.
- Commit `docs/copilot_prompts/sprint_3/13.md` (this prompt) with the PR, per the repo convention.
- **Merge:** after the pipeline is green (wait out iOS; cross-check `gh run view`), the PR reads `BLOCKED` until the `avtansh-code` OWNER approves; **handle any inline review comments first** (address + reply + resolve threads); then squash-merge (`--delete-branch`) with subject `feat(a11y): re-verify WCAG AA and wire the accessibility CI gate (#<pr>)`.

────────────────────────────────────
DEFINITION OF DONE (this PR)
────────────────────────────────────

- All three DC-12 acceptance criteria pass: the verified contrast pairings hold against the resolved tokens with the published-vs-measured figures reconciled in §B.1 (AC-1); every converted hero + dense rows render at 2.0x at 390 and 320 without clip/truncation, light + dark (AC-2); every balance signal survives greyscale/colour-blindness via icon + label with the colour-only negative case asserted to fail, and reduced motion is honoured (AC-3).
- The **contrast and dynamic-type gates are a named, blocking CI gate** in `pr.yml` (selecting the `a11y-contrast` + `a11y-dynamic-type` tags), documented in `04-qa-test-strategy.md` §E; goldens remain DC-13's.
- `next-three-prs.md` reconciled (DC-11 -> MERGED `#138` / `c6bb785`; DC-12 status); the DC-12 accessibility-sign-off row added to `02-conversion-checklist.md`.
- `theme.dart` / `core/theme/*` / the DC-02/03 components unchanged (any token/component gap routed to #128); no invariant weakened; no controller / callable / telemetry behaviour change.
- Analyse clean, `dart format` clean, all tests pass, per-feature coverage >= 70% and overall >= 50%.
- DC-12 issue (#124) closed under the `Sprint 3` milestone; the milestone reconciled per `.github/shared/milestone-tracking.md`.

End the session summary with: "Next: Sprint 3 PR #14 — DC-13 (Visual-regression / golden-test harness: render light + dark goldens for the six `OBT*` widgets, the eleven DC-03 components, and the converted heroes on the ubuntu `golden-a11y-checks` job; bundle the Bricolage/Hanken `.ttf`, un-skip the queued `dc04..dc09` scaffolds, author the baselines, and fail the build on any unintended pixel diff or reintroduced old token / white-on-marigold pairing, per `04-qa-test-strategy.md` §A/§E and `03-foundation-plan.md` §6)."

────────────────────────────────────
FAILURE / REFUSAL PROTOCOL
────────────────────────────────────

- If any change would weaken an invariant or the SRS, refuse it, quote the specific invariant / SRS section, and propose a compliant alternative. In particular: no on-screen `paise / 100` or `double` (Invariant 1); no read/write of `simplifiedBalances` (Invariant 2); no single-app share/messaging target (Invariant 3); no second Firebase project (Invariant 4).
- **AC-3 is non-negotiable:** a balance signal must never convey direction by colour alone — the icon + label carry it, and the colour-blind/greyscale check must prove direction without reading colour (a colour-only signal is a blocking defect; refuse and cite `03-foundation-plan.md` §1.5 / SRS section 5.6).
- Refuse scope creep: convert no screen; build no new behaviour; do **not** edit `lib/app/theme.dart` / `lib/core/theme/*` / the DC-02/03 components (consume the DC-01..03 foundation; any token/component gap routes to issue #128); do **not** author/commit golden PNGs (DC-13 owns that); do not change the `ThemeMode.system` wiring or unlock any India-only constraint (`+91`).
- Do not edit `docs/OneByTwo_Requirements_Spec.md`.

Begin by delegating to the **PM** (confirm #124 + the milestone, reconcile the roadmap so DC-11 reads MERGED `#138` / `c6bb785`, add the DC-12 sign-off row), then the **Architect + QA** (the accessibility-gate ruling: the contrast pairings + the published-vs-measured reconciliation, the per-hero 2.0x coverage, the colour-blind balance negative case, reduced motion, and the Invariant ruling), then the **Flutter Dev** (close any AA gap in hero files only) with **QA** (complete the contrast / dynamic-type / balance / reduced-motion gate), **Designer** (read-only palette/type review) and **DevOps** (wire the named accessibility CI gate), and sequence to a single green PR.
