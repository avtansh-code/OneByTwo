You are the OneByTwo orchestrator agent. Sprint 3 — **Design Conversion (the "Direction A — Haldi" visual system, `design_handoff_one_by_two/`)** — is the active sprint. Six PRs have merged on `main`: **DC-01 (the design-token + type foundation) as PR #126** (squash `d9205d8`), **DC-02 (the six `OBT*` shared-widget reskins) as PR #127** (squash `aca8582`), **DC-03 (the eleven new Haldi shared components) as PR #129** (squash `b0ce9a6`), the **DC-03/06 scope doc as PR #130** (`bd27c38`), **DC-04 (the Auth & onboarding flow conversion) as PR #131** (squash `0391bf2`), and **DC-05 (the Home dashboard hero conversion) as PR #132** (squash `1c3e194`). The Haldi `ColorScheme` (marigold `primary`, **ink** `onPrimary`), the `OBTColors` `ThemeExtension` (incl. the **balance trio** `balancePositive` / `balanceNegative` / `balanceZero`, the **8-hue `category` map**, `OBTColors.heroShadow` / `rowShadow`, `OBTColors.link`, `OBTColors.metaText`), the `AppTheme` radius / soft-warm-shadow / motion tokens, the Bricolage Grotesque / Hanken Grotesk `TextTheme`, the `OBTText.amount` / `OBTText.amountHero` helpers, the six reskinned `OBT*` widgets, the **eleven DC-03 shared components**, the **converted Auth flow**, and the **converted Home dashboard** are all on `main`.

The goal of THIS session is to deliver **Sprint 3 PR #7 — DC-06: convert the Friends flow to Haldi (light + dark)** (issue **#118**). This is the **third per-flow screen-conversion PR**, the **largest of the flow conversions (8 SP)**, and the **first conversion that is BOTH a reskin AND a net-new screen build**. It wires the DC-01..DC-03 primitives into the **Friends surfaces** — **Haldi 9 ★ Friends list (reskin)**, **Haldi 10 Add-friend (reskin + the `match_and_invite_screen` REBUILD)**, **Haldi 11 Friend detail (reskin)** — and **builds the net-new Haldi 12 Friend-history screen**. Work **only** the Friends flow (+ the shared-`BalancePill`→`OBTBalancePill` seam below); Expenses (DC-07), Settlements (DC-08) and the rest are later.

**Unlike Home, Invariant 3 is LIVE on this flow.** The add-friend no-match path hands the invite to the **OS system share sheet only** (`lib/features/friends/data/share_service.dart` → `Share.share` from `share_plus`). A reskin that introduces a WhatsApp / SMS / single-messaging-app button or imports an app-specific share package is a **blocking defect** — refuse and cite Invariant 3 (SRS sections 3.4, 4.11, 12.2). **Invariants 1 + 2 are also LIVE** (friend-list balances, the friend-detail header + `OBTSettleUpCard`, and the net-new friend-history signed amounts): every amount MUST go through `formatInrFromPaise()` (Invariant 1 — no `paise / 100`, no `double`), and every balance figure is a **read-only** `simplifiedBalances` projection read (Invariant 2 — the client never writes it). The existing `friends_list_boundary_contract_test.dart` / friends PII-leak greps MUST stay green, and the new `friend_history_screen.dart` MUST be added to those greps.

**This is an IMPLEMENTATION session** (it changes `lib/**`). The full PR pipeline runs (analyse, format, unit + widget tests, Build Android, Build iOS, Integration Tests, Coverage Gate).

────────────────────────────────────────
READ BEFORE STARTING
────────────────────────────────────────

- `.github/copilot-instructions.md`, `.github/shared/invariants.md`, `.github/shared/srs-pointer.md` (SRS = `docs/OneByTwo_Requirements_Spec.md` v1.1), `.github/shared/coding-standards.md`, `.github/shared/handoffs.md`, `.github/shared/milestone-tracking.md`, `.github/shared/test-strategy.md`.
- **The authoritative spec for this PR:**
  - `docs/sprint-zero/sprint-3-plan.md` — **story DC-06** (the User Story, the **five** Acceptance Criteria — incl. AC-3 the Invariant-3 share-sheet negative case and AC-5 the net-new friend-history build — the Definition of Done, and the Invariant-Compliance clause) and the sprint-wide Definition of Ready / Definition of Done at the top.
  - `docs/audits/design-conversion/02-conversion-checklist.md` **§B → "Friends"** (the per-widget conversion table: rows 9/10/11/12 across `friends_list_screen.dart`, `friend_list_tile.dart`, `balance_pill.dart`, `friend_detail_screen.dart`, `friend_detail_header.dart`, `friend_detail_timeline.dart`, **`friend_history_screen.dart` (NEW — build)**, `friend_detail_states.dart`, `add_friend_screen.dart`, `add_friend_flow.dart` (conform), `match_and_invite_screen.dart` (rebuild), `manual_phone_entry_tab.dart`, `contact_list_tile.dart`, `empty_contacts_state.dart`, `permission_denied_view.dart`, `phone_selector_bottom_sheet.dart`) and **§C #2** (the balance-pill promotion to shared `OBTBalancePill`). Mark the §B Friends rows done on merge.
  - `docs/audits/design-conversion/04-qa-test-strategy.md` — **§A.5/§A.6** (each hero → light **and** dark goldens for **every** state: populated, **empty** illustration, **loading = shimmer skeleton**, error — for Friends list 9, Friend detail 11, **and the net-new Friend history 12**), **§B.1–§B.4** (contrast gate incl. the **balance-trio-on-surface** AA cases + the **white-on-marigold negative case**; labelling + 48 dp; the `OBTBalancePill` must carry **colour + icon + label**), **§C** (dynamic type to 2.0× + reduced motion — dense balance rows; **amounts never truncate, the row may grow** §C.2), **§D** (the per-screen "converted" acceptance gate).
  - `docs/audits/design-conversion/01-coverage-gap.md` — §B row 9 (Friends list) / row 11 (Friend detail) are "covered"; **Haldi 12 Friend history is "designed-not-yet-built" and flips to BUILT this PR** (PM reconcile).
  - `.github/shared/decision-log.md` → **ADR-0024** (scope: visual layer only).
  - `design_handoff_one_by_two/screens/Phase3c - Friends.dc.html` (the Haldi Friends list, add-friend branches, friend detail, **and friend history** — all states, light + dark) + the reference mockups under `docs/design/05-mockups/`, and `design_handoff_one_by_two/screens/Phase2 - Components.dc.html` (the balance pill, skeleton set, empty-state).
- **The foundation now on `main` (consume — read, do not change):** `lib/app/theme.dart`, `lib/core/theme/obt_colors.dart` (the **balance trio** + `category` + `heroShadow`/`rowShadow` + `link` + `metaText`), `lib/core/theme/obt_text.dart` (`OBTText.amount` / `.amountHero`), and the **DC-03 components** the Friends flow consumes:
  - **balance pill** — `lib/core/widgets/indicators/obt_balance_pill.dart` (`OBTBalancePill` — colour + **icon** + label; supports `positiveLabelOverride`, `compact`).
  - **skeleton set** — `lib/core/widgets/feedback/obt_skeleton.dart` (`OBTSkeleton`, `OBTSkeletonList`, `OBTSkeletonRow`, `OBTSkeletonCard`, `OBTSkeletonCircle`).
  - **empty-state** — `lib/core/widgets/feedback/obt_empty_state.dart` (`OBTEmptyState`, ink-on-marigold CTA).
  - **settle-up card** — `lib/features/friends/presentation/widgets/obt_settle_up_card.dart` (already the `OBT*` reskinned card from DC-02; keeps the single `simplifiedBalances` suggested-payment read).
  Reuse the **test harness** `test/support/widget_test_harness.dart` (`pumpThemed`, `expectAllInteractiveNodesLabelled`, `expectAllTapTargetsMeetMinSize`) and the **golden harness** `test/golden/golden_harness.dart`. See the DC-05 reskin test `test/features/home/home_dashboard_haldi_reskin_test.dart` for the §D-gate pattern (hero trio light+dark, contrast incl. the white-on-marigold negative case, 48 dp + labelling, dynamic type 2.0× at 390/320, reduced motion).
- **The surfaces to convert (read each before editing):** the sixteen files in §B Friends above. Read the providers/controllers (`application/friends_list_provider.dart`, `application/friend_detail_provider.dart`, `application/match_and_invite_controller.dart`, `application/contact_*`, `data/share_service.dart`, `data/matching_repository.dart`) **only** to confirm you are NOT changing behaviour, the projection read, the matching/invite flow, or telemetry — and to identify the **read-only** expenses+settlements source the net-new friend-history screen consumes.
- Do **not** read or re-plan the backend/data docs; they are unchanged.

────────────────────────────────────────
STANDING CAVEATS (read first)
────────────────────────────────────────

- **Invariants 1 + 2 + 3 are LIVE and load-bearing here.** Money only via `formatInrFromPaise()` (Invariant 1). Balances are **read-only** `simplifiedBalances` projection reads (Invariant 2 — never a write, never a raw who-owes-whom graph). Invites go to the **OS system share sheet only** (Invariant 3 — no app-specific button/package; the no-match path is AC-3's explicit negative case). Invariant 4 (single project) is N/A but must stay untouched. Any item that weakens one is a blocking defect — refuse and cite it.
- **THE SHARED-`BalancePill` SEAM (Architect must rule).** §C #2 says the friends-feature `balance_pill.dart` (`BalancePill`, colour + label only, `tertiaryContainer`/`errorContainer`) is **promoted to the shared `OBTBalancePill`** (colour + **icon** + label, balance-trio tokens). The canonical resolution mirrors DC-05's stub deletion: **delete `friends/presentation/widgets/balance_pill.dart` and re-point its inbound call sites** — `friend_list_tile.dart:70` and the large pill in `friend_detail_header.dart` — **to `OBTBalancePill`** (DC-05 already did this for the Home top-balance tile, passing `positiveLabelOverride: 'owes you'`). The Architect must rule this before the Flutter Dev starts: confirm no other inbound `BalancePill` reference survives (grep `lib/` + `test/`; update the friends/shell READMEs + any `BalancePill` boundary/widget test), and confirm the `friend_detail_header` large form maps to `OBTBalancePill` (non-compact). **NB the DC-05 width lesson:** `OBTBalancePill` (icon + 16 dp tabular amount) is ~187 dp wide — denser than the old `BalancePill`; the friend-list tile + detail header MUST stay clear of overflow at the 390/320 reference frames and at 2.0× (reflow / wrap; **amounts never truncate** — 04 §C.2).
- **THE FRIEND-HISTORY BUILD SEAM (Architect must rule).** `friend_history_screen.dart` does **not** exist — it is **built net-new** (Haldi 12; FR-FR-04). The Architect must rule its **read-only** data contract before the Flutter Dev starts: the screen consumes the **same friendship expenses + settlements** the Friend-detail timeline already reads (`friend_detail_provider.dart`), but the **full** reverse-chron list (not the top-5 preview), **month-grouped** (IST), with **signed** amounts (you owe = negative tint, owed = positive tint) via `formatInrFromPaise()`. It MUST be a pure read projection (Invariant 2 — never a write, never `simplifiedBalances` math) and integer paise throughout (Invariant 1). The Architect rules whether a new read-only "full friend history" provider/aggregator is needed or the existing detail provider is extended (read-only), the navigation seam (a "View full history" / "See all" affordance on Friend detail 11 supersedes the inline top-5 preview), and the four-state shape (populated / empty / shimmer-loading / error). Add `friend_history_screen.dart` (and any new provider/aggregator) to the friends boundary-contract + PII-leak greps.
- **`match_and_invite_screen.dart` is a REBUILD, `add_friend_flow.dart` CONFORMS.** The bare `Center(Text())` guard states (`RateLimited` / `SelfAddBlocked` / `DuplicateFriendship`) become Haldi-styled guard states; the looking-up spinner → shimmer skeleton; the match-found path → a confirm-add card (with mutual groups if available); the **no-match path → an `OBTEmptyState` invite that hands off to the OS share sheet only** (AC-3). `add_friend_flow.dart` is pure navigation glue — **conform, no visual change** — confirm it stays untouched.
- **Behaviour is UNCHANGED — reskin/rebuild is visual + the one net-new read-only screen.** No change to the friends providers/controllers, the `simplifiedBalances` read, the matching/invite/share flow, the contact-permission flow, or any domain model. Existing friends provider / boundary / PII-leak / screen / widget tests stay green unchanged (update only assertions that legitimately encode an old token literal, the removed `BalancePill`, or a hand-rolled skeleton's removed structure).
- **The balance trio is colour + icon + label — never colour alone (accessibility).** Every `OBTBalancePill` (friend-list rows, the friend-detail header, the friend-history rows) carries the directional **icon** (`arrow_upward` "owes you" / `arrow_downward` "you owe" / `check` "settled") **and** the text label alongside the trio colour. White on marigold ≈ 2.5:1 is the DC-01 contrast negative case — assert it FAILS; **the Settle Up / invite text affordances use `OBTColors.link` (the AA-tuned marigold link), not raw `primary`** (raw `primary` ≈ 2:1 on the warm surface), while **filled** affordances (FAB, `OBTEmptyState` CTA) resolve **ink** on marigold (`onPrimary`, never white).
- **Consume the DC-03 primitives — do not re-implement.** Hand-rolled loading skeletons → the shimmer `OBTSkeleton*` set; empty `Icon`s → `OBTEmptyState` + illustration; the inline pill → the shared `OBTBalancePill` (with icon); the settle-up card stays the DC-02 `OBTSettleUpCard`.
- **No hard-coded Haldi hex at call sites.** All colour via `Theme.of(context).colorScheme` / `OBTColors`; all type via `theme.textTheme` / `OBTText`; all radii via the `AppTheme` scale. `theme.dart` / `core/theme/*` are the single source of truth — consume, do not edit (any token gap routes to the Architect / issue #128).
- **Tooling + lessons carried from DC-01..DC-05:**
  - fvm-pinned (Flutter 3.44.3 / Dart 3.12.2). **The `fvm` wrapper is broken** ("Can't load Kernel binary … expected 127, found 130") — bypass it: `export PATH="/Users/avtanshgupta/fvm/versions/stable/bin:$PATH"` then bare `flutter analyze lib test`, `flutter test`, `dart format .`, and commit/push with bare `git` (the pinned SDK on PATH satisfies the lefthook `dart`/`flutter` pre-commit + pre-push hooks; plain `git` fails "command not found").
  - CI gates on `dart format --set-exit-if-changed .` — run `dart format .` before pushing. `very_good_analysis` is strict: **`directives_ordering`** (imports sorted; group dart: then package: alphabetically), **`prefer_int_literals`** (write `2`, not `2.0`, in a double slot), **`avoid_redundant_argument_values`** (don't pass a default), **`lines_longer_than_80_chars`**, **`prefer_const_constructors`**.
  - **`Closes #118` MUST be plain text, NOT backticked,** in the PR body, or GitHub will not register the closing reference and the Sprint 3 milestone will not advance.
  - **48 dp tap targets + non-vacuous a11y walks.** Any interactive control (friend rows, Settle Up, the invite CTA, the "View full history" affordance, the FAB, segmented Contacts/Manual) must meet the 48 dp minimum and carry a label; reuse `expectAllInteractiveNodesLabelled` + `expectAllTapTargetsMeetMinSize`. The built-in guidelines are defeated by `Semantics(button:true, excludeSemantics:true)` controls and a FAB's `tooltip` does not land on its node — put the label on the child `Icon` (`semanticLabel`).
  - **`OBTBalancePill` is ~187 dp wide.** Dense rows that pair it with an action (friend-list tile, friend-detail header) must reflow / wrap at the 390 + 320 frames and at 2.0× so nothing clips and **amounts never ellipsise** (04 §C.2) — see the DC-05 `top_balance_tile.dart` `LayoutBuilder` reflow pattern.
  - **`google_fonts` in tests.** Pump widget tests that build `AppTheme` inside `testWidgets` (the test zone absorbs the runtime font fetch). Animated surfaces (the shimmer skeleton) never settle — pump with `disableAnimations: true` or `settle: false`; reduced motion freezes the controller so `pumpAndSettle` completes.
  - **Goldens are authored on `ubuntu-latest` by the DC-13 (`#125`) `golden-a11y-checks` job**, not yet wired. Add the light + dark golden tests for the Friends list 9, Friend detail 11, **and the net-new Friend history 12** four states (populated, empty, shimmer-loading, error) to `test/golden/` (tagged `golden`), **group-`skip`ped** with the DC-13 rationale, reusing `golden_harness.dart`. Do **not** author/commit golden PNGs locally (macOS bytes are non-deterministic). The load-bearing proof is the per-screen widget tests, which run for real.
  - If `flutter analyze` surfaces unrelated `flutter_contacts` / `PermissionStatus` errors, the `.dart_tool/package_config.json` drifted — run `flutter pub get` with the **pinned** SDK; never bare `flutter pub get`.
  - **Merge needs an OWNER approval.** `main`'s ruleset requires an approving review from a write-access reviewer; the session is authenticated as the author `avtanshgupta` (a collaborator, cannot self-approve), and every prior DC PR was **approved by the `avtansh-code` OWNER account** before a squash-merge. A green pipeline shows `mergeStateStatus=BLOCKED` until that approval lands; `gh pr merge --admin` does **not** bypass the ruleset. Squash subject convention: `feat(<scope>): … (#<pr>)`.

────────────────────────────────────────
THE TASK — DC-06 (8 SP)
────────────────────────────────────────

1. **Issue + milestone + roadmap (PM).** DC-06 is issue **#118**, already under the **`Sprint 3`** milestone — confirm it; this PR carries `Closes #118` (plain). Per `.github/shared/milestone-tracking.md`, reconcile the Sprint 3 milestone, and on this PR flip the **DC-05** line in `docs/sprint-zero/next-three-prs.md` from "IN FLIGHT" to **"MERGED (#132, `1c3e194`)"** and set DC-06 in flight. **Mark the §B Friends rows done** in `02-conversion-checklist.md`; in `01-coverage-gap.md` flip **Haldi 12 Friend history from "designed-not-yet-built" to "covered (DC-06, built)"** and confirm rows 9/11 stay "covered". (No SRS gap to flag — Friends is FR-FR-01..04, already specified.)

2. **Architect — per-surface contracts + the two seams + the Invariant-1/2/3 contract (review, read-only).** Confirm, for each Friends surface, the token/type mapping (`ColorScheme` slot / `OBTColors` key — incl. the balance trio, `link`, `metaText`, `heroShadow`/`rowShadow` / `AppTheme` radius / `TextTheme` slot / `OBTText.amount` / `.amountHero`) and the four-state shape. **Rule the shared-`BalancePill` seam** (delete `balance_pill.dart`; re-point `friend_list_tile` + `friend_detail_header` to `OBTBalancePill`; confirm no other inbound reference; mind the ~187 dp width / reflow). **Rule the friend-history build seam** (read-only expenses+settlements source, month-grouping, signed amounts, the Friend-detail navigation seam superseding the top-5 preview, the four states). **Confirm the Invariant contract**: amounts only via `formatInrFromPaise()` (1), balances are projection **reads** (2), the no-match invite hands off to the **OS share sheet only** (3). Return the contracts for the Flutter Dev.

3. **Flutter Dev — reskin the Friends surfaces, promote the pill, rebuild add-friend guards, and BUILD friend history:**
   - **Friends list (`friends_list_screen.dart`, `friend_list_tile.dart`)** — marigold tokens + Bricolage/Hanken; the additive **owed/owe summary band**; the hand-rolled skeleton → the shimmer `OBTSkeleton*` set; the empty `Icon` → `OBTEmptyState`; rows adopt the shared **`OBTBalancePill`** (with icon); keep the providers, search, share entry, and telemetry unchanged.
   - **Balance pill (`balance_pill.dart`)** — **delete** and re-point all call sites to `OBTBalancePill` per the Architect's seam ruling.
   - **Friend detail (`friend_detail_screen.dart`, `friend_detail_header.dart`, `friend_detail_timeline.dart`, `friend_detail_states.dart`)** — marigold tokens + type; the large balance pill → `OBTBalancePill`; the `OBTSettleUpCard` reskin; FAB → marigold (ink on marigold); the category icons → the Haldi hue bed; amounts → Bricolage tabular; skeleton → shimmer; empty → `OBTEmptyState`; IST dates; add the **"View full history"** affordance that supersedes the inline top-5 preview and opens Haldi 12.
   - **Friend history (`friend_history_screen.dart`, NEW)** — build the dedicated Haldi screen: reverse-chron, **month-grouped** (IST) list of expenses **and** settlements with **signed amounts** via `formatInrFromPaise()`; shimmer-loading + `OBTEmptyState` + error states; pure read-only projection (Invariants 1 + 2).
   - **Add friend (`add_friend_screen.dart`, `manual_phone_entry_tab.dart`, `contact_list_tile.dart`, `empty_contacts_state.dart`, `permission_denied_view.dart`, `phone_selector_bottom_sheet.dart`)** — marigold tokens + type; segmented Contacts/Manual reskin; search-field radius; the contacts `CircularProgressIndicator` → shimmer skeleton; empties → `OBTEmptyState`; sheet top radius → 28; locked `+91` preserved.
   - **Match-and-invite (`match_and_invite_screen.dart`, REBUILD)** — Haldi-styled `RateLimited`/`SelfAddBlocked`/`DuplicateFriendship` guard states; looking-up spinner → skeleton; match-found → confirm-add card; **no-match → `OBTEmptyState` invite handing off to the OS share sheet only** (AC-3). `add_friend_flow.dart` **conforms** (untouched).

4. **QA + Designer — the per-screen "converted" gate (§D):**
   - **All states present** for Friends list 9, Friend detail 11, **and Friend history 12** — loading = **shimmer skeleton**, **empty** = `OBTEmptyState` + illustration, populated, error.
   - **Light + dark** goldens for **every** state of the three heroes (§A.6), queued in `test/golden/`, **skipped** pending the DC-13 ubuntu path.
   - **Invariant 1 + 2 proof** — the friends boundary/PII greps stay green and are **extended to `friend_history_screen.dart`** (+ any new history provider); a test asserts every on-screen amount routes through `formatInrFromPaise()` (no inline `/100`) and that balances are projection **reads** (no write path introduced).
   - **Invariant 3 proof (AC-3)** — a test asserts the no-match invite path delegates to the share service / system share sheet and that **no** single-app (WhatsApp/SMS/etc.) button or package import appears (mirror the existing `share_boundary_contract_test.dart` grep over `lib/`).
   - **Contrast gate** (§B.1–§B.2) incl. the **balance-trio-on-surface** AA cases + the **white-on-marigold negative case** asserted to FAIL.
   - **Balance trio = colour + icon + label** — assert the directional icon + label on every `OBTBalancePill` (friend rows, detail header, history rows).
   - **Every control labelled + 48 dp** — `expectAllInteractiveNodesLabelled` + `expectAllTapTargetsMeetMinSize` on the populated states (friend rows, Settle Up, the invite CTA, "View full history", the FAB, segmented Contacts/Manual).
   - **Dynamic type 2.0×** (§C) — no clip/overflow at 390 and 320; **amounts never truncate** (the dense balance rows may grow / reflow); reduced motion freezes the shimmer.
   - **Designer (read-only)** reviews the Friends surfaces against `Phase3c - Friends.dc.html` + the mockups and returns findings for the orchestrator to action.

5. **Verify:** `flutter analyze lib test` (no issues), `dart format .` (clean), `flutter test` (all pass; the existing friends provider / boundary / PII-leak / screen / widget suites stay green; per-feature coverage ≥ 70% and overall ≥ 50%). No **existing** provider, controller, matching/invite/share, `simplifiedBalances` read, Firestore read/write, Cloud Function, or domain-model **behaviour** is changed; the only structural changes beyond the reskin are the `balance_pill.dart` delete (→ `OBTBalancePill`) and the net-new read-only `friend_history_screen.dart` build.

────────────────────────────────────────
BRANCH / PR
────────────────────────────────────────

- Branch: `feat/haldi-friends-conversion` (type-prefixed kebab, repo convention).
- Conventional Commits, single-token scope, ASCII subject ≤ 72 chars — e.g. `feat(friends): convert the friends flow to Haldi`.
- This is a **code** PR — the full PR pipeline must be green (Flutter Lint & Test, Build Android, Build iOS, Integration Tests, Coverage Gate) before merge.
- PR body: map each surface to §B + the DC-06 ACs; tick the invariants checklist (1 + 2 **live** — money via `formatInrFromPaise()`, `simplifiedBalances` read-only; **3 live** — invites via the OS share sheet only; 4 unchanged); note "`BalancePill` deleted → shared `OBTBalancePill` (colour + icon + label); friend list gains the owed/owe band; `match_and_invite` guard states rebuilt; **friend history 12 built net-new** (month-grouped, signed amounts, read-only); skeletons → shimmer; empties → `OBTEmptyState`"; **`Closes #118` (plain, NOT backticked)**.
- Include the `Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>` trailer.
- Commit `docs/copilot_prompts/sprint_3/7.md` (this prompt) with the PR, per the repo convention.
- **Merge:** after the pipeline is green, the PR will read `BLOCKED` until the **`avtansh-code` OWNER** approves; then squash-merge (`--delete-branch`) with subject `feat(friends): convert the friends flow to Haldi (#<pr>)`.

────────────────────────────────────────
DEFINITION OF DONE (this PR)
────────────────────────────────────────

- All **five** DC-06 acceptance criteria pass: the **friends list** gains the owed/owe band + balance-pill rows (colour + icon + label) + shimmer + empty-state, light **and** dark (AC-1); **add-friend** `match_and_invite` is rebuilt with Haldi guard states + skeleton + empty-state invite (AC-2); the **no-match invite hands off to the OS system share sheet only** — no single-app button (AC-3 / Invariant 3); **friend detail** header + `OBTSettleUpCard` use the balance trio, amounts via `formatInrFromPaise()`, balances projection-**read** only (AC-4 / Invariants 1 + 2); **friend history (12) is built fresh** — reverse-chron, month-grouped, expenses + settlements, signed amounts, four states, in Haldi (AC-5).
- The §B **Friends** rows in `02-conversion-checklist.md` are marked done; `01-coverage-gap.md` flips **Haldi 12** to built and keeps rows 9/11 "covered".
- `friends/presentation/widgets/balance_pill.dart` is **deleted** and every call site re-points to `OBTBalancePill`; **no** inbound `BalancePill` reference survives.
- All Friends heroes pass the §D per-screen gate (four states, light/dark goldens, contrast incl. the negative case, balance-trio icon+label, labelling + 48 dp, dynamic-type 2.0× with no amount truncation, the Invariant-3 share negative case), with light + dark goldens queued in the DC-13 harness; the friends boundary/PII greps are extended to `friend_history_screen.dart`.
- No hard-coded Haldi hex at call sites; `theme.dart` / `core/theme/*` unchanged; no invariant weakened; no backend/provider/controller/matching/share/telemetry/CF/domain change; the `simplifiedBalances` read stays read-only.
- Analyse clean, `dart format` clean, all tests pass (existing friends suites green unchanged), per-feature coverage ≥ 70% and overall ≥ 50%.
- `docs/sprint-zero/next-three-prs.md` is reconciled (DC-05 → MERGED `#132` / `1c3e194`; DC-06 status).
- DC-06 issue (#118) closed under the `Sprint 3` milestone; the milestone is reconciled per `.github/shared/milestone-tracking.md`.

End the session summary with: "Next: Sprint 3 PR #8 — DC-07 (Expenses flow conversion: Haldi 21 ★ add-expense 3-step sheet, 22 expense detail, and the context picker 8)."

────────────────────────────────────────
FAILURE / REFUSAL PROTOCOL
────────────────────────────────────────

- If any change would weaken an invariant or the SRS, refuse it, quote the specific invariant / SRS section, and propose a compliant alternative. **In particular:** no inline `paise / 100` or `double` for any on-screen amount (Invariant 1 — use `formatInrFromPaise()`); no write to `simplifiedBalances` and no raw who-owes-whom graph (Invariant 2 — projection reads only); **no WhatsApp/SMS/single-messaging-app button, deep link, or package for the invite path (Invariant 3 — the OS system share sheet is the only target)**.
- Refuse scope creep: no change to the friends providers / controllers / matching / invite / share / contact-permission flow or telemetry; do **not** convert any other flow (DC-07+); do **not** build any not-yet-scoped screen (Search 7 / Remove-friend 13 are Sprint 6; the real Groups list 14 is Sprint 4) — the only net-new build here is **Friend history 12**; do not unlock any India-only constraint; no second Firebase project.
- Do not edit `docs/OneByTwo_Requirements_Spec.md` or `lib/app/theme.dart` / `lib/core/theme/*` (consume the DC-01 foundation; any token gap routes to the Architect / issue #128).

Begin by delegating to the **PM** (confirm #118 + the milestone, reconcile the roadmap so DC-05 reads MERGED, the §B Friends reconciliation, and the Haldi-12 coverage flip), then the **Architect** (per-surface token mapping + the shared-`BalancePill` seam ruling + the friend-history read-only build seam + the Invariant-1/2/3 contract + the balance-trio colour+icon+label rule), then the **Flutter Dev** (reskin the Friends surfaces + delete `BalancePill`→`OBTBalancePill` + rebuild `match_and_invite` + build `friend_history_screen`) with **QA** + **Designer** (the §D per-screen gate incl. the four states + light/dark hero goldens + the contrast negative case + the balance-trio icon + the `formatInrFromPaise()` boundary + the Invariant-3 share negative case + dynamic-type + visual review), and sequence to a single green PR.
