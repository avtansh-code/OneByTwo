You are the OneByTwo orchestrator agent. Sprint 3 — **Design Conversion (the "Direction A — Haldi" visual system, `design_handoff_one_by_two/`)** — is the active sprint. Ten PRs have merged on `main`: **DC-01 (design-token + type foundation) as PR #126** (squash `d9205d8`), **DC-02 (the six `OBT*` shared-widget reskins) as PR #127** (squash `aca8582`), **DC-03 (the eleven new Haldi shared components) as PR #129** (squash `b0ce9a6`), the **DC-03/06 scope doc as PR #130** (`bd27c38`), **DC-04 (Auth & onboarding) as PR #131** (squash `0391bf2`), **DC-05 (Home dashboard hero) as PR #132** (squash `1c3e194`), **DC-06 (Friends flow + net-new Friend-history) as PR #133** (squash `006a0fb`), **DC-07 (Expenses flow) as PR #134** (squash `262107d`), **DC-08 (Settlements flow) as PR #135** (squash `b55e9fb`), and **DC-09 (Activity & Notifications surfaces) as PR #136** (squash `1ce84cc`). The Haldi `ColorScheme` (marigold `primary`, **ink** `onPrimary`), the `OBTColors` `ThemeExtension` (the **balance trio** `balancePositive` / `balanceNegative` / `balanceZero`, the **8-hue `category` map**, `OBTColors.heroShadow` / `rowShadow`, `OBTColors.link`, `OBTColors.warning`, `OBTColors.metaText`), the `AppTheme` radius scale (`radiusChipInput` 12 / `radiusButton` 16 / `radiusCard` 20 / `radiusPill` 18 / `radiusSheet` 28 / `radiusFull` 999, plus the **legacy** `radiusLarge` 16 / `radiusXL` 24 / `radiusMedium` 12 retained only for unconverted screens) / soft-warm-shadow / motion tokens, the Bricolage Grotesque / Hanken Grotesk `TextTheme`, the `OBTText.amount` / `OBTText.amountHero` helpers, the **single core IST date formatter** `lib/core/formatters/ist_date_formatter.dart`, the six reskinned `OBT*` widgets (incl. `OBTConfirmationDialog`, the reskinned `OtpInput`/phone input), the **eleven DC-03 shared components** (incl. the `OBTSkeleton*` set, `OBTEmptyState`), and the **converted Auth / Home / Friends / Expenses / Settlements / Activity / Notifications flows** are all on `main`.

The goal of THIS session is to deliver **Sprint 3 PR #11 — DC-10: convert the Profile & Settings cluster to Haldi (light + dark)** (issue **#122**). This is the **seventh and FINAL per-flow screen-conversion PR (5 SP, P1 — the descope-eligible buffer)** and closes Epic C. It wires the DC-01..DC-03 primitives into the **Profile cluster**: profile (Haldi 27), edit-profile (27), the **Phase3g Change Phone** flow, notification preferences (28), contact-support (29), delete-account (30), and the photo-picker sheet. Work **only** the Profile cluster; the Epic D dark-mode-parity pass (DC-11), the motion/a11y pass (DC-12), and the DC-13 golden/a11y CI job are later.

**There are two load-bearing items.** (1) **AC-2 is the change-phone re-auth negative case:** the `change_phone_screen.dart` two-OTP re-auth flow (re-auth intro → re-auth OTP → new-phone entry → new-phone OTP → success, plus the "sync pending → Try again" recovery) is **behaviour-frozen** — weakening or bypassing the re-auth, or rendering an **unmasked** phone number, is a blocking defect; this PR reskins it to the Haldi **Change Phone** design (Phase3g) only. (2) **No hard-coded Haldi hex may remain in the converted profile presentation:** `profile_screen.dart` currently hard-codes the shimmer greys `Color(0xFFE0E0E0)` / `Color(0xFFF5F5F5)` / `Color(0xFFE0E0E0)` (lines ~660–662 in the hand-rolled `_ShimmerEffect`); these MUST be re-pointed to the shared `OBTSkeleton` set (or Haldi tokens), and a grep contract must assert **no `Color(0x…)` literal** survives in the converted profile presentation (the negative case). All colour flows from `Theme.of(context).colorScheme` / `OBTColors`; all type from `theme.textTheme` / `OBTText`; all radii from the `AppTheme` scale — **including migrating every direct `AppTheme.radiusLarge` / `radiusXL` / `radiusMedium` reference** in the cluster to the new scale (`radiusCard` 20 / `radiusButton` 16 / `radiusChipInput` 12 / `radiusSheet` 28). `theme.dart` / `core/theme/*` / the DC-02/03 components are the single source of truth — consume, do not edit (any token/component gap routes to the Architect / issue #128).

**Invariants 1 + 2 are absent/light here; 3 + 4 are present.** The Profile cluster has no on-screen money path (Invariant 1 N/A) and reads/writes no `simplifiedBalances` projection (Invariant 2 — the boundary greps stay green). **Invariant 3 is LIVE:** contact-support must continue to use the device **system mail client** (`mailto:`) with the copy/manual fallback — never a single-app target (the `contact_support_boundary_contract_test.dart` grep enforces). **Invariant 4 is LIVE:** delete-account anonymisation is server-driven (the `delete-user-account` callable) on the single production project and is unchanged. No India-only constraint (`+91` lock) may be unlocked.

**This is an IMPLEMENTATION session** (it changes `lib/**`). The full PR pipeline runs (analyse, format, unit + widget tests, Build Android, Build iOS, Integration Tests, Coverage Gate).

────────────────────────────────────────
READ BEFORE STARTING
────────────────────────────────────────

- `.github/copilot-instructions.md`, `.github/shared/invariants.md`, `.github/shared/srs-pointer.md` (SRS = `docs/OneByTwo_Requirements_Spec.md` v1.1), `.github/shared/coding-standards.md`, `.github/shared/handoffs.md`, `.github/shared/milestone-tracking.md`, `.github/shared/test-strategy.md`.
- **The authoritative spec for this PR:**
  - `docs/sprint-zero/sprint-3-plan.md` — **story DC-10** (the User Story, the **three** Acceptance Criteria — AC-1 the five-screen reskin via marigold tokens + Bricolage/Hanken, the inline sign-out `AlertDialog` → `OBTConfirmationDialog`, the inert "Language" slot, the `radiusLarge`/`radiusXL` → new-scale migration, `+91` locked in edit; AC-2 the **Phase3g Change Phone** five-state conversion with masked numbers, the `warning`-token sync-pending recovery, `+91` locked, and the **re-auth behaviour-frozen negative case**; AC-3 the `profile_placeholder_screen.dart` **deletion**), the Definition of Done, and the Invariant-Compliance clause.
  - `docs/audits/design-conversion/02-conversion-checklist.md` **§B → "Profile"** (the seven rows: `profile_screen.dart` 27 reskin — sign-out `AlertDialog` → `OBTConfirmationDialog`, avatar fallback → `primaryContainer` ink, the `_ShimmerEffect` already present but carrying the hard-coded greys to re-point; `edit_profile_screen.dart` 27 reskin — camera badge → **onPrimary ink, not white**, field radius → 12, migrate `radiusLarge`/`radiusXL`; `change_phone_screen.dart` **Phase3g** reskin — the five states + sync-pending recovery uses the **warning** token not danger; `notification_preferences_screen.dart` 28 reskin — toggles → marigold, OS-permission banner → warm tonal, **add the inert "Language" slot** ("Coming soon"), spinner → skeleton/simple; `delete_account_screen.dart` 30 reskin — destructive → error token, re-auth `OtpInput` reskin, success-state Haldi, migrate `AppTheme.radius*`; `contact_support_fallback_dialog.dart` 29 reskin — dialog radius → 20, selectable email → **link** token, buttons → Hanken/marigold; `profile_placeholder_screen.dart` **delete**; `widgets/photo_picker_sheet.dart` 5/27 reskin — sheet top radius → 28, rows → Hanken, destructive "Remove" → error token). Mark the §B Profile rows done on merge (add a `> **Status — Profile cluster converted (DC-10 …)**` blockquote mirroring the DC-09 one).
  - `docs/audits/design-conversion/04-qa-test-strategy.md` — the per-screen "converted" acceptance gate. **Profile 27/28/29/30 are a NON-HERO cluster** → **light** goldens for each converted surface and state (not the both-brightness hero treatment); the contrast gate incl. the **white-on-marigold negative case**, labelling + 48 dp, dynamic type to 2.0× + reduced motion. The change-phone five states + the delete-account states each need a light golden.
  - `docs/audits/design-conversion/01-coverage-gap.md` — §B rows **27 (Profile view + edit)**, **28 (Notification preferences)**, **29 (Contact support)**, **30 (Delete account)** are **"covered"**; confirm they stay "covered" and note the reskin.
  - `.github/shared/decision-log.md` → **ADR-0024** (scope: visual layer only).
  - `design_handoff_one_by_two/screens/Phase3f - Profile.dc.html` (profile / edit-profile / preferences / delete / support visual reference) and **`Phase3g - Change Phone.dc.html`** (the change-phone five-state design — the authoritative reference for AC-2).
- **The foundation now on `main` (consume — read, do not change):** `lib/app/theme.dart`, `lib/core/theme/obt_colors.dart`, `lib/core/theme/obt_text.dart`, and the components the cluster consumes:
  - **confirmation dialog** — `lib/core/widgets/dialogs/obt_confirmation_dialog.dart` (`OBTConfirmationDialog.show(context, {required title, required body, required confirmLabel, cancelLabel = 'Cancel', isDestructive = false}) → Future<bool>`; radius `radiusCard`, `headlineMedium` title, error tint when `isDestructive`). Replace the hand-rolled sign-out `AlertDialog` (profile_screen.dart ~line 462) with `OBTConfirmationDialog.show`.
  - **skeleton set** — `lib/core/widgets/feedback/obt_skeleton.dart` (`OBTSkeleton` / `OBTSkeletonCircle` / `OBTSkeletonRow` / `OBTSkeletonList`); **empty-state** — `lib/core/widgets/feedback/obt_empty_state.dart`. Use these to replace the hand-rolled `_ShimmerEffect` (killing its hard-coded greys) and the bare `CircularProgressIndicator` page loaders (keep inline button spinners).
  - the **reskinned `OtpInput`** + phone input (DC-02/DC-04) for the change-phone + delete-account re-auth OTP, and the `+91`-locked phone field.
  Reuse the **test harness** `test/support/widget_test_harness.dart` (`pumpThemed`, `expectAllInteractiveNodesLabelled`, `expectAllTapTargetsMeetMinSize`) and the **golden harness** `test/golden/golden_harness.dart`. See the DC-09 reskin gates `test/features/activity/activity_haldi_reskin_test.dart` + `test/features/notifications/notifications_haldi_reskin_test.dart`, the no-hex grep in `test/features/notifications/notifications_boundary_contract_test.dart`, the golden scaffold `test/golden/dc09_activity_golden_test.dart`, and the shared fakes under `test/features/{activity,notifications}/helpers/` for the §D-gate pattern (states, contrast incl. the white-on-marigold negative case, 48 dp + labelling, dynamic type 2.0× at 390/320, reduced motion, group-`skip`ped goldens, shared no-op fakes rather than per-file redefinitions).
- **The surfaces to convert (read each before editing):** `lib/features/profile/presentation/profile_screen.dart` (27 — sign-out `AlertDialog`, the `_ShimmerEffect` greys), `lib/features/profile/presentation/edit_profile_screen.dart` (27 — `radiusLarge`×4 + `radiusXL`×1, camera badge), `lib/features/profile/presentation/change_phone_screen.dart` (Phase3g — `radiusLarge`×2, two spinners, five states), `lib/features/profile/presentation/notification_preferences_screen.dart` (28 — the page spinner, the OS-permission banner, the new "Language" slot), `lib/features/profile/presentation/delete_account_screen.dart` (30 — `radiusLarge`×5 + `radiusMedium`×2, three spinners, the re-auth OTP), `lib/features/profile/presentation/contact_support_fallback_dialog.dart` (29 — `AlertDialog`), `lib/features/profile/presentation/widgets/photo_picker_sheet.dart` (5/27), and **delete** `lib/features/profile/presentation/profile_placeholder_screen.dart`. Read the controllers/domain (`change_phone_controller.dart`, `delete_account_controller.dart`, `edit_profile_controller.dart`, `notification_preferences_controller.dart`, `contact_support_controller.dart`, plus the `*_telemetry.dart`) **only** to confirm you are NOT changing the re-auth flow, the delete-account callable, the permission flow, the `mailto:` fallback, or any telemetry.
- Do **not** read or re-plan the backend/data docs; the `delete-user-account` callable, the FCM token cleanup, and the support diagnostics are unchanged.

────────────────────────────────────────
STANDING CAVEATS (read first)
────────────────────────────────────────

- **AC-2 (change-phone re-auth) is the load-bearing negative case.** The two-OTP re-auth (re-auth the current number, then verify the new number) MUST keep its exact state machine, masking, and `+91` lock; this PR only restyles it to Phase3g. The sync-pending → "Try again" recovery uses `OBTColors.warning` (not the danger/error token). Showing an unmasked number, or any path that skips a re-auth OTP, is a blocking defect — refuse and cite AC-2 / FR-PR-02.
- **No hard-coded Haldi hex may remain in the converted profile presentation.** Re-point the `profile_screen.dart` `_ShimmerEffect` greys (`Color(0xFFE0E0E0)` / `Color(0xFFF5F5F5)`) to the shared `OBTSkeleton` set or Haldi tokens, and add a grep contract (extend a profile boundary-contract test, e.g. `profile_stats_boundary_contract_test.dart`, or add a dedicated one) asserting **no `Color(0x…)` literal** survives in the converted profile presentation files. A literal hex left at a call site is a blocking defect — refuse and cite the no-hex rule.
- **Migrate the legacy radius references.** Every direct `AppTheme.radiusLarge` / `radiusXL` / `radiusMedium` in the cluster (edit-profile `radiusLarge`×4 + `radiusXL`×1, delete-account `radiusLarge`×5 + `radiusMedium`×2, change-phone `radiusLarge`×2 — grep `lib/features/profile` to confirm the live set) re-points to the new scale: cards/tiles/dialogs → `radiusCard` (20), buttons → `radiusButton` (16), fields/chips → `radiusChipInput` (12), bottom sheets → `radiusSheet` (28). Do **not** edit the legacy constants in `theme.dart` (they remain for any not-yet-converted screen); change only the call sites in this cluster.
- **`profile_placeholder_screen.dart` is DELETED, not converted (AC-3).** It is superseded by the real Haldi 27 `profile_screen.dart`. Remove it and any inbound reference; mirror the DC-05 `groups_list_placeholder.dart` / DC-06 `balance_pill.dart` / DC-08 `settle_up_header.dart` deletions. If a live reference remains, re-point it to `profile_screen.dart`, do not keep the placeholder.
- **The sign-out dialog adopts `OBTConfirmationDialog`.** Replace the hand-rolled `AlertDialog` in `profile_screen.dart` with `OBTConfirmationDialog.show(...)` (sign-out is not destructive → `isDestructive: false`; delete-account confirmations are destructive → `isDestructive: true`). Keep the existing sign-out behaviour + the `sign_out_test.dart` assertions green (update only assertions that legitimately encode the old `AlertDialog` structure).
- **The "Language" slot ships INERT.** Add a disabled "Language" row to `notification_preferences_screen.dart` ("Coming soon"), labelled and announced disabled — no language switching is built (that is out of scope, like every "Coming soon" slot in DC-03..DC-09).
- **Page spinners become skeletons; inline button spinners stay.** Replace bare full-page `CircularProgressIndicator` loaders (notification-preferences page load; profile/edit-profile page load) with the shared `OBTSkeleton` set / a simple Haldi treatment. **Keep** the small in-button progress spinners (the Save / Verify / Delete button busy states) — those are inline affordances, not page loaders.
- **Behaviour is UNCHANGED — this is a reskin (+ the placeholder deletion + the `AlertDialog` → `OBTConfirmationDialog` swap + the radius migration + the hex re-point).** No change to the change-phone re-auth state machine, the delete-account callable/anonymisation, the edit-profile save, the notification-preferences permission flow, the contact-support `mailto:` + copy fallback, or any telemetry. Existing profile suites stay green unchanged (update only assertions that legitimately encode an old token literal, a removed spinner, the removed placeholder, a migrated radius, or a re-pointed hex).
- **All colour via `Theme.of(context).colorScheme` / `OBTColors`; all type via `theme.textTheme` / `OBTText`; all radii via the `AppTheme` scale.** No hard-coded Haldi hex at call sites. Any token gap routes to the Architect / issue #128.
- **No new telemetry event is silently dropped.** If `telemetry-plan.md` defines an event for any interaction touched by this PR (profile-stats view, edit-save, change-phone steps, preference toggles, contact-support tap, delete-account steps), confirm it is still emitted; a converted control that drops a planned event is a contract gap (DC-06 lesson).
- **Tooling + lessons carried from DC-01..DC-09:**
  - fvm-pinned (Flutter 3.44.3 / Dart 3.12.2). **The `fvm` wrapper may be broken** ("Can't load Kernel binary … expected 127, found 130") — bypass it: `export PATH="/Users/avtanshgupta/fvm/versions/stable/bin:$PATH"` then bare `flutter analyze lib test`, `flutter test`, `dart format .`, and commit/push with bare `git` (the pinned SDK on PATH satisfies the lefthook `dart`/`flutter` + `coverage-check` pre-commit/pre-push hooks; plain `git` otherwise fails "command not found").
  - CI gates on `dart format --set-exit-if-changed .` — run `dart format .` before pushing. `very_good_analysis` is strict: **`directives_ordering`**, **`prefer_int_literals`**, **`avoid_redundant_argument_values`**, **`lines_longer_than_80_chars`** (incl. `testWidgets` descriptions), **`prefer_const_constructors`**, **`unnecessary_import`**, **`no_adjacent_strings_in_list`** (split a long string into a top-level/local const, never two adjacent literals inside a list), **`comment_references`**, **`only_throw_errors`**.
  - **Never build `AppTheme.light` / `.dark` (or call GoogleFonts) at test COLLECTION time** — e.g. as a `final` at the top of a `for`-loop in `main()`. `AppTheme._buildTextTheme` fetches Bricolage/Hanken via GoogleFonts → the binding is not yet initialised at collection time and the whole file fails to load ("ServicesBinding … not initialized" / "did not complete [E]"). Only reference `AppTheme.*` **inside** `test`/`testWidgets` bodies; in a full-file run the earlier `testWidgets` populate the GoogleFonts cache so a plain `test()` contrast group can build the theme. (Learned in DC-09.)
  - **Animated surfaces never settle.** Skeleton shimmers + the change-phone OTP cursor + any slide never settle — pump with `disableAnimations: true` (reduced motion freezes the controller so `pumpAndSettle` completes) or `settle: false` / a single frame. Build `AppTheme` inside `testWidgets` so the `google_fonts` runtime fetch is absorbed by the test zone.
  - **Reuse shared test fakes.** Add / extend a `test/features/profile/helpers/` shared no-op fakes file rather than redefining `Fake…`/`Noop…` per file (the DC-07/DC-08/DC-09 review lesson).
  - **`Closes #122` MUST be plain text, NOT backticked,** in the PR body, or GitHub will not register the closing reference and the Sprint 3 milestone will not advance.
  - **Goldens are authored on `ubuntu-latest` by the DC-13 (`#125`) `golden-a11y-checks` job**, not yet wired. Add the **light** golden scaffolds for the converted profile surfaces (profile, edit-profile, the change-phone five states, notification preferences incl. the "Language" slot, delete-account states, contact-support dialog) to `test/golden/dc10_profile_golden_test.dart` (tagged `golden`), **group-`skip`ped** with the DC-13 rationale, reusing `golden_harness.dart`. Do **not** author/commit golden PNGs locally (macOS bytes are non-deterministic). The load-bearing proof is the per-screen widget tests + the new no-hex grep, which run for real.
  - If `flutter analyze` surfaces unrelated `flutter_contacts` / `PermissionStatus` / package errors, the `.dart_tool/package_config.json` drifted — run `flutter pub get` with the **pinned** SDK; never bare `flutter pub get`.
  - **Merge needs an OWNER approval.** `main`'s ruleset requires an approving review from a write-access reviewer; the session is authenticated as the author `avtanshgupta` (a collaborator, cannot self-approve), and every prior DC PR was **approved by the `avtansh-code` OWNER account** before a squash-merge. A green pipeline shows `mergeStateStatus=BLOCKED` until that approval lands; `gh pr merge --admin` does **not** bypass the ruleset. **iOS (CocoaPods) is the slowest check (~15–20 min)** — wait it out, and note `gh pr checks` can show a **stale "pending"** for iOS after the job has actually succeeded (cross-check with `gh run view <id> --json jobs`). Squash subject convention: `feat(<scope>): … (#<pr>)` (single-token scope).
  - **Handle review comments.** After CI is green, check the PR for inline review comments, address them with focused commits, reply on each thread, and resolve the threads before merge.

────────────────────────────────────────
THE TASK — DC-10 (5 SP)
────────────────────────────────────────

1. **Issue + milestone + roadmap (PM).** DC-10 is issue **#122**, already under the **`Sprint 3`** milestone — confirm it; this PR carries `Closes #122` (plain). Per `.github/shared/milestone-tracking.md`, reconcile the Sprint 3 milestone, and on this PR flip the **DC-09** line in `docs/sprint-zero/next-three-prs.md` from "IN FLIGHT" to **"MERGED (#136, `1ce84cc`)"** and set DC-10 in flight. **Mark the §B Profile rows done** in `02-conversion-checklist.md` (add a `> **Status — Profile cluster converted (DC-10 …)**` blockquote); in `01-coverage-gap.md` confirm rows **27 / 28 / 29 / 30 stay "covered"** and note the reskin. (No SRS gap to flag — the cluster is FR-PR-01..05 / FR-SH-03/04 / FR-AU-09, already specified.)

2. **Architect — the token/radius mapping + the no-hex contract + the re-auth/Invariant ruling (review, read-only).** Confirm, for each surface, the token/type mapping (`ColorScheme` slot / `OBTColors` key / `AppTheme` radius / `TextTheme` slot) and the loading/empty/error/success shapes. **Rule the radius migration** (`radiusLarge`/`radiusXL`/`radiusMedium` → `radiusCard`/`radiusButton`/`radiusChipInput`/`radiusSheet` per call-site role). **Rule the hex re-point** of the `_ShimmerEffect` greys to the shared `OBTSkeleton` set. **Rule the re-auth freeze:** the change-phone two-OTP state machine + masking + `+91` lock are behaviour-frozen (AC-2). **Confirm the Invariant contract:** no money path (1 N/A); no `simplifiedBalances` (2); contact-support stays `mailto:` + copy fallback (3 LIVE); delete-account server-driven on the single project (4 LIVE). Return the contracts for the Flutter Dev.

3. **Flutter Dev — reskin the Profile cluster:**
   - **profile (`profile_screen.dart`, 27)** — marigold tokens + type; avatar fallback → `primaryContainer` ink; rows → Hanken; **sign-out `AlertDialog` → `OBTConfirmationDialog.show`**; the hand-rolled `_ShimmerEffect` → the shared `OBTSkeleton` set (re-point the hard-coded greys); delete → error token.
   - **edit-profile (`edit_profile_screen.dart`, 27)** — marigold tokens + type; camera badge → **onPrimary ink**; field radius → `radiusChipInput`; Save → marigold; migrate `radiusLarge`/`radiusXL`; `+91` locked.
   - **change-phone (`change_phone_screen.dart`, Phase3g)** — restyle the five states + the sync-pending → "Try again" recovery (`OBTColors.warning`); reuse the reskinned `OtpInput` + phone field; **masked numbers**; `+91` locked; migrate `radiusLarge`; **re-auth state machine behaviour-frozen (AC-2)**.
   - **notification preferences (`notification_preferences_screen.dart`, 28)** — marigold tokens + type; toggles → marigold; OS-permission banner → warm tonal; **add the inert "Language" slot** ("Coming soon"); page spinner → skeleton/simple.
   - **delete-account (`delete_account_screen.dart`, 30)** — marigold tokens + type; destructive → error token; re-auth `OtpInput` reskin; success-state Haldi; migrate `AppTheme.radius*`; the delete callable behaviour-frozen.
   - **contact-support (`contact_support_fallback_dialog.dart`, 29)** — dialog radius → `radiusCard`; selectable email → `OBTColors.link`; buttons → Hanken/marigold; the `mailto:` + copy fallback behaviour-frozen (Invariant 3).
   - **photo picker (`widgets/photo_picker_sheet.dart`, 5/27)** — sheet top radius → `radiusSheet`; rows → Hanken; destructive "Remove" → error token.
   - **delete `profile_placeholder_screen.dart` (AC-3)** and re-point any inbound reference to `profile_screen.dart`.

4. **QA + Designer — the per-screen "converted" gate (§D):**
   - **All states present** for each surface (profile loading skeleton / populated; edit-profile; the change-phone **five states** + sync-pending recovery; preferences incl. the "Language" slot + the OS-permission banner; delete-account intro / re-auth / success; contact-support dialog).
   - **Light** goldens for each converted surface/state (Profile is a NON-HERO cluster — light only), queued in `test/golden/dc10_profile_golden_test.dart`, **skipped** pending the DC-13 ubuntu path.
   - **No-hard-coded-hex proof** — extend a profile boundary-contract test asserting **no `Color(0x…)` literal** survives in the converted profile presentation; the existing profile boundary + change-phone + contact-support + delete-account + notification-preferences suites stay green.
   - **Re-auth unchanged (AC-2)** — a test asserts the change-phone flow still requires both OTP steps and renders a **masked** number (negative case: an unmasked number / a skipped re-auth fails).
   - **Sign-out via `OBTConfirmationDialog`** — a test asserts the sign-out confirm path now routes through `OBTConfirmationDialog` and still signs out on confirm / no-ops on cancel.
   - **Contrast gate** (incl. the **white-on-marigold negative case** asserted to FAIL; ink-on-marigold passes), **every control labelled + 48 dp** (the toggles, the dialog buttons, the OTP boxes, the delete CTA), **dynamic type 2.0×** (no clip/overflow at 390 and 320), reduced motion freezes the skeleton.
   - **Designer (read-only)** reviews the cluster against `Phase3f - Profile.dc.html` and `Phase3g - Change Phone.dc.html`, and returns findings for the orchestrator to action.

5. **Verify:** `flutter analyze lib test` (no issues), `dart format .` (clean), `flutter test` (all pass; the existing profile suites stay green; per-feature coverage ≥ 70% and overall ≥ 50%). No **existing** controller, callable, re-auth, permission, `mailto:`, or telemetry **behaviour** is changed; the only structural changes beyond the reskin are the placeholder deletion, the `AlertDialog` → `OBTConfirmationDialog` swap, the radius migration, the spinner → skeleton swaps, and the hex re-point.

────────────────────────────────────────
BRANCH / PR
────────────────────────────────────────

- Branch: `feat/haldi-profile-conversion` (type-prefixed kebab, repo convention).
- Conventional Commits, single-token scope, ASCII subject ≤ 72 chars — e.g. `feat(profile): convert the profile and settings cluster to Haldi`.
- This is a **code** PR — the full PR pipeline must be green (Flutter Lint & Test, Build Android, Build iOS, Integration Tests, Coverage Gate) before merge.
- PR body: map each surface to §B + the DC-10 ACs; tick the invariants checklist (1 N/A — no money path; 2 — no projection read/write; 3 LIVE — contact-support `mailto:` + copy fallback; 4 LIVE — delete-account server-driven, single project); note "profile/edit-profile/preferences/delete/support reskinned; sign-out `AlertDialog` → `OBTConfirmationDialog`; the inert 'Language' slot added; the change-phone Phase3g five states converted with masked numbers + `+91` locked + the **re-auth behaviour frozen**; `profile_placeholder_screen.dart` **deleted**; legacy `radiusLarge`/`radiusXL` migrated; **no hard-coded hex remains** (grep)"; **`Closes #122` (plain, NOT backticked)**.
- Include the `Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>` trailer.
- Commit `docs/copilot_prompts/sprint_3/11.md` (this prompt) with the PR, per the repo convention.
- **Merge:** after the pipeline is green (wait out the slow iOS build; cross-check `gh run view` for a stale "pending"), the PR will read `BLOCKED` until the **`avtansh-code` OWNER** approves; **handle any inline review comments first** (address + reply + resolve threads); then squash-merge (`--delete-branch`) with subject `feat(profile): convert the profile and settings cluster to Haldi (#<pr>)`.

────────────────────────────────────────
DEFINITION OF DONE (this PR)
────────────────────────────────────────

- All **three** DC-10 acceptance criteria pass: profile (27), edit-profile (27), notification preferences (28), delete-account (30) and contact-support (29) are reskinned to marigold tokens + Bricolage/Hanken with the sign-out `AlertDialog` → `OBTConfirmationDialog`, the inert "Language" slot, and the `radiusLarge`/`radiusXL` → new-scale migration, `+91` locked (AC-1); the **Phase3g Change Phone** five states + the sync-pending recovery are converted with masked numbers, the `warning`-token recovery and `+91` locked, with the **two-OTP re-auth behaviour unchanged** (AC-2, the negative case); `profile_placeholder_screen.dart` is **deleted, not converted** (AC-3).
- The §B **Profile** rows in `02-conversion-checklist.md` are marked done; `01-coverage-gap.md` keeps rows 27 / 28 / 29 / 30 "covered" and notes the reskin.
- Every converted surface passes the §D per-screen gate (states incl. the change-phone five states + the skeleton + the "Language" slot, light goldens queued in the DC-13 harness, contrast incl. the negative case, labelling + 48 dp, dynamic-type 2.0×, reduced motion, re-auth unchanged + masked, sign-out via `OBTConfirmationDialog`); the profile boundary + change-phone + contact-support + delete-account + notification-preferences greps stay green; the **no-hex grep is added and green**.
- No hard-coded Haldi hex at call sites; every legacy `radiusLarge`/`radiusXL`/`radiusMedium` in the cluster migrated; `theme.dart` / `core/theme/*` / the DC-02/03 components unchanged; no invariant weakened; no controller / callable / re-auth / permission / `mailto:` / telemetry behaviour change.
- Analyse clean, `dart format` clean, all tests pass (existing profile suites green unchanged), per-feature coverage ≥ 70% and overall ≥ 50%.
- `docs/sprint-zero/next-three-prs.md` is reconciled (DC-09 → MERGED `#136` / `1ce84cc`; DC-10 status).
- DC-10 issue (#122) closed under the `Sprint 3` milestone; the milestone is reconciled per `.github/shared/milestone-tracking.md`.

End the session summary with: "Next: Sprint 3 PR #12 — DC-11 (Dark-mode parity for every converted hero screen: splash, home, friends, add-expense, settle-up, activity render correctly in dark, per ADR-0024)."

────────────────────────────────────────
FAILURE / REFUSAL PROTOCOL
────────────────────────────────────────

- If any change would weaken an invariant or the SRS, refuse it, quote the specific invariant / SRS section, and propose a compliant alternative. **In particular:** no on-screen `paise / 100` or `double` (Invariant 1 — N/A here, do not introduce one); no read/write of `simplifiedBalances` (Invariant 2); no single-app messaging target for contact-support — the **system mail client (`mailto:`) + copy fallback only** (Invariant 3); no second Firebase project, and no client-side reimplementation of the server-driven delete-account anonymisation (Invariant 4).
- **AC-2 is non-negotiable:** do **not** weaken, bypass, or shortcut the change-phone two-OTP re-auth, and never render an unmasked phone number — a re-auth regression or an unmasked number is a blocking defect (refuse and cite FR-PR-02 / AC-2).
- Refuse scope creep: no change to any controller / callable / re-auth state machine / permission flow / `mailto:` fallback / telemetry; do **not** build real language switching (the "Language" slot is inert); do **not** convert any other flow (Epic D dark-mode parity is DC-11; the golden/a11y CI job is DC-13); do **not** edit the frozen DC-02/03 components (`OBTConfirmationDialog` / `OBTSkeleton` / `OtpInput`); do not unlock any India-only constraint (`+91`).
- Do not edit `docs/OneByTwo_Requirements_Spec.md` or `lib/app/theme.dart` / `lib/core/theme/*` / the DC-02/03 components (consume the DC-01..03 foundation; any token/component gap routes to the Architect / issue #128).
- **The no-hex rule is non-negotiable:** if any converted profile surface would retain a hard-coded `Color(0x…)` hex (the `_ShimmerEffect` greys), refuse and re-point it to a Haldi token / the shared `OBTSkeleton` set (or route a genuine token gap to #128) — a literal hex left at a call site is a blocking defect.

Begin by delegating to the **PM** (confirm #122 + the milestone, reconcile the roadmap so DC-09 reads MERGED, the §B Profile reconciliation, and confirm the Haldi-27/28/29/30 coverage stays "covered"), then the **Architect** (the token/radius mapping + the `_ShimmerEffect` hex re-point + the change-phone re-auth/masking freeze + the no-hex contract + the Invariant-3/4 ruling), then the **Flutter Dev** (reskin profile + edit-profile + change-phone Phase3g + notification preferences + delete-account + contact-support + photo-picker, swap the sign-out dialog, migrate the radii, delete the placeholder) with **QA** + **Designer** (the §D per-screen gate incl. the light goldens + the no-hex grep + the re-auth-unchanged/masked proof + the sign-out-via-OBTConfirmationDialog proof + the contrast negative case + dynamic-type + the Phase3f/Phase3g visual review), and sequence to a single green PR.
