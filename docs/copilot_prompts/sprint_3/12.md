1. You are the OneByTwo orchestrator agent. Sprint 3 — **Design Conversion (the "Direction A — Haldi" visual system, `design_handoff_one_by_two/`)** — is the active sprint. **Eleven** PRs have merged on `main`: **DC-01 (design-token + type foundation) as PR #126** (`d9205d8`), **DC-02 (the six `OBT*` shared-widget reskins) as PR #127** (`aca8582`), **DC-03 (the eleven new Haldi shared components) as PR #129** (`b0ce9a6`), the **DC-03/06 scope doc as PR #130** (`bd27c38`), **DC-04 (Auth & onboarding) as PR #131** (`0391bf2`), **DC-05 (Home dashboard hero) as PR #132** (`1c3e194`), **DC-06 (Friends flow + Friend-history) as PR #133** (`006a0fb`), **DC-07 (Expenses flow) as PR #134** (`262107d`), **DC-08 (Settlements flow) as PR #135** (`b55e9fb`), **DC-09 (Activity & Notifications surfaces) as PR #136** (`1ce84cc`), and **DC-10 (Profile & Settings cluster, the final per-flow conversion; closed Epic C) as PR #137** (squash `d58581b`). The Haldi `ColorScheme` (light **and** dark token sets), the `OBTColors` `ThemeExtension` (the balance trio, the 8-hue `category` map, `heroShadow` / `rowShadow`, `warning`, `link`, `metaText`, `disabledText`), the `AppTheme` radius / soft-warm-shadow / motion tokens, the Bricolage Grotesque / Hanken Grotesk `TextTheme`, the reskinned `OBT*` widgets, the eleven DC-03 shared components, and the **converted Auth / Home / Friends / Expenses / Settlements / Activity / Notifications / Profile flows** are all on `main`. **Every per-flow screen conversion is now complete (Epic C closed).**

2.

3. The goal of THIS session is to deliver **Sprint 3 PR #12 — DC-11: Dark-mode parity across the six hero screens** (issue **#123**). This is the **first PR of Epic D (Dark-Mode Parity), 5 SP, P0 — Must have** (Haldi ships heroes **light and dark**, per ADR-0024). It does **not** convert any new screen — every hero was already token-converted in DC-04..DC-09. DC-11 **verifies** that each converted **hero** renders correctly under `AppTheme.dark`, **fixes** the dark-mode gaps, and **locks** the result with dark-mode widget tests + dark golden scaffolds. Work **only** the six heroes: **Splash (Haldi 1, DC-04)**, **Home dashboard (6, DC-05)**, **Friends list / Add-friend / Friend detail / Friend-history (9/10/11/12, DC-06)**, **Add-expense 3-step sheet + Expense detail (21/22, DC-07)**, **Settle-up sheet + Settlement history (23/24, DC-08)**, and **Activity feed (25, DC-09)**. The **non-hero** clusters (Profile 27/28/29/30, Notifications 26, the standalone components) are **out of scope** — DC-11 is heroes only; the DC-12 accessibility re-verification and the DC-13 golden/a11y CI job are later.

4.

5. **There are two load-bearing items.** (1) **The "warm-fill-takes-ink" rule in dark is a blocking contrast gate.** In dark, `ColorScheme.onError` is **ink `#1A1510`** on the light salmon danger `#F2856B`, and `ColorScheme.onSecondary` is **ink `#1A1510`** on the terracotta `#E07A55` — **white on either is ≈ 2.5:1 and MUST fail the contrast gate** (the same DC-01 ink-not-white rule that governs `onPrimary` on marigold). Any hero call site that hard-codes `Colors.white` (or a light foreground) on a warm `error` / `secondary` / `primary` fill in dark is a blocking defect — refuse and cite `03-foundation-plan.md` §1.2 / §1.3. The `AppTheme.dark` token set is already correct (`_darkOnPrimary` / `_darkOnSecondary` / `_darkOnError` are all `#1A1510`); the work is ensuring every hero **consumes `onError` / `onSecondary` / `onPrimary`** rather than a literal light colour. (2) **The shadow→outline swap.** `OBTColors.dark.rowShadow` is intentionally **empty `[]`** (no soft-warm row shadow in dark); a 1px `ColorScheme.outline` border replaces it for row/tile separation (`03-foundation-plan.md` §2.2). The **canonical reference is already on `main`** — `lib/features/friends/presentation/widgets/friend_list_tile.dart:40` (`final isDark = theme.brightness == Brightness.dark;`) + `:83-84` (`boxShadow: obtColors.rowShadow, border: isDark ? Border.all(color: colors.outline) : null`). Every hero row/tile/card that relies on `rowShadow` for separation must adopt this exact pattern so it does not vanish into the dark surface. (`heroShadow` is **non-empty** in dark — the hero net-balance card keeps its shadow; only `rowShadow` collapses to an outline.)

6.

7. **All four invariants are presentation-only here and hold.** DC-11 touches only rendering (`brightness`-aware borders, token foregrounds, dark goldens/tests). **Invariant 1** (money in paise): no on-screen `paise / 100` or `double` is introduced — every hero amount already routes through `formatInrFromPaise()` and that is unchanged. **Invariant 2** (`simplifiedBalances` server-only): no projection read/write changes; the home / settle-up / expense suggested-payment reads stay read-only. **Invariant 3** (system share sheet only): the friends-invite path stays on the OS share sheet. **Invariant 4** (single Firebase project): unchanged. No India-only constraint (`+91`) is touched.

8.

9. **This is an IMPLEMENTATION session** (it changes `lib/**`). The full PR pipeline runs (analyse, format, unit + widget tests, Build Android, Build iOS, Integration Tests, Coverage Gate).

10.

11. ────────────────────────────────────────
12. READ BEFORE STARTING
13. ────────────────────────────────────────

14.

15. - `.github/copilot-instructions.md`, `.github/shared/invariants.md`, `.github/shared/srs-pointer.md` (SRS = `docs/OneByTwo_Requirements_Spec.md` v1.1), `.github/shared/coding-standards.md`, `.github/shared/handoffs.md`, `.github/shared/milestone-tracking.md`, `.github/shared/test-strategy.md`, `.github/shared/decision-log.md` → **ADR-0024** (the Haldi direction; scope: visual layer only).
16. - **The authoritative spec for this PR:**
17.   - `docs/sprint-zero/sprint-3-plan.md` — **story DC-11** (the User Story, the **three** Acceptance Criteria — AC-1 every converted hero renders the Haldi **dark** tokens (background `#1A1510`, surface `#241D16`, primary `#EAA24A`, `onPrimary` ink `#1A1510`) with **1px `outline` borders replacing the light soft-warm shadows**; AC-2 the **warm-fill-takes-ink** negative case — `onError` ink on dark danger `#F2856B`, `onSecondary` ink, **white on either fails the contrast gate**; AC-3 a **dark-mode golden exists and matches alongside the light golden** for each hero), the Definition of Done, and the Invariant-Compliance clause.
18.   - `docs/audits/design-conversion/03-foundation-plan.md` — **§1.2** (ink-not-white), **§1.3** (the foreground that **flips by theme**: `onSecondary` cream in light → ink in dark; the dark `onError` ink rule), **§2.2** (the soft-warm-shadow model and its **dark → 1px outline** substitution), and the light/dark token table (§1).
19.   - `docs/audits/design-conversion/02-conversion-checklist.md` — the **critical-path table** ("hero screens, ship light + dark"). Mark each hero's **dark** column done on merge (mirror the DC-09 / DC-10 `> **Status — …**` blockquote convention with a DC-11 dark-parity status note).
20.   - `docs/audits/design-conversion/04-qa-test-strategy.md` — the **§A.6 hero rule** (a hero gets **both brightnesses for every state**) and the **contrast gate incl. the white-on-warm-fill negative case**. Confirm each hero's golden scaffold already queues **dark** (it does — `dc04..dc09_*_golden_test.dart` iterate `Brightness.values`); fill any gap and keep them group-`skip`ped pending DC-13.
20a.  - `design_handoff_one_by_two/` — the hero design references in their **dark** variants (Splash 1, Home 6, Friends 9, Add-expense 21, Settle up 23, Activity 25), for the Designer pass.
21. - **The foundation now on `main` (consume — read, do not change):** `lib/app/theme.dart` (`AppTheme.dark`: `_darkBackground #1A1510`, `_darkSurface #241D16`, `_darkPrimary #EAA24A`, `_darkOnPrimary / _darkOnSecondary / _darkOnError = #1A1510`, `_darkDanger #F2856B`, `_darkOutline #3A322A`), `lib/core/theme/obt_colors.dart` (`OBTColors.dark`: `rowShadow: <BoxShadow>[]` **empty**, `heroShadow` **present**, the dark balance trio / category hues / `warning` / `link` / `textTertiary`). `lib/main.dart:209-210` already wires `theme: AppTheme.light` + `darkTheme: AppTheme.dark` (no explicit `themeMode` → `ThemeMode.system`), so dark is reachable at runtime — **do not change the theme wiring**; any token/component gap routes to the Architect / issue #128.
22. - **The reference pattern (already on `main`):** `lib/features/friends/presentation/widgets/friend_list_tile.dart` — the `isDark ? Border.all(color: colors.outline) : null` + `boxShadow: obtColors.rowShadow` shadow→outline swap. Mirror it at every hero row/tile/card that needs it. `lib/features/home/presentation/widgets/spending_category_palette.dart` shows the `brightness == Brightness.dark` token switch for the donut hues.
23. - **The surfaces to audit (read each; FIX only the dark-mode gaps, change nothing else):**
23a.   - **Splash (1)** — `lib/features/auth/presentation/splash_screen.dart` (+ the brand kit): the marigold/ink brand on the dark background.
23b.   - **Home (6)** — `lib/features/home/presentation/**` (`net_balance_header_card.dart` keeps `heroShadow`; the top-balance `OBTBalancePill` row, the spending donut + legend, the skeleton): verify the hero card shadow, the balance-pill separation, and the donut hues in dark.
23c.   - **Friends (9/10/11/12)** — `lib/features/friends/presentation/**`: `friend_list_tile.dart` is the reference (already done); verify Add-friend (`add_friend_screen.dart`, `manual_phone_entry_tab.dart`), Friend detail (`OBTSettleUpCard`, the category timeline), and Friend-history in dark.
23d.   - **Add-expense / Expense detail (21/22)** — `lib/features/expenses/presentation/**`: the 3-step `OBTStepperSheet`, `OBTSegmentedSplitControl` (the green "adds up" / red over-under validation — verify the success/error tints read on dark), the step-3 callouts (`Border.all(outline)` already), the expense-detail per-person split. Note `widgets/receipt_fullscreen_viewer.dart` uses `Colors.white` on a black photo backdrop — that is a **theme-agnostic media scrim**, leave it.
23e.   - **Settle-up / Settlement history (23/24)** — `lib/features/settlements/presentation/**`: the `OBTSettleUpSheet` (the suggested-payment header, `OBTAmountInput`, the in-sheet success moment), the history rows (sent/received direction icon + sign) in dark.
23f.   - **Activity feed (25)** — `lib/features/activity/presentation/**`: the `OBTActivityRow` rows, the shimmer `OBTSkeleton` set, `OBTEmptyState`, the error-state `OBTColors.metaText` in dark.
24. - Do **not** read or re-plan the backend/data docs; no Cloud Function, rule, trigger, schema, callable, or telemetry changes.

25.

26. ────────────────────────────────────────
27. STANDING CAVEATS (read first)
28. ────────────────────────────────────────

29.

30. - **The warm-fill-takes-ink rule (AC-2) is the load-bearing negative case.** In dark, `onError` / `onSecondary` / `onPrimary` are **ink `#1A1510`**; a hero that renders **white** (or any light foreground) on the warm `error` / `secondary` / `primary` fill is a blocking defect (white-on-`#F2856B` ≈ 2.5:1). Every foreground must come from `Theme.of(context).colorScheme.on*` — never a literal. A test must assert the **white-on-dark-danger negative case FAILS** the contrast gate while **ink-on-dark-danger passes** (mirror the DC-09 / DC-10 white-on-marigold negative pattern).
31. - **The shadow→outline swap (AC-1).** `OBTColors.dark.rowShadow` is empty; every hero row/tile/card relying on it for separation adopts the `friend_list_tile.dart` pattern (`border: isDark ? Border.all(color: colors.outline) : null` alongside `boxShadow: obtColors.rowShadow`). `heroShadow` is non-empty in dark — the hero net-balance card keeps it; do **not** add an outline to a `heroShadow` surface.
32. - **This is verification + targeted gap-fix, NOT a re-theme.** The heroes are already token-built; most will render correctly in dark already. Change ONLY what is needed for dark parity (a missing outline border, a literal light foreground, a brightness-unaware tint). Do not restyle light mode, do not touch `lib/app/theme.dart` / `lib/core/theme/*` / the DC-02/03 components, and do not alter any behaviour, controller, callable, or telemetry. Existing light-mode suites stay green unchanged.
33. - **Dark goldens stay queued + skipped (DC-13 owns authoring).** Each hero golden scaffold already iterates `Brightness.values` (light + dark) and is group-`skip`ped pending the DC-13 (#125) `golden-a11y-checks` ubuntu job — confirm every hero queues its dark states; do **not** author/commit golden PNGs locally (macOS bytes are non-deterministic). The load-bearing proof is the **dark-mode widget tests** (pump each hero under `AppTheme.dark`; assert no overflow at 390/320 + 2.0× dynamic type, outline-not-shadow separation in dark, and the warm-fill-takes-ink contrast gate) — which run for real.
34. - **No new telemetry, no behaviour change.** A dark-mode render must not drop or add any event, change any controller/callable, or alter the simplified-debts read. If a planned event for a hero interaction would be affected, confirm it still fires (the DC-06 lesson).
35. - **Tooling + lessons carried from DC-01..DC-10:**
36.   - fvm-pinned (Flutter 3.44.3 / Dart 3.12.2). **The `fvm` wrapper may be broken** ("Can't load Kernel binary … expected 127, found 130") — bypass it: `export PATH="/Users/avtanshgupta/fvm/versions/stable/bin:$PATH"` then bare `flutter analyze lib test`, `flutter test`, `dart format .`, and commit/push/merge with bare `git`/`gh` (the pinned SDK on PATH satisfies the lefthook `dart`/`flutter` + `coverage-check` pre-commit/pre-push hooks).
37.   - CI gates on `dart format --set-exit-if-changed .` — run `dart format .` before pushing. `very_good_analysis` is strict (`directives_ordering`, `prefer_int_literals`, `avoid_redundant_argument_values`, `lines_longer_than_80_chars` incl. `testWidgets` descriptions, `prefer_const_constructors`, `unnecessary_import`, `comment_references`, `only_throw_errors`, `discarded_futures`).
38.   - **Never build `AppTheme.light` / `.dark` (or call GoogleFonts) at test COLLECTION time** — only inside `test`/`testWidgets` bodies (the binding is not initialised at collection time). Put any pure-`test()` contrast group **last** so earlier `testWidgets` populate the GoogleFonts cache.
39.   - **Animated surfaces never settle** (skeleton shimmers, OTP cursor, slides) — pump with `disableAnimations: true`, or `settle: false` / a single frame. Reuse the **test harness** `test/support/widget_test_harness.dart` (`pumpThemed` takes `brightness:`; `expectAllInteractiveNodesLabelled`, `expectAllTapTargetsMeetMinSize`) and the **golden harness** `test/golden/golden_harness.dart` (`pumpForGolden(..., brightness:)`, `loadHaldiFonts`). Add / extend shared no-op fakes under `test/features/<feature>/helpers/` rather than redefining per file.
40.   - Mirror the existing dark contrast patterns in the per-hero reskin gates (`test/features/{home,friends,expenses,settlements,activity}/*_haldi_reskin_test.dart` already carry some `Brightness.dark` / `AppTheme.dark` assertions) and the golden scaffolds (`test/golden/dc05..dc09_*_golden_test.dart`, which already iterate `Brightness.values`).
41.   - **`Closes #123` MUST be plain text, NOT backticked,** in the PR body, or the Sprint 3 milestone will not advance.
42.   - **Merge needs an OWNER approval.** `main`'s ruleset requires an approving review from a write-access reviewer; the session authenticates as the author `avtanshgupta` (a collaborator, cannot self-approve), and repo auto-merge is disabled, so `gh pr merge --auto` is rejected and `--admin` does **not** bypass the ruleset. A green pipeline reads `mergeStateStatus=BLOCKED` / `REVIEW_REQUIRED` until the **`avtansh-code` OWNER** approves; then squash-merge (`--delete-branch`) with subject `feat(<scope>): … (#<pr>)` (single-token scope). **iOS (CocoaPods) is the slowest check (~15–20 min)** — wait it out; `gh pr checks` can show a **stale "pending"** for iOS after the job actually succeeded (cross-check `gh run view <id> --json jobs`).
43.   - **Handle review comments.** After CI is green, check the PR for inline review comments, address them with focused commits, reply on each thread, and resolve the threads before merge.

44.

45. ────────────────────────────────────────
46. THE TASK — DC-11 (5 SP)
47. ────────────────────────────────────────

48.

49. 1. **Issue + milestone + roadmap (PM).** DC-11 is issue **#123**, already under the **`Sprint 3`** milestone — confirm it; this PR carries `Closes #123` (plain). Per `.github/shared/milestone-tracking.md`, reconcile the Sprint 3 milestone, and on this PR flip the **DC-10** line in `docs/sprint-zero/next-three-prs.md` from "IN FLIGHT" to **"MERGED (#137, `d58581b`)"** and set DC-11 in flight. In `02-conversion-checklist.md` mark each hero's **dark** column / critical-path row done (a `> **Status — Heroes verified in dark (DC-11 …)**` blockquote). No SRS gap (dark mode is ADR-0024, already specified).
50. 2. **Architect — the dark token / shadow / contrast ruling (review, read-only).** For each hero, confirm: the dark surfaces (`background #1A1510` / `surface #241D16`); the **shadow→outline** decision per row/tile/card (which surfaces keep `heroShadow`, which swap `rowShadow` → `Border.all(outline)`); the **warm-fill-takes-ink** foregrounds (`onError` / `onSecondary` / `onPrimary` ink); and the loading/empty/error/success shapes in dark. Confirm the Invariant contract (all presentation-only). Return the per-hero contract for the Flutter Dev.
51. 3. **Flutter Dev — make the six heroes render correctly in dark:** audit each hero (Splash, Home, Friends 9/10/11/12, Add-expense 21/22, Settle-up 23/24, Activity 25); FIX the gaps only — adopt the `friend_list_tile` shadow→outline pattern at any row/tile/card that needs separation in dark, re-point any literal light foreground on a warm fill to `colorScheme.on*` (ink), and make any brightness-unaware tint `brightness`-aware. Change nothing in light mode.
52. 4. **QA + Designer — the dark-parity gate (§A.6 + the contrast gate):**
52a.   - **Dark-mode widget tests** per hero: pump under `AppTheme.dark` (`pumpThemed(..., brightness: Brightness.dark)`); assert it renders without overflow at 390 **and** 320 dp and at 2.0× dynamic type; assert **outline-not-shadow** separation in dark (a row/tile carries a `Border` in dark where light used `rowShadow`); reduced motion freezes skeletons.
52b.   - **Contrast gate incl. the negative case** (a pure `test()` group, last): **white on dark danger `#F2856B` FAILS** (< 3.0) and **ink (`onError`) on it passes** (≥ 4.5); the same for `onSecondary` on `#E07A55`; `onPrimary` ink on `#EAA24A` passes; the dark balance trio / `metaText` clear AA on the dark surface.
52c.   - **Dark goldens** queued (light **and** dark for every hero state) in the existing `dc05..dc09` golden scaffolds (+ splash in `dc04`), group-`skip`ped pending DC-13; confirm none is missing a dark entry.
52d.   - **Designer (read-only)** reviews each hero against its **dark** design reference and returns findings for the orchestrator to action (as DC-10's B1 heading-contrast catch was actioned).
53. 5. **Verify:** `flutter analyze lib test` (no issues), `dart format .` (clean), `flutter test` (all pass; existing light suites green unchanged; per-feature coverage ≥ 70% and overall ≥ 50%). No behaviour / controller / callable / telemetry change; the only changes are dark-mode rendering fixes + dark tests/goldens.

54.

55. ────────────────────────────────────────
56. BRANCH / PR
57. ────────────────────────────────────────

58.

59. - Branch: `feat/haldi-dark-mode-parity` (type-prefixed kebab, repo convention).
60. - Conventional Commits, single-token scope, ASCII subject ≤ 72 chars — e.g. `feat(theme): dark-mode parity across the six hero screens`.
61. - This is a **code** PR — the full PR pipeline must be green (Flutter Lint & Test, Build Android, Build iOS, Integration Tests, Coverage Gate) before merge.
62. - PR body: map each hero to its dark-parity changes + the DC-11 ACs; tick the invariants checklist (all presentation-only — 1 no money path change, 2 no projection write, 3 share-sheet untouched, 4 single project); note "shadow→outline swap where `rowShadow` collapses in dark; `onError`/`onSecondary`/`onPrimary` ink (warm-fill-takes-ink) with the white-on-dark-danger negative case asserted to FAIL; dark goldens queued (skipped, DC-13); no behaviour change"; **`Closes #123` (plain, NOT backticked)**.
63. - Include the `Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>` trailer.
64. - Commit `docs/copilot_prompts/sprint_3/12.md` (this prompt) with the PR, per the repo convention.
65. - **Merge:** after the pipeline is green (wait out the slow iOS build; cross-check `gh run view`), the PR reads `BLOCKED` until the **`avtansh-code` OWNER** approves; **handle any inline review comments first** (address + reply + resolve threads); then squash-merge (`--delete-branch`) with subject `feat(theme): dark-mode parity across the six hero screens (#<pr>)`.

66.

67. ────────────────────────────────────────
68. DEFINITION OF DONE (this PR)
69. ────────────────────────────────────────

70.

71. - All **three** DC-11 acceptance criteria pass: every converted hero (Splash 1, Home 6, Friends 9, Add-expense 21, Settle up 23, Activity 25) renders the Haldi **dark** tokens with **1px `outline` borders replacing the light soft-warm row shadows** (AC-1); the **warm-fill-takes-ink** rule holds with the **white-on-dark-danger negative case asserted to FAIL** the contrast gate (AC-2); a **dark-mode golden is queued alongside the light golden** for every hero state (AC-3, skipped pending DC-13).
72. - The hero **dark** rows in `02-conversion-checklist.md` (critical-path table) are marked done; `next-three-prs.md` is reconciled (DC-10 → MERGED `#137` / `d58581b`; DC-11 status).
73. - Dark-mode widget tests cover every hero (render-without-overflow at 390/320 + 2.0×, outline-not-shadow separation, reduced motion) and the contrast gate incl. the negative case; existing light suites stay green unchanged; no hard-coded light foreground remains on a warm fill in dark.
74. - `theme.dart` / `core/theme/*` / the DC-02/03 components unchanged; no invariant weakened; no controller / callable / telemetry behaviour change.
75. - Analyse clean, `dart format` clean, all tests pass, per-feature coverage ≥ 70% and overall ≥ 50%.
76. - DC-11 issue (#123) closed under the `Sprint 3` milestone; the milestone is reconciled per `.github/shared/milestone-tracking.md`.

77.

78. End the session summary with: "Next: Sprint 3 PR #13 — DC-12 (Accessibility re-verification against the new palette: WCAG 2.1 AA across the Haldi palette + type, dynamic type to 2.0×, colour-blind-safe balance signals, per the Haldi README and `03-foundation-plan.md` §1.2/§1.5/§2.3)."

79.

80. ────────────────────────────────────────
81. FAILURE / REFUSAL PROTOCOL
82. ────────────────────────────────────────

83.

84. - If any change would weaken an invariant or the SRS, refuse it, quote the specific invariant / SRS section, and propose a compliant alternative. **In particular:** no on-screen `paise / 100` or `double` (Invariant 1); no read/write of `simplifiedBalances` (Invariant 2); no single-app share/messaging target (Invariant 3); no second Firebase project (Invariant 4).
85. - **AC-2 is non-negotiable:** do **not** render white (or any light foreground) on a warm `error` / `secondary` / `primary` fill in dark — it must be ink via `colorScheme.on*`; a white-on-dark-danger regression is a blocking defect (refuse and cite `03-foundation-plan.md` §1.2 / §1.3).
86. - Refuse scope creep: convert no non-hero surface (Profile / Notifications / components are out of scope); build no new behaviour; do **not** edit `lib/app/theme.dart` / `lib/core/theme/*` / the DC-02/03 components (consume the DC-01..03 foundation; any token/component gap routes to the Architect / issue #128); do **not** author/commit golden PNGs locally (DC-13 owns that); do not change the `ThemeMode.system` wiring or unlock any India-only constraint (`+91`).
87. - Do not edit `docs/OneByTwo_Requirements_Spec.md`.

88.

89. Begin by delegating to the **PM** (confirm #123 + the milestone, reconcile the roadmap so DC-10 reads MERGED `#137` / `d58581b`, mark the hero dark critical-path rows), then the **Architect** (the per-hero dark token / shadow→outline / warm-fill-takes-ink contract + the Invariant ruling), then the **Flutter Dev** (audit + fix the six heroes for dark parity using the `friend_list_tile` pattern) with **QA** + **Designer** (the dark-mode widget tests + the contrast negative case + the dark goldens + the per-hero dark visual review), and sequence to a single green PR.
