You are the OneByTwo orchestrator agent. Sprint 3 — **Design Conversion (the "Direction A — Haldi" visual system, `design_handoff_one_by_two/`)** — is the active sprint. Eight PRs have merged on `main`: **DC-01 (the design-token + type foundation) as PR #126** (squash `d9205d8`), **DC-02 (the six `OBT*` shared-widget reskins) as PR #127** (squash `aca8582`), **DC-03 (the eleven new Haldi shared components) as PR #129** (squash `b0ce9a6`), the **DC-03/06 scope doc as PR #130** (`bd27c38`), **DC-04 (the Auth & onboarding flow conversion) as PR #131** (squash `0391bf2`), **DC-05 (the Home dashboard hero conversion) as PR #132** (squash `1c3e194`), **DC-06 (the Friends flow conversion + the net-new Friend-history screen) as PR #133** (squash `006a0fb`), and **DC-07 (the Expenses flow conversion: the 3-step add-expense sheet + step-2 rebuild + expense detail + the context picker) as PR #134** (squash `262107d`). The Haldi `ColorScheme` (marigold `primary`, **ink** `onPrimary`), the `OBTColors` `ThemeExtension` (incl. the **balance trio** `balancePositive` / `balanceNegative` / `balanceZero`, the **8-hue `category` map**, `OBTColors.heroShadow` / `rowShadow`, `OBTColors.link`, `OBTColors.metaText`), the `AppTheme` radius / soft-warm-shadow / motion tokens, the Bricolage Grotesque / Hanken Grotesk `TextTheme`, the `OBTText.amount` / `OBTText.amountHero` helpers, the **single core IST date formatter** `lib/core/formatters/ist_date_formatter.dart` (`kIstOffset` / `toIst` / `formatIstLongDate`, day-first `dd MMM yyyy`; promoted in DC-07), the six reskinned `OBT*` widgets, the **eleven DC-03 shared components**, and the **converted Auth / Home / Friends / Expenses flows** are all on `main`.

The goal of THIS session is to deliver **Sprint 3 PR #9 — DC-08: convert the Settlements flow to Haldi (light + dark)** (issue **#120**). This is the **fifth per-flow screen-conversion PR (3 SP)** and the **second hero rebuild** (after DC-07 step 2). It wires the DC-01..DC-03 primitives into the **Settlements surfaces** — **Haldi 23 ★ Settle-up bottom sheet (REBUILD onto the shared `OBTSettleUpSheet`, incl. the success moment + the inert "Pay via UPI" slot)**, **Haldi 24 Settlement history (reskin)** — and resolves the **`settle_up_header.dart` seam** that the `OBTSettleUpSheet` adoption supersedes. Work **only** the Settlements flow; Activity/Notifications (DC-09), Profile (DC-10..12), the DC-13 golden/a11y CI job, and the rest are later.

**Invariants 1 + 2 are LIVE on this flow.** Every on-screen amount (the suggested-payment focal amount, the editable amount input, the per-row settlement-history amounts) MUST go through `formatInrFromPaise()` (Invariant 1 — no `paise / 100`, no `double`; the `OBTAmountInput` integer-paise `onChanged(int paise)` contract is untouched). The settle-up sheet shows **exactly one** pre-filled suggested payment read from the `simplifiedBalances` projection — **never a who-owes-who debt graph**, and the client **never writes** the field (Invariant 2). The existing `settle_up_boundary_contract_test.dart`, `settle_up_pii_leak_test.dart`, `settlement_history_pii_leak_test.dart` MUST stay green, and any net-new on-screen-money surface MUST be added to those greps. Note the settlements boundary contract forbids a `simplifiedBalances =` **write/assignment** (a projection **read** via a named argument such as `netBalancePaise(simplifiedBalances: …)` is permitted, unlike the stricter Expenses grep). **Invariants 3 + 4 must stay untouched** — if a reminder affordance is surfaced it must use the **system share sheet only** (no per-app messaging path; Invariant 3), and no second Firebase project (Invariant 4).

**This is an IMPLEMENTATION session** (it changes `lib/**`). The full PR pipeline runs (analyse, format, unit + widget tests, Build Android, Build iOS, Integration Tests, Coverage Gate).

────────────────────────────────────────
READ BEFORE STARTING
────────────────────────────────────────

- `.github/copilot-instructions.md`, `.github/shared/invariants.md`, `.github/shared/srs-pointer.md` (SRS = `docs/OneByTwo_Requirements_Spec.md` v1.1), `.github/shared/coding-standards.md`, `.github/shared/handoffs.md`, `.github/shared/milestone-tracking.md`, `.github/shared/test-strategy.md`.
- **The authoritative spec for this PR:**
  - `docs/sprint-zero/sprint-3-plan.md` — **story DC-08** (the User Story, the **three** Acceptance Criteria — incl. AC-2 the "exactly one suggested payment / never a debt graph / read-only `simplifiedBalances`" negative case and AC-3 the settlement-history direction icon + sign + the **reminder-compose / friend-group-filter PM-reconcile flags** — the Definition of Done, and the Invariant-Compliance clause) and the sprint-wide Definition of Ready / Definition of Done at the top.
  - `docs/audits/design-conversion/02-conversion-checklist.md` **§B → "Settlements"** (the per-widget conversion table: `settle_up_bottom_sheet.dart` **(rebuild)**, `widgets/settle_up_header.dart`, `settlement_history_screen.dart`). Mark the §B Settlements rows done on merge.
  - `docs/audits/design-conversion/04-qa-test-strategy.md` — **§A.4** (the **rebuild** row: fresh goldens for the **new** settle-up **success-moment** state; new **behavioural** widget test — the success moment fires **once** with a single haptic), **§A.5/§A.6** (each hero → light **and** dark goldens for **every** state; Settle up 23 and Settlement history 24 are money/marigold surfaces so both brightnesses are required; the §A.4 row lists Settle-up 23 states as **populated (pre-filled, incl. inert "Pay via UPI"), success-moment, error**), **§D** (the per-screen "converted" acceptance gate), the **balance-trio = colour + icon + label** assertion, the contrast gate incl. the **white-on-marigold negative case**, labelling + 48 dp, dynamic type to 2.0× + reduced motion. The settle-up sheet + any balance surface **read** the server projection only — **no widget writes it** (§A INV-2 clause).
  - `docs/audits/design-conversion/01-coverage-gap.md` — §B row **23 (Settle up)** and **24 (Settlement history)** are **"covered"**; confirm they stay "covered" and note the settle-up **rebuild**. **§C item 6 (the reminder placement/scope divergence)** and the **friend/group filter** on Settlement history are **PM-reconcile flags** — they are NOT silently dropped by this conversion; the PM records the reconcile decision, the conversion ships the **existing** one-tap "Remind" behaviour reskinned (no new compose, no new free-text path) pending that decision.
  - `.github/shared/decision-log.md` → **ADR-0024** (scope: visual layer only) and any ADR covering the inert "Pay via UPI" slot.
  - `design_handoff_one_by_two/screens/Phase2 - Components.dc.html` — the Haldi **settle-up bottom sheet (#6)** (the suggested-payment header, the editable amount, the inert "Pay via UPI" row, the **success moment**) and the skeleton set. **There is no standalone "Settlements" screen mockup** (the handoff `Phase3*` files are Auth / Home / Friends / Groups / Expenses / Profile / Change-Phone); the **settlement-history (24)** row visual follows the established Haldi row/token pattern and the **Friends timeline** rows in `design_handoff_one_by_two/screens/Phase3c - Friends.dc.html` (signed amounts + direction + Bricolage tabular). If a richer Settlements mockup is needed, that is an Architect/Designer gap to flag, not a blocker.
- **The foundation now on `main` (consume — read, do not change):** `lib/app/theme.dart`, `lib/core/theme/obt_colors.dart` (the **balance trio** + `link` + `metaText`), `lib/core/theme/obt_text.dart` (`OBTText.amount` / `.amountHero`), `lib/core/formatters/inr_formatter.dart` (`formatInrFromPaise()`), `lib/core/formatters/ist_date_formatter.dart` (`formatIstLongDate()` — the single IST date source of truth; **do not add a feature-local IST offset/format duplicate**, per the DC-07 review), and the **DC-03 components** the Settlements flow consumes:
  - **settle-up bottom sheet** — `lib/core/widgets/sheets/obt_settle_up_sheet.dart` (`OBTSettleUpSheet` — props `payerDisplayName` / `payeeDisplayName` / `suggestedAmountPaise` / `onAmountChanged(int paise)` / `onRecord` / `payerPhotoUrl?` / `payeePhotoUrl?` / `isLoading` / `isSaving` / `isSuccess` / `amountErrorText?`; renders **exactly one** suggested payment from the projection, the four states **loading-skeleton / settled-guard / editing / success-moment**, the **inert "Pay via UPI" "Coming soon"** row (labelled, announced disabled), and on `isSuccess` a check + a **single** `HapticFeedback.lightImpact()`).
  - **skeleton set** — `lib/core/widgets/feedback/obt_skeleton.dart`; **balance pill** — `lib/core/widgets/indicators/obt_balance_pill.dart`; **empty-state** — `lib/core/widgets/feedback/obt_empty_state.dart`.
  Reuse the **test harness** `test/support/widget_test_harness.dart` (`pumpThemed`, `expectAllInteractiveNodesLabelled`, `expectAllTapTargetsMeetMinSize`) and the **golden harness** `test/golden/golden_harness.dart`. See the DC-07 reskin gate `test/features/expenses/expenses_haldi_reskin_test.dart` + golden scaffold `test/golden/dc07_expenses_golden_test.dart` + the shared fakes in `test/features/expenses/helpers/fake_services.dart` for the §D-gate pattern (hero states light+dark, contrast incl. the white-on-marigold negative case, 48 dp + labelling, dynamic type 2.0× at 390/320, reduced motion, group-`skip`ped goldens, **shared no-op fakes rather than per-file redefinitions**).
- **The surfaces to convert (read each before editing):** `lib/features/settlements/presentation/settle_up_bottom_sheet.dart` (23 ★ rebuild), `lib/features/settlements/presentation/widgets/settle_up_header.dart` (23 ★ — superseded by `OBTSettleUpSheet`; see the seam below), `lib/features/settlements/presentation/settlement_history_screen.dart` (24 reskin). Read the controller/providers/domain (`application/settle_up_controller.dart`, `application/settle_up_state.dart`, `application/settle_up_telemetry.dart`, `application/settlement_history_provider.dart`, `application/settlement_history_telemetry.dart`, `domain/settle_up_draft.dart`, `domain/settlement_doc.dart`, `data/settlement_repository.dart`) **only** to confirm you are NOT changing behaviour, the single-suggested-payment projection read, the create/record flow, the state machine (`SettleUpEditing` / `SettleUpSaving` / `SettleUpSuccess` / `SettleUpError`), or telemetry.
- Do **not** read or re-plan the backend/data docs; the `onSettlementWrite` recompute trigger and the simplified-debts algorithm are unchanged.

────────────────────────────────────────
STANDING CAVEATS (read first)
────────────────────────────────────────

- **Invariants 1 + 2 are LIVE and load-bearing here.** Money only via `formatInrFromPaise()` (Invariant 1 — the `OBTAmountInput` integer-paise contract is untouched). The settle-up sheet shows **one** pre-filled suggested payment **read** from the projection (Invariant 2 — never a who-owes-who graph; the client never writes `simplifiedBalances`; settlement recompute stays server-side in `onSettlementWrite`). Any item that weakens one is a blocking defect — refuse and cite it.
- **THE SETTLE-UP REBUILD SEAM (Architect must rule).** §B says `settle_up_bottom_sheet.dart` is a **rebuild**: replace the bespoke body (the inlined `SettleUpHeader` + amount input + date + note + "Record Settlement" button + the snackbar/`SizedBox` success placeholder) with the shared **`OBTSettleUpSheet`**, wiring its props to the **existing** `settle_up_controller.dart` state machine (the controller, `settle_up_draft.dart`, and the projection read are **behaviour-frozen** — the rebuild is the visual surface + the success-moment wiring only). The Architect must rule: how `SettleUpEditing` / `SettleUpSaving` / `SettleUpSuccess` / `SettleUpError` map to `isLoading` / `isSaving` / `isSuccess` / `amountErrorText`; how `onAmountChanged` / `onRecord` thread into the controller setters/record action; and **the success-moment state flow** — `SettleUpSuccess` must render the `OBTSettleUpSheet` **success moment in-sheet** (check + single haptic) **before** the sheet dismisses, replacing the current "auto-dismiss + `Settlement recorded.` snackbar" behaviour (AC-1). The `OBTAmountInput` paise contract must not change.
- **THE SUCCESS-COPY RECONCILE (Architect must rule).** AC-1 specifies the success-moment copy **"You're all settled up — high five!"**, but the shipped DC-03 `OBTSettleUpSheet._buildSuccess` currently renders **"Payment recorded"** + "You paid {payee} {amount}.". The DC-03 component is **frozen** (consume, do not edit). The Architect must reconcile: either (a) **accept the shipped component copy** and record that the AC-1 "high five" wording is superseded by the DC-03 ruling (update the story/checklist note, do NOT edit the component), or (b) route a genuine copy change through the **Architect / issue #128** token-and-component-gap process (a separate, ruled change to the DC-03 component — NOT an ad-hoc edit inside this flow PR). Default to (a) unless the Architect rules otherwise. Do **not** silently diverge.
- **THE `settle_up_header.dart` SEAM (Architect must rule).** `OBTSettleUpSheet` renders its **own** payer → arrow → payee suggested-payment header, which **supersedes** the bespoke `widgets/settle_up_header.dart` (`SettleUpHeader`). The Architect must rule whether `settle_up_header.dart` is **deleted** (the clean outcome — mirrors DC-05 deleting `groups_list_placeholder.dart` and DC-06 deleting `balance_pill.dart`) once no call site references it, or retained. If deleted: remove its widget test, re-point/clean any imports, and confirm no inbound references remain. The §B "reskin settle_up_header" row is satisfied by the supersession.
- **THE INERT EXTENSION SLOT.** The **"Pay via UPI"** row is already built **inert** inside `OBTSettleUpSheet` ("Coming soon", labelled, announced disabled, not wired). Do **not** ship it as a live control and do **not** add a UPI/payment integration. Mirror the DC-03 / DC-07 inert "Coming soon" pattern — labelled, disabled, asserted disabled; no new write path, no backend call.
- **Behaviour is UNCHANGED — the rebuild is visual + the success-moment surface.** No change to the settle-up controller, the draft, the single-suggested-payment projection read, the record/create flow, the `onSettlementWrite` recompute, or any domain model or telemetry. Existing settle-up controller / boundary / PII-leak / screen / widget / draft / repository / history-provider tests stay green unchanged (update only assertions that legitimately encode an old token literal, the removed bespoke header / success placeholder, or a hand-rolled spinner's removed structure).
- **Settlement history (24) reskin.** Marigold tokens + type; the loading `CircularProgressIndicator` → the shimmer `OBTSkeleton*` set; each row gains a **sent/received direction icon + sign** (derived from `settlement_doc.dart` `fromUserId` / `toUserId` vs the current user) with **Bricolage tabular** amounts via `formatInrFromPaise()`; empty `Icon` → `OBTEmptyState`. The **reminder-compose** and the **friend/group filter** are **PM-reconcile flags** (§C item 6) — surfaced to the PM, NOT silently dropped and NOT newly built in this PR (the existing one-tap "Remind" affordance is reskinned in place; if it shares anything it uses the **system share sheet only**, Invariant 3).
- **Dates render in IST.** Any settlement date on screen renders in IST as `24 Jun 2026` via the **single core formatter** `formatIstLongDate()` (`lib/core/formatters/ist_date_formatter.dart`, fixed UTC+5:30, no DST, day-first `dd MMM yyyy`). Do **not** add a feature-local IST offset/format duplicate (the DC-07 review consolidated these). Replace any `intl` `DateFormat` device-local date in the settlements presentation with `formatIstLongDate()`.
- **All colour via `Theme.of(context).colorScheme` / `OBTColors`; all type via `theme.textTheme` / `OBTText`; all radii via the `AppTheme` scale.** `theme.dart` / `core/theme/*` are the single source of truth — consume, do not edit (any token gap routes to the Architect / issue #128). No hard-coded Haldi hex at call sites.
- **No new telemetry event is silently dropped.** If the telemetry plan (`docs/design/07-technical/telemetry-plan.md`) defines an event for any interaction touched by this PR (`settle_up_screen_viewed`, the record action, the history view), confirm it is still emitted; a converted control that drops a planned event is a contract gap (DC-06 lesson). Reskins must not remove existing analytics calls.
- **Any unbounded on-screen list uses `ListView.builder`** (lazy viewport) — never an eager `ListView(children: [...])` over a provider-driven list (DC-06 lesson). The settlement-history list must stay lazy.
- **Tooling + lessons carried from DC-01..DC-07:**
  - fvm-pinned (Flutter 3.44.3 / Dart 3.12.2). **The `fvm` wrapper may be broken** ("Can't load Kernel binary … expected 127, found 130") — bypass it: `export PATH="/Users/avtanshgupta/fvm/versions/stable/bin:$PATH"` then bare `flutter analyze lib test`, `flutter test`, `dart format .`, and commit/push with bare `git` (the pinned SDK on PATH satisfies the lefthook `dart`/`flutter` + `coverage-check` pre-commit/pre-push hooks; plain `git` otherwise fails "command not found").
  - CI gates on `dart format --set-exit-if-changed .` — run `dart format .` before pushing. `very_good_analysis` is strict: **`directives_ordering`** (imports sorted; `dart:` then `package:` alphabetically; relative imports sorted too), **`prefer_int_literals`** (use `48`, not `48.0`, for a `double`), **`avoid_redundant_argument_values`**, **`lines_longer_than_80_chars`** (incl. `testWidgets` descriptions), **`prefer_const_constructors`**, **`unnecessary_import`**, **`comment_references`** (a `[Symbol]` doc link needs the symbol in scope — use backticks for an out-of-scope reference), **`only_throw_errors`** (throw `Exception`, not a bare `Object`).
  - **The OBT sheet shells consume vertical space.** As in DC-07 (`OBTStepperSheet`), the `OBTSettleUpSheet` grabber/header/footer can push the "Record payment" CTA **below the fold** in the default test viewport — widget tests that tap it must `tester.ensureVisible(finder)` first (a silent missed tap reads as "nothing happened"). Add a small `tapCta` helper as in `add_expense_bottom_sheet_widget_test.dart`.
  - **Reuse the shared test fakes.** Use / extend `test/features/expenses/helpers/fake_services.dart`-style shared no-op fakes (or the settlements feature's existing fakes) rather than redefining `Fake…`/`Noop…` per file (DC-07 review lesson).
  - **`google_fonts` in tests.** Pump widget tests that build `AppTheme` inside `testWidgets`. Animated surfaces (the shimmer skeleton) never settle — pump with `disableAnimations: true` or `settle: false`; reduced motion freezes the controller so `pumpAndSettle` completes. The **success-moment haptic** must fire **exactly once** — assert via a `HapticFeedback`/`SystemChannels.platform` mock-handler capture, and assert it does **not** re-fire on rebuild.
  - **`Closes #120` MUST be plain text, NOT backticked,** in the PR body, or GitHub will not register the closing reference and the Sprint 3 milestone will not advance.
  - **Goldens are authored on `ubuntu-latest` by the DC-13 (`#125`) `golden-a11y-checks` job**, not yet wired. Add the light + dark golden scaffolds for Settle up 23 (incl. the **success-moment** + the inert "Pay via UPI" populated state + error) and Settlement history 24 to `test/golden/dc08_settlements_golden_test.dart` (tagged `golden`), **group-`skip`ped** with the DC-13 rationale, reusing `golden_harness.dart`. Do **not** author/commit golden PNGs locally (macOS bytes are non-deterministic). The load-bearing proof is the per-screen widget tests + the new success-moment behavioural test, which run for real.
  - If `flutter analyze` surfaces unrelated `flutter_contacts` / `PermissionStatus` / package errors, the `.dart_tool/package_config.json` drifted — run `flutter pub get` with the **pinned** SDK; never bare `flutter pub get`.
  - **Merge needs an OWNER approval.** `main`'s ruleset requires an approving review from a write-access reviewer; the session is authenticated as the author `avtanshgupta` (a collaborator, cannot self-approve), and every prior DC PR was **approved by the `avtansh-code` OWNER account** before a squash-merge. A green pipeline shows `mergeStateStatus=BLOCKED` until that approval lands; `gh pr merge --admin` does **not** bypass the ruleset. **iOS (CocoaPods) is the slowest check (~15–20 min)** — wait it out, and note `gh pr checks` can show a **stale "pending"** for iOS after the job has actually succeeded (cross-check with `gh run view <id> --json jobs`). Squash subject convention: `feat(<scope>): … (#<pr>)` (single-token scope).
  - **Handle review comments.** Prior DC PRs received inline review comments (e.g. DC-07: IST-formatter duplication, an unjustified `ignore_for_file`, triplicated fakes). After CI is green, check the PR for inline review comments, address them with focused commits, reply on each thread, and resolve the threads before merge.

────────────────────────────────────────
THE TASK — DC-08 (3 SP)
────────────────────────────────────────

1. **Issue + milestone + roadmap (PM).** DC-08 is issue **#120**, already under the **`Sprint 3`** milestone — confirm it; this PR carries `Closes #120` (plain). Per `.github/shared/milestone-tracking.md`, reconcile the Sprint 3 milestone, and on this PR flip the **DC-07** line in `docs/sprint-zero/next-three-prs.md` from "IN FLIGHT" to **"MERGED (#134, `262107d`)"** and set DC-08 in flight. **Mark the §B Settlements rows done** in `02-conversion-checklist.md`; in `01-coverage-gap.md` confirm rows **23 / 24 stay "covered"**, note the settle-up **rebuild**, and **record the §C item 6 reminder/filter reconcile decision** (the conversion reskins the existing one-tap "Remind" in place; the pre-filled compose / free-text reminder and the friend-group filter remain flagged, not built). (No SRS gap to flag — Settlements is FR-SE-05..09, already specified.)

2. **Architect — the rebuild seam + the success-copy reconcile + the header seam + the Invariant-1/2 contract (review, read-only).** Confirm, for each Settlements surface, the token/type mapping (`ColorScheme` slot / `OBTColors` key — incl. the balance trio, `link`, `metaText` / `AppTheme` radius / `TextTheme` slot / `OBTText.amount` / `.amountHero`) and the loading/empty/error/success shape. **Rule the settle-up rebuild seam** (`OBTSettleUpSheet` wiring: `SettleUpEditing`/`Saving`/`Success`/`Error` ↔ `isLoading`/`isSaving`/`isSuccess`/`amountErrorText`; `onAmountChanged`/`onRecord` ↔ the **frozen** `settle_up_controller`; the in-sheet **success-moment** before dismiss). **Rule the success-copy reconcile** ("high five" AC-1 vs the shipped "Payment recorded" — default to accepting the component copy, do not edit the frozen DC-03 component). **Rule the `settle_up_header.dart` seam** (delete vs retain). **Confirm the Invariant contract**: amounts only via `formatInrFromPaise()` (1); the suggested payment is the **single** projection read (2), never a debt graph, never a client write, recompute server-side only. Return the contracts for the Flutter Dev.

3. **Flutter Dev — REBUILD the settle-up sheet onto `OBTSettleUpSheet`, wire the success moment, resolve the header seam, and reskin settlement history:**
   - **Settle-up sheet (`settle_up_bottom_sheet.dart`, REBUILD)** — replace the bespoke body with `OBTSettleUpSheet`, wiring its props to the existing `settle_up_controller` state machine; on `SettleUpSuccess` render the in-sheet **success moment** (check + single haptic) then dismiss (AC-1); keep `settle_up_screen_viewed` single-fire telemetry, the `source` token, and the record/error snackbar semantics the controller drives; the inert "Pay via UPI" slot ships disabled.
   - **Header seam (`widgets/settle_up_header.dart`)** — per the Architect's ruling, **delete** it (superseded by `OBTSettleUpSheet`'s own header) and remove its test + any imports, OR retain+reskin if ruled otherwise.
   - **Settlement history (`settlement_history_screen.dart`)** — marigold tokens + type; loading `CircularProgressIndicator` → shimmer `OBTSkeleton*`; rows gain the **sent/received direction icon + sign** + **Bricolage tabular** amounts via `formatInrFromPaise()`; empty `Icon` → `OBTEmptyState`; IST dates via `formatIstLongDate()`; keep the list lazy (`ListView.builder`); the reminder/filter stay PM-reconcile flags (existing behaviour reskinned, nothing new built).

4. **QA + Designer — the per-screen "converted" gate (§D):**
   - **All states present** for Settle up 23 (loading = **shimmer skeleton**, settled-guard, populated incl. the inert "Pay via UPI", **success-moment**, error) and Settlement history 24 (loading-skeleton, empty, populated, error).
   - **Light + dark** goldens for **every** state of the heroes (§A.5/§A.6; both required for the money/marigold surfaces 23/24), queued in `test/golden/dc08_settlements_golden_test.dart`, **skipped** pending the DC-13 ubuntu path.
   - **Success-moment behavioural test (rebuild)** — recording a payment transitions to the success moment, the check + **single** haptic fire **exactly once** (and do not re-fire on rebuild); the `OBTAmountInput` paise contract asserts unchanged.
   - **Invariant 1 + 2 proof** — the settlements boundary/PII greps stay green (extend to any net-new on-screen-money surface); a test asserts every on-screen amount routes through `formatInrFromPaise()` (no inline `/100`), the suggested payment is **one** projection **read** (no write path, no debt graph), and the settlement-history direction/sign is derived from `fromUserId`/`toUserId` not a recomputed balance.
   - **Contrast gate** (incl. the **white-on-marigold negative case** asserted to FAIL; ink-on-marigold passes), **balance-trio = colour + icon + label** where a balance surface renders, **every control labelled + 48 dp** (the editable amount, Record payment, the inert "Pay via UPI", history rows), **dynamic type 2.0×** (no clip/overflow at 390 and 320; amounts never truncate), reduced motion freezes the shimmer + the success-moment animation.
   - **Designer (read-only)** reviews the Settlements surfaces against `Phase2 - Components.dc.html` (the settle-up sheet #6) + the Friends-timeline rows in `Phase3c - Friends.dc.html` (the settlement-history row pattern) and returns findings for the orchestrator to action.

5. **Verify:** `flutter analyze lib test` (no issues), `dart format .` (clean), `flutter test` (all pass; the existing settle-up controller / boundary / PII-leak / screen / widget / draft / repository / history suites stay green; per-feature coverage ≥ 70% and overall ≥ 50%). No **existing** controller, provider, projection read, settlement record/create, `onSettlementWrite`, Cloud Function, or domain-model **behaviour** is changed; the only structural change beyond the reskin is the settle-up **rebuild** onto `OBTSettleUpSheet` (visual + success-moment surface) and the `settle_up_header.dart` deletion (if ruled).

────────────────────────────────────────
BRANCH / PR
────────────────────────────────────────

- Branch: `feat/haldi-settlements-conversion` (type-prefixed kebab, repo convention).
- Conventional Commits, single-token scope, ASCII subject ≤ 72 chars — e.g. `feat(settlements): convert the settle-up flow to Haldi`.
- This is a **code** PR — the full PR pipeline must be green (Flutter Lint & Test, Build Android, Build iOS, Integration Tests, Coverage Gate) before merge.
- PR body: map each surface to §B + the DC-08 ACs; tick the invariants checklist (1 + 2 **live** — money via `formatInrFromPaise()`, the `OBTAmountInput` paise contract untouched, the settle-up suggestion a **single** `simplifiedBalances` projection read with recompute server-side; 3 + 4 unchanged); note "settle-up rebuilt onto `OBTSettleUpSheet` with the in-sheet success moment + single haptic; inert 'Pay via UPI' Coming-soon; `settle_up_header.dart` deleted (superseded); settlement history reskinned with sent/received direction + sign + tabular amounts + shimmer skeleton; dates → IST via the core formatter; reminder/filter remain §C PM-reconcile flags"; **`Closes #120` (plain, NOT backticked)**.
- Include the `Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>` trailer.
- Commit `docs/copilot_prompts/sprint_3/9.md` (this prompt) with the PR, per the repo convention.
- **Merge:** after the pipeline is green (wait out the slow iOS build; cross-check `gh run view` for a stale "pending"), the PR will read `BLOCKED` until the **`avtansh-code` OWNER** approves; **handle any inline review comments first** (address + reply + resolve threads); then squash-merge (`--delete-branch`) with subject `feat(settlements): convert the settle-up flow to Haldi (#<pr>)`.

────────────────────────────────────────
DEFINITION OF DONE (this PR)
────────────────────────────────────────

- All **three** DC-08 acceptance criteria pass: the **settle-up sheet (23 ★)** is **rebuilt** onto `OBTSettleUpSheet` showing **one** pre-filled suggested payment (recipient + amount) from the projection, an editable amount, the disabled "Pay via UPI" slot ("Coming soon"), and the **success moment** (check + single haptic) replacing the placeholder, light **and** dark (AC-1); the sheet renders **exactly one** suggested payment and **never a debt graph**, the client **reads** `simplifiedBalances` and never writes it, and amounts render via `formatInrFromPaise()` (AC-2 / Invariants 1 + 2); **settlement history (24)** rows gain a sent/received direction icon + sign and Bricolage tabular amounts, the spinner → skeleton, and the **reminder-compose + friend/group filter are flagged as PM-reconcile items** (not silently dropped) (AC-3).
- The §B **Settlements** rows in `02-conversion-checklist.md` are marked done; `01-coverage-gap.md` keeps rows 23 / 24 "covered", notes the settle-up rebuild, and records the §C item 6 reminder/filter reconcile decision.
- Both Settlements heroes pass the §D per-screen gate (states incl. the success-moment, light/dark goldens, contrast incl. the negative case, balance-trio icon+label where rendered, labelling + 48 dp, dynamic-type 2.0× with no amount truncation, the **once-only haptic**), with light + dark goldens queued in the DC-13 harness; the settlements boundary/PII greps stay green (extended to any net-new on-screen-money surface).
- No hard-coded Haldi hex at call sites; `theme.dart` / `core/theme/*` / the DC-03 component unchanged; no invariant weakened; no controller/provider/projection-read/record/`onSettlementWrite`/CF/domain/telemetry behaviour change; the `OBTAmountInput` paise contract is intact; no feature-local IST duplicate introduced.
- Analyse clean, `dart format` clean, all tests pass (existing settlements suites green unchanged), per-feature coverage ≥ 70% and overall ≥ 50%.
- `docs/sprint-zero/next-three-prs.md` is reconciled (DC-07 → MERGED `#134` / `262107d`; DC-08 status).
- DC-08 issue (#120) closed under the `Sprint 3` milestone; the milestone is reconciled per `.github/shared/milestone-tracking.md`.

End the session summary with: "Next: Sprint 3 PR #10 — DC-09 (Activity & Notifications conversion: Haldi 25 ★ activity feed + 26 in-app banner / pre-permission dialog)."

────────────────────────────────────────
FAILURE / REFUSAL PROTOCOL
────────────────────────────────────────

- If any change would weaken an invariant or the SRS, refuse it, quote the specific invariant / SRS section, and propose a compliant alternative. **In particular:** no inline `paise / 100` or `double` for any on-screen amount (Invariant 1 — use `formatInrFromPaise()`; the `OBTAmountInput` integer-paise contract stays untouched); no write to `simplifiedBalances`, no raw who-owes-whom graph, and **never more than one** suggested payment in the settle-up sheet (Invariant 2 — projection reads only; recompute stays server-side); no per-app messaging path for any reminder (Invariant 3 — system share sheet only).
- Refuse scope creep: no change to the settle-up controller / providers / projection read / record flow / telemetry; do **not** build the pre-filled reminder compose, the free-text reminder, or the friend/group filter (they are §C PM-reconcile flags, not this PR); do **not** edit the frozen DC-03 `OBTSettleUpSheet` to change the success copy (route any genuine copy change through the Architect / issue #128); do **not** convert any other flow (DC-09+); do **not** ship the "Pay via UPI" slot as a live control or add a UPI integration; do not unlock any India-only constraint; no second Firebase project.
- Do not edit `docs/OneByTwo_Requirements_Spec.md` or `lib/app/theme.dart` / `lib/core/theme/*` / the DC-03 components (consume the DC-01..03 foundation; any token/component gap routes to the Architect / issue #128).

Begin by delegating to the **PM** (confirm #120 + the milestone, reconcile the roadmap so DC-07 reads MERGED, the §B Settlements reconciliation, the §C item 6 reminder/filter reconcile decision, and confirm the Haldi-23/24 coverage stays "covered"), then the **Architect** (the settle-up rebuild seam ruling + the success-copy reconcile + the `settle_up_header.dart` seam + the Invariant-1/2 contract + the IST date rule), then the **Flutter Dev** (REBUILD the settle-up sheet onto `OBTSettleUpSheet` + wire the success moment + resolve the header seam + reskin settlement history) with **QA** + **Designer** (the §D per-screen gate incl. the success-moment + once-only-haptic behavioural test + light/dark hero goldens + the contrast negative case + the `formatInrFromPaise()` boundary + the single-projection-read proof + dynamic-type + visual review), and sequence to a single green PR.
