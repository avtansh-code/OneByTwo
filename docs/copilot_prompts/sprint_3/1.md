You are the OneByTwo orchestrator agent. Sprints 1 (Foundation and Authentication) and 2 (Friends and Core Expenses) are delivered and their milestones reconciled. A new **high-fidelity design system** has landed in the repository at `design_handoff_one_by_two/` — the "Direction A — Haldi" visual language (warm marigold palette, Bricolage Grotesque + Hanken Grotesk type, 30 documented screens, light and dark). It is now the authoritative visual framework for the whole app.

The goal of THIS session is to **PLAN a new sprint — the "Design Conversion Sprint"** — that brings the already-built app and every upcoming feature onto this new design system. Per the stakeholder, this new sprint is inserted as **Sprint 3**, and every currently-numbered sprint moves forward by one:

| New № | Theme | Was |
|---|---|---|
| **Sprint 3** | **Design Conversion (this new sprint)** | — (new) |
| Sprint 4 | Groups and Settlements (FR-GR-01..07, FR-SE-05..08) | Sprint 3 |
| Sprint 5 | Notifications, Activity, Dashboard | Sprint 4 |
| Sprint 6 | Polish, Support, Offline, Search | Sprint 5 |
| Sprint 7 | QA, Performance, Release Prep (v1.0 RC) | Sprint 6 |

`Post-v1.0` is unchanged.

**This is a PLANNING session. No application code is reskinned and no screen is converted here.** The output is a ratified plan, a gap analysis, a conversion checklist, the renumbered roadmap, and the forward-alignment of the shifted sprints. The first conversion PR is the *next* session.

────────────────────────────────────────
READ BEFORE STARTING
────────────────────────────────────────

- `.github/copilot-instructions.md`, `.github/shared/invariants.md`, `.github/shared/srs-pointer.md` (SRS = `docs/OneByTwo_Requirements_Spec.md` v1.1, the single source of truth).
- `.github/shared/milestone-tracking.md` (milestone lifecycle — the renumber must follow this), `.github/shared/handoffs.md`, `.github/shared/coding-standards.md`, `.github/shared/decision-log.md` (ADRs through Sprint 2).
- `design_handoff_one_by_two/README.md` (the complete token set, type scale, formatting rules, the 30 screens, motion, accessibility ratios, and the three disabled "extension slots") and the screen references under `design_handoff_one_by_two/screens/` (open `Prototype - Complete` for the canonical end-to-end behaviour).
- `.github/agents/designer.md` — especially the **"Implemented design-system surface (current)"** note: today the app ships **Plus Jakarta Sans + Inter** via `google_fonts` in `lib/app/theme.dart` (`AppTheme.light`/`.dark`, Material 3). The new design replaces these with **Bricolage Grotesque + Hanken Grotesk** and the Haldi palette.
- The current visual surface to be converted: `lib/app/theme.dart` (lines 57–58 set the two font families), `lib/core/widgets/` (`dialogs/`, `inputs/`, `lists/`, `nav/` — the `OBT*` shared widgets), `lib/core/formatters/inr_formatter.dart` (`formatInrFromPaise()`), and all 20 built screens under `lib/features/*/presentation/*screen*.dart`.
- The OLD design docs the new handoff supersedes (visual layer only): `docs/design/02-design-system/{tokens,components,typography-and-formatting,motion-and-interaction}.md`, `docs/design/04-wireframes/*`, `docs/design/05-mockups/*`, `docs/design/06-screen-specs/*`.
- The roadmap docs the renumber touches: `docs/design/08-plan/sprint-sequence.md`, `docs/sprint-zero/next-three-prs.md`, `docs/sprint-zero/sprint-3-kickoff-readiness.md` (currently the **Groups** readiness — it becomes Sprint 4).

────────────────────────────────────────
STANDING CAVEATS (read first)
────────────────────────────────────────

- **The four invariants are untouched and re-affirmed by the new design** (integer paise; `simplifiedBalances` server-written/client-read-only; OS system share sheet only; single Firebase project). The Haldi README restates all four (integer paise stored, "simplified debts only" single suggested payment, OS share sheet only). Any plan item that appears to weaken one is a blocking defect — refuse and cite the invariant.
- **This redesign is VISUAL/UX only.** The data model, Firestore security rules, the simplified-debts algorithm, Cloud Functions, and the backend contracts do **not** change. Do not re-plan `docs/design/03-architecture/*` or `docs/design/07-technical/{firestore-schema,firestore-security-rules,cloud-functions-catalogue}.md`; they remain authoritative.
- **SRS drift.** SRS §6 (User Experience and Design Requirements — §6.2 Visual System tokens/typography, §6.3 Core Screens) describes the *old* visual system. The new Haldi system changes tokens, fonts, and screen count. Because the SRS is the single source of truth, the plan MUST include an `update-srs` proposal (skill: `update-srs`) to ratify the Haldi visual system into §6.2/§6.3 — do not let the design and the SRS diverge. Resolve any design-vs-SRS conflict in the SRS's favour and propose the SRS update rather than silently following the design.
- **Milestone renumber affects live issues.** Sprint 3 currently holds ~13 open issues (Groups/Settlements), Sprint 4 ~4, Sprint 5 ~1, Sprint 6 ~5. Renumbering must re-home every one of these per `.github/shared/milestone-tracking.md` (no issue left unmilestoned; record the rationale as a comment).
- **PR/issue numbering** share one sequential namespace; any planning-output PR lands as the next available number. **CI title-lint** (`.github/workflows/pr.yml`) enforces a single-token conventional-commit scope and an ASCII subject ≤72 chars. **Change-detection**: a docs-only PR (`docs/**`) runs only the title-lint job. **fvm**: commit with `fvm exec git commit` and push with `fvm exec git push`; run any pub/build via the fvm-pinned SDK.

────────────────────────────────────────
PHASE 0 — FRAMING (orchestrator)
────────────────────────────────────────

Commit the session framing in writing:
- This is planning, not conversion. No `lib/**` reskin in this session.
- `design_handoff_one_by_two/` is the authoritative visual system and supersedes the old visual docs (listed above); the backend/data docs and the four invariants are unchanged.
- Decide the output shape (do not pre-commit): a **planning-doc set** under `docs/sprint-zero/` + `docs/design/08-plan/` (+ `docs/audits/` style gap report), and/or a thin PR carrying the renumbered roadmap and milestone reconciliation. Let the work dictate which.

────────────────────────────────────────
PHASE 1 — DESIGN-COVERAGE GAP ANALYSIS (designer lead, pm consulting)
────────────────────────────────────────

Map the 30 Haldi screens against (a) the app's currently-built screens and (b) the SRS FRs. Produce a **coverage matrix** with three columns: *Haldi screen №* · *app screen / FR* · *status (covered / built-not-yet-designed / designed-not-yet-built / mismatch)*.

Explicitly produce the list the stakeholder asked for: **every current-or-required screen that the 30-screen handoff does NOT cover**, so designs can be commissioned. Probable candidates to verify, not assume: the **change-phone OTP flow** (`lib/features/profile/presentation/change_phone_screen.dart`, FR-PR-02 — the handoff locks +91 in Profile edit and does not show a change-number flow), any onboarding/search/context-picker surfaces, and any internal placeholder screens (e.g. `profile_placeholder_screen.dart`). The conversion plan carries an explicit **dependency** on these missing designs arriving.

────────────────────────────────────────
PHASE 2 — IMPLEMENTATION-CONFORMANCE GAP ANALYSIS (designer + flutter-dev consulting)
────────────────────────────────────────

For every built screen (the 20 files under `lib/features/*/presentation/`) and every shared widget in `lib/core/widgets/`, compare the current implementation to the Haldi spec for that screen/component and classify each as **conform / reskin (tokens+type only) / rebuild (layout change)**. Output the **conversion checklist** — one row per screen and per shared component — with the classification, the Haldi screen reference, and a rough effort. This checklist is the backbone of the Sprint 3 backlog.

────────────────────────────────────────
PHASE 3 — TOKEN AND COMPONENT FOUNDATION PLAN (designer + architect)
────────────────────────────────────────

Plan (do not implement) the foundation that every screen depends on:
- `lib/app/theme.dart` migration to the Haldi tokens: full semantic colour set (light+dark), `primary #E0922E` with **ink** `onPrimary #2A211B` (not white), the balance trio (colour+icon+label, never colour alone), the 8-hue colour-blind-safe category palette, radii/elevation/soft-warm shadows, and motion (200–300ms, reduced-motion fallback).
- Typography swap to **Bricolage Grotesque** (display/headings, tabular figures for amounts) + **Hanken Grotesk** (text/UI) via `google_fonts`, mapped to the Haldi type roles.
- Shared-widget impact: which `OBT*` widgets reskin vs change, and which new components the catalogue requires (skeleton loaders, balance pill, category chip/tile, 3-step bottom sheet, etc.).
- An **ADR** in `.github/shared/decision-log.md` recording the adoption of the Haldi visual system, what it supersedes, and the integer-paise/`formatInrFromPaise` rupee-display rule it preserves.

────────────────────────────────────────
PHASE 4 — RENUMBER THE ROADMAP (pm + devops)
────────────────────────────────────────

- Update `docs/design/08-plan/sprint-sequence.md` and the table in `.github/shared/milestone-tracking.md` to insert Sprint 3 = Design Conversion and shift Sprints 3–6 → 4–7 (themes per the table above).
- GitHub milestones (DevOps Bash, `gh`): create **Sprint 7**, rename/re-home so each existing open issue lands on its new number per `.github/shared/milestone-tracking.md` (Groups/Settlements → Sprint 4, etc.), and comment the rationale on each moved issue. No issue left unmilestoned.
- Re-home `docs/sprint-zero/sprint-3-kickoff-readiness.md` (Groups) to the Sprint 4 identity and reconcile `docs/sprint-zero/next-three-prs.md` so the written roadmap and the milestone state never drift.

────────────────────────────────────────
PHASE 5 — AUTHOR THE SPRINT 3 (DESIGN CONVERSION) PLAN (pm lead, designer + qa consulting)
────────────────────────────────────────

Produce the Sprint 3 plan (mirror the structure of `docs/sprint-zero/sprint-1-plan.md` / `sprint-2-plan.md`): goal, epics/stories with acceptance criteria (skills: `new-user-story`, `refine-acceptance-criteria`), SP estimates, Definition of Ready/Done, sprint-end demo, risks, and the sequencing/critical path. Suggested epic shape:
1. **Token + type foundation** (`theme.dart` → Haldi; Bricolage/Hanken) — blocks everything else.
2. **Shared component library** (`OBT*` reskin + new components, all states: populated/empty/loading-skeleton/error).
3. **Per-flow screen conversion** (Auth, Home, Friends, Expenses, Settlements, Activity, Profile) — one story per flow, each citing its Haldi screen(s) and the conversion-checklist rows.
4. **Dark mode parity** for every converted hero screen.
5. **Accessibility re-verification** against the new palette (the README gives the verified AA/AAA contrast pairings) + dynamic type to 2.0×.
6. **Visual-regression / golden-test** harness so conversion is provable and non-regressing.

Each story must reference the exact Haldi screen and tokens, and must restate the integer-paise display rule (`formatInrFromPaise`) and "simplified debts only" UI rule where relevant.

────────────────────────────────────────
PHASE 6 — FORWARD-ALIGN THE SHIFTED SPRINTS (pm + designer + architect)
────────────────────────────────────────

Ensure no future feature is built in the old design. For each shifted sprint — **especially Sprint 4 Groups and Settlements** (whose Groups UI is not built yet) — annotate its stories/specs to cite the Haldi screens (Groups list/create/detail, the **Balances** tab with the minimum-payments set, invite via OS share sheet, etc.) and the new component library, and switch their "design artefacts required" references from the old `docs/design/0{2,4,5,6}-*` to the `design_handoff_one_by_two/` equivalents. The deliverable is that every downstream sprint's Definition of Ready now points at the Haldi system.

────────────────────────────────────────
PHASE 7 — QA AND TEST STRATEGY (qa)
────────────────────────────────────────

Define how conversion is verified: golden/screenshot regression per converted screen (light+dark), automated contrast/a11y checks for the AA pairings in the README, dynamic-type 2.0× no-clipping checks, and a per-screen "converted" acceptance gate. If a new CI job is needed (e.g. golden tests), plan it via the `add-github-actions-job` skill (do not add it in this planning session).

────────────────────────────────────────
DEFINITION OF DONE (this planning session)
────────────────────────────────────────

- A ratified **Sprint 3 (Design Conversion) plan** (stories, AC, SP, DoR/DoD, demo, risks, critical path).
- A **design-coverage gap report** including the explicit list of screens needing newly-commissioned designs (with the conversion plan's dependency on them noted).
- A **conversion checklist** (every built screen + shared component, classified conform/reskin/rebuild, each tied to a Haldi screen).
- The **renumbered roadmap**: `sprint-sequence.md` + `milestone-tracking.md` updated; GitHub milestones renumbered with every open issue re-homed and rationale-commented; `next-three-prs.md` and the Groups readiness doc reconciled.
- An **ADR** adopting the Haldi system and an **`update-srs` proposal** for SRS §6.2/§6.3.
- **Forward-alignment annotations** on the shifted sprints (Sprint 4 first).
- **No `lib/**` reskin** in this session. End the summary with: "Next: Sprint 3 PR #1 — design-token foundation (`lib/app/theme.dart` → Haldi tokens + Bricolage/Hanken)."

────────────────────────────────────────
FAILURE / REFUSAL PROTOCOL
────────────────────────────────────────

- If any planned item would violate an invariant or the SRS, refuse it, quote the specific invariant/SRS section, and propose a compliant alternative (per the Refusal Protocol in `.github/copilot-instructions.md`).
- Where the new design and the SRS conflict, the SRS wins — capture the delta as the `update-srs` proposal rather than following the design silently.
- Do not widen this session into actual conversion work. If an agent proposes "just reskin the theme now to save time," decline and file it as Sprint 3 PR #1.

Begin by delegating to the designer (Phase 1 coverage matrix) and, in parallel, to the pm (Phase 4 renumber prep), then sequence Phases 2–7.
