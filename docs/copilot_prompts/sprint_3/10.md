You are the OneByTwo orchestrator agent. Sprint 3 — **Design Conversion (the "Direction A — Haldi" visual system, `design_handoff_one_by_two/`)** — is the active sprint. Nine PRs have merged on `main`: **DC-01 (design-token + type foundation) as PR #126** (squash `d9205d8`), **DC-02 (the six `OBT*` shared-widget reskins) as PR #127** (squash `aca8582`), **DC-03 (the eleven new Haldi shared components) as PR #129** (squash `b0ce9a6`), the **DC-03/06 scope doc as PR #130** (`bd27c38`), **DC-04 (Auth & onboarding) as PR #131** (squash `0391bf2`), **DC-05 (Home dashboard hero) as PR #132** (squash `1c3e194`), **DC-06 (Friends flow + net-new Friend-history) as PR #133** (squash `006a0fb`), **DC-07 (Expenses flow) as PR #134** (squash `262107d`), and **DC-08 (Settlements flow: the settle-up `OBTSettleUpSheet` rebuild + the settlement-history reskin) as PR #135** (squash `b55e9fb`). The Haldi `ColorScheme` (marigold `primary`, **ink** `onPrimary`), the `OBTColors` `ThemeExtension` (incl. the **balance trio** `balancePositive` / `balanceNegative` / `balanceZero`, the **8-hue `category` map**, `OBTColors.heroShadow` / `rowShadow`, `OBTColors.link`, `OBTColors.warning`, `OBTColors.metaText`), the `AppTheme` radius (`radiusChipInput` 12 / `radiusButton` 16 / `radiusCard` 20 / `radiusSheet` 28 / `radiusFull` 999 / `radiusMedium` 12) / soft-warm-shadow / motion tokens, the Bricolage Grotesque / Hanken Grotesk `TextTheme`, the `OBTText.amount` / `OBTText.amountHero` helpers, the **single core IST date formatter** `lib/core/formatters/ist_date_formatter.dart`, the six reskinned `OBT*` widgets, the **eleven DC-03 shared components** (incl. `OBTActivityRow`, the `OBTSkeleton*` set, `OBTEmptyState`), and the **converted Auth / Home / Friends / Expenses / Settlements flows** are all on `main`.

The goal of THIS session is to deliver **Sprint 3 PR #10 — DC-09: convert the Activity & Notifications surfaces to Haldi (light + dark)** (issue **#121**). This is the **sixth per-flow screen-conversion PR (3 SP)**. It wires the DC-01..DC-03 primitives into the **Activity feed (Haldi 25 ★ — a hero)** and the **Notifications surfaces (Haldi 26 — the foreground in-app banner + the "Stay in the loop" pre-permission dialog + the lifecycle host)**. Work **only** the Activity + Notifications surfaces; Profile (DC-10..12), the DC-13 golden/a11y CI job, and the rest are later.

**The load-bearing item is AC-3: NO hard-coded Haldi hex may remain.** The in-app banner currently hard-codes `#2A9D8F` (success) and `#F4A261` (secondary) at `lib/features/notifications/presentation/widgets/in_app_notification_banner.dart` lines 209–213; these MUST be re-pointed to Haldi tokens, and a grep contract must assert that no `Color(0x…)` literal survives in the converted notifications presentation (the negative case — a literal hex fails review). All colour flows from `Theme.of(context).colorScheme` / `OBTColors`; all type from `theme.textTheme` / `OBTText`; all radii from the `AppTheme` scale. `theme.dart` / `core/theme/*` / the DC-03 components are the single source of truth — consume, do not edit (any token/component gap routes to the Architect / issue #128).

**Invariants 1 + 2 are present but light here.** Any amount in a feed row renders via `formatInrFromPaise()` (Invariant 1) — this is already handled inside the frozen `OBTActivityRow` (the trailing amount uses `formatInrFromPaise` + `OBTText.amount`), so the screen reskin introduces no new money path; the `activity_boundary_contract_test.dart` + `notifications_boundary_contract_test.dart` greps MUST stay green. No surface reads or writes `simplifiedBalances` (Invariant 2). **Invariants 3 + 4 must stay untouched** — no per-app messaging path, no second Firebase project. The OS lock-screen banner is **system-rendered and out of scope** (Haldi 26 covers only the foreground in-app surfaces).

**This is an IMPLEMENTATION session** (it changes `lib/**`). The full PR pipeline runs (analyse, format, unit + widget tests, Build Android, Build iOS, Integration Tests, Coverage Gate).

────────────────────────────────────────
READ BEFORE STARTING
────────────────────────────────────────

- `.github/copilot-instructions.md`, `.github/shared/invariants.md`, `.github/shared/srs-pointer.md` (SRS = `docs/OneByTwo_Requirements_Spec.md` v1.1), `.github/shared/coding-standards.md`, `.github/shared/handoffs.md`, `.github/shared/milestone-tracking.md`, `.github/shared/test-strategy.md`.
- **The authoritative spec for this PR:**
  - `docs/sprint-zero/sprint-3-plan.md` — **story DC-09** (the User Story, the **three** Acceptance Criteria — AC-1 the activity-feed reskin via `OBTActivityRow` with the static-skeleton → shimmer + the empty `Icon` → empty-state scaffold + rows still deep-linking unchanged in light **and** dark; AC-2 the in-app banner + pre-permission dialog hex re-point with the lifecycle host **conforming** (no visual change); AC-3 the **no-hard-coded-hex negative case**), the Definition of Done, and the Invariant-Compliance clause).
  - `docs/audits/design-conversion/02-conversion-checklist.md` **§B → "Activity"** (`activity_feed_screen.dart` 25 ★ reskin; `widgets/activity_feed_skeleton.dart` 25 ★ reskin → shimmer) and **§B → "Notifications"** (`notifications_lifecycle_host.dart` 26 **conform**; `widgets/in_app_notification_banner.dart` 26 reskin — re-point the hex, card radius → 20, black → warm shadow, type → Hanken; `pre_permission_dialog.dart` 26 reskin — marigold tokens, dialog radius → card 20, CTA → onPrimary ink + radius 16, replace the literal `fontSize` with type slots, bell → brand illustration optional). Mark the §B Activity + Notifications rows done on merge (add a `> **Status — … converted (DC-09 …)**` blockquote mirroring the DC-08 one).
  - `docs/audits/design-conversion/04-qa-test-strategy.md` — **§A.6** (Activity **25 is a hero** → light **and** dark goldens for **every** state: loading-skeleton, empty, populated, error; the in-app banner 26 is a **non-hero** converted surface → **light** goldens), the per-screen "converted" acceptance gate, the contrast gate incl. the **white-on-marigold negative case**, labelling + 48 dp, dynamic type to 2.0× + reduced motion.
  - `docs/audits/design-conversion/01-coverage-gap.md` — §B rows **25 (Activity feed)** and **26 (Push notifications)** are **"covered"**; confirm they stay "covered" and note the reskin. §A note: the OS lock-screen banner is system-rendered and not an app surface.
  - `.github/shared/decision-log.md` → **ADR-0024** (scope: visual layer only).
  - `design_handoff_one_by_two/screens/Phase3* ` — there is no standalone Activity/Notifications `Phase3*` mockup file (the handoff `Phase3*` files are Auth / Home / Friends / Groups / Expenses / Profile / Change-Phone); the **activity feed (25)** follows the established Haldi row/token pattern and `OBTActivityRow` (already built), and the **in-app banner + pre-permission dialog (26)** follow the foundation `Phase2 - Components.dc.html` card/shadow/dialog tokens. If a richer Activity/Notifications mockup is needed, that is an Architect/Designer gap to flag, not a blocker.
- **The foundation now on `main` (consume — read, do not change):** `lib/app/theme.dart`, `lib/core/theme/obt_colors.dart` (the balance trio + `warning` + `link` + `metaText`), `lib/core/theme/obt_text.dart`, `lib/core/formatters/inr_formatter.dart`, and the DC-03 components the surfaces consume:
  - **activity row** — `lib/core/widgets/lists/obt_activity_row.dart` (`OBTActivityRow` — already renders the leading icon tile, primary/secondary text, the deep-link tap target, and the optional trailing amount via `formatInrFromPaise()` + `OBTText.amount`; the activity feed already uses it, so AC-1's row reskin is satisfied by the component — do **not** re-implement the row).
  - **skeleton set** — `lib/core/widgets/feedback/obt_skeleton.dart` (`OBTSkeleton` / `OBTSkeletonCircle` / `OBTSkeletonRow` / `OBTSkeletonList` — shimmer that freezes under reduced motion); **empty-state** — `lib/core/widgets/feedback/obt_empty_state.dart` (`OBTEmptyState`).
  Reuse the **test harness** `test/support/widget_test_harness.dart` (`pumpThemed`, `expectAllInteractiveNodesLabelled`, `expectAllTapTargetsMeetMinSize`) and the **golden harness** `test/golden/golden_harness.dart`. See the DC-08 reskin gate `test/features/settlements/settlements_haldi_reskin_test.dart` + golden scaffold `test/golden/dc08_settlements_golden_test.dart` + the shared fakes in `test/features/settlements/helpers/fake_services.dart` for the §D-gate pattern (hero states light+dark, contrast incl. the white-on-marigold negative case, 48 dp + labelling, dynamic type 2.0× at 390/320, reduced motion, group-`skip`ped goldens, shared no-op fakes rather than per-file redefinitions).
- **The surfaces to convert (read each before editing):** `lib/features/activity/presentation/activity_feed_screen.dart` (25 ★), `lib/features/activity/presentation/widgets/activity_feed_skeleton.dart` (25 ★ — static grey blocks → shimmer, sourced from the shared `OBTSkeleton*` set; the file's own comment defers shimmer — this PR resolves it), `lib/features/notifications/presentation/widgets/in_app_notification_banner.dart` (26 — the hex re-point + radius/shadow/type), `lib/features/notifications/presentation/pre_permission_dialog.dart` (26 — tokens/radius/type/CTA), `lib/features/notifications/presentation/notifications_lifecycle_host.dart` (26 — **conform**; it owns no visual surface of its own, it renders the banner + dialog below, so the only change here is whatever the banner/dialog adoption requires). Read the providers/domain (`activity_feed_provider.dart`, `relative_timestamp_formatter.dart`, `activity_feed_item.dart`, `notification_permission_controller.dart`, `notification_payload.dart`, `deep_link_handler.dart`, `notification_deep_links.dart`) **only** to confirm you are NOT changing behaviour, the deep-link routing contract, the permission flow, or telemetry.
- Do **not** read or re-plan the backend/data docs; FCM, the deep-link routing, and the activity-feed read model are unchanged.

────────────────────────────────────────
STANDING CAVEATS (read first)
────────────────────────────────────────

- **AC-3 is the load-bearing negative case.** No hard-coded Haldi hex may remain in the converted notifications presentation. Re-point `#2A9D8F` (success — the `settlementReceived` icon) and `#F4A261` (secondary — the `reminder` icon) to Haldi tokens (the **Architect rules the exact mapping**: the natural candidates are `OBTColors.balancePositive` for the success teal-green and `theme.colorScheme.secondary` / `OBTColors.warning` for the saffron — confirm against the foundation token set). Add a grep contract (extend `notifications_boundary_contract_test.dart`) that asserts **no `Color(0x…)` literal** survives in `in_app_notification_banner.dart` / `pre_permission_dialog.dart`. A literal hex left at a call site is a blocking defect — refuse and cite AC-3.
- **The lifecycle host CONFORMS (no visual change).** `notifications_lifecycle_host.dart` renders the banner + dialog below it and owns no visual surface; do not introduce a visual treatment there. Its behaviour (overlay insertion, auto-dismiss timer, deep-link forwarding) is frozen.
- **`OBTActivityRow` is FROZEN — do not re-implement the row.** The activity feed already renders rows via the DC-03 `OBTActivityRow`. AC-1's "rows reskin via `OBTActivityRow`" is already satisfied; the screen-level work is the **loading skeleton** (static → shimmer), the **empty state** (hand-rolled `Icon` + `FilledButton.icon` → `OBTEmptyState`), the **error state** reskin (the `onSurface.withValues(alpha:)` greys → `OBTColors.metaText`), and the marigold tokens/type. The row tap still deep-links unchanged (FR-AC-02) — the `NotificationDeepLinks.navigate` contract and the `activity_item_tapped` / `activity_feed_viewed` / `activity_feed_refreshed` / `activity_feed_error` telemetry are behaviour-frozen.
- **The skeleton becomes a shimmer.** `activity_feed_skeleton.dart` renders 5 static grey rows; rebuild it on the shared `OBTSkeletonList` / `OBTSkeletonRow` (shimmer that freezes under reduced motion). Keep the `Key('activity_feed_skeleton')` and the `liveRegion` "Loading…" announcement so the screen test + a11y stay green.
- **Relative timestamps stay relative.** The activity feed labels rows with `formatRelativeTimestamp` (e.g. "2h ago") via `relative_timestamp_formatter.dart` (which already uses the fixed IST offset for day boundaries). Do **not** swap this for the absolute `formatIstLongDate()`; the relative formatter is correct for this surface. No new feature-local IST duplicate.
- **Behaviour is UNCHANGED — this is a reskin (+ the skeleton shimmer rebuild + the hex re-point).** No change to the activity provider/read model, the deep-link routing, the permission controller, the FCM token service, the banner auto-dismiss/swipe-dismiss, or any telemetry. Existing activity / notifications / deep-link / permission / banner / dialog / lifecycle suites stay green unchanged (update only assertions that legitimately encode an old token literal, the removed static-skeleton structure, the removed hand-rolled empty `Icon`, or a re-pointed hex).
- **The in-app banner reskin.** Card `borderRadius` 16 → `AppTheme.radiusCard` (20); the hard `Colors.black.withValues(alpha: 0.12)` shadow → the warm `OBTColors.rowShadow` (light) / outline-border treatment (dark); the `onSurface.withValues(alpha:)` body greys → `OBTColors.metaText`; type → Hanken via `theme.textTheme`; the per-type icon colour via tokens (no hex). Keep the `Dismissible` swipe-up + the `Semantics(button: true, liveRegion: true)` label.
- **The pre-permission dialog reskin.** `Dialog` `borderRadius` 24 → `AppTheme.radiusCard` (20); the CTA `borderRadius` 12 → `AppTheme.radiusButton` (16) with `onPrimary` **ink** (never white); replace the literal `TextStyle(fontSize: 16, …)` on the CTA with a `theme.textTheme` slot; the `onSurface.withValues(alpha:)` greys → `OBTColors.metaText`; the bell stays `colorScheme.primary` (a brand illustration is optional, not required). Keep the explicit `Semantics(header: true)` heading + the two button labels (a11y AC-20).
- **All colour via `Theme.of(context).colorScheme` / `OBTColors`; all type via `theme.textTheme` / `OBTText`; all radii via the `AppTheme` scale.** No hard-coded Haldi hex at call sites (AC-3). Any token gap routes to the Architect / issue #128.
- **No new telemetry event is silently dropped.** If `telemetry-plan.md` defines an event for any interaction touched by this PR (the feed view/tap/refresh/error, the banner tap/dismiss, the permission prompt), confirm it is still emitted; a converted control that drops a planned event is a contract gap (DC-06 lesson).
- **Any unbounded on-screen list uses `ListView.builder`** (lazy viewport). The activity feed list is already `ListView.builder` — keep it lazy.
- **Tooling + lessons carried from DC-01..DC-08:**
  - fvm-pinned (Flutter 3.44.3 / Dart 3.12.2). **The `fvm` wrapper may be broken** ("Can't load Kernel binary … expected 127, found 130") — bypass it: `export PATH="/Users/avtanshgupta/fvm/versions/stable/bin:$PATH"` then bare `flutter analyze lib test`, `flutter test`, `dart format .`, and commit/push with bare `git` (the pinned SDK on PATH satisfies the lefthook `dart`/`flutter` + `coverage-check` pre-commit/pre-push hooks; plain `git` otherwise fails "command not found").
  - CI gates on `dart format --set-exit-if-changed .` — run `dart format .` before pushing. `very_good_analysis` is strict: **`directives_ordering`**, **`prefer_int_literals`**, **`avoid_redundant_argument_values`**, **`lines_longer_than_80_chars`** (incl. `testWidgets` descriptions), **`prefer_const_constructors`**, **`unnecessary_import`**, **`comment_references`** (a `[Symbol]` doc link needs the symbol in scope — use backticks for an out-of-scope reference), **`only_throw_errors`** (throw `Exception`, not a bare `Object`).
  - **Animated surfaces never settle.** The shimmer skeleton + the banner slide animation never settle — pump widget tests with `disableAnimations: true` (reduced motion freezes the controller so `pumpAndSettle` completes) or pump a single frame / `settle: false`. Build `AppTheme` inside `testWidgets` so the `google_fonts` runtime fetch is absorbed by the test zone.
  - **Reuse the shared test fakes.** Add / extend a `test/features/activity/helpers/` (or `notifications/helpers/`) shared no-op fakes file rather than redefining `Fake…`/`Noop…` per file (the DC-07/DC-08 review lesson).
  - **`Closes #121` MUST be plain text, NOT backticked,** in the PR body, or GitHub will not register the closing reference and the Sprint 3 milestone will not advance.
  - **Goldens are authored on `ubuntu-latest` by the DC-13 (`#125`) `golden-a11y-checks` job**, not yet wired. Add the light + dark golden scaffolds for Activity 25 (loading-skeleton, empty, populated, error) and the **light** scaffolds for the in-app banner 26 (per type / state) to `test/golden/dc09_activity_golden_test.dart` (tagged `golden`), **group-`skip`ped** with the DC-13 rationale, reusing `golden_harness.dart`. Do **not** author/commit golden PNGs locally (macOS bytes are non-deterministic). The load-bearing proof is the per-screen widget tests + the new no-hex grep, which run for real.
  - If `flutter analyze` surfaces unrelated `flutter_contacts` / `PermissionStatus` / package errors, the `.dart_tool/package_config.json` drifted — run `flutter pub get` with the **pinned** SDK; never bare `flutter pub get`.
  - **Merge needs an OWNER approval.** `main`'s ruleset requires an approving review from a write-access reviewer; the session is authenticated as the author `avtanshgupta` (a collaborator, cannot self-approve), and every prior DC PR was **approved by the `avtansh-code` OWNER account** before a squash-merge. A green pipeline shows `mergeStateStatus=BLOCKED` until that approval lands; `gh pr merge --admin` does **not** bypass the ruleset. **iOS (CocoaPods) is the slowest check (~15–20 min)** — wait it out, and note `gh pr checks` can show a **stale "pending"** for iOS after the job has actually succeeded (cross-check with `gh run view <id> --json jobs`). Squash subject convention: `feat(<scope>): … (#<pr>)` (single-token scope).
  - **Handle review comments.** After CI is green, check the PR for inline review comments, address them with focused commits, reply on each thread, and resolve the threads before merge.

────────────────────────────────────────
THE TASK — DC-09 (3 SP)
────────────────────────────────────────

1. **Issue + milestone + roadmap (PM).** DC-09 is issue **#121**, already under the **`Sprint 3`** milestone — confirm it; this PR carries `Closes #121` (plain). Per `.github/shared/milestone-tracking.md`, reconcile the Sprint 3 milestone, and on this PR flip the **DC-08** line in `docs/sprint-zero/next-three-prs.md` from "IN FLIGHT" to **"MERGED (#135, `b55e9fb`)"** and set DC-09 in flight. **Mark the §B Activity + Notifications rows done** in `02-conversion-checklist.md` (add a `> **Status — Activity + Notifications converted (DC-09 …)**` blockquote); in `01-coverage-gap.md` confirm rows **25 / 26 stay "covered"** and note the reskin. (No SRS gap to flag — Activity/Notifications are FR-AC-01..03/05, already specified.)

2. **Architect — the token-mapping + the no-hex contract + the conform/Invariant-1 ruling (review, read-only).** Confirm, for each surface, the token/type mapping (`ColorScheme` slot / `OBTColors` key / `AppTheme` radius / `TextTheme` slot) and the loading/empty/error shape. **Rule the hex re-point** — the exact Haldi token for `#2A9D8F` (success) and `#F4A261` (secondary), confirming against the foundation token set (no new token; if a gap, route to #128). **Rule the lifecycle host conformance** (no visual surface). **Confirm the Invariant contract:** the feed-row amount is the only money path and it stays inside the frozen `OBTActivityRow` via `formatInrFromPaise()` (1); no surface reads/writes `simplifiedBalances` (2). Return the contracts for the Flutter Dev.

3. **Flutter Dev — reskin the Activity feed + the Notifications surfaces:**
   - **Activity feed (`activity_feed_screen.dart`)** — loading `ActivityFeedSkeleton` → shimmer; empty hand-rolled `Icon` + `FilledButton.icon` → `OBTEmptyState`; error state greys → `OBTColors.metaText`; marigold tokens + type; rows stay on `OBTActivityRow`; the list stays lazy; deep-link + telemetry behaviour-frozen.
   - **Activity skeleton (`widgets/activity_feed_skeleton.dart`)** — rebuild the static grey rows on the shared `OBTSkeletonList` / `OBTSkeletonRow` (shimmer, reduced-motion freeze); keep the `Key('activity_feed_skeleton')` + the `liveRegion` announcement.
   - **In-app banner (`widgets/in_app_notification_banner.dart`)** — re-point the hard-coded hex to Haldi tokens (AC-2/AC-3); card radius → `radiusCard`; black → warm shadow; body greys → `metaText`; type → Hanken; keep swipe-up dismiss + the `Semantics` label + the auto-dismiss timer.
   - **Pre-permission dialog (`pre_permission_dialog.dart`)** — marigold tokens; dialog radius → `radiusCard`; CTA radius → `radiusButton` + `onPrimary` ink; replace the literal `fontSize` with a type slot; greys → `metaText`; keep the `Semantics(header: true)` heading + button labels.
   - **Lifecycle host (`notifications_lifecycle_host.dart`)** — **conform** only (no visual change beyond what the banner/dialog adoption requires).

4. **QA + Designer — the per-screen "converted" gate (§D):**
   - **All states present** for Activity 25 (loading = **shimmer skeleton**, empty = **`OBTEmptyState`**, populated, error) and the in-app banner 26 (per-type populated; the pre-permission dialog states).
   - **Light + dark** goldens for **every** state of the Activity hero (§A.6), and **light** goldens for the non-hero banner 26, queued in `test/golden/dc09_activity_golden_test.dart`, **skipped** pending the DC-13 ubuntu path.
   - **No-hard-coded-hex proof (AC-3)** — extend `notifications_boundary_contract_test.dart` (or add an activity/notifications hex grep) asserting **no `Color(0x…)` literal** survives in the converted banner/dialog; the existing activity/notifications boundary + deep-link + permission suites stay green.
   - **Deep-link unchanged** — a test asserts a row tap still routes via `NotificationDeepLinks.navigate` (FR-AC-02) and the `activity_item_tapped` telemetry still fires; the banner tap still forwards its payload.
   - **Contrast gate** (incl. the **white-on-marigold negative case** asserted to FAIL; ink-on-marigold passes), **every control labelled + 48 dp** (the empty-state CTA, the dialog Enable / Not now, the banner tap target), **dynamic type 2.0×** (no clip/overflow at 390 and 320), reduced motion freezes the shimmer + the banner slide.
   - **Designer (read-only)** reviews the Activity feed against the `OBTActivityRow` row pattern and the banner/dialog against the foundation `Phase2 - Components.dc.html` card/shadow/dialog tokens, and returns findings for the orchestrator to action.

5. **Verify:** `flutter analyze lib test` (no issues), `dart format .` (clean), `flutter test` (all pass; the existing activity / notifications / deep-link / permission / banner / dialog / lifecycle suites stay green; per-feature coverage ≥ 70% and overall ≥ 50%). No **existing** provider, read-model, deep-link, permission, FCM, or telemetry **behaviour** is changed; the only structural changes beyond the reskin are the skeleton shimmer rebuild and the hex re-point.

────────────────────────────────────────
BRANCH / PR
────────────────────────────────────────

- Branch: `feat/haldi-activity-notifications-conversion` (type-prefixed kebab, repo convention).
- Conventional Commits, single-token scope, ASCII subject ≤ 72 chars — e.g. `feat(activity): convert the activity and notification surfaces to Haldi` (or scope `notifications` — pick one single token; trim to ≤ 72 chars).
- This is a **code** PR — the full PR pipeline must be green (Flutter Lint & Test, Build Android, Build iOS, Integration Tests, Coverage Gate) before merge.
- PR body: map each surface to §B + the DC-09 ACs; tick the invariants checklist (1 + 2 present — feed-row amounts via `formatInrFromPaise()` inside the frozen `OBTActivityRow`, no projection read/write; 3 + 4 unchanged); note "activity feed reskinned (static skeleton → shimmer, empty `Icon` → `OBTEmptyState`, rows stay on `OBTActivityRow`, deep-link unchanged); in-app banner + pre-permission dialog reskinned with the **hard-coded hex re-pointed to Haldi tokens** + radius/shadow/type; lifecycle host conforms; **no hard-coded hex remains** (AC-3 grep)"; **`Closes #121` (plain, NOT backticked)**.
- Include the `Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>` trailer.
- Commit `docs/copilot_prompts/sprint_3/10.md` (this prompt) with the PR, per the repo convention.
- **Merge:** after the pipeline is green (wait out the slow iOS build; cross-check `gh run view` for a stale "pending"), the PR will read `BLOCKED` until the **`avtansh-code` OWNER** approves; **handle any inline review comments first** (address + reply + resolve threads); then squash-merge (`--delete-branch`) with subject `feat(activity): convert the activity and notification surfaces to Haldi (#<pr>)`.

────────────────────────────────────────
DEFINITION OF DONE (this PR)
────────────────────────────────────────

- All **three** DC-09 acceptance criteria pass: the **activity feed (25 ★)** is reskinned via `OBTActivityRow` with the **static skeleton → shimmer**, the empty `Icon` → the **empty-state scaffold**, rows still deep-linking unchanged, in light **and** dark (AC-1); the **in-app banner + pre-permission dialog (26)** re-point the **hard-coded hex** (`#2A9D8F` success, `#F4A261` secondary) to Haldi tokens and the lifecycle host **conforms** with no visual change (AC-2); **no hard-coded hex remains** — all colour flows from tokens, asserted by a grep (AC-3, the negative case).
- The §B **Activity + Notifications** rows in `02-conversion-checklist.md` are marked done; `01-coverage-gap.md` keeps rows 25 / 26 "covered" and notes the reskin.
- Both the Activity hero and the banner pass the §D per-screen gate (states incl. the shimmer skeleton + the `OBTEmptyState`, light/dark goldens for 25 + light goldens for 26, contrast incl. the negative case, labelling + 48 dp, dynamic-type 2.0×, reduced motion, deep-link unchanged), with goldens queued in the DC-13 harness; the activity/notifications boundary + deep-link + permission greps stay green; the **no-hex grep is added and green**.
- No hard-coded Haldi hex at call sites; `theme.dart` / `core/theme/*` / the DC-03 components unchanged; no invariant weakened; no provider / read-model / deep-link / permission / FCM / telemetry behaviour change.
- Analyse clean, `dart format` clean, all tests pass (existing activity/notifications suites green unchanged), per-feature coverage ≥ 70% and overall ≥ 50%.
- `docs/sprint-zero/next-three-prs.md` is reconciled (DC-08 → MERGED `#135` / `b55e9fb`; DC-09 status).
- DC-09 issue (#121) closed under the `Sprint 3` milestone; the milestone is reconciled per `.github/shared/milestone-tracking.md`.

End the session summary with: "Next: Sprint 3 PR #11 — DC-10 (Profile & Settings cluster conversion: Haldi 27 profile/edit-profile + 28 notification preferences + 29 contact-support + 30 delete-account + the Phase3g change-phone reskin; delete `profile_placeholder_screen.dart`)."

────────────────────────────────────────
FAILURE / REFUSAL PROTOCOL
────────────────────────────────────────

- If any change would weaken an invariant or the SRS, refuse it, quote the specific invariant / SRS section, and propose a compliant alternative. **In particular:** no inline `paise / 100` or `double` for any on-screen amount (Invariant 1 — feed-row amounts stay inside the frozen `OBTActivityRow` via `formatInrFromPaise()`); no read/write of `simplifiedBalances` on any of these surfaces (Invariant 2); no per-app messaging path (Invariant 3 — system share sheet only); no second Firebase project (Invariant 4).
- Refuse scope creep: no change to the activity read-model / providers / deep-link routing / permission flow / FCM / telemetry; do **not** build the deferred multi-context FAB chooser; do **not** add a shimmer dependency (use the shared `OBTSkeleton*` set); do **not** convert any other flow (DC-10+); do **not** edit the frozen DC-03 `OBTActivityRow` / `OBTSkeleton` / `OBTEmptyState`; do not touch the OS lock-screen banner (system-rendered, out of scope); do not unlock any India-only constraint.
- Do not edit `docs/OneByTwo_Requirements_Spec.md` or `lib/app/theme.dart` / `lib/core/theme/*` / the DC-03 components (consume the DC-01..03 foundation; any token/component gap routes to the Architect / issue #128).
- **AC-3 is non-negotiable:** if any converted notifications surface would retain a hard-coded `Color(0x…)` hex, refuse and re-point it to a Haldi token (or route a genuine token gap to #128) — a literal hex left at a call site is a blocking defect.

Begin by delegating to the **PM** (confirm #121 + the milestone, reconcile the roadmap so DC-08 reads MERGED, the §B Activity + Notifications reconciliation, and confirm the Haldi-25/26 coverage stays "covered"), then the **Architect** (the token-mapping for the `#2A9D8F` / `#F4A261` re-point + the lifecycle-host conformance + the no-hex contract + the Invariant-1 feed-row ruling), then the **Flutter Dev** (reskin the activity feed + the skeleton shimmer + the in-app banner + the pre-permission dialog) with **QA** + **Designer** (the §D per-screen gate incl. the light/dark hero goldens + the no-hex grep + the deep-link-unchanged proof + the contrast negative case + dynamic-type + visual review), and sequence to a single green PR.
