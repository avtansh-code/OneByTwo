You are the OneByTwo orchestrator agent. Sprint 3 — **Design Conversion (the "Direction A — Haldi" visual system, `design_handoff_one_by_two/`)** — is the active sprint. Five PRs have merged on `main`: **DC-01 (the design-token + type foundation) as PR #126** (squash `d9205d8`), **DC-02 (the six `OBT*` shared-widget reskins) as PR #127** (squash `aca8582`), **DC-03 (the eleven new Haldi shared components) as PR #129** (squash `b0ce9a6`), the **DC-03/06 scope doc as PR #130** (`bd27c38`), and **DC-04 (the Auth & onboarding flow conversion) as PR #131** (squash `0391bf2`). The Haldi `ColorScheme` (marigold `primary`, **ink** `onPrimary`), the `OBTColors` `ThemeExtension` (incl. the **balance trio** `balancePositive` / `balanceNegative` / `balanceZero` and the **8-hue `category` map**), the `AppTheme` radius / soft-warm-shadow / motion tokens, the Bricolage Grotesque / Hanken Grotesk `TextTheme`, the `OBTText.amount` / `OBTText.amountHero` helpers, the six reskinned `OBT*` widgets, the **eleven DC-03 shared components**, and the **converted Auth flow** are all on `main`.

The goal of THIS session is to deliver **Sprint 3 PR #6 — DC-05: convert the Home dashboard hero to Haldi (light + dark)** (issue **#117**). This is the **second per-flow screen-conversion PR** and the **first hero with money on screen** — it wires the DC-01..DC-03 primitives into the **six Home surfaces** — **Haldi 6 ★ Home dashboard (reskin)**: `home_dashboard_screen.dart`, `widgets/net_balance_header_card.dart`, `widgets/top_balance_tile.dart`, `widgets/spending_breakdown_card.dart`, `widgets/spending_donut_chart.dart`, `widgets/spending_category_palette.dart` — and **deletes** the throwaway Groups-tab stub `shell/presentation/groups_list_placeholder.dart`. Work **only** the Home dashboard (+ the Groups-tab-stub seam below); the other flows are DC-06 onward.

**Unlike DC-04, Invariants 1 + 2 are LIVE on this screen.** Home renders **money** (the net-balance hero, the top-balance tiles, the monthly-spend total) and **reads the `simplifiedBalances` projection**. Every amount MUST go through `formatInrFromPaise()` (Invariant 1 — no `paise / 100`, no `double`); every balance figure is a **read-only** projection read (Invariant 2 — the client never writes `simplifiedBalances`). A reskin that introduces inline `/100`, a float amount, or any projection write is a **blocking defect** — refuse and cite the invariant. The existing `test/features/home/home_boundary_contract_test.dart` (the `formatInrFromPaise()` boundary grep) and `home_dashboard_pii_leak_test.dart` MUST stay green unchanged.

**This is an IMPLEMENTATION session** (it changes `lib/**`). The full PR pipeline runs (analyse, format, unit + widget tests, Build Android, Build iOS, coverage gate).

────────────────────────────────────────
READ BEFORE STARTING
────────────────────────────────────────

- `.github/copilot-instructions.md`, `.github/shared/invariants.md`, `.github/shared/srs-pointer.md` (SRS = `docs/OneByTwo_Requirements_Spec.md` v1.1), `.github/shared/coding-standards.md`, `.github/shared/handoffs.md`, `.github/shared/milestone-tracking.md`, `.github/shared/test-strategy.md`.
- **The authoritative spec for this PR:**
  - `docs/sprint-zero/sprint-3-plan.md` — **story DC-05** (the User Story, the **three** Acceptance Criteria — incl. AC-2 the Invariant-1+2 money/projection clause and AC-3 the `groups_list_placeholder.dart` delete — the Definition of Done, and the Invariant-Compliance clause) and the sprint-wide Definition of Ready / Definition of Done at the top.
  - `docs/audits/design-conversion/02-conversion-checklist.md` **§B → "Home"** (the six-surface per-widget conversion table) and **§B → "Shell / nav"** (the `groups_list_placeholder.dart` **delete** row + the `authenticated_shell.dart` **conform** row). Mark the §B Home rows + the Shell delete row done on merge.
  - `docs/audits/design-conversion/04-qa-test-strategy.md` — **§A.5/§A.6** (Home 6 is a **hero** → light **and** dark goldens for **every** state: populated, **empty** illustration, **loading = shimmer skeleton**, error), **§B.1–§B.2** (contrast gate incl. the **balance-trio on surface** AA cases + the white-on-marigold negative case), **§B.3/§B.4** (labelling + 48 dp — reuse the harness walks; the `OBTBalancePill` must carry **colour + icon + label**, never colour alone), **§C** (dynamic type to 2.0× + reduced motion), **§D** (the per-screen "converted" acceptance gate).
  - `docs/audits/design-conversion/01-coverage-gap.md` — §B matrix **row 6 (Home dashboard)** is already "covered"; no status flip needed, but confirm.
  - `.github/shared/decision-log.md` → **ADR-0024** (scope: visual layer only).
  - `design_handoff_one_by_two/screens/Phase3b - Home.dc.html` (the Haldi Home dashboard — all states, light + dark) + the reference mockup `docs/design/05-mockups/03-home-dashboard.html`, and `design_handoff_one_by_two/screens/Phase2 - Components.dc.html` (the balance pill, donut + legend, skeleton set, empty-state the screen consumes).
- **The foundation now on `main` (consume — read, do not change):** `lib/app/theme.dart`, `lib/core/theme/obt_colors.dart` (the **balance trio** + the **`category` 8-hue map** + `OBTColors.metaText`), `lib/core/theme/obt_text.dart` (`OBTText.amount` / `OBTText.amountHero`), and the **DC-03 components** the Home dashboard consumes:
  - **skeleton set** — `lib/core/widgets/feedback/obt_skeleton.dart` (`OBTSkeleton`, `OBTSkeletonList`, `OBTSkeletonCard`, `OBTSkeletonRow`, `OBTSkeletonCircle`) for the hand-rolled loading silhouettes.
  - **balance pill** — `lib/core/widgets/indicators/obt_balance_pill.dart` (`OBTBalancePill` — colour + **icon** + label).
  - **donut + legend** — `lib/core/widgets/charts/obt_spending_donut.dart` (`OBTSpendingDonut`, `OBTCategoryLegend`, `OBTCategorySlice`).
  - **category chip/tile** — `lib/core/widgets/inputs/obt_category_chip.dart` (`OBTCategoryChip`, `OBTCategoryTile`) + the `OBTCategory` enum + `OBTColors.category` hues.
  - **empty-state** — `lib/core/widgets/feedback/obt_empty_state.dart` (`OBTEmptyState`).
  Reuse the **test harness** `test/support/widget_test_harness.dart` (`pumpThemed`, `expectAllInteractiveNodesLabelled`, `expectAllTapTargetsMeetMinSize`) and the **golden harness** `test/golden/golden_harness.dart`.
- **The surfaces to convert (read each before editing):** `lib/features/home/presentation/home_dashboard_screen.dart`, `widgets/net_balance_header_card.dart`, `widgets/top_balance_tile.dart`, `widgets/spending_breakdown_card.dart`, `widgets/spending_donut_chart.dart`, `widgets/spending_category_palette.dart`. Read the providers/aggregators (`application/home_balances_providers.dart`, `application/monthly_spend_breakdown_provider.dart`, `domain/monthly_spend_aggregator.dart`, `application/home_telemetry.dart`) **only** to confirm you are NOT changing behaviour, the projection read, or telemetry.
- **The Groups-tab stub to delete:** `lib/features/shell/presentation/groups_list_placeholder.dart` (`GroupsListPlaceholder`). **NB:** unlike DC-04's orphaned `authenticated_screen.dart`, this stub IS wired into the live shell — see the seam below.
- Do **not** read or re-plan the backend/data docs; they are unchanged.

────────────────────────────────────────
STANDING CAVEATS (read first)
────────────────────────────────────────

- **Invariants 1 + 2 are LIVE and load-bearing here.** Money only via `formatInrFromPaise()` (Invariant 1 — no `paise / 100`, no `double`, no inline arithmetic); balances are **read-only** `simplifiedBalances` projection reads showing the simplified figures (Invariant 2 — never a write, never a raw who-owes-whom graph). Invariants 3 (share) + 4 (project) are N/A on this surface but must stay untouched. Any item that weakens one is a blocking defect — refuse and cite it.
- **THE GROUPS-TAB SEAM (Architect must rule).** `groups_list_placeholder.dart` is marked **delete** (the real Haldi 14 Groups list is built fresh in **Sprint 4**), but it is **referenced live** by `authenticated_shell.dart` (import at `:14`, instantiated as **tab 2** in `_resolveTabContent()` at `:75`), by `test/features/shell/shell_boundary_contract_test.dart:26`, and by the shell/groups READMEs. The DC-05 DoD says the `authenticated_shell.dart` **5-tab + FAB structure conforms (no change)** — yet tab 2 currently *is* `GroupsListPlaceholder()`. **The Architect must rule the resolution before the Flutter Dev starts:** the canonical answer is to **delete the stub file and replace the shell's tab-2 reference with an inline Haldi `OBTEmptyState`** ("Groups — coming soon", Sprint 4; NOT the stale "Coming in Sprint 3" copy), so the **5-tab structure is preserved** (structure conforms; tab-2 content swapped to the shared empty-state component — that is "deleting the throwaway", not "converting the stub"). Update `shell_boundary_contract_test.dart` + the shell/groups READMEs to match. Confirm this is the minimal shell change and that no other inbound reference to `GroupsListPlaceholder` survives.
- **Behaviour is UNCHANGED — reskin is visual only.** No change to `home_balances_providers`, `monthly_spend_breakdown_provider`, `monthly_spend_aggregator`, `home_telemetry`, the `simplifiedBalances` read, the bottom-nav controller, or any domain model. Existing home provider / aggregator / boundary / PII-leak / screen tests stay green unchanged (update only assertions that legitimately encode an old token literal or the hand-rolled skeleton's removed structure).
- **The balance trio is colour + icon + label — never colour alone (accessibility).** The net-balance hero and every `OBTBalancePill` (top-balance tiles) must carry the directional **icon** (`arrow_upward` "you are owed" / `arrow_downward` "you owe" / `check` "settled") **and** the text label alongside the balance-trio colour, so the signal survives colour-blindness and greyscale (§B / §C). White on marigold ≈ 2.5:1 is the DC-01 contrast negative case — assert it FAILS.
- **Consume the DC-03 primitives — do not re-implement.** The hand-rolled loading skeleton → the shimmer **`OBTSkeleton*`** set; the empty `Icon` → **`OBTEmptyState`** + illustration; the inline balance pill → the shared **`OBTBalancePill`** (with icon); the donut + legend stays the DC-03 **`OBTSpendingDonut` / `OBTCategoryLegend`**; the category hues re-point to **`OBTColors.category`** (the Haldi 8-hue map) — do **not** hard-code the old `#B23A48…` palette.
- **No hard-coded Haldi hex at call sites.** All colour via `Theme.of(context).colorScheme` / `OBTColors`; all type via `theme.textTheme` / `OBTText`; all radii via the `AppTheme` scale. `theme.dart` / `core/theme/*` are the single source of truth — consume, do not edit (any token gap routes to the Architect / issue #128).
- **Tooling + lessons carried from DC-01..DC-04:**
  - fvm-pinned (Flutter 3.44.3 / Dart 3.12.2). **The `fvm` wrapper is broken** ("Can't load Kernel binary … expected 127, found 130") — bypass it: `export PATH="/Users/avtanshgupta/fvm/versions/stable/bin:$PATH"` then bare `flutter analyze lib test`, `flutter test`, `dart format .`, and commit/push with bare `git` (the pinned SDK on PATH satisfies the lefthook `dart`/`flutter` hooks). CI gates on `dart format --set-exit-if-changed .` — run it before pushing.
  - **`Closes #117` MUST be plain text, NOT backticked,** in the PR body, or GitHub will not register the closing reference and the Sprint 3 milestone will not advance.
  - **48 dp tap targets + non-vacuous a11y walks.** Any interactive control (the Settle Up buttons on the top-balance tiles, the FAB, any "see all" affordance) must meet the 48 dp minimum and carry a label; reuse `expectAllInteractiveNodesLabelled` + `expectAllTapTargetsMeetMinSize`. The built-in guidelines are defeated by `Semantics(button:true, excludeSemantics:true)` controls and a FAB's `tooltip` does not land on its node — put the label on the child `Icon` (`semanticLabel`).
  - **`google_fonts` in tests.** Pump widget tests that build `AppTheme` inside `testWidgets` (the test zone absorbs the runtime font fetch). Animated surfaces (the shimmer skeleton) never settle — pump with `disableAnimations: true` or `settle: false`.
  - **Goldens are authored on `ubuntu-latest` by the DC-13 (`#125`) `golden-a11y-checks` job**, not yet wired. Add the light + dark golden tests for the Home dashboard's **four** states (populated, empty, shimmer-loading, error) to `test/golden/` (tagged `golden`), **group-`skip`ped** with the DC-13 rationale, reusing `golden_harness.dart`. Do **not** author/commit golden PNGs locally. The load-bearing proof is the per-screen widget tests (states + labelling + 48 dp + the contrast negative case + the balance-trio icon presence + the `formatInrFromPaise()` boundary), which run for real.
  - If `flutter analyze` surfaces unrelated `flutter_contacts` / `PermissionStatus` errors, the `.dart_tool/package_config.json` drifted — run `flutter pub get` with the **pinned** SDK; never bare `flutter pub get`.

────────────────────────────────────────
THE TASK — DC-05 (5 SP)
────────────────────────────────────────

1. **Issue + milestone + roadmap (PM).** DC-05 is issue **#117**, already under the **`Sprint 3`** milestone — confirm it; this PR carries `Closes #117` (plain). Per `.github/shared/milestone-tracking.md`, reconcile the Sprint 3 milestone, and on this PR flip the **DC-04** line in `docs/sprint-zero/next-three-prs.md` from "IN FLIGHT" to **"MERGED (#131, `0391bf2`)"** and set DC-05 in flight. **Mark the §B Home rows + the §B Shell `groups_list_placeholder.dart` delete row done** in `02-conversion-checklist.md`; confirm `01-coverage-gap.md` §B row 6 stays "covered". (No SRS gap to flag this PR — Home is FR-HD-01..04, already specified.)

2. **Architect — per-surface contracts + the Groups-tab seam ruling (review, read-only).** Confirm, for each of the **six** Home surfaces, the token/type mapping (`ColorScheme` slot / `OBTColors` key — incl. the balance trio + the `category` 8-hue map / `AppTheme` radius / `TextTheme` slot / `OBTText.amount` / `OBTText.amountHero`) and the four-state shape (populated / empty / shimmer-loading / error). **Rule the Groups-tab seam** (delete `groups_list_placeholder.dart`; replace the shell tab-2 reference with an inline Haldi `OBTEmptyState`, 5-tab structure preserved; update the shell boundary-contract test + READMEs; confirm no other inbound `GroupsListPlaceholder` reference). **Confirm the Invariant-1+2 contract**: the net-balance hero, the top-balance tiles, and the monthly-spend total render money **only** via `formatInrFromPaise()` and **read** the `simplifiedBalances` projection (never write). **Confirm the balance-trio = colour + icon + label** rule for the hero amount + every `OBTBalancePill`. Return the contracts for the Flutter Dev.

3. **Flutter Dev — reskin the six surfaces + delete the stub:**
   - **Home dashboard (`home_dashboard_screen.dart`)** — marigold tokens + Bricolage/Hanken; the **hand-rolled loading skeleton → the shimmer `OBTSkeleton*` set**; the empty `Icon` → **`OBTEmptyState`** + illustration; keep the pull-to-refresh, the providers, and the telemetry unchanged.
   - **Net-balance header (`net_balance_header_card.dart`)** — `tertiaryContainer` / `errorContainer` → the Haldi `surfaceVariant` tonal hero + the marigold-tinted soft shadow (`OBTColors.heroShadow`); the amount → **Bricolage tabular hero** (`OBTText.amountHero`, ~46–50) via `formatInrFromPaise()`; **add the directional icon** so the balance trio = colour + icon + label; light + **dark** hero.
   - **Top-balance tile (`top_balance_tile.dart`)** — name → Hanken; the Settle Up button → marigold (ink on marigold); the balance pill → the shared **`OBTBalancePill`** with its icon; amounts via `formatInrFromPaise()`.
   - **Spending breakdown card (`spending_breakdown_card.dart`)** — card radius → `radiusCard` (20); legend → Hanken; swatches → the Haldi 8-hue; the skeleton → shimmer; keep the donut as the DC-03 **`OBTSpendingDonut` / `OBTCategoryLegend`**.
   - **Spending donut (`spending_donut_chart.dart`)** — re-point the segment colours to the Haldi 8-hue (`OBTColors.category`); the centre total → Bricolage tabular via `formatInrFromPaise()`; the gap colour → `surface`.
   - **Spending category palette (`spending_category_palette.dart`)** — re-point all eight light + dark category hues from the old hex (`food #B23A48…`) to the Haldi palette, **sourced from `OBTColors.category`** (do not duplicate the hex).
   - **Delete `groups_list_placeholder.dart`** — per the Architect's seam ruling (replace the shell tab-2 with the inline Haldi `OBTEmptyState`; update the boundary-contract test + READMEs).

4. **QA + Designer — the per-screen "converted" gate (§D):**
   - **All states present** — loading = **shimmer skeleton** (not a spinner), **empty** = `OBTEmptyState` + illustration, populated, error.
   - **Light + dark** — Home 6 is a **hero**: light **and** dark goldens for **every** state (§A.6).
   - **Invariant 1 + 2 proof** — a test (or the existing boundary/PII contracts kept green) asserts every on-screen amount routes through `formatInrFromPaise()` (no inline `/100`) and that the balance figures are projection **reads** (no write path introduced).
   - **Contrast gate** (§B.1–§B.2) incl. the **balance-trio-on-surface** AA cases + the **white-on-marigold negative case** asserted to FAIL.
   - **Balance trio = colour + icon + label** — assert the directional icon + label are present on the hero amount + every `OBTBalancePill` (not colour alone).
   - **Every control labelled + 48 dp** — run `expectAllInteractiveNodesLabelled` + `expectAllTapTargetsMeetMinSize` on the populated state (Settle Up buttons, FAB, any "see all").
   - **Dynamic type 2.0×** (§C) — no clip/overflow at 390 and 320 width; reduced-motion freezes the shimmer.
   - **Shell conformance** — the 5-tab + FAB structure still renders; tab 2 now shows the Haldi `OBTEmptyState`; `shell_boundary_contract_test.dart` updated and green; deleting `groups_list_placeholder.dart` breaks nothing.
   - **Goldens** — the Home dashboard's four states (light + dark) queued in `test/golden/`, **skipped** pending the DC-13 ubuntu path.
   - **Designer (read-only)** reviews the six surfaces against `Phase3b - Home.dc.html` + the mockup and returns findings for the orchestrator to action.

5. **Verify:** `flutter analyze lib test` (no issues), `dart format .` (clean), `flutter test` (all pass; the existing home provider / aggregator / boundary / PII-leak / screen suites stay green; per-feature coverage ≥ 70% and overall ≥ 50%). No **existing** provider, aggregator, `home_telemetry`, `simplifiedBalances` read, Firestore read/write, Cloud Function, or domain-model **behaviour** is changed; the only structural change beyond the reskin is the `groups_list_placeholder.dart` delete + the shell tab-2 swap to `OBTEmptyState`.

────────────────────────────────────────
BRANCH / PR
────────────────────────────────────────

- Branch: `feat/haldi-home-conversion` (type-prefixed kebab, repo convention).
- Conventional Commits, single-token scope, ASCII subject ≤ 72 chars — e.g. `feat(home): convert the home dashboard hero to Haldi`.
- This is a **code** PR — the full PR pipeline must be green (Flutter Lint & Test, Build Android, Build iOS, Coverage Gate) before merge.
- PR body: map each surface to §B + the DC-05 ACs; tick the invariants checklist (1 + 2 **live** — money via `formatInrFromPaise()`, `simplifiedBalances` read-only; 3 + 4 unchanged); note "net-balance hero = Bricolage tabular + balance trio (colour + icon + label); hand-rolled skeleton → shimmer `OBTSkeleton`; empty → `OBTEmptyState`; donut re-points to the Haldi 8-hue (`OBTColors.category`); `groups_list_placeholder.dart` deleted + shell tab-2 → inline `OBTEmptyState` (5-tab structure conforms)"; **`Closes #117` (plain, NOT backticked)**.
- Include the `Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>` trailer.
- Commit `docs/copilot_prompts/sprint_3/6.md` (this prompt) with the PR, per the repo convention.

────────────────────────────────────────
DEFINITION OF DONE (this PR)
────────────────────────────────────────

- All **three** DC-05 acceptance criteria pass: the **net header** renders as a **Bricolage tabular hero** carrying the **balance trio (colour + icon + label)**, the **hand-rolled skeleton → shimmer skeleton-loader**, the **empty `Icon` → empty-state scaffold + illustration**, and the **spending donut re-points to the Haldi 8-hue** — in light **and** dark (AC-1); the net-balance + top-balance tiles **read the `simplifiedBalances` projection only** and render amounts via **`formatInrFromPaise()`** (AC-2 / Invariants 1 + 2); **`groups_list_placeholder.dart` is deleted, not converted** (AC-3 — the shell tab-2 swaps to an inline Haldi `OBTEmptyState`; the 5-tab + FAB structure conforms).
- The §B **Home** rows + the §B **Shell** `groups_list_placeholder.dart` delete row in `02-conversion-checklist.md` are marked done; `01-coverage-gap.md` §B row 6 stays "covered".
- All six surfaces pass the §D per-screen gate (four states, light/dark hero goldens, contrast incl. the negative case, balance-trio icon+label, labelling + 48 dp, dynamic-type 2.0×, shell conformance), with light + dark goldens queued in the DC-13 harness.
- No hard-coded Haldi hex at call sites; `theme.dart` / `core/theme/*` unchanged; no invariant weakened; no backend/provider/aggregator/telemetry/CF/domain change; the `simplifiedBalances` read stays read-only.
- Analyse clean, `dart format` clean, all tests pass (existing home suites green unchanged), per-feature coverage ≥ 70% and overall ≥ 50%.
- `docs/sprint-zero/next-three-prs.md` is reconciled (DC-04 → MERGED `#131` / `0391bf2`; DC-05 status).
- DC-05 issue (#117) closed under the `Sprint 3` milestone; the milestone is reconciled per `.github/shared/milestone-tracking.md`.

End the session summary with: "Next: Sprint 3 PR #7 — DC-06 (Friends flow conversion: Haldi 9 ★, 10, 11, 12 — incl. the net-new Friend-history build)."

────────────────────────────────────────
FAILURE / REFUSAL PROTOCOL
────────────────────────────────────────

- If any change would weaken an invariant or the SRS, refuse it, quote the specific invariant / SRS section, and propose a compliant alternative. **In particular:** no inline `paise / 100` or `double` for any on-screen amount (Invariant 1 — use `formatInrFromPaise()`); no write to `simplifiedBalances` and no raw who-owes-whom graph (Invariant 2 — projection reads only).
- Refuse scope creep: no change to the home providers / aggregators / telemetry / `simplifiedBalances` read; do **not** convert any other flow (DC-06+); do **not** build any not-yet-built screen (the real Groups list 14 is Sprint 4, Search 7 / Remove-friend 13 are Sprint 6); do **not** reskin the Groups stub — delete it and swap the shell tab-2 to the shared `OBTEmptyState`; do not unlock any India-only constraint; no second Firebase project.
- Do not edit `docs/OneByTwo_Requirements_Spec.md` or `lib/app/theme.dart` / `lib/core/theme/*` (consume the DC-01 foundation; any token gap routes to the Architect / issue #128).

Begin by delegating to the **PM** (confirm #117 + the milestone, reconcile the roadmap so DC-04 reads MERGED, and the §B Home + Shell-delete reconciliation), then the **Architect** (per-surface token mapping + the Groups-tab seam ruling + the Invariant-1+2 money/projection contract + the balance-trio colour+icon+label rule), then the **Flutter Dev** (reskin the six Home surfaces + delete the stub + swap the shell tab-2) with **QA** + **Designer** (the §D per-screen gate incl. the four states + light/dark hero goldens + the contrast negative case + the balance-trio icon + the `formatInrFromPaise()` boundary + dynamic-type + shell conformance + visual review), and sequence to a single green PR.
