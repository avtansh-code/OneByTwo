You are the OneByTwo orchestrator agent. Sprint 3 — **Design Conversion (the "Direction A — Haldi" visual system, `design_handoff_one_by_two/`)** — is the active sprint. Three PRs have merged on `main`: **DC-01 (the design-token + type foundation) as PR #126** (squash `d9205d8`), **DC-02 (the six `OBT*` shared-widget reskins) as PR #127** (squash `aca8582`), and **DC-03 (the eleven new Haldi shared components) as PR #129** (squash `b0ce9a6`). The Haldi `ColorScheme` (marigold `primary`, **ink** `onPrimary`), the `OBTColors` `ThemeExtension`, the `AppTheme` radius / soft-warm-shadow / motion tokens, the Bricolage Grotesque / Hanken Grotesk `TextTheme`, the `OBTText.amount` / `OBTText.amountHero` helpers, the six reskinned `OBT*` widgets, and the **eleven DC-03 shared components** — including the **brand kit** (`lib/core/widgets/branding/obt_brand.dart`: `OBTBrandMark`, `OBTWordmark`, `OBTBrandLockup`, `OBTSplashGradient`), the **`OBTOtpInput`** (`lib/core/widgets/inputs/obt_otp_input.dart`), the **skeleton-loader set** and **`OBTEmptyState`** (`lib/core/widgets/feedback/**`), plus the shared component-test harness (`test/support/widget_test_harness.dart`: `pumpThemed`, `expectAllInteractiveNodesLabelled`, `expectAllTapTargetsMeetMinSize`) — are now on `main`.

The goal of THIS session is to deliver **Sprint 3 PR #5 — DC-04: convert the Auth and onboarding flow to Haldi** (issue **#116**). This is the **first per-flow screen-conversion PR**: it wires the DC-01..DC-03 primitives into the **five Auth screens** — **Haldi 1 splash (rebuild), 2 onboarding (BUILD — net-new in Haldi), 3 phone-entry (reskin), 4 OTP (reskin), 5 profile-setup (reskin)** — and **deletes** the orphaned `authenticated_screen.dart`. Work **only** the Auth + onboarding flow; the other flows are DC-05 onward.

**Onboarding (Haldi 2) IS in scope for DC-04 — built fresh in Haldi** (PM decision, 2026-06-26). It is the one **net-new build** in this PR (the other four screens are conversions). The Haldi design exists in `design_handoff_one_by_two/screens/Phase3a - Auth.dc.html` (the same file as the other auth screens — which is why the issue is titled "Auth **and** onboarding flow"), with the reference mockup at `docs/design/05-mockups/01-splash-and-onboarding.html`. Build the **3 illustrated slides (Track / Split / Settle)** with **Skip**, page **pagination dots**, a **"Get started"** CTA (ink-on-marigold), and the **Terms of Service / Privacy Policy links** (SRS §10 requires they link from onboarding). Consume the DC-03 **brand kit** (`OBTBrandMark` / illustration holders, foundation-plan §4.2 #13 — "Screens 1, 2") + `OBTEmptyState` shapes; no hard-coded hex. **First-launch gating:** onboarding shows once on first launch (before phone-entry), gated by a persisted "seen onboarding" flag (use the existing `shared_preferences` persistence pattern); **Skip** and **Get started** both advance to phone-entry and set the flag. **FR note:** onboarding has no dedicated FR id yet — build it against the Haldi Phase3a design + the SRS §6 first-time journey, and flag the **FR formalisation to the PM's `update-srs` proposal** (`docs/sprint-zero/srs-update-proposal-haldi.md`); this is a tracked note, not a blocker.

**This is an IMPLEMENTATION session** (it changes `lib/**`). The full PR pipeline runs (analyse, format, unit + widget tests, Build Android, Build iOS, coverage gate).

────────────────────────────────────────
READ BEFORE STARTING
────────────────────────────────────────

- `.github/copilot-instructions.md`, `.github/shared/invariants.md`, `.github/shared/srs-pointer.md` (SRS = `docs/OneByTwo_Requirements_Spec.md` v1.1), `.github/shared/coding-standards.md`, `.github/shared/handoffs.md`, `.github/shared/milestone-tracking.md`, `.github/shared/test-strategy.md`.
- **The authoritative spec for this PR:**
  - `docs/sprint-zero/sprint-3-plan.md` — **story DC-04** (the User Story, the **four** Acceptance Criteria, the Definition of Done incl. the **onboarding build** + the `authenticated_screen.dart` delete, the Invariant-Compliance clause) and the sprint-wide Definition of Ready / Definition of Done at the top.
  - `docs/audits/design-conversion/02-conversion-checklist.md` **§B → "Auth"** — the per-screen conversion table (splash 1 ★ rebuild, **onboarding 2 BUILD**, phone-entry 3 reskin, otp-entry 4 reskin, otp_input 4 reskin/already-built, profile-setup 5 reskin, authenticated_screen delete). Mark the Auth rows done on merge (the onboarding row is now a **build**, not "out of scope").
  - `docs/audits/design-conversion/04-qa-test-strategy.md` — **§A.5** (golden scope: **rebuild** = fresh baselines for the splash's new tree incl. the dark splash brand moment; **reskin** = re-baseline the *same* tree, structural assertions unchanged), **§A.6** (the splash 1 ★ is a **hero** → light **and** dark goldens for every state; **onboarding 2, the net-new brand moment, ships light + dark too**; phone/OTP/profile are non-hero reskins → light goldens, plus a **dark** golden for the marigold/danger surfaces — phone CTA, OTP active border, profile camera badge), **§B.1–§B.2** (contrast gate incl. the **white-on-marigold negative case**), **§B.3/§B.4** (labelling — every control labelled + the inert-slot rule; reuse the harness walks), **§C** (dynamic type to 2.0× + reduced motion), **§D** (the **per-screen "converted" acceptance gate** — every box ticked).
  - `.github/shared/decision-log.md` → **ADR-0024** (scope: visual layer only).
  - `design_handoff_one_by_two/screens/Phase3a - Auth.dc.html` (the Haldi auth screens — **onboarding 2**, phone-entry 3, OTP 4, profile-setup 5, all states, light + dark) + the onboarding reference mockup `docs/design/05-mockups/01-splash-and-onboarding.html`, `design_handoff_one_by_two/screens/Phase1 - Foundations.dc.html` (the brand foundation / splash 1 brand moment), and `design_handoff_one_by_two/screens/Phase2 - Components.dc.html` (the brand kit + OTP component the screens consume).
- **The foundation now on `main` (consume — read, do not change):** `lib/app/theme.dart`, `lib/core/theme/obt_colors.dart`, `lib/core/theme/obt_text.dart`, the six `OBT*` widgets, and the **DC-03 components** the Auth flow consumes: the **brand kit** (`lib/core/widgets/branding/obt_brand.dart`), **`OBTOtpInput`** (`lib/core/widgets/inputs/obt_otp_input.dart`), the **skeleton set** + **`OBTEmptyState`** (`lib/core/widgets/feedback/**`). Reuse the **test harness** `test/support/widget_test_harness.dart`.
- **The screens to convert (read each before editing):** `lib/features/auth/presentation/splash_screen.dart` (rebuild), `phone_entry_screen.dart` (reskin), `otp_entry_screen.dart` (reskin), `profile_setup_screen.dart` (reskin), `widgets/otp_input.dart` (the local partial — see the seam below), and `authenticated_screen.dart` (delete). Read their controllers/providers (`application/*_controller.dart`, `application/auth_state_provider.dart`) only to confirm you are NOT changing behaviour.
- Do **not** read or re-plan the backend/data docs; they are unchanged.

────────────────────────────────────────
STANDING CAVEATS (read first)
────────────────────────────────────────

- **The four invariants are untouched and re-affirmed.** Auth touches **no money, balance, share, or project surface** — so all four hold unchanged. Still: introduce no `paise / 100`, no `simplifiedBalances` write, no app-specific share, no second Firebase project. Any item that weakens one is a blocking defect — refuse and cite the invariant.
- **`onPrimary` is INK, never white.** The splash marigold gradient, the phone-entry CTA, and the **profile-setup camera-badge icon** all take **ink** `onPrimary`. White on marigold ≈ 2.5:1 fails AA and is the DC-01 contrast-gate negative case — assert it FAILS in a test.
- **The `+91` prefix stays LOCKED.** Phone-entry keeps the non-editable `+91` chip (on `surfaceVariant`); the reskin restyles it but never makes it editable (India-only, SRS).
- **Behaviour is UNCHANGED — reskin/rebuild is visual only.** No change to the auth controllers, repositories, `auth_state_provider`, phone-auth wiring, OTP auto-retrieval/resend-cooldown logic, validation, or session lifecycle. The phone-auth `verificationCompleted` instant-verification path stays intact. Existing auth controller / boundary / session tests stay green unchanged (update only assertions that legitimately encode an old token literal, per §D item 13).
- **Consume the DC-03 primitives — do not re-implement.** The **splash** is rebuilt from the **brand kit** (`OBTSplashGradient` + `OBTBrandLockup` / `OBTBrandMark` + `OBTWordmark`, ink on marigold). The **OTP screen adopts the shared `OBTOtpInput`**.
- **The OtpInput seam (Architect must rule).** The local `lib/features/auth/presentation/widgets/otp_input.dart` (`OtpInput`) is **also consumed by Profile** — `change_phone_controller.dart`, `change_phone_screen.dart`, `delete_account_screen.dart` (DC-10). **DC-04 migrates ONLY the auth `otp_entry_screen.dart` to `OBTOtpInput` and must NOT delete `otp_input.dart`** (that would break Profile); the local partial's retirement is **DC-10**. The Architect confirms this seam (migrate auth → `OBTOtpInput`; leave `otp_input.dart` for Profile) before the Flutter Dev starts.
- **`authenticated_screen.dart` is DELETED, not converted (negative case).** Before deleting, grep for any inbound reference to `AuthenticatedScreen` / the file — if any exists, the deletion is **blocked** until the reference is removed; do not convert it.
- **No hard-coded Haldi hex at call sites.** All colour via `Theme.of(context).colorScheme` / `OBTColors`; all type via `theme.textTheme` / `OBTText`; all radii via the `AppTheme` scale. `theme.dart` / `core/theme/*` are the single source of truth — consume, do not edit (any token gap routes to the Architect / issue #128).
- **Tooling + lessons carried from DC-01..DC-03:**
  - fvm-pinned (Flutter 3.44.3 / Dart 3.12.2). **The `fvm` wrapper is broken** ("Can't load Kernel binary … expected 127, found 130") — bypass it: `export PATH="/Users/avtanshgupta/fvm/versions/stable/bin:$PATH"` then bare `flutter analyze lib test`, `flutter test`, `dart format .`, and commit/push with bare `git` (the pinned SDK on PATH satisfies the lefthook `dart`/`flutter` hooks). CI gates on `dart format --set-exit-if-changed .` — run it before pushing.
  - **`Closes #116` MUST be plain text, NOT backticked,** in the PR body, or GitHub will not register the closing reference and the Sprint 3 milestone will not advance (the DC-03 review caught exactly this).
  - **48 dp tap targets + non-vacuous a11y walks.** Any interactive control (CTAs, the camera badge button, retry/resend) must meet the 48 dp minimum and carry a label; reuse `expectAllInteractiveNodesLabelled` + `expectAllTapTargetsMeetMinSize` from the harness (the built-in guidelines are defeated by `Semantics(button:true, excludeSemantics:true)` controls).
  - **`google_fonts` in tests.** Pump widget tests that build `AppTheme` inside `testWidgets` (the test zone absorbs the runtime font fetch). Animated surfaces never settle — pump with `disableAnimations: true` or `settle: false`.
  - **Goldens are authored on `ubuntu-latest` by the DC-13 (`#125`) `golden-a11y-checks` job**, not yet wired. Add the light + dark golden tests for the **five** screens' states to `test/golden/` (tagged `golden`), **group-`skip`ped** with the DC-13 rationale, reusing `golden_harness.dart`. Do **not** author/commit golden PNGs locally. The load-bearing proof is the per-screen widget tests (states + labelling + 48 dp + the contrast negative case + the onboarding first-launch gating + reachability), which run for real.
  - If `flutter analyze` surfaces unrelated `flutter_contacts` / `PermissionStatus` errors, the `.dart_tool/package_config.json` drifted — run `flutter pub get` with the **pinned** SDK; never bare `flutter pub get`.

────────────────────────────────────────
THE TASK — DC-04 (8 SP — was 5; +3 for the net-new onboarding build)
────────────────────────────────────────

1. **Issue + milestone + roadmap (PM).** DC-04 is issue **#116**, already under the **`Sprint 3`** milestone — confirm it; this PR carries `Closes #116` (plain). Per `.github/shared/milestone-tracking.md`, reconcile the Sprint 3 milestone, and on this PR flip the **DC-03** line in `docs/sprint-zero/next-three-prs.md` from "IN FLIGHT" to **"MERGED (#129, `b0ce9a6`)"** (the rolling-roadmap reconciliation for the previous PR) and set DC-04 in flight. **Mark the §B Auth rows done** in `02-conversion-checklist.md` — including the **onboarding (Haldi 2)** row (now built, not "out of scope") — and update the `01-coverage-gap.md` §B matrix row 2 to **covered (DC-04, built)**.

2. **Architect — per-screen contracts (review, read-only).** Confirm, for each of the **five screens** (incl. the net-new **onboarding** build), the token/type mapping (`ColorScheme` slot / `OBTColors` key / `AppTheme` radius / `TextTheme` slot / `OBTText` helper) and the four-state shape. **Contract the onboarding build:** the 3-slide `PageView` + pagination + Skip + "Get started" structure, the **first-launch gating** (the persisted "seen onboarding" flag + where it is read in the auth/splash routing) and the route placement (onboarding → phone-entry), the **Terms/Privacy** link targets, and the brand-kit consumption — plus flag the **onboarding FR formalisation to the PM's `update-srs` proposal** (no FR id today). **Rule on the OtpInput seam** (migrate auth `otp_entry_screen` → `OBTOtpInput`; leave `otp_input.dart` for Profile/DC-10). **Confirm the splash rebuild** (`OBTSplashGradient` + `OBTBrandLockup`, ink `onPrimary`, the dark splash treatment). **Confirm `authenticated_screen.dart` has zero inbound references** (safe to delete). Confirm the `+91`-lock and camera-badge-ink contracts. Return the contracts for the Flutter Dev.

3. **Flutter Dev — build onboarding + convert the four screens + delete the throwaway:**
   - **Splash (Haldi 1 ★, rebuild)** — replace the placeholder `Icons.vertical_split_rounded` with the **÷ brand mark + "OneByTwo" wordmark** on a **full-bleed marigold gradient** (`OBTSplashGradient` + `OBTBrandLockup`), **ink (not white) on marigold**, tagline in Hanken; ship **light + dark**.
   - **Onboarding (Haldi 2, BUILD — net-new in `lib/features/auth/presentation/`)** — the **3 illustrated slides (Track / Split / Settle)** in a `PageView` with **pagination dots**, a **Skip** affordance, and a **"Get started"** CTA (`primary` + **`onPrimary` ink**), plus the **Terms of Service / Privacy Policy links** (SRS §10). Use the DC-03 **brand kit** illustration holders + tokens (no hard-coded hex). **First-launch gating:** show once before phone-entry, gated by a persisted "seen onboarding" flag (`shared_preferences`); Skip and Get started both set the flag and route to phone-entry. Ship **light + dark**. This is a hero brand moment — treat its goldens like the splash.
   - **Phone-entry (Haldi 3, reskin)** — marigold tokens + Bricolage/Hanken; the **locked `+91` chip** on `surfaceVariant`; field radius → `radiusChipInput`; CTA → `primary` with **`onPrimary` ink**; keep the error + loading states.
   - **OTP (Haldi 4, reskin)** — **adopt `OBTOtpInput`**; resend-cooldown caption → `OBTColors.metaText` (`textTertiary`); error microcopy; OTP / validation / session behaviour **unchanged**.
   - **Profile-setup (Haldi 5, reskin)** — marigold tokens + type; **camera-badge icon → `onPrimary` ink (not white)**; avatar fallback from the scheme; field radius; keep validation.
   - **Delete `authenticated_screen.dart`** — after verifying no inbound `AuthenticatedScreen` reference.

4. **QA + Designer — the per-screen "converted" gate (§D):**
   - **All states present** — loading = shimmer/skeleton (not a spinner), empty (where applicable), populated, error; the empty/error use the DC-03 scaffolds where relevant.
   - **Light + dark** — the splash **and onboarding** heroes in both; phone/OTP/profile light + a **dark** golden for their marigold/danger surface (§A.6).
   - **Contrast gate** (§B.1–§B.2) incl. the **white-on-marigold negative case** (splash, **onboarding "Get started"**, camera badge, phone CTA) asserted to FAIL.
   - **Every control labelled + 48 dp** — run `expectAllInteractiveNodesLabelled` + `expectAllTapTargetsMeetMinSize` on each screen's populated state (incl. onboarding's Skip / Get started / pagination / Terms-Privacy links).
   - **Dynamic type 2.0×** (§C) — no clip/overflow at 390 and 320 width; reduced-motion fallback verified where animated (the splash entrance + the onboarding slide transition).
   - **Onboarding first-launch gating** — a widget/flow test proves onboarding shows once on first launch, that **Skip** and **Get started** both set the persisted "seen" flag and route to phone-entry, and that a second launch goes straight to phone-entry.
   - **Reachability (§D item 11)** — the auth flow's executable journey still passes and the screens stay reachable; `authenticated_screen.dart` deletion breaks nothing (no dangling route/provider). The skipped `test/integration/**` emulator stubs stay skipped.
   - **Goldens** — splash (rebuild → fresh light + dark hero) + **onboarding (net-new → fresh light + dark, the 3 slides)** + phone/OTP/profile (reskin → re-baseline same tree) queued in `test/golden/`, **skipped** pending the DC-13 ubuntu path.
   - **Designer (read-only)** reviews the **five screens** against `Phase3a - Auth.dc.html` + the splash/onboarding brand moment and returns findings for the orchestrator to action.

5. **Verify:** `flutter analyze lib test` (no issues), `dart format .` (clean), `flutter test` (all pass; the existing auth controller / boundary / session suites stay green unchanged; per-feature coverage ≥ 70% and overall ≥ 50%). No **existing** provider, repository, `auth_state_provider`, phone-auth wiring, Firestore read/write, Cloud Function, or domain-model **behaviour** is changed — the onboarding build adds only its own screen + a first-launch "seen" flag (`shared_preferences`) + its route into the auth flow; `otp_input.dart` is **retained** (Profile still consumes it); `authenticated_screen.dart` is **deleted**.

────────────────────────────────────────
BRANCH / PR
────────────────────────────────────────

- Branch: `feat/haldi-auth-conversion` (type-prefixed kebab, repo convention).
- Conventional Commits, single-token scope, ASCII subject ≤ 72 chars — e.g. `feat(auth): convert the auth and onboarding flow to Haldi`.
- This is a **code** PR — the full PR pipeline must be green (Flutter Lint & Test, Build Android, Build iOS, Coverage Gate) before merge.
- PR body: map each screen to §B + the DC-04 ACs; tick the invariants checklist (all four **unchanged** — auth has no money/balance/share/project surface); note "onboarding **built** (3 slides + pagination + Skip + ink 'Get started' + Terms/Privacy + first-launch gating); splash rebuilt with the brand kit (ink on marigold); OTP adopts `OBTOtpInput`; `otp_input.dart` retained for Profile/DC-10; `authenticated_screen.dart` deleted (no inbound refs); `+91` locked; camera badge ink-not-white"; **`Closes #116` (plain, NOT backticked)**.
- Include the `Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>` trailer.
- Commit `docs/copilot_prompts/sprint_3/5.md` (this prompt) with the PR, per the repo convention.

────────────────────────────────────────
DEFINITION OF DONE (this PR)
────────────────────────────────────────

- All **four** DC-04 acceptance criteria pass: the **splash** (Haldi 1, rebuild) shows the ÷ brand mark + OneByTwo wordmark on a full-bleed marigold gradient with **ink** (not white) on marigold, light **and** dark; **onboarding (Haldi 2, built)** ships the 3 Track/Split/Settle slides with pagination, Skip, an ink-on-marigold "Get started", the Terms/Privacy links, and the first-launch "seen" gating, light **and** dark; **phone-entry (3)**, **OTP (4)** via `OBTOtpInput`, and **profile-setup (5)** use marigold tokens + Bricolage/Hanken with the `+91` prefix **locked** and the profile camera-badge icon **ink, not white**, with OTP / validation / session behaviour **unchanged**; **`authenticated_screen.dart`** is **deleted, not converted** (negative case — any `AuthenticatedScreen` reference blocks the deletion).
- The §B **Auth** rows in `02-conversion-checklist.md` are marked done (incl. the onboarding **build** row); the `01-coverage-gap.md` §B matrix row 2 reads **covered (DC-04)**; the onboarding FR gap is flagged to the PM's `update-srs` proposal.
- All **five** screens (splash rebuild, **onboarding build**, phone-entry / OTP / profile-setup reskins) pass the §D per-screen gate (states, light/dark, contrast, labelling + 48 dp, dynamic-type 2.0×, reachability), with light + dark goldens queued in the DC-13 harness.
- No hard-coded Haldi hex at call sites; `theme.dart` / `core/theme/*` unchanged; no invariant weakened; no backend/provider/repository/CF/domain change; `otp_input.dart` retained for Profile.
- Analyse clean, `dart format` clean, all tests pass (existing auth suites green unchanged), per-feature coverage ≥ 70% and overall ≥ 50%.
- `docs/sprint-zero/next-three-prs.md` is reconciled (DC-03 → MERGED `#129` / `b0ce9a6`; DC-04 status).
- DC-04 issue (#116) closed under the `Sprint 3` milestone; the milestone is reconciled per `.github/shared/milestone-tracking.md`.

End the session summary with: "Next: Sprint 3 PR #6 — DC-05 (Home dashboard conversion: Haldi 6 ★)."

────────────────────────────────────────
FAILURE / REFUSAL PROTOCOL
────────────────────────────────────────

- If any change would weaken an invariant or the SRS, refuse it, quote the specific invariant / SRS section, and propose a compliant alternative.
- Refuse scope creep: no change to the auth controllers / repositories / session logic; do **not** delete `otp_input.dart` while Profile consumes it; build **only** the onboarding (Haldi 2) net-new screen — do **not** build any other not-yet-built screen (Search 7, Remove-friend 13, Groups 14–20 are later sprints); do not convert any other flow (DC-05+); do not unlock the `+91` prefix; do not wire any inert slot; no second Firebase project. Do not introduce `paise / 100`, a white-on-marigold foreground, or a hard-coded Haldi hex at a call site.
- Do not edit `docs/OneByTwo_Requirements_Spec.md` or `lib/app/theme.dart` / `lib/core/theme/*` (consume the DC-01 foundation; any token gap routes to the Architect / issue #128).

Begin by delegating to the **PM** (confirm #116 + the milestone, reconcile the roadmap so DC-03 reads MERGED, and the §B/coverage-gap onboarding reconciliation), then the **Architect** (per-screen token mapping + the onboarding build contract incl. first-launch gating + the OtpInput seam ruling + the splash rebuild + the `authenticated_screen` delete-safety check), then the **Flutter Dev** (rebuild the splash + **build onboarding** + reskin phone/OTP/profile + delete the throwaway) with **QA** + **Designer** (the §D per-screen gate incl. the onboarding first-launch gating + labelling + 48 dp + the contrast negative case + dynamic-type + queued goldens + visual review), and sequence to a single green PR.
