You are the OneByTwo orchestrator agent. Sprint 3 — **Design Conversion (the "Direction A — Haldi" visual system, `design_handoff_one_by_two/`)** — is the active sprint. Seven PRs have merged on `main`: **DC-01 (the design-token + type foundation) as PR #126** (squash `d9205d8`), **DC-02 (the six `OBT*` shared-widget reskins) as PR #127** (squash `aca8582`), **DC-03 (the eleven new Haldi shared components) as PR #129** (squash `b0ce9a6`), the **DC-03/06 scope doc as PR #130** (`bd27c38`), **DC-04 (the Auth & onboarding flow conversion) as PR #131** (squash `0391bf2`), **DC-05 (the Home dashboard hero conversion) as PR #132** (squash `1c3e194`), and **DC-06 (the Friends flow conversion + the net-new Friend-history screen) as PR #133** (squash `006a0fb`). The Haldi `ColorScheme` (marigold `primary`, **ink** `onPrimary`), the `OBTColors` `ThemeExtension` (incl. the **balance trio** `balancePositive` / `balanceNegative` / `balanceZero`, the **8-hue `category` map**, `OBTColors.heroShadow` / `rowShadow`, `OBTColors.link`, `OBTColors.metaText`), the `AppTheme` radius / soft-warm-shadow / motion tokens, the Bricolage Grotesque / Hanken Grotesk `TextTheme`, the `OBTText.amount` / `OBTText.amountHero` helpers, the six reskinned `OBT*` widgets, the **eleven DC-03 shared components**, and the **converted Auth / Home / Friends flows** are all on `main`.

The goal of THIS session is to deliver **Sprint 3 PR #8 — DC-07: convert the Expenses flow to Haldi (light + dark)** (issue **#119**). This is the **fourth per-flow screen-conversion PR (5 SP)** and the **first that contains a step-level REBUILD inside a hero sheet**. It wires the DC-01..DC-03 primitives into the **Expenses surfaces** — **Haldi 21 ★ Add-expense 3-step sheet (sheet-shell reskin + step-2 REBUILD + step-3 additive)**, **Haldi 22 Expense detail (reskin)** — and reskins the shared **Haldi 8 Add-expense context picker** that opens the sheet. Work **only** the Expenses flow (+ the shared context-picker seam below); Settlements (DC-08), Activity/Notifications (DC-09) and the rest are later.

**Invariants 1 + 2 are LIVE on this flow.** Every on-screen amount (the amount input, the per-step running total, the split rows, the validation pill, the expense-detail per-person split) MUST go through `formatInrFromPaise()` (Invariant 1 — no `paise / 100`, no `double`; the `OBTAmountInput` integer-paise `onChanged(int paise)` contract is untouched). The expense-detail per-person split shows the **simplified** figure read from the `simplifiedBalances` projection — never a raw who-owes-who web, and the client never writes the field (Invariant 2). The existing `expense_creation_boundary_contract_test.dart` and `expense_telemetry_pii_leak_test.dart` MUST stay green, and any net-new on-screen-money surface MUST be added to those greps. **Invariants 3 + 4 are N/A to this flow but must stay untouched** (no share path, no second Firebase project).

**This is an IMPLEMENTATION session** (it changes `lib/**`). The full PR pipeline runs (analyse, format, unit + widget tests, Build Android, Build iOS, Integration Tests, Coverage Gate).

────────────────────────────────────────
READ BEFORE STARTING
────────────────────────────────────────

- `.github/copilot-instructions.md`, `.github/shared/invariants.md`, `.github/shared/srs-pointer.md` (SRS = `docs/OneByTwo_Requirements_Spec.md` v1.1), `.github/shared/coding-standards.md`, `.github/shared/handoffs.md`, `.github/shared/milestone-tracking.md`, `.github/shared/test-strategy.md`.
- **The authoritative spec for this PR:**
  - `docs/sprint-zero/sprint-3-plan.md` — **story DC-07** (the User Story, the **four** Acceptance Criteria — incl. AC-2 the step-2 "adds up" negative case (Next disabled / red over-under) and AC-3 the expense-detail projection-read split — the Definition of Done, and the Invariant-Compliance clause) and the sprint-wide Definition of Ready / Definition of Done at the top.
  - `docs/audits/design-conversion/02-conversion-checklist.md` **§B → "Expenses"** (the per-widget conversion table: rows 21/22 across `add_expense_bottom_sheet.dart`, `steps/step_1_amount_details.dart`, `steps/step_2_split_and_payer.dart` **(rebuild)**, `steps/step_3_receipt_and_confirm.dart`, `expense_detail_screen.dart`, `widgets/expense_category_grid.dart`, `widgets/split_row.dart`, `widgets/split_validation_message.dart`, `widgets/changed_field_indicator.dart`, `widgets/receipt_fullscreen_viewer.dart`) and **§B → "Shell / nav"** row 8 (`shell/presentation/add_expense_context_picker_sheet.dart` reskin). Mark the §B Expenses rows + the context-picker row done on merge.
  - `docs/audits/design-conversion/04-qa-test-strategy.md` — **§A.5/§A.6** (each hero → light **and** dark goldens for **every** state; Add-expense 21 and Expense detail 22 are money/marigold surfaces so both brightnesses are required; the **rebuild row** in §A.4 mandates **fresh** goldens for the **new** step-2 states — "adds up" green / over-under red), **§D** (the per-screen "converted" acceptance gate), the contrast gate incl. the **white-on-marigold negative case**, labelling + 48 dp, dynamic type to 2.0× + reduced motion.
  - `docs/audits/design-conversion/01-coverage-gap.md` — §B rows **8 (context picker), 21 (Add expense), 22 (Expense detail) are all "covered"**; confirm they stay "covered" and note the step-2 **rebuild** + the context-picker reskin. (No SRS gap to flag — Expenses is FR-EX-01..08, FR-HD-04, already specified.)
  - `.github/shared/decision-log.md` → **ADR-0024** (scope: visual layer only).
  - `design_handoff_one_by_two/screens/Phase3e - Expenses.dc.html` (the Haldi add-expense 3-step sheet — all three steps + the "adds up" valid / over-under invalid states — and the expense detail, light + dark) + `design_handoff_one_by_two/screens/Phase2 - Components.dc.html` (the 3-step sheet shell, the segmented split-method control, the category chip/tile, the amount input, the skeleton set).
- **The foundation now on `main` (consume — read, do not change):** `lib/app/theme.dart`, `lib/core/theme/obt_colors.dart` (the **balance trio** + `category` 8-hue + `link` + `metaText` + `heroShadow`/`rowShadow`), `lib/core/theme/obt_text.dart` (`OBTText.amount` / `.amountHero`), and the **DC-03 components** the Expenses flow consumes:
  - **3-step sheet shell** — `lib/core/widgets/sheets/obt_stepper_sheet.dart` (`OBTStepperSheet` — 28-radius top + grabber + the visual stepper replacing the `(N/3)` text; `currentStep` / `totalSteps` / `title` / `stepBodies` / `footer`).
  - **segmented split-method control** — `lib/core/widgets/inputs/obt_segmented_split_control.dart` (`OBTSegmentedSplitControl` — Equally / Unequal / % / Shares / Exact via `enabledMethods`; live "adds up" green / over-under red from `totalPaise` vs `allocatedPaise`; `onBalancedChanged` / `onNext`; integer paise, Invariant 1).
  - **category chip / tile** — `lib/core/widgets/inputs/obt_category_chip.dart` (`OBTCategoryChip` / `OBTCategoryTile` — full-hue icon on a ~10%-opacity `OBTColors.category` bed).
  - **amount input** — `lib/core/widgets/inputs/obt_amount_input.dart` (`OBTAmountInput` — keeps the integer-paise `onChanged(int paise)` contract + the `formatInrFromPaise()` boundary; reskin only, do not change the contract).
  - **skeleton set** — `lib/core/widgets/feedback/obt_skeleton.dart`; **empty-state** — `lib/core/widgets/feedback/obt_empty_state.dart`.
  Reuse the **test harness** `test/support/widget_test_harness.dart` (`pumpThemed`, `expectAllInteractiveNodesLabelled`, `expectAllTapTargetsMeetMinSize`) and the **golden harness** `test/golden/golden_harness.dart`. See the DC-06 reskin gate `test/features/friends/friends_haldi_reskin_test.dart` + golden scaffold `test/golden/dc06_friends_golden_test.dart` for the §D-gate pattern (hero trio light+dark, contrast incl. the white-on-marigold negative case, 48 dp + labelling, dynamic type 2.0× at 390/320, reduced motion, group-`skip`ped goldens).
- **The surfaces to convert (read each before editing):** the eleven files in §B Expenses + the §B Shell context-picker row above. Read the controller/providers (`application/add_expense_controller.dart`, `application/expense_detail_provider.dart`, `application/expense_telemetry.dart`, `domain/add_expense_state.dart`, `domain/split_calculator.dart`, `domain/split_method.dart`, `data/expense_repository.dart`, `data/receipt_storage_service.dart`) **only** to confirm you are NOT changing behaviour, the split maths, the `simplifiedBalances` projection read, the create/edit/delete flow, the receipt upload, or telemetry.
- Do **not** read or re-plan the backend/data docs; they are unchanged.

────────────────────────────────────────
STANDING CAVEATS (read first)
────────────────────────────────────────

- **Invariants 1 + 2 are LIVE and load-bearing here.** Money only via `formatInrFromPaise()` (Invariant 1 — the `OBTAmountInput` integer-paise contract is untouched, the split maths in `split_calculator.dart` is untouched). The expense-detail per-person split is the **simplified** projection **read** (Invariant 2 — never a who-owes-who graph, never a client write). Any item that weakens one is a blocking defect — refuse and cite it.
- **THE STEP-2 REBUILD SEAM (Architect must rule).** §B says `step_2_split_and_payer.dart` is a **rebuild**: replace the `Wrap` of `ChoiceChip` split methods with the shared **`OBTSegmentedSplitControl`** (Equally / Unequal / % / Shares / Exact) and wire the live "adds up" green / over-under red validation, with **Next disabled** when the split does not sum to the total (AC-2 negative case). The Architect must rule the wiring contract before the Flutter Dev starts: which `SplitMethod` values map to the control's `enabledMethods`, how `totalPaise` / `allocatedPaise` feed the live validation, how `onMethodSelected` / `onBalancedChanged` / `onNext` thread into the **existing** `add_expense_controller.dart` state (the controller, the draft, and `split_calculator.dart` are **behaviour-frozen** — the rebuild is the control + the live validation surface only), and that `split_row.dart` + `split_validation_message.dart` (red over/under + the **new** green "adds up" variant) conform. **The `OBTAmountInput` paise contract and the split maths must not change.**
- **THE INERT EXTENSION SLOTS (Architect must rule).** Step 3 (`step_3_receipt_and_confirm.dart`) gains a **note** field (additive, wired to the existing draft if a note field already exists, else a no-op display slot the Architect rules) and the inert **"Make recurring"** slot rendered **"Coming soon"** (disabled, announced disabled — never a live control). The context picker (8) renders the Groups section **"Coming soon"**. Mirror the DC-03 inert "Pay via UPI" / "Coming soon" pattern — labelled, disabled, asserted disabled; no new write path, no backend call.
- **Behaviour is UNCHANGED — reskin + the one step-2 rebuild is visual + the live-validation surface.** No change to the add-expense controller, the draft, `split_calculator.dart`, the `simplifiedBalances` read, the create/edit/delete flow, the receipt upload, the context-picker's friend/group selection, or any domain model or telemetry. Existing expenses controller / boundary / PII-leak / screen / widget / split-calculator-property tests stay green unchanged (update only assertions that legitimately encode an old token literal, the removed `ChoiceChip` `Wrap`, or a hand-rolled skeleton's removed structure).
- **Consume the DC-03 primitives — do not re-implement.** The `(N/3)` text + ad-hoc sheet → `OBTStepperSheet` (28 radius + grabber + visual stepper); the `ChoiceChip` split `Wrap` → `OBTSegmentedSplitControl`; the `ChoiceChip` category grid → `OBTCategoryChip`/`OBTCategoryTile` (full-hue icon on the `OBTColors.category` bed); the amount field stays `OBTAmountInput`; hand-rolled loading `CircularProgressIndicator`s → the shimmer `OBTSkeleton*` set; empty `Icon`s → `OBTEmptyState`.
- **Dates render in IST.** The expense-detail date (and any expense date on screen) renders in IST as `24 Jun 2026` via a **fixed UTC+5:30 offset** (no device-local `DateFormat`, no DST) — reuse the established IST convention (`Duration(hours: 5, minutes: 30)`; see `lib/features/friends/presentation/widgets/transaction_visuals.dart` `toIst` / `formatIstLongDate`, the home aggregator, and the activity formatter). Do not reintroduce the device-local-vs-IST divergence DC-06 fixed.
- **The category palette is the Haldi 8-hue, never hard-coded hex.** Re-point `expense_category_grid.dart` to `OBTColors.category` (the domain `ExpenseCategory.travel` maps to the palette `OBTCategory.transport`; reuse the `spending_category_palette.dart` / friends `friendCategoryKey` mapping pattern). All colour via `Theme.of(context).colorScheme` / `OBTColors`; all type via `theme.textTheme` / `OBTText`; all radii via the `AppTheme` scale. `theme.dart` / `core/theme/*` are the single source of truth — consume, do not edit (any token gap routes to the Architect / issue #128).
- **No new telemetry event is silently dropped.** If the telemetry plan (`docs/design/07-technical/telemetry-plan.md`) defines an event for any interaction touched by this PR (e.g. a step advance, a context-picker selection), confirm it is still emitted; a converted control that drops a planned event is a contract gap (DC-06 lesson — the `friend_history_tapped` miss). Reskins must not remove existing analytics calls.
- **Any unbounded on-screen list uses `ListView.builder`** (lazy viewport) — never an eager `ListView(children: [...])` over a provider-driven list (DC-06 lesson).
- **Tooling + lessons carried from DC-01..DC-06:**
  - fvm-pinned (Flutter 3.44.3 / Dart 3.12.2). **The `fvm` wrapper may be broken** ("Can't load Kernel binary … expected 127, found 130") — bypass it: `export PATH="/Users/avtanshgupta/fvm/versions/stable/bin:$PATH"` then bare `flutter analyze lib test`, `flutter test`, `dart format .`, and commit/push with bare `git` (the pinned SDK on PATH satisfies the lefthook `dart`/`flutter` pre-commit + pre-push hooks; plain `git`/`fvm exec git` otherwise fails "command not found").
  - CI gates on `dart format --set-exit-if-changed .` — run `dart format .` before pushing. `very_good_analysis` is strict: **`directives_ordering`** (imports sorted; dart: then package: alphabetically), **`prefer_int_literals`**, **`avoid_redundant_argument_values`** (don't pass a default; e.g. a `DateTime(2026, 6, 1)` day, a `crossAxisAlignment: CrossAxisAlignment.center`), **`lines_longer_than_80_chars`**, **`prefer_const_constructors`**, **`unnecessary_import`**.
  - **`Closes #119` MUST be plain text, NOT backticked,** in the PR body, or GitHub will not register the closing reference and the Sprint 3 milestone will not advance.
  - **48 dp tap targets + non-vacuous a11y walks.** Any interactive control (the segmented split tabs, the category chips, the payer toggle, Next/Back, the context-picker rows + search) must meet the 48 dp minimum and carry a label; reuse `expectAllInteractiveNodesLabelled` + `expectAllTapTargetsMeetMinSize`. A FAB's `tooltip` does not land on its node — put the label on the child `Icon` (`semanticLabel`); a labelled `Semantics(container/button)` over its own text uses `excludeSemantics: true` to avoid a double-announce (DC-06 lesson).
  - **`google_fonts` in tests.** Pump widget tests that build `AppTheme` inside `testWidgets`. Animated surfaces (the shimmer skeleton) never settle — pump with `disableAnimations: true` or `settle: false`; reduced motion freezes the controller so `pumpAndSettle` completes.
  - **Goldens are authored on `ubuntu-latest` by the DC-13 (`#125`) `golden-a11y-checks` job**, not yet wired. Add the light + dark golden scaffolds for Add-expense 21 (incl. the step-2 "adds up" green + over-under red states), Expense detail 22, and the context picker 8 to `test/golden/` (tagged `golden`), **group-`skip`ped** with the DC-13 rationale, reusing `golden_harness.dart`. Do **not** author/commit golden PNGs locally (macOS bytes are non-deterministic). The load-bearing proof is the per-screen widget tests + the new step-2 behavioural test, which run for real.
  - If `flutter analyze` surfaces unrelated `flutter_contacts` / `PermissionStatus` / package errors, the `.dart_tool/package_config.json` drifted — run `flutter pub get` with the **pinned** SDK; never bare `flutter pub get`.
  - **Merge needs an OWNER approval.** `main`'s ruleset requires an approving review from a write-access reviewer; the session is authenticated as the author `avtanshgupta` (a collaborator, cannot self-approve), and every prior DC PR was **approved by the `avtansh-code` OWNER account** before a squash-merge. A green pipeline shows `mergeStateStatus=BLOCKED` until that approval lands; `gh pr merge --admin` does **not** bypass the ruleset. **iOS (CocoaPods) is the slowest check (~15–20 min)** — wait it out. Squash subject convention: `feat(<scope>): … (#<pr>)` (single-token scope; comma-separated multi-scopes are rejected by the title-lint).

────────────────────────────────────────
THE TASK — DC-07 (5 SP)
────────────────────────────────────────

1. **Issue + milestone + roadmap (PM).** DC-07 is issue **#119**, already under the **`Sprint 3`** milestone — confirm it; this PR carries `Closes #119` (plain). Per `.github/shared/milestone-tracking.md`, reconcile the Sprint 3 milestone, and on this PR flip the **DC-06** line in `docs/sprint-zero/next-three-prs.md` from "IN FLIGHT" to **"MERGED (#133, `006a0fb`)"** and set DC-07 in flight. **Mark the §B Expenses rows + the §B Shell context-picker (8) row done** in `02-conversion-checklist.md`; in `01-coverage-gap.md` confirm rows **8 / 21 / 22 stay "covered"** and note the step-2 **rebuild** + the context-picker reskin. (No SRS gap to flag — Expenses is FR-EX-01..08 / FR-HD-04, already specified.)

2. **Architect — per-surface contracts + the two seams + the Invariant-1/2 contract (review, read-only).** Confirm, for each Expenses surface, the token/type mapping (`ColorScheme` slot / `OBTColors` key — incl. the balance trio, `category` 8-hue, `link`, `metaText` / `AppTheme` radius / `TextTheme` slot / `OBTText.amount`) and the loading/empty/error shape. **Rule the step-2 rebuild seam** (`OBTSegmentedSplitControl` wiring: `enabledMethods` ↔ the five `SplitMethod` values, `totalPaise`/`allocatedPaise` ↔ the live validation, `onMethodSelected`/`onBalancedChanged`/`onNext` ↔ the **frozen** `add_expense_controller`; the red over/under + new green "adds up" variant in `split_validation_message.dart`; Next-disabled negative case). **Rule the inert-slot seam** (step-3 note field + the "Make recurring" Coming-soon slot; the context-picker Groups "Coming soon" — labelled, disabled, no write path). **Confirm the Invariant contract**: amounts only via `formatInrFromPaise()` (1) and the `OBTAmountInput` paise contract + `split_calculator` maths untouched; the expense-detail per-person split is the **simplified projection read** (2), never a debt graph, never a client write. Return the contracts for the Flutter Dev.

3. **Flutter Dev — reskin the sheet shell, REBUILD step 2, add the step-3 slots, reskin expense detail + the widgets, and reskin the context picker:**
   - **Add-expense sheet (`add_expense_bottom_sheet.dart`)** — adopt `OBTStepperSheet` (28-radius top + grabber + the visual stepper); header → Bricolage; keep the 3-step routing and the controller wiring unchanged.
   - **Step 1 (`step_1_amount_details.dart`)** — `OBTAmountInput` amount → Bricolage tabular (contract untouched); the category grid → `OBTCategoryChip`/`OBTCategoryTile` (Haldi 8-hue bed); date → IST `24 Jun 2026`; Next → marigold (ink on marigold).
   - **Step 2 (`step_2_split_and_payer.dart`, REBUILD)** — replace the `ChoiceChip` `Wrap` with `OBTSegmentedSplitControl` (Equally / Unequal / % / Shares / Exact); wire the live "adds up" green / over-under red validation; **Next disabled** when the split ≠ total (AC-2); reskin the payer toggle. `split_row.dart` amounts → Bricolage tabular; `split_validation_message.dart` → red over/under + the **new green "adds up"** variant.
   - **Step 3 (`step_3_receipt_and_confirm.dart`)** — add the **note** field + the inert **"Make recurring"** slot ("Coming soon", announced disabled); reskin the receipt area + the summary card.
   - **Expense detail (`expense_detail_screen.dart`, `widgets/changed_field_indicator.dart`, `widgets/receipt_fullscreen_viewer.dart`)** — marigold tokens + type; the category avatar → the Haldi hue bed; amounts → Bricolage tabular; the per-person **simplified** split with the balance trio (payer "gets back", others "owe") **read from the projection**; loading `CircularProgressIndicator` → shimmer skeleton; IST date; `changed_field_indicator` left border → the secondary/terracotta token; receipt viewer chrome → scrim/close tokens.
   - **Context picker (`shell/presentation/add_expense_context_picker_sheet.dart`)** — sheet radius → 28; section headers → overline; the friends loading spinner → shimmer skeleton; the Groups stub copy → **"Coming soon"**; add the additive Haldi search field; selecting a context still opens the add sheet (no backend change).

4. **QA + Designer — the per-screen "converted" gate (§D):**
   - **All states present** for Add-expense 21, Expense detail 22, and the context picker 8 — loading = **shimmer skeleton**, empty/error where applicable, populated, **plus the step-2 "adds up" green + over-under red states**.
   - **Light + dark** goldens for **every** state of the heroes (§A.6; both required for the money/marigold surfaces 21/22 and the context picker 8 per §A.5), queued in `test/golden/`, **skipped** pending the DC-13 ubuntu path.
   - **Step-2 behavioural test (rebuild)** — valid split → Next enabled / green "adds up"; invalid split → Next disabled / red over-under (AC-2); the `OBTAmountInput` paise contract + the `split_calculator` maths assert unchanged.
   - **Invariant 1 + 2 proof** — the expenses boundary/PII greps stay green (extend to any net-new on-screen-money surface); a test asserts every on-screen amount routes through `formatInrFromPaise()` (no inline `/100`), the expense-detail split is a projection **read** (no write path, no debt graph).
   - **Contrast gate** (incl. the **white-on-marigold negative case** asserted to FAIL; ink-on-marigold passes), **balance-trio = colour + icon + label** on the expense-detail split, **every control labelled + 48 dp**, **dynamic type 2.0×** (no clip/overflow at 390 and 320; amounts never truncate), reduced motion freezes the shimmer.
   - **Designer (read-only)** reviews the Expenses surfaces against `Phase3e - Expenses.dc.html` + `Phase2 - Components.dc.html` and returns findings for the orchestrator to action.

5. **Verify:** `flutter analyze lib test` (no issues), `dart format .` (clean), `flutter test` (all pass; the existing expenses controller / boundary / PII-leak / screen / widget / split-calculator suites stay green; per-feature coverage ≥ 70% and overall ≥ 50%). No **existing** controller, provider, split maths, `simplifiedBalances` read, receipt upload, Firestore read/write, Cloud Function, or domain-model **behaviour** is changed; the only structural change beyond the reskin is the step-2 control **rebuild** (visual + live-validation surface) and the additive step-3 / context-picker inert slots.

────────────────────────────────────────
BRANCH / PR
────────────────────────────────────────

- Branch: `feat/haldi-expenses-conversion` (type-prefixed kebab, repo convention).
- Conventional Commits, single-token scope, ASCII subject ≤ 72 chars — e.g. `feat(expenses): convert the add-expense flow to Haldi`.
- This is a **code** PR — the full PR pipeline must be green (Flutter Lint & Test, Build Android, Build iOS, Integration Tests, Coverage Gate) before merge.
- PR body: map each surface to §B + the DC-07 ACs; tick the invariants checklist (1 + 2 **live** — money via `formatInrFromPaise()`, the `OBTAmountInput` paise contract + split maths untouched, the expense-detail split a `simplifiedBalances` projection read; 3 + 4 unchanged); note "step-2 rebuilt to `OBTSegmentedSplitControl` with live adds-up validation; sheet shell → `OBTStepperSheet`; category grid → `OBTCategoryChip`/`Tile`; step-3 note + inert 'Make recurring' slot; context picker (8) reskinned with 'Coming soon' Groups + additive search; spinners → shimmer; dates → IST"; **`Closes #119` (plain, NOT backticked)**.
- Include the `Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>` trailer.
- Commit `docs/copilot_prompts/sprint_3/8.md` (this prompt) with the PR, per the repo convention.
- **Merge:** after the pipeline is green (wait out the slow iOS build), the PR will read `BLOCKED` until the **`avtansh-code` OWNER** approves; then squash-merge (`--delete-branch`) with subject `feat(expenses): convert the add-expense flow to Haldi (#<pr>)`.

────────────────────────────────────────
DEFINITION OF DONE (this PR)
────────────────────────────────────────

- All **four** DC-07 acceptance criteria pass: the **add-expense sheet (21 ★)** gains the 28-radius top + grabber + visual stepper via `OBTStepperSheet`, **step 2 is rebuilt** to `OBTSegmentedSplitControl` (Equally / Unequal / % / Shares / Exact), and **step 3** adds the note field + the inert "Make recurring" slot ("Coming soon"), light **and** dark (AC-1); **step 2** disables Next + shows the red over/under state when the split ≠ total, and the expense cannot be saved (AC-2 / Invariant 1); **expense detail (22)** renders the per-person **simplified** split with balance tokens **read from the projection**, the spinner → skeleton, and the IST date (AC-3 / Invariants 1 + 2); the **context picker (8)** shows Groups "Coming soon", selecting a context opens the add sheet, and no backend call changes (AC-4).
- The §B **Expenses** rows + the §B **Shell** context-picker (8) row in `02-conversion-checklist.md` are marked done; `01-coverage-gap.md` keeps rows 8 / 21 / 22 "covered" and notes the step-2 rebuild + the context-picker reskin.
- All Expenses heroes pass the §D per-screen gate (states incl. the step-2 green/red, light/dark goldens, contrast incl. the negative case, balance-trio icon+label on the detail split, labelling + 48 dp, dynamic-type 2.0× with no amount truncation), with light + dark goldens queued in the DC-13 harness; the expenses boundary/PII greps stay green (extended to any net-new on-screen-money surface).
- No hard-coded Haldi hex at call sites; `theme.dart` / `core/theme/*` unchanged; no invariant weakened; no controller/provider/split-maths/`simplifiedBalances`-read/receipt/CF/domain/telemetry behaviour change; the `OBTAmountInput` paise contract is intact.
- Analyse clean, `dart format` clean, all tests pass (existing expenses suites green unchanged), per-feature coverage ≥ 70% and overall ≥ 50%.
- `docs/sprint-zero/next-three-prs.md` is reconciled (DC-06 → MERGED `#133` / `006a0fb`; DC-07 status).
- DC-07 issue (#119) closed under the `Sprint 3` milestone; the milestone is reconciled per `.github/shared/milestone-tracking.md`.

End the session summary with: "Next: Sprint 3 PR #9 — DC-08 (Settlements flow conversion: Haldi 23 ★ settle-up rebuild + 24 settlement history)."

────────────────────────────────────────
FAILURE / REFUSAL PROTOCOL
────────────────────────────────────────

- If any change would weaken an invariant or the SRS, refuse it, quote the specific invariant / SRS section, and propose a compliant alternative. **In particular:** no inline `paise / 100` or `double` for any on-screen amount (Invariant 1 — use `formatInrFromPaise()`; the `OBTAmountInput` integer-paise contract and the `split_calculator` maths stay untouched); no write to `simplifiedBalances` and no raw who-owes-whom graph in the expense-detail split (Invariant 2 — projection reads only).
- Refuse scope creep: no change to the expenses controller / providers / split maths / receipt upload / context-picker selection or telemetry; do **not** convert any other flow (DC-08+); do **not** ship the "Make recurring" / "Pay via UPI" / Groups slots as live controls (they are inert "Coming soon"); do **not** build any not-yet-scoped screen; do not unlock any India-only constraint; no second Firebase project.
- Do not edit `docs/OneByTwo_Requirements_Spec.md` or `lib/app/theme.dart` / `lib/core/theme/*` (consume the DC-01 foundation; any token gap routes to the Architect / issue #128).

Begin by delegating to the **PM** (confirm #119 + the milestone, reconcile the roadmap so DC-06 reads MERGED, the §B Expenses + context-picker reconciliation, and confirm the Haldi-8/21/22 coverage stays "covered"), then the **Architect** (per-surface token mapping + the step-2 rebuild seam ruling + the inert-slot seam + the Invariant-1/2 contract + the IST date rule), then the **Flutter Dev** (reskin the sheet shell + REBUILD step 2 to `OBTSegmentedSplitControl` + add the step-3 slots + reskin expense detail + reskin the context picker) with **QA** + **Designer** (the §D per-screen gate incl. the step-2 green/red states + light/dark hero goldens + the contrast negative case + the `formatInrFromPaise()` boundary + the projection-read split proof + dynamic-type + visual review), and sequence to a single green PR.
