// Basic smoke test for the OneByTwo app.
//
// This test will be updated as features are implemented.

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test placeholder', (WidgetTester tester) async {
    // TODO(sprint-1): Update this test once bootstrap/Firebase is testable.
    expect(1 + 1, equals(2));
  });
}
